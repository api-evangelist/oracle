openapi: 3.2.0
info:
  description: Use the OCI Cache API to create and manage clusters. A cluster is a memory-based storage solution. For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).
  title: Oci Cache Config Set API
  version: '20220315'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/0c0709de34f2014ed9d1948ceeb3f59f7c24fee03209fa8269bdb6c37117b40d.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the OCI Cache API OCI service and stored verbatim; API Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/0c0709de34f2014ed9d1948ceeb3f59f7c24fee03209fa8269bdb6c37117b40d.yaml
    what: the harvested document for OCI Cache API
servers:
- url: http://127.0.0.1/20220315
- url: https://127.0.0.1/20220315
tags:
- name: ociCacheConfigSet
paths:
  /ociCacheConfigSets:
    get:
      description: 'Lists the OCI Cache Config Sets in the specified compartment.

        '
      operationId: ListOciCacheConfigSets
      parameters:
      - $ref: '#/components/parameters/CompartmentIdQueryParam'
      - $ref: '#/components/parameters/OciCacheConfigSetLifecycleStateQueryParam'
      - $ref: '#/components/parameters/SoftwareVersionQueryParam'
      - $ref: '#/components/parameters/DisplayNameQueryParam'
      - $ref: '#/components/parameters/OciCacheConfigSetIdentifierQueryParam'
      - $ref: '#/components/parameters/PaginationLimitQueryParam'
      - $ref: '#/components/parameters/PaginationTokenQueryParam'
      - $ref: '#/components/parameters/SortOrderQueryParam'
      - $ref: '#/components/parameters/SortByQueryParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page:
              description: 'For pagination of a list of items. When paging through a list, if this header appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter for the

                subsequent GET request to get the next batch of items.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OciCacheConfigSetCollection'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Get a list of all OCI Cache Config Sets in a compartment
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSetSummary'
    post:
      description: 'Create a new OCI Cache Config Set for the given OCI cache engine version.

        '
      operationId: CreateOciCacheConfigSet
      parameters:
      - $ref: '#/components/parameters/RetryTokenHeader'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for creating an OCI Cache Config Set.
          headers:
            etag:
              description: 'For optimistic concurrency control. See `if-match`.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
            opc-work-request-id:
              description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use this to query status of the asynchronous operation.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OciCacheConfigSet'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        409:
          $ref: '#/components/responses/409'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Create a new OCI Cache Config Set for the given OCI cache engine version
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSet'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOciCacheConfigSetDetails'
        description: Details for the new OCI Cache Config Set.
        required: true
  /ociCacheConfigSets/{ociCacheConfigSetId}:
    delete:
      description: Deletes the specified OCI Cache Config Set.
      operationId: DeleteOciCacheConfigSet
      parameters:
      - $ref: '#/components/parameters/OciCacheConfigSetIdentifierPathParam'
      - $ref: '#/components/parameters/IfMatchHeader'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for deleting the OCI Cache Config Set.
          headers:
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
            opc-work-request-id:
              description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use this to query status of the asynchronous operation.

                '
              schema:
                type: string
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        412:
          $ref: '#/components/responses/412'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Delete an existing OCI Cache Config Set
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSet'
    get:
      description: Retrieves the specified OCI Cache Config Set.
      operationId: GetOciCacheConfigSet
      parameters:
      - $ref: '#/components/parameters/OciCacheConfigSetIdentifierPathParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        200:
          description: Successfully retrieved the OCI Cache Config Set.
          headers:
            etag:
              description: 'For optimistic concurrency control. See `if-match`.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OciCacheConfigSet'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Get OciCacheConfigSet
      tags:
      - ociCacheConfigSet
    put:
      description: Updates the specified OCI Cache Config Set.
      operationId: UpdateOciCacheConfigSet
      parameters:
      - $ref: '#/components/parameters/OciCacheConfigSetIdentifierPathParam'
      - $ref: '#/components/parameters/IfMatchHeader'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for updating the OCI Cache Config Set.
          headers:
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
            opc-work-request-id:
              description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use this to query status of the asynchronous operation.

                '
              schema:
                type: string
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        412:
          $ref: '#/components/responses/412'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Update the OCI Cache Config Set for the given OCI Cache Config Set id.
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSet'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateOciCacheConfigSetDetails'
        description: The information to be updated for OCI Cache Config Set.
        required: true
  /ociCacheConfigSets/{ociCacheConfigSetId}/actions/changeCompartment:
    post:
      description: Moves an OCI Cache Config Set into a different compartment within the same tenancy.
      operationId: ChangeOciCacheConfigSetCompartment
      parameters:
      - $ref: '#/components/parameters/OciCacheConfigSetIdentifierPathParam'
      - $ref: '#/components/parameters/IfMatchHeader'
      - $ref: '#/components/parameters/RequestIdHeader'
      - $ref: '#/components/parameters/RetryTokenHeader'
      responses:
        202:
          description: The request was accepted for moving the OCI Cache Config Set to a different compartment.
          headers:
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
            opc-work-request-id:
              description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use this to query status of the asynchronous operation.

                '
              schema:
                type: string
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        409:
          $ref: '#/components/responses/409'
        412:
          $ref: '#/components/responses/412'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
      summary: Moves a resource into a different compartment.
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSet'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeOciCacheConfigSetCompartmentDetails'
        description: The information to be updated.
        required: true
  /ociCacheConfigSets/{ociCacheConfigSetId}/actions/listAssociatedOciCacheClusters:
    post:
      description: 'Gets a list of associated OCI Cache clusters for an OCI Cache Config Set.

        '
      operationId: ListAssociatedOciCacheClusters
      parameters:
      - $ref: '#/components/parameters/OciCacheConfigSetIdentifierPathParam'
      - $ref: '#/components/parameters/PaginationLimitQueryParam'
      - $ref: '#/components/parameters/PaginationTokenQueryParam'
      - $ref: '#/components/parameters/SortOrderQueryParam'
      - $ref: '#/components/parameters/SortByQueryParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of IDs of AssociatedOciCacheClusters to which the OCI Cache Config Set is attached.
          headers:
            opc-next-page:
              description: 'For pagination of a list of items. When paging through a list, if this header appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter for the

                subsequent GET request to get the next batch of items.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AssociatedOciCacheClusterCollection'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Get a list of all clusters for a OCI Cache Config Set id.
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/AssociatedOciCacheClusterSummary'
components:
  schemas:
    OciCacheConfigSet:
      description: Configurations for OCI Cache to manage the behavior, performance, and functionality of the underlying cache engine.
      properties:
        compartmentId:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment that contains the OCI Cache Config Set.
          type: string
        configurationDetails:
          $ref: '#/components/schemas/ConfigurationDetails'
        defaultConfigSetId:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the default OCI Cache Config Set which the custom OCI Cache Config Set is based upon.
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

            Example: `{"foo-namespace": {"bar-key": "value"}}`

            '
          type: object
        description:
          description: A description of the OCI Cache Config Set.
          maxLength: 500
          type: string
        displayName:
          description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering confidential information.
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

            Example: `{"bar-key": "value"}`

            '
          type: object
        id:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the OCI Cache Config Set.
          type: string
        lifecycleState:
          description: The current state of the OCI Cache Config Set.
          enum:
          - CREATING
          - UPDATING
          - ACTIVE
          - DELETING
          - DELETED
          - FAILED
          type: string
        softwareVersion:
          description: The OCI Cache engine version that the cluster is running.
          enum:
          - V7_0_5
          - VALKEY_7_2
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

            '
          type: object
        timeCreated:
          description: The date and time the OCI Cache Config Set was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339) formatted datetime string.
          format: date-time
          type: string
        timeUpdated:
          description: The date and time the OCI Cache Config Set was updated. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339) formatted datetime string.
          format: date-time
          type: string
      required:
      - id
      - compartmentId
      - lifecycleState
      - softwareVersion
      type: object
    ConfigurationInfo:
      description: Details of a configuration setting in the OCI Cache Config Set.
      properties:
        configKey:
          description: Key is the configuration key.
          type: string
        configValue:
          description: 'Value of the configuration as a string. Can represent a string, boolean, or number.

            Example: "true", "42", or "someString".

            '
          type: string
      required:
      - configKey
      - configValue
      type: object
    ChangeOciCacheConfigSetCompartmentDetails:
      description: The information for the move operation.
      properties:
        compartmentId:
          description: 'The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment

            into which the resource should be moved.

            '
          type: string
      required:
      - compartmentId
      type: object
    ConfigurationDetails:
      description: List of OCI Cache Config Set Values.
      properties:
        items:
          description: List of ConfigurationInfo objects.
          items:
            $ref: '#/components/schemas/ConfigurationInfo'
          type: array
      required:
      - items
      type: object
    AssociatedOciCacheClusterSummary:
      description: Summary of the Clusters associated with an OCI Cache Config Set.
      properties:
        id:
          description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster.

            '
          type: string
      required:
      - id
      type: object
    Error:
      description: Specifies details for an error.
      properties:
        code:
          description: A machine-usable code for the error that occured.
          type: string
        message:
          description: A human-readable error string for the error that occured.
          type: string
      required:
      - code
      - message
    AssociatedOciCacheClusterCollection:
      description: Associated OCI Cache Clusters to which the OCI Cache Config Set is associated.
      properties:
        items:
          description: List of clusters with the same OCI Cache Config Set ID.
          items:
            $ref: '#/components/schemas/AssociatedOciCacheClusterSummary'
          type: array
      required:
      - items
      type: object
    CreateOciCacheConfigSetDetails:
      description: The information to create a new OCI Cache Config Set.
      properties:
        compartmentId:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment that contains the OCI Cache Config Set.
          type: string
        configurationDetails:
          $ref: '#/components/schemas/ConfigurationDetails'
        definedTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

            Example: `{"foo-namespace": {"bar-key": "value"}}`

            '
          type: object
        description:
          description: Description for the custom OCI Cache Config Set.
          type: string
        displayName:
          description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering confidential information.
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

            Example: `{"bar-key": "value"}`

            '
          type: object
        softwareVersion:
          description: The OCI Cache engine version that the cluster is running.
          type: string
          x-obmcs-enumref: '#/definitions/OciCacheConfigSet/softwareVersion'
      required:
      - compartmentId
      - displayName
      - configurationDetails
      - softwareVersion
      type: object
    OciCacheConfigSetSummary:
      description: Summary of information about the OCI Cache Config Set.
      properties:
        compartmentId:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment that contains the OCI Cache Config Set.
          type: string
        defaultConfigSetId:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the default OCI Cache Config Set which the custom OCI Cache Config Set is based upon.
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

            Example: `{"foo-namespace": {"bar-key": "value"}}`

            '
          type: object
        displayName:
          description: A user-friendly name of the OCI Cache Config Set.
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

            Example: `{"bar-key": "value"}`

            '
          type: object
        id:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the OCI Cache Config Set.
          type: string
        lifecycleState:
          description: The current state of the OCI Cache Config Set.
          type: string
          x-obmcs-enumref: '#/definitions/OciCacheConfigSet/lifecycleState'
        softwareVersion:
          description: The engine version of the OCI Cache Config Set.
          type: string
          x-obmcs-enumref: '#/definitions/OciCacheConfigSet/softwareVersion'
        systemTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

            '
          type: object
        timeCreated:
          description: The date and time the configuration was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339) formatted datetime string.
          format: date-time
          type: string
        timeUpdated:
          description: The date and time the configuration was updated. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339) formatted datetime string.
          format: date-time
          type: string
      required:
      - id
      - compartmentId
      type: object
    UpdateOciCacheConfigSetDetails:
      description: The information to be updated for an existing OCI Cache Config Set.
      properties:
        definedTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

            Example: `{"foo-namespace": {"bar-key": "value"}}`

            '
          type: object
        description:
          description: Description for the custom OCI Cache Config Set.
          type: string
        displayName:
          description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering confidential information.
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

            Example: `{"bar-key": "value"}`

            '
          type: object
      type: object
    OciCacheConfigSetCollection:
      description: A list of OCI Cache Config Sets that match filter criteria, if any.
      properties:
        items:
          description: List of OCI Cache Config Sets.
          items:
            $ref: '#/components/schemas/OciCacheConfigSetSummary'
          type: array
      required:
      - items
      type: object
  parameters:
    SoftwareVersionQueryParam:
      description: A filter to return the OCI Cache Config Set resources, whose software version matches with the given software version.
      in: query
      name: softwareVersion
      required: false
      x-default-description: 'null'
      x-obmcs-enumref: '#/definitions/OciCacheConfigSet/softwareVersion'
      schema:
        type: string
    SortByQueryParam:
      description: 'The field to sort by. Only one sort order may be provided. Default order for timeCreated is descending. Default order for displayName is ascending.

        '
      in: query
      name: sortBy
      schema:
        type: string
        enum:
        - timeCreated
        - displayName
        default: timeCreated
    OciCacheConfigSetLifecycleStateQueryParam:
      description: A filter to return the OCI Cache Config Set resources, whose lifecycle state matches with the given lifecycle state.
      in: query
      name: lifecycleState
      required: false
      x-default-description: 'null'
      x-obmcs-enumref: '#/definitions/OciCacheConfigSet/lifecycleState'
      schema:
        type: string
    SortOrderQueryParam:
      description: The sort order to use, either 'ASC' or 'DESC'.
      in: query
      name: sortOrder
      x-default-description: 'The default value depends upon `sortBy`, and

        in general is ''DESC'' when sorting by time and ''ASC'' otherwise.

        '
      x-obmcs-top-level-enum: '#/definitions/SortOrder'
      schema:
        type: string
        enum:
        - ASC
        - DESC
    OciCacheConfigSetIdentifierPathParam:
      description: Unique OCI Cache Config Set identifier.
      in: path
      name: ociCacheConfigSetId
      required: true
      schema:
        type: string
    RequestIdHeader:
      description: The client request ID for tracing.
      in: header
      name: opc-request-id
      schema:
        type: string
    PaginationTokenQueryParam:
      description: A token representing the position at which to start retrieving results. This must come from the `opc-next-page` header field of a previous response.
      in: query
      name: page
      x-default-description: 'null'
      schema:
        type: string
        minLength: 1
    DisplayNameQueryParam:
      description: A filter to return only resources that match the entire display name given.
      in: query
      name: displayName
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 255
        minLength: 1
    RetryTokenHeader:
      description: 'A token that uniquely identifies a request so it can be retried in case of a timeout or

        server error without risk of executing that same action again. Retry tokens expire after 24

        hours, but can be invalidated before then due to conflicting operations. For example, if a resource

        has been deleted and purged from the system, then a retry of the original creation request

        might be rejected.

        '
      in: header
      name: opc-retry-token
      required: false
      schema:
        type: string
        maxLength: 64
        minLength: 1
    PaginationLimitQueryParam:
      description: The maximum number of items to return.
      in: query
      name: limit
      schema:
        type: integer
        default: 10
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam:
      description: The ID of the compartment in which to list resources.
      in: query
      name: compartmentId
      x-default-description: 'null'
      schema:
        type: string
    IfMatchHeader:
      description: 'For optimistic concurrency control. In the PUT or DELETE call

        for a resource, set the `if-match` parameter to the value of the

        etag from a previous GET or POST response for that resource.

        The resource will be updated or deleted only if the etag you

        provide matches the resource''s current etag value.

        '
      in: header
      name: if-match
      required: false
      schema:
        type: string
    OciCacheConfigSetIdentifierQueryParam:
      description: Unique OCI Cache Config Set identifier.
      in: query
      name: id
      x-default-description: 'null'
      schema:
        type: string
  responses:
    default:
      description: Unknown Error
      headers:
        opc-request-id:
          description: 'Unique Oracle-assigned identifier for the request. If you need to contact

            Oracle about a particular request, please provide the request ID.

            '
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
x-anchors:
  x-headers:
    etag:
      description: 'For optimistic concurrency control. See `if-match`.

        '
      type: string
    opc-next-page:
      description: 'For pagination of a list of items. When paging through a list, if this header appears in the response,

        then a partial list might have been returned. Include this value as the `page` parameter for the

        subsequent GET request to get the next batch of items.

        '
      type: string
    opc-request-id:
      description: 'Unique Oracle-assigned identifier for the request. If you need to contact

        Oracle about a particular request, please provide the request ID.

        '
      type: string
    opc-work-request-id:
      description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use this to query status of the asynchronous operation.

        '
      type: string
    retry-after:
      description: A decimal number representing the number of seconds the client should wait before polling this endpoint again.
      type: integer
  x-properties:
    definedTags:
      additionalProperties:
        additionalProperties:
          description: 'The value of the tag. Only string, integer and boolean types are supported.

            '
          type: object
        description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

          Example: `{"bar-key": "value"}`

          '
        type: object
      description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

        Example: `{"foo-namespace": {"bar-key": "value"}}`

        '
      type: object
    freeformTags:
      additionalProperties:
        type: string
      description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

        Example: `{"bar-key": "value"}`

        '
      type: object
    securityAttributes:
      additionalProperties:
        additionalProperties:
          description: 'Attribute value and mode. Only the String type is supported for value. Mode is currently always "enforce".

            '
          type: object
        description: 'Key-value pair representing a security attribute key and value, scoped to a namespace.


          Example: `{"MaxEgressCount": {"value": "42", "mode": "enforce"}}`

          '
        type: object
      description: 'Security attributes for redis cluster resource. Each key is predefined and scoped to a namespace.

        For more information, see [Resource Tags](/iaas/Content/General/Concepts/resourcetags.htm).


        Example: `{"Oracle-ZPR": {"MaxEgressCount": {"value": "42", "mode": "enforce"}}}`

        '
      type: object
    systemTags:
      additionalProperties:
        additionalProperties:
          description: 'The value of the tag. Only string, integer and boolean types are supported.

            '
          type: object
        description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

          Example: `{"bar-key": "value"}`

          '
        type: object
      description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

        Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

        '
      type: object
x-obmcs-client-retries-enabled: true
x-oracle-package: com.oracle.pic.redisservice
