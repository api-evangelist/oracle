openapi: 3.2.0
info:
  title: Oracle Work Request API
  x-refined-note:
  - x-evidence differs across the merged source definitions and was not carried
  - x-provenance differs across the merged source definitions and was not carried
  version: '1.0'
  description: 'Operations tagged workRequest across 10 of this provider''s published API definitions: oci-database-multicloud-integrations-openapi.yaml, oci-delegate-access-control-openapi.yaml, oci-managed-access-openapi.yaml, oci-organizations-1-openapi.yaml, oci-organizations-openapi.yaml, oci-osmh-openapi.yaml, oci-vmware-1-openapi.yaml, oci-vmware-openapi.yaml, oci-waa-openapi.yaml, oci-workrequests-openapi.yaml. Each path carries the servers of the definition it was published in.'
servers:
- url: http://127.0.0.1/20240501
- url: https://127.0.0.1/20240501
- url: http://127.0.0.1/20230801
- url: https://127.0.0.1/20230801
- url: http://127.0.0.1/20220126
- url: https://127.0.0.1/20220126
- url: http://127.0.0.1/20230401
- url: https://127.0.0.1/20230401
- url: http://127.0.0.1/20200801
- url: https://127.0.0.1/20200801
- url: http://127.0.0.1/20220901
- url: https://127.0.0.1/20220901
- url: http://127.0.0.1/20200501
- url: https://127.0.0.1/20200501
- url: http://127.0.0.1/20230701
- url: https://127.0.0.1/20230701
- url: http://127.0.0.1/20211230
- url: https://127.0.0.1/20211230
- url: /20160918
tags:
- name: workRequest
paths:
  /workRequests:
    get:
      description: 'Lists the work requests in a compartment.

        '
      operationId: ListWorkRequests
      parameters:
      - $ref: '#/components/parameters/CompartmentIdQueryParam'
      - $ref: '#/components/parameters/WorkRequestIdQueryParam'
      - $ref: '#/components/parameters/WorkRequestStatusQueryParam'
      - $ref: '#/components/parameters/ResourceIdentifierQueryParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      - $ref: '#/components/parameters/PaginationTokenQueryParam'
      - $ref: '#/components/parameters/PaginationLimitQueryParam'
      - $ref: '#/components/parameters/SortOrderQueryParam'
      - $ref: '#/components/parameters/WorkRequestSortByQueryParam'
      responses:
        200:
          description: The list of work request summaries.
          headers:
            opc-next-page:
              description: 'For list pagination. When this header appears in the response, additional pages of results remain. For

                important details about how pagination works, see [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkRequestSummaryCollection'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: List work requests
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequest'
    servers:
    - url: http://127.0.0.1/20240501
    - url: https://127.0.0.1/20240501
  /workRequests/{workRequestId}:
    delete:
      description: Cancels a work request.
      operationId: CancelWorkRequest
      parameters:
      - $ref: '#/components/parameters/WorkRequestIdPathParam'
      - $ref: '#/components/parameters/IfMatchHeader'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        202:
          description: The work request will be canceled.
          headers:
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        409:
          $ref: '#/components/responses/409'
        412:
          $ref: '#/components/responses/412'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Cancel work request
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequest'
    get:
      description: Gets details of the work request with the given ID.
      operationId: GetWorkRequest
      parameters:
      - $ref: '#/components/parameters/WorkRequestIdPathParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        200:
          description: The details of the work request.
          headers:
            etag:
              description: 'For optimistic concurrency control. See `if-match`.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
            retry-after:
              description: A decimal number representing the number of seconds the client should wait before polling this endpoint again.
              schema:
                type: integer
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkRequest'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Get work request status
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequest'
    servers:
    - url: http://127.0.0.1/20240501
    - url: https://127.0.0.1/20240501
  /workRequests/{workRequestId}/errors:
    get:
      description: 'Returns a (paginated) list of errors for the work request with the given ID.

        '
      operationId: ListWorkRequestErrors
      parameters:
      - $ref: '#/components/parameters/WorkRequestIdPathParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      - $ref: '#/components/parameters/PaginationTokenQueryParam'
      - $ref: '#/components/parameters/PaginationLimitQueryParam'
      - $ref: '#/components/parameters/WorkRequestLogAndErrorSortByQueryParam'
      - $ref: '#/components/parameters/SortOrderQueryParam'
      responses:
        200:
          description: The list of work request errors.
          headers:
            opc-next-page:
              description: 'For list pagination. When this header appears in the response, additional pages of results remain. For

                important details about how pagination works, see [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkRequestErrorCollection'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: List work request errors
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequestError'
    servers:
    - url: http://127.0.0.1/20240501
    - url: https://127.0.0.1/20240501
  /workRequests/{workRequestId}/logs:
    get:
      description: 'Returns a (paginated) list of logs for the work request with the given ID.

        '
      operationId: ListWorkRequestLogs
      parameters:
      - $ref: '#/components/parameters/WorkRequestIdPathParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      - $ref: '#/components/parameters/PaginationTokenQueryParam'
      - $ref: '#/components/parameters/PaginationLimitQueryParam'
      - $ref: '#/components/parameters/WorkRequestLogAndErrorSortByQueryParam'
      - $ref: '#/components/parameters/SortOrderQueryParam'
      responses:
        200:
          description: The list of work request logs.
          headers:
            opc-next-page:
              description: 'For list pagination. When this header appears in the response, additional pages of results remain. For

                important details about how pagination works, see [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkRequestLogEntryCollection'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: List work request logs
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequestLogEntry'
    servers:
    - url: http://127.0.0.1/20240501
    - url: https://127.0.0.1/20240501
  /workRequests/{workRequestId}/actions/rerun:
    post:
      description: 'Reruns a failed work for the specified work request [OCID](/iaas/Content/General/Concepts/identifiers.htm). Rerunning restarts the work on failed targets.

        '
      operationId: RerunWorkRequest
      parameters:
      - $ref: '#/components/parameters/WorkRequestIdPathParam_2'
      - $ref: '#/components/parameters/RequestIdHeader_2'
      - $ref: '#/components/parameters/RetryTokenHeader'
      - $ref: '#/components/parameters/IfMatchHeader'
      responses:
        202:
          description: Accepted the request. The work request is being rerun.
          headers:
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
            opc-work-request-id:
              description: 'Unique Oracle-assigned identifier for the asynchronous work. You can use this to query its status.

                '
              schema:
                type: string
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        409:
          $ref: '#/components/responses/409'
        412:
          $ref: '#/components/responses/412'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Reruns a failed work request based on a work request id.
      tags:
      - workRequest
      x-example: "POST 20220901/workRequests/<var>&lt;resource_OCID&gt;</var>/actions/rerun\nHost: osmh.us-phoenix-1.oci.oraclecloud.com\n<authorization and other headers>\n{\n    \"managedInstances\": [\n        \"ocid1.managementagent.oc1.phx.exampleuniqueID\"\n    ],\n    \"workRequestDetails\": {\n        \"displayName\": \"rerun of ocid1.osmhworkrequest.oc1.us-phoenix-1.exampleuniqueID\",\n        \"description\": \"rerun of failed update jobs in group\"\n    }\n}\n"
      x-related-resource: '#/definitions/WorkRequest'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RerunWorkRequestDetails'
        description: The resource [OCIDs](/iaas/Content/General/Concepts/identifiers.htm) to rerun the work requests for. If no OCIDs are provided, rerun will apply to all failed work requests associated with the specified work request id.
        required: true
    servers:
    - url: http://127.0.0.1/20220901
    - url: https://127.0.0.1/20220901
components:
  schemas:
    WorkRequestErrorCollection:
      description: Results of a workRequestError search. Contains both WorkRequestError items and other information, such as metadata.
      properties:
        items:
          description: List of workRequestError objects.
          items:
            $ref: '#/components/schemas/WorkRequestError'
          type: array
      required:
      - items
      type: object
    WorkRequestError:
      description: An error encountered while executing a work request.
      properties:
        code:
          description: 'A machine-usable code for the error that occured. Error codes are listed on

            (https://docs.cloud.oracle.com/Content/API/References/apierrors.htm)

            '
          type: string
        message:
          description: A human readable description of the issue encountered.
          type: string
        timestamp:
          description: The time the error occured. An RFC3339 formatted datetime string.
          format: date-time
          type: string
      required:
      - code
      - message
      - timestamp
    WorkRequestSummary:
      description: A summary of the status of a work request.
      properties:
        compartmentId:
          description: 'The ID of the compartment that contains the work request. Work requests should be scoped to

            the same compartment as the resource the work request affects. If the work request affects multiple resources,

            and those resources are not in the same compartment, it is up to the service team to pick the primary

            resource whose compartment should be used

            '
          type: string
        id:
          description: The id of the work request.
          type: string
        operationType:
          description: Type of the work request.
          enum:
          - CREATE_AZURE_CONNECTOR
          - DELETE_AZURE_CONNECTOR
          - UPDATE_AZURE_CONNECTOR
          - MOVE_AZURE_CONNECTOR
          - REFRESH_AZURE_CONNECTOR
          - CREATE_AZURE_BLOB_CONTAINER
          - DELETE_AZURE_BLOB_CONTAINER
          - UPDATE_AZURE_BLOB_CONTAINER
          - MOVE_AZURE_BLOB_CONTAINER
          - CREATE_AZURE_BLOB_MOUNT
          - MOVE_AZURE_BLOB_MOUNT
          - UPDATE_AZURE_BLOB_MOUNT
          - DELETE_AZURE_BLOB_MOUNT
          - CREATE_MULTICLOUD_DISCOVERY
          - DELETE_MULTICLOUD_DISCOVERY
          - UPDATE_MULTICLOUD_DISCOVERY
          - MOVE_MULTICLOUD_DISCOVERY
          - CREATE_AZURE_VAULT
          - DELETE_AZURE_VAULT
          - UPDATE_AZURE_VAULT
          - MOVE_AZURE_VAULT
          - REFRESH_AZURE_VAULT
          - CREATE_AZURE_VAULT_ASSOCIATION
          - DELETE_AZURE_VAULT_ASSOCIATION
          - UPDATE_AZURE_VAULT_ASSOCIATION
          - MOVE_AZURE_VAULT_ASSOCIATION
          - PATCH_DB_RESOURCE
          - CREATE_GCP_IDENTITY_CONFIGURATION
          - DELETE_GCP_IDENTITY_CONFIGURATION
          - CREATE_GCP_CONNECTOR
          - DELETE_GCP_CONNECTOR
          - UPDATE_GCP_CONNECTOR
          - MOVE_GCP_CONNECTOR
          - REFRESH_GCP_CONNECTOR
          - GCP_DISCOVERY
          - CREATE_GCP_KEY_RING
          - DELETE_GCP_KEY_RING
          - UPDATE_GCP_KEY_RING
          - MOVE_GCP_KEY_RING
          - REFRESH_GCP_KEY_RING
          - CREATE_AWS_CONNECTOR
          - DELETE_AWS_CONNECTOR
          - UPDATE_AWS_CONNECTOR
          - MOVE_AWS_CONNECTOR
          - REFRESH_AWS_CONNECTOR
          - CREATE_AWS_KEY
          - DELETE_AWS_KEY
          - UPDATE_AWS_KEY
          - MOVE_AWS_KEY
          - REFRESH_AWS_KEY
          - CREATE_REPLICATE_RESOURCE
          - UPDATE_REPLICATE_RESOURCE
          - DELETE_REPLICATE_RESOURCE
          - MOVE_REPLICATE_RESOURCE
          - REFRESH_REPLICATE_RESOURCE
          - REPLICATE
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource'
          type: array
        status:
          description: Status of current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - WAITING
          - NEEDS_ATTENTION
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequest:
      description: A description of work request status.
      properties:
        compartmentId:
          description: 'The ID of the compartment that contains the work request. Work requests should be scoped to

            the same compartment as the resource the work request affects. If the work request affects multiple resources,

            and those resources are not in the same compartment, it is up to the service team to pick the primary

            resource whose compartment should be used

            '
          type: string
        id:
          description: The id of the work request.
          type: string
        operationType:
          description: Type of the work request.
          enum:
          - CREATE_AZURE_CONNECTOR
          - DELETE_AZURE_CONNECTOR
          - UPDATE_AZURE_CONNECTOR
          - MOVE_AZURE_CONNECTOR
          - REFRESH_AZURE_CONNECTOR
          - CREATE_AZURE_BLOB_CONTAINER
          - DELETE_AZURE_BLOB_CONTAINER
          - UPDATE_AZURE_BLOB_CONTAINER
          - MOVE_AZURE_BLOB_CONTAINER
          - CREATE_AZURE_BLOB_MOUNT
          - MOVE_AZURE_BLOB_MOUNT
          - UPDATE_AZURE_BLOB_MOUNT
          - DELETE_AZURE_BLOB_MOUNT
          - CREATE_MULTICLOUD_DISCOVERY
          - DELETE_MULTICLOUD_DISCOVERY
          - UPDATE_MULTICLOUD_DISCOVERY
          - MOVE_MULTICLOUD_DISCOVERY
          - CREATE_AZURE_VAULT
          - DELETE_AZURE_VAULT
          - UPDATE_AZURE_VAULT
          - MOVE_AZURE_VAULT
          - REFRESH_AZURE_VAULT
          - CREATE_AZURE_VAULT_ASSOCIATION
          - DELETE_AZURE_VAULT_ASSOCIATION
          - UPDATE_AZURE_VAULT_ASSOCIATION
          - MOVE_AZURE_VAULT_ASSOCIATION
          - PATCH_DB_RESOURCE
          - CREATE_GCP_IDENTITY_CONFIGURATION
          - DELETE_GCP_IDENTITY_CONFIGURATION
          - CREATE_GCP_CONNECTOR
          - DELETE_GCP_CONNECTOR
          - UPDATE_GCP_CONNECTOR
          - MOVE_GCP_CONNECTOR
          - REFRESH_GCP_CONNECTOR
          - GCP_DISCOVERY
          - CREATE_GCP_KEY_RING
          - DELETE_GCP_KEY_RING
          - UPDATE_GCP_KEY_RING
          - MOVE_GCP_KEY_RING
          - REFRESH_GCP_KEY_RING
          - CREATE_AWS_CONNECTOR
          - DELETE_AWS_CONNECTOR
          - UPDATE_AWS_CONNECTOR
          - MOVE_AWS_CONNECTOR
          - REFRESH_AWS_CONNECTOR
          - CREATE_AWS_KEY
          - DELETE_AWS_KEY
          - UPDATE_AWS_KEY
          - MOVE_AWS_KEY
          - REFRESH_AWS_KEY
          - CREATE_REPLICATE_RESOURCE
          - UPDATE_REPLICATE_RESOURCE
          - DELETE_REPLICATE_RESOURCE
          - MOVE_REPLICATE_RESOURCE
          - REFRESH_REPLICATE_RESOURCE
          - REPLICATE
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource'
          type: array
        status:
          description: Status of current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - WAITING
          - NEEDS_ATTENTION
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequestLogEntryCollection:
      description: Results of a workRequestLog search. Contains both workRequestLog items and other information, such as metadata.
      properties:
        items:
          description: List of workRequestLogEntries.
          items:
            $ref: '#/components/schemas/WorkRequestLogEntry'
          type: array
      required:
      - items
      type: object
    Error:
      description: Error information.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error message.
          type: string
      required:
      - code
      - message
    WorkRequestLogEntry:
      description: A log message from the execution of a work request.
      properties:
        message:
          description: Human-readable log message.
          type: string
        timestamp:
          description: The time the log message was written. An RFC3339 formatted datetime string.
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequestSummaryCollection:
      description: Results of a workRequest search. Contains both WorkRequest items and other information, such as metadata.
      properties:
        items:
          description: List of workRequestSummary objects.
          items:
            $ref: '#/components/schemas/WorkRequestSummary'
          type: array
      required:
      - items
      type: object
    WorkRequestResource:
      description: A resource created or operated on by a work request.
      properties:
        actionType:
          description: 'The way in which this resource is affected by the work tracked in the work request.

            A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

            work is complete for that resource at which point it will transition to CREATED, UPDATED,

            or DELETED, respectively.

            '
          enum:
          - CREATING
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - FAILED
          type: string
          x-obmcs-top-level-enum: '#/definitions/ActionType'
        entityType:
          description: The resource type the work request affects.
          type: string
        entityUri:
          description: The URI path that the user can do a GET on to access the resource metadata.
          type: string
        identifier:
          description: The identifier of the resource the work request affects.
          type: string
        metadata:
          additionalProperties:
            type: string
          description: Additional information that helps to explain the resource.
          enum:
          - VM_CLUSTER_ID
          - HOSTNAMES
          - COMMANDS
          - RESULT_LOCATION
          - IS_DRY_RUN
          type: object
          x-obmcs-top-level-enum: '#/definitions/WorkRequestResourceMetadataKey'
      required:
      - actionType
      - entityType
      - identifier
    WorkRequestSummary_2:
      description: A summary of the status of a work request.
      properties:
        compartmentId:
          description: 'The ocid of the compartment that contains the work request. Work requests should be scoped to

            the same compartment as the resource the work request affects. If the work request affects multiple resources,

            and those resources are not in the same compartment, it is up to the service team to pick the primary

            resource whose compartment should be used

            '
          type: string
        id:
          description: The id of the work request.
          type: string
        operationType:
          description: Type of the work request
          enum:
          - CREATE_DELEGATION_CONTROL
          - UPDATE_DELEGATION_CONTROL
          - DELETE_DELEGATION_CONTROL
          - MOVE_DELEGATION_CONTROL
          - CREATE_DELEGATED_RESOURCE_ACCESS_REQUEST
          - APPROVE_DELEGATED_RESOURCE_ACCESS_REQUEST
          - REJECT_DELEGATED_RESOURCE_ACCESS_REQUEST
          - REVOKE_DELEGATED_RESOURCE_ACCESS_REQUEST
          - CREATE_DELEGATION_SUBSCRIPTION
          - UPDATE_DELEGATION_SUBSCRIPTION
          - DELETE_DELEGATION_SUBSCRIPTION
          - MOVE_DELEGATION_SUBSCRIPTION
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_2'
          type: array
        status:
          description: Status of current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - WAITING
          - NEEDS_ATTENTION
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequest_2:
      description: A description of workrequest status
      properties:
        compartmentId:
          description: 'The ocid of the compartment that contains the work request. Work requests should be scoped to

            the same compartment as the resource the work request affects. If the work request affects multiple resources,

            and those resources are not in the same compartment, it is up to the service team to pick the primary

            resource whose compartment should be used

            '
          type: string
        id:
          description: The id of the work request.
          type: string
        operationType:
          description: Type of the work request
          enum:
          - CREATE_DELEGATION_CONTROL
          - UPDATE_DELEGATION_CONTROL
          - DELETE_DELEGATION_CONTROL
          - MOVE_DELEGATION_CONTROL
          - CREATE_DELEGATED_RESOURCE_ACCESS_REQUEST
          - APPROVE_DELEGATED_RESOURCE_ACCESS_REQUEST
          - REJECT_DELEGATED_RESOURCE_ACCESS_REQUEST
          - REVOKE_DELEGATED_RESOURCE_ACCESS_REQUEST
          - CREATE_DELEGATION_SUBSCRIPTION
          - UPDATE_DELEGATION_SUBSCRIPTION
          - DELETE_DELEGATION_SUBSCRIPTION
          - MOVE_DELEGATION_SUBSCRIPTION
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_2'
          type: array
        status:
          description: Status of current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - WAITING
          - NEEDS_ATTENTION
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    Error_2:
      description: Error Information.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    WorkRequestLogEntry_2:
      description: A log message from the execution of a work request.
      properties:
        message:
          description: Human-readable log message.
          type: string
        timestamp:
          description: The time the log message was written. An RFC3339 formatted datetime string
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequestResource_2:
      description: A resource created or operated on by a work request.
      properties:
        actionType:
          description: 'The way in which this resource is affected by the work tracked in the work request.

            A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

            work is complete for that resource at which point it will transition to CREATED, UPDATED,

            or DELETED, respectively.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - RELATED
          - FAILED
          type: string
          x-obmcs-top-level-enum: '#/definitions/ActionType'
        entityType:
          description: The resource type the work request affects.
          type: string
        entityUri:
          description: The URI path that the user can do a GET on to access the resource metadata
          type: string
        identifier:
          description: The identifier of the resource the work request affects.
          type: string
        metadata:
          additionalProperties:
            type: string
          description: Additional information that helps to explain the resource.
          enum:
          - VM_CLUSTER_ID
          - HOSTNAMES
          - COMMANDS
          - RESULT_LOCATION
          type: object
          x-obmcs-top-level-enum: '#/definitions/WorkRequestResourceMetadataKey'
      required:
      - actionType
      - entityType
      - identifier
    WorkRequestSummary_3:
      description: A summary of the status of a work request.
      properties:
        compartmentId:
          description: 'The ocid of the compartment that contains the work request. Work requests should be scoped to

            the same compartment as the resource the work request affects. If the work request affects multiple resources,

            and those resources are not in the same compartment, it is up to the service team to pick the primary

            resource whose compartment should be used

            '
          type: string
        id:
          description: The id of the work request.
          type: string
        operationType:
          description: Type of the work request
          enum:
          - CREATE_LOCKBOX
          - UPDATE_LOCKBOX
          - DELETE_LOCKBOX
          - MOVE_LOCKBOX
          - CREATE_ACCESS_REQUEST
          - APPROVE_ACCESS_REQUEST
          - REVOKE_ACCESS_REQUEST
          - CREATE_APPROVAL_TEMPLATE
          - MOVE_APPROVAL_TEMPLATE
          - UPDATE_APPROVAL_TEMPLATE
          - DELETE_APPROVAL_TEMPLATE
          - CREATE_PARTNER
          - REMIND_ACCESS_REQUEST
          - CREATE_ACCESSCONTEXTATTRIBUTE
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_3'
          type: array
        status:
          description: Status of current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - WAITING
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          - NEEDS_ATTENTION
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequest_3:
      description: A description of workrequest status
      properties:
        compartmentId:
          description: 'The ocid of the compartment that contains the work request. Work requests should be scoped to

            the same compartment as the resource the work request affects. If the work request affects multiple resources,

            and those resources are not in the same compartment, it is up to the service team to pick the primary

            resource whose compartment should be used

            '
          type: string
        id:
          description: The id of the work request.
          type: string
        operationType:
          description: Type of the work request
          enum:
          - CREATE_LOCKBOX
          - UPDATE_LOCKBOX
          - DELETE_LOCKBOX
          - MOVE_LOCKBOX
          - CREATE_ACCESS_REQUEST
          - APPROVE_ACCESS_REQUEST
          - REVOKE_ACCESS_REQUEST
          - CREATE_APPROVAL_TEMPLATE
          - MOVE_APPROVAL_TEMPLATE
          - UPDATE_APPROVAL_TEMPLATE
          - DELETE_APPROVAL_TEMPLATE
          - CREATE_PARTNER
          - REMIND_ACCESS_REQUEST
          - CREATE_ACCESSCONTEXTATTRIBUTE
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_3'
          type: array
        status:
          description: Status of current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - WAITING
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          - NEEDS_ATTENTION
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    Error_3:
      description: Error Information.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    WorkRequestLogEntry_3:
      description: A log message from the execution of a work request.
      properties:
        message:
          description: Human-readable log message.
          type: string
        timestamp:
          description: The time the log message was written. An RFC3339 formatted datetime string
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequestResource_3:
      description: A resource created or operated on by a work request.
      properties:
        actionType:
          description: 'The way in which this resource is affected by the work tracked in the work request.

            A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

            work is complete for that resource at which point it will transition to CREATED, UPDATED,

            or DELETED, respectively.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - RELATED
          - FAILED
          type: string
          x-obmcs-top-level-enum: '#/definitions/ActionType'
        entityType:
          description: The resource type the work request affects.
          type: string
        entityUri:
          description: The URI path that the user can do a GET on to access the resource metadata
          type: string
        identifier:
          description: The identifier of the resource the work request affects.
          type: string
        metadata:
          additionalProperties:
            type: string
          description: Additional information that helps to explain the resource.
          enum:
          - IS_DRY_RUN
          type: object
          x-obmcs-top-level-enum: '#/definitions/WorkRequestResourceMetadataKey'
      required:
      - actionType
      - entityType
      - identifier
    WorkRequest_4:
      description: A description of the work request status.
      properties:
        compartmentId:
          description: 'The OCID of the compartment that contains the work request.

            '
          type: string
        id:
          description: The ID of the work request.
          type: string
        operationType:
          description: Type of the work request.
          enum:
          - CREATE_SENDER_INVITATION
          - ACCEPT_RECIPIENT_INVITATION
          - CANCEL_SENDER_INVITATION
          - COMPLETE_ORDER_ACTIVATION
          - ACTIVATE_ORDER_EXISTING_TENANCY
          - REGISTER_DOMAIN
          - RELEASE_DOMAIN
          - CREATE_CHILD_TENANCY
          - ASSIGN_DEFAULT_SUBSCRIPTION
          - MANUAL_LINK_CREATION
          - TERMINATE_ORGANIZATION_TENANCY
          - UPDATE_SAAS_CAPABILITY
          - SOFT_TERMINATE_TENANCY
          - HARD_TERMINATE_TENANCY
          - RESTORE_TENANCY
          - LOG_TENANCY_TERMINATION_REQUEST
          - STANDALONE_TENANCY_TERMINATION_REQUEST
          - SELF_OPT_IN
          - SELF_OPT_OUT
          - ORDER_PAYLOADS_BACKFILL_ETL
          - PUBLISH_TO_TOPIC
          - TERMINATE_LINK
          - TRANSFER_SUBSCRIPTION
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by the work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_4'
          type: array
        status:
          description: Status of the current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequestLogEntryCollection_2:
      description: Result of a query request for a list of work request log entries. Contains WorkRequestLogEntry items.
      properties:
        items:
          description: Array containing WorkRequestLogEntry items.
          items:
            $ref: '#/components/schemas/WorkRequestLogEntry_4'
          type: array
      required:
      - items
      type: object
    WorkRequestError_2:
      description: An error encountered while executing a work request.
      properties:
        code:
          description: 'A machine-usable code for the error that occured. Error codes are listed in

            [API Errors](/iaas/Content/API/References/apierrors.htm).

            '
          type: string
        message:
          description: A human-readable description of the issue encountered.
          type: string
        timestamp:
          description: The time the error occured. An RFC 3339 formatted date and time string.
          format: date-time
          type: string
      required:
      - code
      - message
      - timestamp
    WorkRequestResource_4:
      description: A resource created or operated on by a work request.
      properties:
        actionType:
          description: 'Indicates how the resource is affected by the work tracked in the work request.

            A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

            work is complete for that resource, at which point it will transition to CREATED, UPDATED,

            or DELETED, respectively.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - RELATED
          type: string
          x-obmcs-top-level-enum: '#/definitions/ActionType'
        entityType:
          description: The resource type the work request affects.
          type: string
        entityUri:
          description: The URI path so that the user can do a GET to access the resource metadata.
          type: string
        identifier:
          description: The identifier of the resource the work request affects.
          type: string
      required:
      - actionType
      - entityType
      - identifier
    WorkRequestErrorCollection_2:
      description: Result of a query request for a list of work request errors. Contains WorkRequestError items.
      properties:
        items:
          description: Array containing WorkRequestError items.
          items:
            $ref: '#/components/schemas/WorkRequestError_2'
          type: array
      required:
      - items
      type: object
    Error_4:
      description: Error information.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    WorkRequestCollection:
      description: Result of a query request for a list of work requests. Contains WorkRequestSummary items.
      properties:
        items:
          description: Array containing WorkRequestSummary items.
          items:
            $ref: '#/components/schemas/WorkRequestSummary_4'
          type: array
      required:
      - items
      type: object
    WorkRequestLogEntry_4:
      description: A log message from the execution of a work request.
      properties:
        message:
          description: Human-readable log message.
          type: string
        timestamp:
          description: The time the log message was written. An RFC 3339 formatted date and time string.
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequestSummary_4:
      description: A summary of work request status.
      properties:
        compartmentId:
          description: 'The OCID of the compartment that contains the work request.

            '
          type: string
        id:
          description: The ID of the work request.
          type: string
        operationType:
          description: Type of the work request.
          enum:
          - CREATE_SENDER_INVITATION
          - ACCEPT_RECIPIENT_INVITATION
          - CANCEL_SENDER_INVITATION
          - COMPLETE_ORDER_ACTIVATION
          - ACTIVATE_ORDER_EXISTING_TENANCY
          - REGISTER_DOMAIN
          - RELEASE_DOMAIN
          - CREATE_CHILD_TENANCY
          - ASSIGN_DEFAULT_SUBSCRIPTION
          - MANUAL_LINK_CREATION
          - TERMINATE_ORGANIZATION_TENANCY
          - UPDATE_SAAS_CAPABILITY
          - SOFT_TERMINATE_TENANCY
          - HARD_TERMINATE_TENANCY
          - RESTORE_TENANCY
          - LOG_TENANCY_TERMINATION_REQUEST
          - STANDALONE_TENANCY_TERMINATION_REQUEST
          - SELF_OPT_IN
          - SELF_OPT_OUT
          - ORDER_PAYLOADS_BACKFILL_ETL
          - PUBLISH_TO_TOPIC
          - TERMINATE_LINK
          - TRANSFER_SUBSCRIPTION
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by the work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_4'
          type: array
        status:
          description: Status of the current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequest_5:
      description: A description of the work request status.
      properties:
        compartmentId:
          description: 'The OCID of the compartment that contains the work request.

            '
          type: string
        id:
          description: The ID of the work request.
          type: string
        operationType:
          description: Type of the work request.
          enum:
          - CREATE_SENDER_INVITATION
          - ACCEPT_RECIPIENT_INVITATION
          - CANCEL_SENDER_INVITATION
          - COMPLETE_ORDER_ACTIVATION
          - ACTIVATE_ORDER_EXISTING_TENANCY
          - REGISTER_DOMAIN
          - RELEASE_DOMAIN
          - CREATE_CHILD_TENANCY
          - ASSIGN_DEFAULT_SUBSCRIPTION
          - MANUAL_LINK_CREATION
          - TERMINATE_ORGANIZATION_TENANCY
          - UPDATE_SAAS_CAPABILITY
          - SOFT_TERMINATE_TENANCY
          - HARD_TERMINATE_TENANCY
          - RESTORE_TENANCY
          - LOG_TENANCY_TERMINATION_REQUEST
          - STANDALONE_TENANCY_TERMINATION_REQUEST
          - SELF_OPT_IN
          - SELF_OPT_OUT
          - ORDER_PAYLOADS_BACKFILL_ETL
          - PUBLISH_TO_TOPIC
          - TERMINATE_LINK
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_5'
          type: array
        status:
          description: Status of the current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequestLogEntryCollection_3:
      description: Result of a query request for a list of work request log entries. Contains WorkRequestLogEntry items.
      properties:
        items:
          description: Array containing WorkRequestLogEntry items.
          items:
            $ref: '#/components/schemas/WorkRequestLogEntry_5'
          type: array
      required:
      - items
      type: object
    WorkRequestError_3:
      description: An error encountered while executing a work request.
      properties:
        code:
          description: 'A machine-usable code for the error that occured. Error codes are listed in

            [API Errors](/iaas/Content/API/References/apierrors.htm).

            '
          type: string
        message:
          description: A human-readable description of the issue encountered.
          type: string
        timestamp:
          description: The time the error occured. An RFC 3339-formatted date and time string.
          format: date-time
          type: string
      required:
      - code
      - message
      - timestamp
    WorkRequestResource_5:
      description: A resource created or operated on by a work request.
      properties:
        actionType:
          description: 'The way in which this resource is affected by the work tracked in the work request.

            A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

            work is complete for that resource, at which point it will transition to CREATED, UPDATED,

            or DELETED, respectively.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - RELATED
          type: string
          x-obmcs-top-level-enum: '#/definitions/ActionType'
        entityType:
          description: The resource type the work request affects.
          type: string
        entityUri:
          description: The URI path that the user can do a GET on to access the resource metadata.
          type: string
        identifier:
          description: The identifier of the resource the work request affects.
          type: string
      required:
      - actionType
      - entityType
      - identifier
    WorkRequestErrorCollection_3:
      description: Result of a query request for a list of work request errors. Contains WorkRequestError items.
      properties:
        items:
          description: Array containing WorkRequestError items.
          items:
            $ref: '#/components/schemas/WorkRequestError_3'
          type: array
      required:
      - items
      type: object
    Error_5:
      description: Error information.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    WorkRequestCollection_2:
      description: Result of a query request for a list of work requests. Contains WorkRequestSummary items.
      properties:
        items:
          description: Array containing WorkRequestSummary items.
          items:
            $ref: '#/components/schemas/WorkRequestSummary_5'
          type: array
      required:
      - items
      type: object
    WorkRequestLogEntry_5:
      description: A log message from the execution of a work request.
      properties:
        message:
          description: Human-readable log message.
          type: string
        timestamp:
          description: The time the log message was written. An RFC 3339-formatted date and time string.
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequestSummary_5:
      description: A summary of work request status.
      properties:
        compartmentId:
          description: 'The OCID of the compartment that contains the work request.

            '
          type: string
        id:
          description: The ID of the work request.
          type: string
        operationType:
          description: Type of the work request.
          enum:
          - CREATE_SENDER_INVITATION
          - ACCEPT_RECIPIENT_INVITATION
          - CANCEL_SENDER_INVITATION
          - COMPLETE_ORDER_ACTIVATION
          - ACTIVATE_ORDER_EXISTING_TENANCY
          - REGISTER_DOMAIN
          - RELEASE_DOMAIN
          - CREATE_CHILD_TENANCY
          - ASSIGN_DEFAULT_SUBSCRIPTION
          - MANUAL_LINK_CREATION
          - TERMINATE_ORGANIZATION_TENANCY
          - UPDATE_SAAS_CAPABILITY
          - SOFT_TERMINATE_TENANCY
          - HARD_TERMINATE_TENANCY
          - RESTORE_TENANCY
          - LOG_TENANCY_TERMINATION_REQUEST
          - STANDALONE_TENANCY_TERMINATION_REQUEST
          - SELF_OPT_IN
          - SELF_OPT_OUT
          - ORDER_PAYLOADS_BACKFILL_ETL
          - PUBLISH_TO_TOPIC
          - TERMINATE_LINK
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationType'
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_5'
          type: array
        status:
          description: Status of current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

            section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequestErrorCollection_4:
      description: The set of work request errors returned for the [ListWorkRequestErrors](#/en/osmh/latest/WorkRequest/ListWorkRequestErrors) operation.
      properties:
        items:
          description: List of work request error objects.
          items:
            $ref: '#/components/schemas/WorkRequestError_4'
          type: array
      required:
      - items
      type: object
      x-example: "{\n  \"items\": [\n    {\n      \"code\": \"1\",\n      \"message\": \"Failed to get Installed: exit status 1: [Errno 28] No space left on device: '/var/cache/dnf/metadata_lock.pid'\",\n      \"timestamp\": \"2025-01-21T21:37:22.360Z\"\n    }\n  ]\n}\n"
    WorkRequestError_4:
      description: An error encountered while executing a work request.
      properties:
        code:
          description: 'A machine-usable code for the error that occurred. Error codes are listed on

            (https://docs.cloud.oracle.com/Content/API/References/apierrors.htm).

            '
          type: string
        message:
          description: A human readable description of the issue encountered.
          type: string
        timestamp:
          description: The time the error occurred. (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).
          format: date-time
          type: string
      required:
      - code
      - message
      - timestamp
    WorkRequestSummary_6:
      description: The summary of a work request.
      properties:
        childrenId:
          description: The list of [OCIDs](/iaas/Content/General/Concepts/identifiers.htm) for child work requests.
          items:
            type: string
          type: array
        compartmentId:
          description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment that contains the work request.

            Work requests should be scoped to the same compartment as the resource it affects.

            If the work request affects multiple resources the different compartments, the services selects the compartment of the primary resource.

            '
          type: string
        description:
          description: A short description about the work request.
          type: string
        displayName:
          description: A short display name for the work request.
          type: string
        id:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
          type: string
        isManagedByAutonomousLinux:
          description: Indicates whether this work request is managed by Autonomous Linux
          type: boolean
        message:
          description: A progress or error message, if there is any.
          type: string
        operationType:
          description: Type of the work request.
          enum:
          - INSTALL_PACKAGES
          - REMOVE_PACKAGES
          - UPDATE_PACKAGES
          - UPDATE_ALL_PACKAGES
          - UPDATE_SECURITY
          - UPDATE_BUGFIX
          - UPDATE_ENHANCEMENT
          - UPDATE_OTHER
          - UPDATE_KSPLICE_KERNEL
          - UPDATE_KSPLICE_USERSPACE
          - ENABLE_MODULE_STREAMS
          - DISABLE_MODULE_STREAMS
          - SWITCH_MODULE_STREAM
          - INSTALL_MODULE_PROFILES
          - REMOVE_MODULE_PROFILES
          - SET_SOFTWARE_SOURCES
          - LIST_PACKAGES
          - SET_MANAGEMENT_STATION_CONFIG
          - SYNC_MANAGEMENT_STATION_MIRROR
          - UPDATE_MANAGEMENT_STATION_SOFTWARE
          - UPDATE
          - MODULE_ACTIONS
          - LIFECYCLE_PROMOTION
          - CREATE_SOFTWARE_SOURCE
          - UPDATE_SOFTWARE_SOURCE
          - IMPORT_CONTENT
          - SYNC_AGENT_CONFIG
          - INSTALL_WINDOWS_UPDATES
          - LIST_WINDOWS_UPDATE
          - GET_WINDOWS_UPDATE_DETAILS
          - INSTALL_ALL_WINDOWS_UPDATES
          - INSTALL_SECURITY_WINDOWS_UPDATES
          - INSTALL_BUGFIX_WINDOWS_UPDATES
          - INSTALL_ENHANCEMENT_WINDOWS_UPDATES
          - INSTALL_OTHER_WINDOWS_UPDATES
          - REMOVE_CONTENT
          - UNREGISTER_MANAGED_INSTANCE
          - REBOOT
          - REGISTER_MANAGED_INSTANCE
          - LIST_SNAPS
          - INSTALL_SNAPS
          - REMOVE_SNAPS
          - SWITCH_SNAP_CHANNEL
          type: string
          x-obmcs-top-level-enum: '#/definitions/WorkRequestOperationType'
        parentId:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the parent work request, if there is any.
          type: string
        percentComplete:
          description: The percentage complete of the operation tracked by this work request.
          format: float
          type: number
        rebootTimeoutInMins:
          description: The number of minutes the service waits for the reboot to complete. If the managed instance doesn't reboot within the timeout, the service marks the reboot job as failed.
          maximum: 240
          minimum: 5
          type: integer
        status:
          description: Status of the work request.
          enum:
          - WAITING
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          - SKIPPED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeCreated:
          description: 'The date and time the request was created - as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the work request completed (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).

            '
          format: date-time
          type: string
        timeScheduled:
          description: The scheduled date and time to retry the work request (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - timeCreated
    WorkRequest_6:
      description: An object that defines a work request.
      properties:
        childrenId:
          description: The list of [OCIDs](/iaas/Content/General/Concepts/identifiers.htm) for the child work requests.
          items:
            type: string
          type: array
        compartmentId:
          description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment that contains the work request.

            Work requests should be scoped to the same compartment as the resource it affects.

            If the work request affects multiple resources the different compartments, the services selects the compartment of the primary resource.

            '
          type: string
        contentChecksum:
          description: 'The EventFingerprint associated with the content. This property is required when the work request type is IMPORT_CONTENT or REMOVE_CONTENT.

            '
          type: string
        contentLocation:
          description: 'The location of the bundle in the filesystem of the resource associated to this work request.

            '
          type: string
        description:
          description: A short description about the work request.
          type: string
        displayName:
          description: A short display name for the work request.
          type: string
        eventId:
          description: 'The event id of the content. This property is required when the work request type is IMPORT_CONTENT or REMOVE_CONTENT.

            '
          type: string
        id:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
          type: string
        initiatorId:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the resource that initiated the work request.
          type: string
        isManagedByAutonomousLinux:
          description: Indicates whether this work request is managed by the Autonomous Linux service.
          type: boolean
        managementStation:
          $ref: '#/components/schemas/WorkRequestManagementStationDetails'
        message:
          description: A progress or error message, if there is any.
          type: string
        moduleSpecs:
          description: The list of appstream modules being operated on.
          items:
            $ref: '#/components/schemas/ModuleSpecDetails'
          type: array
        operationType:
          description: Type of the work request.
          enum:
          - INSTALL_PACKAGES
          - REMOVE_PACKAGES
          - UPDATE_PACKAGES
          - UPDATE_ALL_PACKAGES
          - UPDATE_SECURITY
          - UPDATE_BUGFIX
          - UPDATE_ENHANCEMENT
          - UPDATE_OTHER
          - UPDATE_KSPLICE_KERNEL
          - UPDATE_KSPLICE_USERSPACE
          - ENABLE_MODULE_STREAMS
          - DISABLE_MODULE_STREAMS
          - SWITCH_MODULE_STREAM
          - INSTALL_MODULE_PROFILES
          - REMOVE_MODULE_PROFILES
          - SET_SOFTWARE_SOURCES
          - LIST_PACKAGES
          - SET_MANAGEMENT_STATION_CONFIG
          - SYNC_MANAGEMENT_STATION_MIRROR
          - UPDATE_MANAGEMENT_STATION_SOFTWARE
          - UPDATE
          - MODULE_ACTIONS
          - LIFECYCLE_PROMOTION
          - CREATE_SOFTWARE_SOURCE
          - UPDATE_SOFTWARE_SOURCE
          - IMPORT_CONTENT
          - SYNC_AGENT_CONFIG
          - INSTALL_WINDOWS_UPDATES
          - LIST_WINDOWS_UPDATE
          - GET_WINDOWS_UPDATE_DETAILS
          - INSTALL_ALL_WINDOWS_UPDATES
          - INSTALL_SECURITY_WINDOWS_UPDATES
          - INSTALL_BUGFIX_WINDOWS_UPDATES
          - INSTALL_ENHANCEMENT_WINDOWS_UPDATES
          - INSTALL_OTHER_WINDOWS_UPDATES
          - REMOVE_CONTENT
          - UNREGISTER_MANAGED_INSTANCE
          - REBOOT
          - REGISTER_MANAGED_INSTANCE
          - LIST_SNAPS
          - INSTALL_SNAPS
          - REMOVE_SNAPS
          - SWITCH_SNAP_CHANNEL
          type: string
          x-obmcs-top-level-enum: '#/definitions/WorkRequestOperationType'
        packageNames:
          description: A list of package names to be installed, updated, or removed.
          items:
            type: string
          type: array
        parentId:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the parent work request, if there is any.
          type: string
        percentComplete:
          description: The percentage complete of the operation tracked by this work request.
          format: float
          type: number
        rebootTimeoutInMins:
          description: The number of minutes the service waits for the reboot to complete. If the managed instance doesn't reboot within the timeout, the service marks the reboot job as failed.
          maximum: 240
          minimum: 5
          type: integer
        rerunOfId:
          description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the original work request that is being rerun.

            '
          type: string
        resources:
          description: The list of [OCIDs](/iaas/Content/General/Concepts/identifiers.htm) for the resources affected by the work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_6'
          type: array
        retryIntervals:
          description: "The amount of time in minutes to wait until retrying the work request. If set, the service will automatically retry \na failed work request after the interval. For example, An interval set to [2,5,10]. If the initial \nexecution of the work request fails, the service waits 2 minutes and then retries. If that fails, the service waits 5 minutes \nand then retries. If that fails, the service waits 10 minutes and then retries.\n"
          items:
            format: int32
            type: integer
          maxItems: 5
          type: array
        retryOfId:
          description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the original work request that is being retried.

            '
          type: string
        snapSpecs:
          description: The details about the snap.
          items:
            $ref: '#/components/schemas/SnapSpecDetails'
          maxItems: 255
          type: array
        status:
          description: Status of the work request.
          enum:
          - WAITING
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          - SKIPPED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeCreated:
          description: 'The date and time the work request was created (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the work request completed (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).

            '
          format: date-time
          type: string
        timeScheduled:
          description: The scheduled date and time to retry the work request (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the work request started (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).

            '
          format: date-time
          type: string
        timeUpdated:
          description: 'The date and time the work request started (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).

            '
          format: date-time
          type: string
        windowsUpdateNames:
          description: The UUIDs of the target Windows update (only used when operation type is INSTALL_WINDOWS_UPDATES).
          items:
            type: string
          maxItems: 255
          type: array
      required:
      - operationType
      - status
      - id
      - resources
      - compartmentId
      - percentComplete
      - timeCreated
      x-example: "{\n  \"operationType\": \"INSTALL_PACKAGES\",\n  \"status\": \"SUCCEEDED\",\n  \"id\": \"ocid1.osmhworkrequest.oc1.phx.exampleuniqueID\",\n  \"description\": null,\n  \"displayName\": \"Install ilorest-4.6.0.0-11.x86_64.rpm to noss-demo-a\",\n  \"message\": \"Instance SUCCESSFULLY completed execution of this work request.\",\n  \"parentId\": \"ocid1.osmhworkrequest.oc1.phx.exampleuniqueID\",\n  \"childrenId\": [],\n  \"compartmentId\": \"ocid1.tenancy.oc1..exampleuniqueID\",\n  \"resources\": [\n    {\n      \"entityType\": \"INSTANCE\",\n      \"actionType\": \"UPDATED\",\n      \"identifier\": \"ocid1.instance.oc1.phx.exampleuniqueID\",\n      \"entityUri\": null,\n      \"name\": null,\n      \"metadata\": null\n    }\n  ],\n  \"packageNames\": [\n    \"ilorest-4.6.0.0-11.x86_64\"\n  ],\n  \"windowsUpdateNames\": [],\n  \"moduleSpecs\": [],\n  \"percentComplete\": 100,\n  \"timeCreated\": \"2025-01-22T00:43:18.203Z\",\n  \"timeUpdated\": \"2025-01-22T00:43:50.137Z\",\n  \"timeStarted\": \"2025-01-22T00:43:22.033Z\",\n  \"timeFinished\": \"2025-01-22T00:43:50.043Z\",\n  \"initiatorId\": \"ocid1.osmhscheduledjob.oc1.phx.exampleuniqueID\",\n  \"managementStation\": null,\n  \"timeScheduled\": \"2025-01-22T00:43:18.203Z\",\n  \"contentLocation\": null,\n  \"eventId\": null,\n  \"contentChecksum\": null,\n  \"retryOfId\": null,\n  \"rerunOfId\": null,\n  \"retryIntervals\": [\n    5,\n    5,\n    5\n  ],\n  \"isManagedByAutonomousLinux\": false,\n  \"rebootTimeoutInMins\": null\n}\n"
    WorkRequestLogEntryCollection_4:
      description: The set of work request logs returned for the [ListWorkRequestLogs](#/en/osmh/latest/WorkRequest/ListWorkRequestLogs) operation.
      properties:
        items:
          description: List of work request log entries.
          items:
            $ref: '#/components/schemas/WorkRequestLogEntry_6'
          type: array
      required:
      - items
      type: object
      x-example: "{\n    \"items\": [\n        {\n            \"message\": \"This system is receiving updates from OSMH\",\n            \"timestamp\": \"2025-01-28T20:37:53.364Z\"\n        }\n    ]\n}\n"
    ModuleSpecDetails:
      description: Details about a specific appstream module.
      properties:
        name:
          description: Name of the module.
          type: string
        profile:
          description: The module profile to be used.
          type: string
        stream:
          description: The stream of the module.
          type: string
      required:
      - name
      type: object
    RerunWorkRequestDetails:
      description: Provides the information used to target specific resources for the rerun of a work request.
      properties:
        managedInstances:
          description: List of managed instance [OCIDs](/iaas/Content/General/Concepts/identifiers.htm) to affected by the rerun of the work request.
          items:
            type: string
          type: array
        workRequestDetails:
          $ref: '#/components/schemas/WorkRequestDetails'
      type: object
    WorkRequestManagementStationDetails:
      description: Details about management station actions.
      properties:
        config:
          description: The configuration of the management station.
          format: byte
          type: string
        managementStationVersion:
          description: Target version to update the management station software.
          type: string
        softwareSourceIds:
          description: Optional list for mirrors to sync.
          items:
            type: string
          type: array
      type: object
    Error_6:
      description: Provides the information for an error.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    SnapSpecDetails:
      description: Details about a specific snap.
      properties:
        channel:
          description: 'The channel to install from (e.g. stable, edge, beta, candidate, or a custom channel).

            '
          type: string
        isSigned:
          default: false
          description: 'If true, allows installing snaps not signed by the Snap Store. E.g., snaps from local file. Use with caution.

            '
          type: boolean
        mode:
          default: DEFAULT
          description: The modes for the snap.
          enum:
          - DEFAULT
          - CLASSIC
          - JAIL_MODE
          - DEV_MODE
          type: string
          x-obmcs-top-level-enum: '#/definitions/SnapModes'
        name:
          description: 'The name of the snap to install.

            '
          type: string
        revision:
          description: 'The revision to install.

            '
          type: string
        version:
          description: 'The version of the snap.

            '
          type: string
      required:
      - name
      type: object
    WorkRequestLogEntry_6:
      description: A log message from the execution of a work request.
      properties:
        message:
          description: A human readable log message.
          type: string
        timestamp:
          description: The date and time the log message was written (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format).
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequestSummaryCollection_2:
      description: The set of work requests returned for the [ListWorkRequests](#/en/osmh/latest/WorkRequest/ListWorkRequests) operation.
      properties:
        items:
          description: List of work requests.
          items:
            $ref: '#/components/schemas/WorkRequestSummary_6'
          type: array
      required:
      - items
      type: object
      x-example: "{\n  \"items\" : [{\n      \"operationType\": \"SYNC_MANAGEMENT_STATION_MIRROR\",\n      \"status\": \"SUCCEEDED\",\n      \"id\": \"ocid1.osmhworkrequest.oc1.phx.exampleuniqueID\",\n      \"description\": \"Recurring software source mirror sync for management station\",\n      \"displayName\": \"Sync software sources to station\",\n      \"message\": null,\n      \"parentId\": null,\n      \"childrenId\": [\n          \"ocid1.osmhworkrequest.oc1.phx.exampleuniqueID\"\n      ],\n      \"compartmentId\": \"ocid1.tenancy.oc1..exampleuniqueID\",\n      \"percentComplete\": 0.0,\n      \"timeCreated\": \"2025-01-28T01:01:37.737Z\",\n      \"timeScheduled\": null,\n      \"isManagedByAutonomousLinux\": false,\n      \"rebootTimeoutInMins\": null\n  },\n    {\n      \"operationType\": \"UPDATE\",\n      \"status\": \"SUCCEEDED\",\n      \"id\": \"ocid1.osmhworkrequest.oc1.phx.exampleuniqueID\",\n      \"description\": null,\n      \"displayName\": \"Attach instances to third_party-mig-noss-exampleuniqueID\",\n      \"message\": null,\n      \"parentId\": null,\n      \"childrenId\": [\n          \"ocid1.osmhworkrequest.oc1.phx.exampleuniqueID\"\n      ],\n      \"compartmentId\": \"ocid1.tenancy.oc1..exampleuniqueID\",\n      \"percentComplete\": 100.0,\n      \"timeCreated\": \"2025-01-16T17:49:33.877Z\",\n      \"timeScheduled\": null,\n      \"isManagedByAutonomousLinux\": false,\n      \"rebootTimeoutInMins\": null\n  }]\n}\n"
    WorkRequestDetails:
      description: Provides the name and description of the job.
      properties:
        description:
          description: User-specified information about the job. Avoid entering confidential information.
          maxLength: 400
          minLength: 0
          type: string
        displayName:
          description: A user-friendly name for the job. The name does not have to be unique. Avoid entering confidential information.
          maxLength: 255
          minLength: 1
          type: string
      type: object
    WorkRequestResource_6:
      description: A resource created or operated on by a work request.
      properties:
        actionType:
          description: 'The way in which this resource is affected by the work tracked in the work request.

            A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

            work is complete for that resource at which point it will transition to CREATED, UPDATED,

            or DELETED, respectively.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - RELATED
          - FAILED
          type: string
          x-obmcs-top-level-enum: '#/definitions/ActionType'
        entityType:
          description: The resource type that the work request affects.
          enum:
          - INSTANCE
          - GROUP
          - COMPARTMENT
          - LIFECYCLE_ENVIRONMENT
          - SOFTWARE_SOURCE
          - DYNAMIC_SET
          type: string
          x-obmcs-top-level-enum: '#/definitions/TargetResourceEntityType'
        entityUri:
          description: The URI path that the user can do a GET on to access the resource metadata.
          type: string
        identifier:
          description: The identifier of the resource the work request affects.
          type: string
        metadata:
          additionalProperties:
            type: string
          description: Additional information that helps to explain the resource.
          enum:
          - IS_DRY_RUN
          type: object
          x-obmcs-top-level-enum: '#/definitions/WorkRequestResourceMetadataKey'
        name:
          description: The name of the resource. Not all resources will have a name specified.
          type: string
      required:
      - actionType
      - entityType
      - identifier
    WorkRequest_7:
      description: An asynchronous work request.
      properties:
        compartmentId:
          description: 'The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment that

            contains the work request.

            '
          type: string
        id:
          description: The [OCID](/Content/General/Concepts/identifiers.htm) of the work request.
          type: string
        operationType:
          description: The asynchronous operation tracked by this work request.
          enum:
          - CREATE_SDDC
          - DELETE_SDDC
          - CREATE_ESXI_HOST
          - DELETE_ESXI_HOST
          - UPGRADE_HCX
          - DOWNGRADE_HCX
          - CANCEL_DOWNGRADE_HCX
          - REFRESH_HCX_LICENSE_STATUS
          - SWAP_BILLING
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationTypes'
        percentComplete:
          description: The percentage complete of the operation tracked by this work request.
          format: float
          type: number
        resources:
          description: The resources that are affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_7'
          type: array
        status:
          description: The status of the work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the work request was created, in the format defined by

            [RFC3339](https://tools.ietf.org/html/rfc3339).


            Example: `2016-08-25T21:10:29.600Z`

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the work request reached a terminal state, either `FAILED` OR

            `SUCCEEDED`. Format is defined by [RFC3339](https://tools.ietf.org/html/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the work request transitioned from `ACCEPTED` to `IN_PROGRESS`,

            in the format defined by [RFC3339](https://tools.ietf.org/html/rfc3339).

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequestLogEntryCollection_5:
      description: A list of work request logs.
      properties:
        items:
          description: A list of work request logs.
          items:
            $ref: '#/components/schemas/WorkRequestLogEntry_7'
          type: array
      required:
      - items
      type: object
    WorkRequestError_5:
      description: 'An error encountered while executing an operation that is tracked by a work request.

        '
      properties:
        code:
          description: 'A machine-usable code for the error that occurred.

            '
          type: string
        message:
          description: A human-readable error string.
          type: string
        timestamp:
          description: 'The date and time the error occurred, in the format defined

            by [RFC3339](https://tools.ietf.org/html/rfc3339).

            '
          format: date-time
          type: string
      required:
      - code
      - message
      - timestamp
    WorkRequestResource_7:
      description: 'A resource that is created or operated on by an asynchronous operation that is

        tracked by a work request.

        '
      properties:
        actionType:
          description: 'The way in which this resource was affected by the operation that spawned the

            work request.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - RELATED
          - FAILED
          type: string
          x-obmcs-top-level-enum: '#/definitions/ActionTypes'
        entityType:
          description: The resource type the work request affects.
          type: string
        entityUri:
          description: 'The URI path that you can use for a GET request to access the resource metadata.

            '
          type: string
        identifier:
          description: 'An [OCID](/Content/General/Concepts/identifiers.htm) or other unique identifier

            for the resource.

            '
          type: string
      required:
      - actionType
      - entityType
      - identifier
    WorkRequestErrorCollection_5:
      description: A list of work request errors.
      properties:
        items:
          description: A list of work request errors.
          items:
            $ref: '#/components/schemas/WorkRequestError_5'
          type: array
      required:
      - items
      type: object
    Error_7:
      description: Error Information.
      properties:
        code:
          description: 'A short error code that defines the error, meant for programmatic parsing. See

            [API Errors](/Content/API/References/apierrors.htm).

            '
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    WorkRequestCollection_3:
      description: A list of work requests.
      properties:
        items:
          description: A list of work requests.
          items:
            $ref: '#/components/schemas/WorkRequest_7'
          type: array
      required:
      - items
      type: object
    WorkRequestLogEntry_7:
      description: 'A log message from executing an operation that is tracked by a work request.

        '
      properties:
        message:
          description: A human-readable log message.
          type: string
        timestamp:
          description: 'The date and time the log message was written, in the format defined

            by [RFC3339](https://tools.ietf.org/html/rfc3339).

            '
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequest_8:
      description: An asynchronous work request.
      properties:
        compartmentId:
          description: 'The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment that

            contains the work request.

            '
          type: string
        id:
          description: The [OCID](/Content/General/Concepts/identifiers.htm) of the work request.
          type: string
        operationType:
          description: The asynchronous operation tracked by this work request.
          enum:
          - CREATE_SDDC
          - DELETE_SDDC
          - CREATE_CLUSTER
          - DELETE_CLUSTER
          - CREATE_ESXI_HOST
          - DELETE_ESXI_HOST
          - UPGRADE_HCX
          - DOWNGRADE_HCX
          - CANCEL_DOWNGRADE_HCX
          - REFRESH_HCX_LICENSE_STATUS
          - SWAP_BILLING
          - REPLACE_HOST
          - IN_PLACE_UPGRADE
          - CREATE_DATASTORE
          - UPDATE_DATASTORE
          - ADD_BLOCK_VOLUME_TO_DATASTORE
          - DELETE_DATASTORE
          - CREATE_DATASTORE_CLUSTER
          - UPDATE_DATASTORE_CLUSTER
          - ATTACH_DATASTORE_CLUSTER_TO_ESXI_HOST
          - ATTACH_DATASTORE_CLUSTER_TO_CLUSTER
          - DETACH_DATASTORE_CLUSTER_FROM_ESXI_HOST
          - DETACH_DATASTORE_CLUSTER_FROM_CLUSTER
          - DELETE_DATASTORE_CLUSTER
          - ADD_DATASTORE_TO_DATASTORE_CLUSTER
          - REMOVE_DATASTORE_FROM_DATASTORE_CLUSTER
          - CREATE_MANAGEMENT_APPLIANCE
          - UPDATE_MANAGEMENT_APPLIANCE
          - DELETE_MANAGEMENT_APPLIANCE
          - ABANDON_MANAGEMENT_APPLIANCE_JOBS
          - BATCH_DELETE_MANAGEMENT_APPLIANCE_JOBS
          - MANAGEMENT_APPLIANCE_ESXI_HOST_PROVISIONING
          - MANAGEMENT_APPLIANCE_ESXI_HOST_DELETION
          - MANAGEMENT_APPLIANCE_DATASTORE_PROVISIONING
          - MANAGEMENT_APPLIANCE_DATASTORE_DELETE
          - CREATE_BYOL
          - UPDATE_BYOL
          - DELETE_BYOL
          - CREATE_BYOL_ALLOCATION
          - UPDATE_BYOL_ALLOCATION
          - DELETE_BYOL_ALLOCATION
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationTypes'
        percentComplete:
          description: The percentage complete of the operation tracked by this work request.
          format: float
          type: number
        resources:
          description: The resources that are affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_8'
          type: array
        status:
          description: The status of the work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
        timeAccepted:
          description: 'The date and time the work request was created, in the format defined by

            [RFC3339](https://tools.ietf.org/html/rfc3339).


            Example: `2016-08-25T21:10:29.600Z`

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the work request reached a terminal state, either `FAILED` OR

            `SUCCEEDED`. Format is defined by [RFC3339](https://tools.ietf.org/html/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the work request transitioned from `ACCEPTED` to `IN_PROGRESS`,

            in the format defined by [RFC3339](https://tools.ietf.org/html/rfc3339).

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequestLogEntryCollection_6:
      description: A list of work request logs.
      properties:
        items:
          description: A list of work request logs.
          items:
            $ref: '#/components/schemas/WorkRequestLogEntry_8'
          type: array
      required:
      - items
      type: object
    WorkRequestError_6:
      description: 'An error encountered while executing an operation that is tracked by a work request.

        '
      properties:
        code:
          description: 'A machine-usable code for the error that occurred.

            '
          type: string
        message:
          description: A human-readable error string.
          type: string
        timestamp:
          description: 'The date and time the error occurred, in the format defined

            by [RFC3339](https://tools.ietf.org/html/rfc3339).

            '
          format: date-time
          type: string
      required:
      - code
      - message
      - timestamp
    WorkRequestResource_8:
      description: 'A resource that is created or operated on by an asynchronous operation that is

        tracked by a work request.

        '
      properties:
        actionType:
          description: 'The way in which this resource was affected by the operation that spawned the

            work request.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - RELATED
          - FAILED
          type: string
          x-obmcs-top-level-enum: '#/definitions/ActionTypes'
        entityType:
          description: The resource type the work request affects.
          type: string
        entityUri:
          description: 'The URI path that you can use for a GET request to access the resource metadata.

            '
          type: string
        identifier:
          description: 'An [OCID](/Content/General/Concepts/identifiers.htm) or other unique identifier

            for the resource.

            '
          type: string
      required:
      - actionType
      - entityType
      - identifier
    WorkRequestErrorCollection_6:
      description: A list of work request errors.
      properties:
        items:
          description: A list of work request errors.
          items:
            $ref: '#/components/schemas/WorkRequestError_6'
          type: array
      required:
      - items
      type: object
    Error_8:
      description: Error Information.
      properties:
        code:
          description: 'A short error code that defines the error, meant for programmatic parsing. See

            [API Errors](/Content/API/References/apierrors.htm).

            '
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    WorkRequestCollection_4:
      description: A list of work requests.
      properties:
        items:
          description: A list of work requests.
          items:
            $ref: '#/components/schemas/WorkRequest_8'
          type: array
      required:
      - items
      type: object
    WorkRequestLogEntry_8:
      description: 'A log message from executing an operation that is tracked by a work request.

        '
      properties:
        message:
          description: A human-readable log message.
          type: string
        timestamp:
          description: 'The date and time the log message was written, in the format defined

            by [RFC3339](https://tools.ietf.org/html/rfc3339).

            '
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequest_9:
      description: A description of WorkRequest status
      properties:
        compartmentId:
          description: 'The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment that contains the WorkRequest.

            WorkRequests should be scoped to the same compartment as the resource the work request affects.

            '
          type: string
        id:
          description: The [OCID](/Content/General/Concepts/identifiers.htm) of the WorkRequest.
          type: string
        operationType:
          description: Type of the WorkRequest
          enum:
          - CREATE_WAA_POLICY
          - UPDATE_WAA_POLICY
          - DELETE_WAA_POLICY
          - MOVE_WAA_POLICY
          - CREATE_WEB_APP_ACCELERATION
          - UPDATE_WEB_APP_ACCELERATION
          - DELETE_WEB_APP_ACCELERATION
          - MOVE_WEB_APP_ACCELERATION
          - PURGE_WEB_APP_ACCELERATION_CACHE
          type: string
        percentComplete:
          description: Percentage of the request completed.
          format: float
          type: number
        resources:
          description: The resources affected by this WorkRequest.
          items:
            $ref: '#/components/schemas/WorkRequestResource_9'
          type: array
        status:
          description: Status of current work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
        timeAccepted:
          description: 'The date and time the request was created, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the request was started, as described in

            [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
    WorkRequestLogEntryCollection_7:
      description: Result of a WorkRequestLog search. Contains both WorkRequestLog items and other information, such as metadata.
      properties:
        items:
          description: List of WorkRequestLogEntries.
          items:
            $ref: '#/components/schemas/WorkRequestLogEntry_9'
          type: array
      required:
      - items
      type: object
    WorkRequestError_7:
      description: An error encountered while executing a WorkRequest.
      properties:
        code:
          description: 'A machine-usable code for the error that occured. Error codes are listed on

            https://docs.cloud.oracle.com/Content/API/References/apierrors.htm.

            '
          type: string
        message:
          description: A human readable description of the issue encountered.
          type: string
        timestamp:
          description: The time the error occured. An RFC3339 formatted datetime string.
          format: date-time
          type: string
      required:
      - code
      - message
      - timestamp
    WorkRequestResource_9:
      description: A resource created or operated on by a WorkRequest.
      properties:
        actionType:
          description: 'The way in which this resource is affected by the work tracked in the WorkRequest.

            A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

            work is complete for that resource at which point it will transition to CREATED, UPDATED,

            or DELETED, respectively.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - IN_PROGRESS
          - RELATED
          type: string
        entityType:
          description: The resource type the WorkRequest affects.
          type: string
        entityUri:
          description: The URI path that the user can do a GET on to access the resource metadata.
          type: string
        identifier:
          description: The [OCID](/Content/General/Concepts/identifiers.htm) of the resource the WorkRequest affects.
          type: string
      required:
      - actionType
      - entityType
      - identifier
    WorkRequestErrorCollection_7:
      description: Result of a WorkRequestError search. Contains both WorkRequestError items and other information, such as metadata.
      properties:
        items:
          description: List of WorkRequestError objects.
          items:
            $ref: '#/components/schemas/WorkRequestError_7'
          type: array
      required:
      - items
      type: object
    Error_9:
      description: Error Information.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    WorkRequestCollection_5:
      description: Result of a WorkRequest search. Contains both WorkRequest items and other information, such as metadata.
      properties:
        items:
          description: List of WorkRequests.
          items:
            $ref: '#/components/schemas/WorkRequest_9'
          type: array
      required:
      - items
      type: object
    WorkRequestLogEntry_9:
      description: A log message from the execution of a WorkRequest.
      properties:
        message:
          description: Human-readable log message.
          type: string
        timestamp:
          description: The time the log message was written. An RFC3339 formatted datetime string.
          format: date-time
          type: string
      required:
      - message
      - timestamp
    WorkRequest_10:
      description: An asynchronous work request.
      properties:
        compartmentId:
          description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment

            that contains the work request.

            '
          type: string
        id:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
          type: string
        operationType:
          description: The asynchronous operation tracked by this work request.
          type: string
        percentComplete:
          description: The percentage complete of the operation tracked by this work request.
          format: float
          type: number
        resources:
          description: The resources that are affected by this work request.
          items:
            $ref: '#/components/schemas/WorkRequestResource_10'
          type: array
        status:
          description: The status of the work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
        timeAccepted:
          description: The date and time the work request was created, in the format defined by RFC3339.
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the work request reached a terminal state, either `FAILED` or `SUCCEEDED`.

            Format is defined by RFC3339.

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the work request transitioned from `ACCEPTED` to `IN_PROGRESS`,

            in the format defined by RFC3339.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - resources
      - percentComplete
      - timeAccepted
      x-example: "Example response: Work request that tracks the launch instance operation\n{\n  \"operationType\": \"LaunchInstance\",\n  \"status\": \"SUCCEEDED\",\n  \"id\": \"ocid1.coreservicesworkrequest.oc1.phx.exampleunique_ID\",\n  \"compartmentId\": \"ocid1.tenancy.oc1..exampleunique_ID\",\n  \"resources\": [\n    {\n      \"actionType\": \"CREATED\",\n      \"entityType\": \"instance\",\n      \"identifier\": \"ocid1.instance.oc1.phx.exampleunique_ID\",\n      \"entityUri\": \"/20160918/instances/ocid1.instance.oc1.phx.exampleunique_ID\"\n    }\n  ],\n  \"percentComplete\": 100.0,\n  \"timeAccepted\": \"2019-05-06T23:02:52.319Z\",\n  \"timeStarted\": \"2019-05-06T23:02:57.592Z\",\n  \"timeFinished\": \"2019-05-06T23:03:59.744Z\"\n}\n"
    WorkRequestResource_10:
      description: 'A resource that is created or operated on by an asynchronous operation that is tracked by

        a work request.

        '
      properties:
        actionType:
          description: 'The way in which this resource was affected by the operation that spawned the work

            request.

            '
          enum:
          - CREATED
          - UPDATED
          - DELETED
          - RELATED
          - IN_PROGRESS
          type: string
        entityType:
          description: The resource type the work request affects.
          type: string
        entityUri:
          description: The URI path that you can use for a GET request to access the resource metadata.
          type: string
        identifier:
          description: 'An [OCID](/iaas/Content/General/Concepts/identifiers.htm) or other unique identifier for the

            resource.

            '
          type: string
      required:
      - actionType
      - entityType
      - identifier
    Error_10:
      description: An error code and message from an API request failure.
      properties:
        code:
          description: 'A short error code that defines the error, meant for programmatic parsing. See

            [API Errors](/iaas/Content/API/References/apierrors.htm).

            '
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    WorkRequestError_8:
      description: An error encountered while executing an operation that is tracked by a work request.
      properties:
        code:
          description: A machine-usable code for the error that occured.
          type: string
        message:
          description: A human-readable error string.
          type: string
        timestamp:
          description: The date and time the error occurred.
          format: date-time
          type: string
      required:
      - code
      - message
      - timestamp
      x-example: "{\n  \"code\" : \"InternalError\",\n  \"message\" : \"Launch instance failed, Retrying failed to complete successfully after 5 attempts.\",\n  \"timestamp\" : \"2019-05-07T01:04:43.543Z\"\n}\n"
    WorkRequestLogEntry_10:
      description: A log message from executing an operation that is tracked by a work request.
      properties:
        message:
          description: A human-readable log message.
          type: string
        timestamp:
          description: The date and time the log message was written.
          format: date-time
          type: string
      required:
      - message
      - timestamp
      x-example: "{\n  \"message\" : \"Powering on virtual machine.\",\n  \"timestamp\" : \"2019-05-07T00:50:44.446Z\"\n}\n"
    WorkRequestSummary_7:
      description: A summary of the status of a work request.
      properties:
        compartmentId:
          description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment containing

            this work request.

            '
          type: string
        id:
          description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
          type: string
        operationType:
          description: The asynchronous operation tracked by this work request.
          type: string
        percentComplete:
          description: The percentage complete of the operation tracked by this work request.
          format: float
          type: number
        status:
          description: The status of the work request.
          enum:
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          type: string
        timeAccepted:
          description: The date and time the work request was created, in the format defined by RFC3339.
          format: date-time
          type: string
        timeFinished:
          description: 'The date and time the work request reached a terminal state, either `FAILED` or `SUCCEEDED`.

            Format is defined by RFC3339.

            '
          format: date-time
          type: string
        timeStarted:
          description: 'The date and time the work request transitioned from `ACCEPTED` to `IN_PROGRESS`, in

            the format defined by RFC3339.

            '
          format: date-time
          type: string
      required:
      - operationType
      - status
      - id
      - compartmentId
      - percentComplete
      - timeAccepted
      x-example: "{\n  \"operationType\":\"LaunchInstancesInPool\",\n  \"status\":\"SUCCEEDED\",\n  \"id\":\"ocid1.coreservicesworkrequest.oc1.phx.exampleunique_ID\",\n  \"compartmentId\":\"ocid1.tenancy.oc1..exampleuniqueID\",\n  \"percentComplete\":100.0,\n  \"timeAccepted\":\"2019-05-07T00:50:03.248Z\",\n  \"timeStarted\":\"2019-05-07T00:50:03.374Z\",\n  \"timeFinished\":\"2019-05-07T00:52:05.704Z\"\n}\n"
  parameters:
    WorkRequestSortByQueryParam:
      description: 'The field to sort by. Only one sort order may be provided. Default order for timeAccepted is descending.

        '
      in: query
      name: sortBy
      schema:
        type: string
        enum:
        - timeAccepted
        default: timeAccepted
    SortOrderQueryParam:
      description: The sort order to use, either 'asc' or 'desc'.
      in: query
      name: sortOrder
      x-default-description: 'null'
      x-obmcs-top-level-enum: '#/definitions/SortOrder'
      schema:
        type: string
        enum:
        - ASC
        - DESC
        default: DESC
    WorkRequestIdPathParam:
      description: The ID of the asynchronous request.
      in: path
      name: workRequestId
      required: true
      schema:
        type: string
    WorkRequestStatusQueryParam:
      description: A filter to return only work requests with the given OperationStatus.
      in: query
      name: status
      required: false
      x-default-description: 'null'
      x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      schema:
        type: string
        enum:
        - ACCEPTED
        - IN_PROGRESS
        - WAITING
        - NEEDS_ATTENTION
        - FAILED
        - SUCCEEDED
        - CANCELING
        - CANCELED
    WorkRequestLogAndErrorSortByQueryParam:
      description: 'The field to sort by. Only one sort order may be provided. Default order for timestamp is descending.

        '
      in: query
      name: sortBy
      schema:
        type: string
        enum:
        - timestamp
        default: timestamp
    RequestIdHeader:
      description: The client request ID for tracing.
      in: header
      name: opc-request-id
      schema:
        type: string
    PaginationTokenQueryParam:
      description: The page token representing the page at which to start retrieving results. This is usually retrieved from a previous list call.
      in: query
      name: page
      x-default-description: 'null'
      schema:
        type: string
        minLength: 1
    WorkRequestIdQueryParam:
      description: The ID of the asynchronous work request.
      in: query
      name: workRequestId
      x-default-description: 'null'
      schema:
        type: string
    PaginationLimitQueryParam:
      description: The maximum number of items to return.
      in: query
      name: limit
      x-default-description: 'null'
      schema:
        type: integer
        default: 10
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam:
      description: The [ID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment.
      in: query
      name: compartmentId
      required: true
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 255
        minLength: 1
    ResourceIdentifierQueryParam:
      description: The ID of the resource affected by the work request.
      in: query
      name: resourceId
      x-default-description: 'null'
      schema:
        type: string
    IfMatchHeader:
      description: 'For optimistic concurrency control. In the PUT or DELETE call

        for a resource, set the `if-match` parameter to the value of the

        etag from a previous GET or POST response for that resource.

        The resource will be updated or deleted only if the etag you

        provide matches the resource''s current etag value.

        '
      in: header
      name: if-match
      required: false
      schema:
        type: string
    SortOrderQueryParam_2:
      description: The sort order to use, either 'asc' or 'desc'.
      in: query
      name: sortOrder
      x-obmcs-top-level-enum: '#/definitions/SortOrders'
      schema:
        type: string
        enum:
        - ASC
        - DESC
        default: DESC
    PaginationTokenQueryParam_2:
      description: The page token representing the page at which to start retrieving results. This is usually retrieved from a previous list call.
      in: query
      name: page
      schema:
        type: string
        minLength: 1
    PaginationLimitQueryParam_2:
      description: The maximum number of items to return.
      in: query
      name: limit
      schema:
        type: integer
        default: 10
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam_2:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment.
      in: query
      name: compartmentId
      required: true
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 255
        minLength: 1
    SortOrderQueryParam_3:
      description: The sort order to use, either 'ASC' or 'DESC'.
      in: query
      name: sortOrder
      x-default-description: 'The default value depends upon `sortBy`, and

        in general is ''DESC'' when sorting by time and ''ASC'' otherwise.

        '
      x-obmcs-top-level-enum: '#/definitions/SortOrder'
      schema:
        type: string
        enum:
        - ASC
        - DESC
    WorkRequestStatusQueryParam_2:
      description: A filter to return only resources their lifecycleState matches the given OperationStatus.
      in: query
      name: status
      required: false
      x-default-description: 'null'
      x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      schema:
        type: string
        enum:
        - ACCEPTED
        - IN_PROGRESS
        - WAITING
        - FAILED
        - SUCCEEDED
        - CANCELING
        - CANCELED
        - NEEDS_ATTENTION
    PaginationTokenQueryParam_3:
      description: A token representing the position at which to start retrieving results. This must come from the `opc-next-page` header field of a previous response.
      in: query
      name: page
      x-default-description: 'null'
      schema:
        type: string
        minLength: 1
    PaginationLimitQueryParam_3:
      description: The maximum number of items to return.
      in: query
      name: limit
      schema:
        type: integer
        default: 10
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam_3:
      description: The ID of the compartment in which to list resources.
      in: query
      name: compartmentId
      x-default-description: 'null'
      schema:
        type: string
    SortOrderQueryParam_4:
      description: The sort order to use, whether 'asc' or 'desc'.
      in: query
      name: sortOrder
      x-default-description: The default order depends on what parameter is being sorted by.
      x-obmcs-top-level-enum: '#/definitions/SortOrder'
      schema:
        type: string
        enum:
        - ASC
        - DESC
    PaginationLimitQueryParam_4:
      description: The maximum number of items to return.
      in: query
      name: limit
      schema:
        type: integer
        default: 100
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam_4:
      description: The ID of the compartment in which to list resources.
      in: query
      name: compartmentId
      required: true
      schema:
        type: string
        maxLength: 255
    SortOrderQueryParam_5:
      description: The sort order to use, whether 'asc' or 'desc'.
      in: query
      name: sortOrder
      x-default-description: The default order depends on what parameter is being sorted by.
      x-obmcs-top-level-enum: '#/definitions/SortOrder'
      schema:
        type: string
        enum:
        - ASC
        - DESC
    PaginationLimitQueryParam_5:
      description: The maximum number of items to return.
      in: query
      name: limit
      schema:
        type: integer
        default: 100
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam_5:
      description: OCID of the compartment. Always a tenancy OCID.
      in: query
      name: compartmentId
      required: true
      schema:
        type: string
        maxLength: 255
    RetryOfIdQueryParam:
      description: 'A filter to return the work requests that are a retry of the given work request [OCIDs](/iaas/Content/General/Concepts/identifiers.htm).

        '
      in: query
      name: retryOfId
      x-default-description: 'null'
      schema:
        type: string
    IsManagedByAutonomousLinuxQueryParam:
      description: 'Indicates whether to list only resources managed by the Autonomous Linux service.

        '
      in: query
      name: isManagedByAutonomousLinux
      required: false
      x-default-description: 'null'
      schema:
        type: boolean
    CompartmentIdInSubTreeQueryParam:
      description: Indicates whether to include subcompartments in the returned results. Default is false.
      in: query
      name: compartmentIdInSubtree
      required: false
      x-default-description: false
      schema:
        type: boolean
    ParentResourcesNotEqualTo:
      description: 'A filter to return the resources whose parent resources are not the same as the given resource [OCIDs](/iaas/Content/General/Concepts/identifiers.htm).

        '
      in: query
      name: parentResourcesNotEqualTo
      required: false
      x-default-description: 'null'
      style: form
      explode: true
      schema:
        type: array
        items:
          type: string
        maxItems: 5
    RerunOfIdQueryParam:
      description: 'A filter to return the work requests that are a rerun of the given work request [OCIDs](/iaas/Content/General/Concepts/identifiers.htm).

        '
      in: query
      name: rerunOfId
      x-default-description: 'null'
      schema:
        type: string
    WorkRequestOperationTypeQueryParam:
      description: The asynchronous operation tracked by this work request. The filter returns only resources that match the given OperationType.
      in: query
      name: operationType
      required: false
      x-default-description: 'null'
      style: form
      explode: true
      schema:
        type: array
        items:
          enum:
          - INSTALL_PACKAGES
          - REMOVE_PACKAGES
          - UPDATE_PACKAGES
          - UPDATE_ALL_PACKAGES
          - UPDATE_SECURITY
          - UPDATE_BUGFIX
          - UPDATE_ENHANCEMENT
          - UPDATE_OTHER
          - UPDATE_KSPLICE_KERNEL
          - UPDATE_KSPLICE_USERSPACE
          - ENABLE_MODULE_STREAMS
          - DISABLE_MODULE_STREAMS
          - SWITCH_MODULE_STREAM
          - INSTALL_MODULE_PROFILES
          - REMOVE_MODULE_PROFILES
          - SET_SOFTWARE_SOURCES
          - LIST_PACKAGES
          - SET_MANAGEMENT_STATION_CONFIG
          - SYNC_MANAGEMENT_STATION_MIRROR
          - UPDATE_MANAGEMENT_STATION_SOFTWARE
          - UPDATE
          - MODULE_ACTIONS
          - LIFECYCLE_PROMOTION
          - CREATE_SOFTWARE_SOURCE
          - UPDATE_SOFTWARE_SOURCE
          - IMPORT_CONTENT
          - SYNC_AGENT_CONFIG
          - INSTALL_WINDOWS_UPDATES
          - LIST_WINDOWS_UPDATE
          - GET_WINDOWS_UPDATE_DETAILS
          - INSTALL_ALL_WINDOWS_UPDATES
          - INSTALL_SECURITY_WINDOWS_UPDATES
          - INSTALL_BUGFIX_WINDOWS_UPDATES
          - INSTALL_ENHANCEMENT_WINDOWS_UPDATES
          - INSTALL_OTHER_WINDOWS_UPDATES
          - REMOVE_CONTENT
          - UNREGISTER_MANAGED_INSTANCE
          - REBOOT
          - REGISTER_MANAGED_INSTANCE
          - LIST_SNAPS
          - INSTALL_SNAPS
          - REMOVE_SNAPS
          - SWITCH_SNAP_CHANNEL
          type: string
          x-obmcs-top-level-enum: '#/definitions/WorkRequestOperationType'
    WorkRequestSortByQueryParam_2:
      description: 'The field to sort by. Only one sort order may be provided.

        Default order for timeCreated is descending.

        '
      in: query
      name: sortBy
      schema:
        type: string
        enum:
        - timeCreated
        - displayName
        default: timeCreated
    WorkRequestTimeCreatedLessThanQueryParam:
      description: 'A filter to return work requests that were created before the date provided (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format). Example: ''2023-08-25T21:10:29.600Z''

        '
      in: query
      name: timeCreatedLessThan
      x-default-description: 'null'
      schema:
        type: string
        format: date-time
    InitiatorIdQueryParam:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the schedule job that initiated the work request.
      in: query
      name: initiatorId
      x-default-description: 'null'
      schema:
        type: string
    SortOrderQueryParam_6:
      description: The sort order to use, either 'ASC' or 'DESC'.
      in: query
      name: sortOrder
      x-default-description: 'The default value depends upon `sortBy`, and

        in general is ''DESC'' when sorting by time and ''ASC'' otherwise.

        '
      x-obmcs-top-level-enum: '#/definitions/SortOrder'
      schema:
        type: string
        enum:
        - ASC
        - DESC
    ParentIdQueryParam:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the parent work request.
      in: query
      name: parentId
      schema:
        type: string
    WorkRequestIdPathParam_2:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
      in: path
      name: workRequestId
      required: true
      schema:
        type: string
    DisplayNameContainsQueryParam:
      description: A filter to return resources that may partially match the given display name.
      in: query
      name: displayNameContains
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 255
        minLength: 1
    WorkRequestStatusQueryParam_3:
      description: A filter to return work requests that match the given status.
      in: query
      name: status
      required: false
      x-default-description: 'null'
      style: form
      explode: true
      schema:
        type: array
        items:
          enum:
          - WAITING
          - ACCEPTED
          - IN_PROGRESS
          - FAILED
          - SUCCEEDED
          - CANCELING
          - CANCELED
          - SKIPPED
          type: string
          x-obmcs-top-level-enum: '#/definitions/OperationStatus'
    RequestIdHeader_2:
      description: Unique Oracle-assigned identifier for the request. If you need to contact Oracle about a particular request, please provide the request ID.
      in: header
      name: opc-request-id
      schema:
        type: string
    PaginationTokenQueryParam_4:
      description: 'For list pagination. The value of the `opc-next-page` response header from the previous "List" call.

        For important details about how pagination works, see [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).


        Example: `3`

        '
      in: query
      name: page
      x-default-description: 'null'
      schema:
        type: string
        minLength: 1
    WorkRequestIdQueryParam_2:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
      in: query
      name: workRequestId
      x-default-description: 'null'
      schema:
        type: string
    RetryTokenHeader:
      description: 'A token that uniquely identifies a request so it can be retried in case of a timeout or

        server error without risk of executing that same action again. Retry tokens expire after 24

        hours, but can be invalidated before then due to conflicting operations. For example, if a resource

        has been deleted and purged from the system, then a retry of the original creation request

        might be rejected.

        '
      in: header
      name: opc-retry-token
      required: false
      schema:
        type: string
        maxLength: 64
        minLength: 1
    PaginationLimitQueryParam_6:
      description: 'For list pagination. The maximum number of results per page, or items to return in a paginated "List" call.

        For important details about how pagination works, see [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).


        Example: `50`

        '
      in: query
      name: limit
      schema:
        type: integer
        default: 10
        maximum: 100
        minimum: 1
    CompartmentIdQueryParam_6:
      description: The OCID of the compartment that contains the resources to list. This filter returns only resources contained within the specified compartment.
      in: query
      name: compartmentId
      x-default-description: 'null'
      schema:
        type: string
    ResourceIdentifierQueryParam_2:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the resource. This filter returns resources associated with the specified resource.
      in: query
      name: resourceId
      x-default-description: 'null'
      schema:
        type: string
    WorkRequestTimeCreatedGreaterThanOrEqualToQueryParam:
      description: 'A filter to return work requests that were created on or after the date provided (in [RFC 3339](https://tools.ietf.org/rfc/rfc3339) format). Example: ''2023-08-25T21:10:29.600Z''

        '
      in: query
      name: timeCreatedGreaterThanOrEqualTo
      x-default-description: 'null'
      schema:
        type: string
        format: date-time
    PaginationTokenQueryParam_5:
      description: 'For list pagination. The value of the `opc-next-page` response header from the previous "List"

        call. For important details about how pagination works, see

        [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

        '
      in: query
      name: page
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 1024
        minLength: 1
    ResourceIdQueryParam:
      description: 'The [OCID](/Content/General/Concepts/identifiers.htm) of the resource.

        '
      in: query
      name: resourceId
      required: false
      x-default-description: 'null'
      schema:
        type: string
    WorkRequestIdPathParam_3:
      description: The [OCID](/Content/General/Concepts/identifiers.htm) of the work request.
      in: path
      name: workRequestId
      required: true
      schema:
        type: string
    PaginationLimitQueryParam_7:
      description: 'For list pagination. The maximum number of results per page, or items to return in a paginated

        "List" call. For important details about how pagination works, see

        [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

        '
      in: query
      name: limit
      x-default-description: 'null'
      schema:
        type: integer
        default: 10
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam_7:
      description: The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment.
      in: query
      name: compartmentId
      required: true
      schema:
        type: string
    RequestIdHeader_3:
      description: 'Unique identifier for the request. If you need to contact Oracle about a particular

        request, please provide the request ID.

        '
      in: header
      name: opc-request-id
      schema:
        type: string
    PaginationTokenQueryParam_6:
      description: 'For list pagination. The value of the `opc-next-page` response header from the previous "List"

        call. For important details about how pagination works, see

        [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

        '
      in: query
      name: page
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 1024
        minLength: 1
    WorkRequestIdPathParam_4:
      description: The [OCID](/Content/General/Concepts/identifiers.htm) of the work request.
      in: path
      name: workRequestId
      required: true
      schema:
        type: string
    PaginationLimitQueryParam_8:
      description: 'For list pagination. The maximum number of results per page, or items to return in a paginated

        "List" call. For important details about how pagination works, see

        [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

        '
      in: query
      name: limit
      x-default-description: 'null'
      schema:
        type: integer
        default: 10
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam_8:
      description: The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment.
      in: query
      name: compartmentId
      required: true
      schema:
        type: string
    PaginationTokenQueryParam_7:
      description: 'A token representing the position at which to start retrieving results.

        This must come from the `opc-next-page` header field of a previous response.

        '
      in: query
      name: page
      schema:
        type: string
        minLength: 1
    WorkRequestIdPathParam_5:
      description: The [OCID](/Content/General/Concepts/identifiers.htm) of the asynchronous request.
      in: path
      name: workRequestId
      required: true
      schema:
        type: string
    WorkRequestIdQueryParam_3:
      description: The [OCID](/Content/General/Concepts/identifiers.htm) of the asynchronous work request.
      in: query
      name: workRequestId
      x-default-description: 'null'
      schema:
        type: string
    PaginationLimitQueryParam_9:
      description: The maximum number of items to return.
      in: query
      name: limit
      schema:
        type: integer
        default: 100
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam_9:
      description: The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment in which to list resources.
      in: query
      name: compartmentId
      required: true
      schema:
        type: string
    SortOrderQueryParam_7:
      description: 'The sort order to use, either ascending (`ASC`) or descending (`DESC`).

        '
      in: query
      name: sortOrder
      required: false
      x-default-description: no sorting
      schema:
        type: string
        enum:
        - ASC
        - DESC
    PaginationTokenQueryParam_8:
      description: 'For list pagination. The value of the `opc-next-page` response header from the

        previous "List" call. For important details about how pagination works, see

        [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

        '
      in: query
      name: page
      required: false
      schema:
        type: string
        default: null
        maxLength: 2000
        minLength: 1
    ResourceIdQueryParam_2:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the resource.
      in: query
      name: resourceId
      required: false
      schema:
        type: string
        default: null
        maxLength: 255
        minLength: 1
    WorkRequestIdPathParam_6:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
      in: path
      name: workRequestId
      required: true
      schema:
        type: string
        maxLength: 255
        minLength: 1
    PaginationLimitQueryParam_10:
      description: 'For list pagination. The maximum number of results per page, or items to return in a

        paginated "List" call. For important details about how pagination works, see

        [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

        '
      in: query
      name: limit
      required: false
      schema:
        type: integer
        default: 100
        maximum: 1000
        minimum: 1
    CompartmentIdQueryParam_10:
      description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment.
      in: query
      name: compartmentId
      required: true
      schema:
        type: string
        maxLength: 255
        minLength: 1
    OpcRequestIdHeaderParam:
      description: 'Unique Oracle-assigned identifier for the request. If you need to contact Oracle about a

        particular request, please provide the request ID.

        '
      in: header
      name: opc-request-id
      required: false
      schema:
        type: string
        pattern: '[A-Za-z0-9\-_\.]+'
  responses:
    default:
      description: Unknown Error
      headers:
        opc-request-id:
          description: 'Unique Oracle-assigned identifier for the request. If you need to contact

            Oracle about a particular request, please provide the request ID.

            '
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    default_2:
      description: Unknown Error
      headers:
        Sunset:
          description: Sunset date when this operation will stop working, in <HTTP-date> format, e.g. "Wed, 21 Jun 2023 11:11:11 GMT"
          schema:
            type: string
        opc-request-id:
          description: 'Unique Oracle-assigned identifier for the request. If you need to contact

            Oracle about a particular request, please provide the request ID.

            '
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error_7'
    DefaultError:
      description: An error has occurred.
      headers:
        opc-request-id:
          description: 'Unique Oracle-assigned identifier for the request. If you need to contact Oracle about

            a particular request, please provide the request ID.

            '
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error_10'
x-refined-from:
- oci-database-multicloud-integrations-openapi.yaml
- oci-delegate-access-control-openapi.yaml
- oci-managed-access-openapi.yaml
- oci-organizations-1-openapi.yaml
- oci-organizations-openapi.yaml
- oci-osmh-openapi.yaml
- oci-vmware-1-openapi.yaml
- oci-vmware-openapi.yaml
- oci-waa-openapi.yaml
- oci-workrequests-openapi.yaml
x-anchors:
  x-headers:
    etag:
      description: 'For optimistic concurrency control. See `if-match`.

        '
      type: string
    opc-next-page:
      description: 'For pagination of a list of items. When paging through a list, if this header appears in the response,

        then a partial list might have been returned. Include this value as the `page` parameter for the

        subsequent GET request to get the next batch of items.

        '
      type: string
    opc-request-id:
      description: 'Unique Oracle-assigned identifier for the request. If you need to contact

        Oracle about a particular request, please provide the request ID.

        '
      type: string
    opc-work-request-id:
      description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use this to query status of the asynchronous operation.

        '
      type: string
    retry-after:
      description: A decimal number representing the number of seconds the client should wait before polling this endpoint again.
      type: integer
  x-properties:
    definedTags:
      additionalProperties:
        additionalProperties:
          description: 'The value of the tag. Only string, integer and boolean types are supported.

            '
          type: object
        description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

          Example: `{"bar-key": "value"}`

          '
        type: object
      description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

        Example: `{"foo-namespace": {"bar-key": "value"}}`

        '
      type: object
    freeformTags:
      additionalProperties:
        type: string
      description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

        Example: `{"bar-key": "value"}`

        '
      type: object
    systemTags:
      additionalProperties:
        additionalProperties:
          description: 'The value of the tag. Only string, integer and boolean types are supported.

            '
          type: object
        description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

          Example: `{"bar-key": "value"}`

          '
        type: object
      description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

        Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

        '
      type: object
x-obmcs-client-retries-enabled: true
x-oracle-package: com.oracle.idm.agcs.cp
