openapi: 3.2.0
info:
  title: Oracle Domain Governance API
  version: '1.0'
  description: 'Operations tagged domainGovernance across 2 of this provider''s published API definitions: oci-organizations-1-openapi.yaml, oci-organizations-openapi.yaml. Each path carries the servers of the definition it was published in.'
servers:
- url: http://127.0.0.1/20220518
- url: https://127.0.0.1/20220518
tags:
- name: domainGovernance
paths:
  /domainGovernances:
    get:
      description: 'Return a (paginated) list of domain governance entities.

        '
      operationId: ListDomainGovernances
      parameters:
      - $ref: '#/components/parameters/CompartmentIdQueryParam'
      - $ref: '#/components/parameters/DomainIdQueryParam'
      - $ref: '#/components/parameters/DomainGovernanceIdQueryParam'
      - $ref: '#/components/parameters/LifecycleStateQueryParam'
      - $ref: '#/components/parameters/NameQueryParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      - $ref: '#/components/parameters/PaginationTokenQueryParam'
      - $ref: '#/components/parameters/PaginationLimitQueryParam'
      - $ref: '#/components/parameters/SortByQueryParam'
      - $ref: '#/components/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of domain governance entities.
          headers:
            opc-next-page:
              description: 'For pagination of a list of items. When paging through a list, if this header appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter for the

                subsequent GET request to get the next batch of items.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DomainGovernanceCollection'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Lists domain governance entities
      tags:
      - domainGovernance
      x-related-resource: '#/definitions/DomainGovernance'
    post:
      description: Adds domain governance to a claimed domain.
      operationId: CreateDomainGovernance
      parameters:
      - $ref: '#/components/parameters/RetryTokenHeader'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        200:
          description: Domain governance entity created.
          headers:
            etag:
              description: 'For optimistic concurrency control. See `if-match`.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DomainGovernance'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        409:
          $ref: '#/components/responses/409'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Adds domain governance to a claimed domain.
      tags:
      - domainGovernance
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDomainGovernanceDetails'
        description: Parameters for adding domain governance to a claimed domain.
        required: true
  /domainGovernances/{domainGovernanceId}:
    delete:
      description: Removes domain governance from a claimed domain.
      operationId: DeleteDomainGovernance
      parameters:
      - $ref: '#/components/parameters/DomainGovernanceIdPathParam'
      - $ref: '#/components/parameters/IfMatchHeader'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        204:
          description: Domain governance removed from domain.
          headers:
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        412:
          $ref: '#/components/responses/412'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Removes domain governance from a claimed domain.
      tags:
      - domainGovernance
      x-related-resource: '#/definitions/DomainGovernance'
    get:
      description: Gets information about the domain governance entity.
      operationId: GetDomainGovernance
      parameters:
      - $ref: '#/components/parameters/DomainGovernanceIdPathParam'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        200:
          description: Domain governance entity retrieved.
          headers:
            etag:
              description: 'For optimistic concurrency control. See `if-match`.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DomainGovernance'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Gets information about the domain governance entity.
      tags:
      - domainGovernance
    put:
      description: Updates the domain governance entity.
      operationId: UpdateDomainGovernance
      parameters:
      - $ref: '#/components/parameters/DomainGovernanceIdPathParam'
      - $ref: '#/components/parameters/IfMatchHeader'
      - $ref: '#/components/parameters/RequestIdHeader'
      responses:
        200:
          description: Domain governance entity updated.
          headers:
            etag:
              description: 'For optimistic concurrency control. See `if-match`.

                '
              schema:
                type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DomainGovernance'
        400:
          $ref: '#/components/responses/400'
        401:
          $ref: '#/components/responses/401'
        404:
          $ref: '#/components/responses/404'
        412:
          $ref: '#/components/responses/412'
        429:
          $ref: '#/components/responses/429'
        500:
          $ref: '#/components/responses/500'
        default:
          $ref: '#/components/responses/default'
      summary: Update the domain governance entity identified by the ID.
      tags:
      - domainGovernance
      x-related-resource: '#/definitions/DomainGovernance'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDomainGovernanceDetails'
        description: The information to be updated.
        required: true
components:
  parameters:
    NameQueryParam:
      description: A filter to return only resources that exactly match the name given.
      in: query
      name: name
      required: false
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 255
    PaginationTokenQueryParam:
      description: The page token representing the page at which to start retrieving results. This is usually retrieved from a previous list call.
      in: query
      name: page
      x-default-description: 'null'
      schema:
        type: string
        minLength: 1
    SortByQueryParam:
      description: 'The field to sort by. Only one sort order can be provided.

        * The default order for timeCreated is descending.

        * The default order for displayName is ascending.

        * If no value is specified, timeCreated is the default.

        '
      in: query
      name: sortBy
      schema:
        type: string
        enum:
        - timeCreated
        - displayName
        default: timeCreated
    IfMatchHeader:
      description: 'For optimistic concurrency control. In the PUT or DELETE call

        for a resource, set the `if-match` parameter to the value of the

        etag from a previous GET or POST response for that resource.

        The resource will be updated or deleted only if the etag you

        provide matches the resource''s current etag value.

        '
      in: header
      name: if-match
      required: false
      schema:
        type: string
    CompartmentIdQueryParam:
      description: The ID of the compartment in which to list resources.
      in: query
      name: compartmentId
      required: true
      schema:
        type: string
        maxLength: 255
    PaginationLimitQueryParam:
      description: The maximum number of items to return.
      in: query
      name: limit
      schema:
        type: integer
        default: 100
        maximum: 1000
        minimum: 1
    DomainGovernanceIdPathParam:
      description: The domain governance OCID.
      in: path
      name: domainGovernanceId
      required: true
      schema:
        type: string
        maxLength: 255
    SortOrderQueryParam:
      description: The sort order to use, whether 'asc' or 'desc'.
      in: query
      name: sortOrder
      x-default-description: The default order depends on what parameter is being sorted by.
      x-obmcs-top-level-enum: '#/definitions/SortOrder'
      schema:
        type: string
        enum:
        - ASC
        - DESC
    DomainGovernanceIdQueryParam:
      description: The domain governance OCID.
      in: query
      name: domainGovernanceId
      required: false
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 255
    DomainIdQueryParam:
      description: The domain OCID.
      in: query
      name: domainId
      required: false
      x-default-description: 'null'
      schema:
        type: string
        maxLength: 255
    RetryTokenHeader:
      description: 'A token that uniquely identifies a request, so it can be retried in case of a timeout or

        server error, without risk of executing that same action again. Retry tokens expire after 24

        hours, but can be invalidated before then due to conflicting operations. For example, if a resource

        has been deleted and purged from the system, then a retry of the original creation request

        will be rejected.

        '
      in: header
      name: opc-retry-token
      required: false
      schema:
        type: string
        maxLength: 64
        minLength: 1
    RequestIdHeader:
      description: The client request ID for tracing.
      in: header
      name: opc-request-id
      schema:
        type: string
    LifecycleStateQueryParam:
      description: The lifecycle state of the resource.
      in: query
      name: lifecycleState
      x-default-description: 'null'
      x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      schema:
        type: string
        enum:
        - CREATING
        - ACTIVE
        - INACTIVE
        - UPDATING
        - FAILED
        - TERMINATED
  schemas:
    DomainGovernanceCollection:
      description: Result of a query request for a list of domain governance entities. Contains DomainGovernanceSummary items.
      properties:
        items:
          description: Array containing DomainGovernanceSummary items.
          items:
            $ref: '#/components/schemas/DomainGovernanceSummary'
          type: array
      required:
      - items
      type: object
    Error:
      description: Error information.
      properties:
        code:
          description: A short error code that defines the error, meant for programmatic parsing.
          type: string
        message:
          description: A human-readable error string.
          type: string
      required:
      - code
      - message
    DomainGovernance:
      description: The model for a domain governance entity.
      properties:
        compartmentId:
          description: The OCID of the tenancy that owns this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer, and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

            Example: `{"foo-namespace": {"bar-key": "value"}}`

            '
          type: object
        domainId:
          description: The OCID of the domain associated with this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

            Example: `{"bar-key": "value"}`

            '
          type: object
        id:
          description: The OCID of the domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        isGovernanceEnabled:
          description: Indicates whether governance is enabled for this domain.
          example: true
          type: boolean
        lifecycleState:
          description: Lifecycle state of the domain governance entity.
          enum:
          - ACTIVE
          - INACTIVE
          example: ACTIVE
          type: string
        onsSubscriptionId:
          description: The ONS subscription associated with this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        onsTopicId:
          description: The ONS topic associated with this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        ownerId:
          description: The OCID of the tenancy that owns this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        subscriptionEmail:
          description: Email address to be used to notify the user, and that the ONS subscription will be created with.
          maxLength: 255
          minLength: 6
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer, and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

            '
          type: object
        timeCreated:
          description: Date-time when this domain governance was created. An RFC 3339-formatted date and time string.
          example: 2016-08-29 09:12:33.001000+00:00
          format: date-time
          type: string
        timeUpdated:
          description: Date-time when this domain governance was last updated. An RFC 3339-formatted date and time string.
          example: 2016-08-29 09:12:33.001000+00:00
          format: date-time
          type: string
      required:
      - id
      - ownerId
      - compartmentId
      - domainId
      - lifecycleState
      - onsTopicId
      - onsSubscriptionId
    CreateDomainGovernanceDetails:
      description: The parameters for creating a domain governance entity.
      properties:
        compartmentId:
          description: OCID of the tenancy.
          maxLength: 255
          minLength: 1
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer, and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

            Example: `{"foo-namespace": {"bar-key": "value"}}`

            '
          type: object
        domainId:
          description: OCID of the domain.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

            Example: `{"bar-key": "value"}`

            '
          type: object
        onsSubscriptionId:
          description: The ONS subscription associated with this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        onsTopicId:
          description: The ONS topic associated with this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        subscriptionEmail:
          description: Email address to be used to notify the user, and that the ONS subscription will be created with.
          maxLength: 255
          minLength: 6
          type: string
      required:
      - compartmentId
      - domainId
      - subscriptionEmail
      - onsTopicId
      - onsSubscriptionId
    UpdateDomainGovernanceDetails:
      description: The parameters for updating a domain govenance entity.
      properties:
        definedTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer, and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

            Example: `{"foo-namespace": {"bar-key": "value"}}`

            '
          type: object
        freeformTags:
          additionalProperties:
            type: string
          description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

            Example: `{"bar-key": "value"}`

            '
          type: object
        isGovernanceEnabled:
          description: Indicates whether governance is enabled for this domain.
          example: true
          type: boolean
        subscriptionEmail:
          description: Email address to be used to notify the user, and that the ONS subscription will be created with. The ONS subscription for the previous email will also be deleted.
          maxLength: 255
          minLength: 6
          type: string
    DomainGovernanceSummary:
      description: The summary of a domain govenance entity owned by a tenancy.
      properties:
        compartmentId:
          description: The OCID of the tenancy that has started the registration process for this domain.
          maxLength: 255
          minLength: 1
          type: string
        definedTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer, and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

            Example: `{"foo-namespace": {"bar-key": "value"}}`

            '
          type: object
        domainId:
          description: The OCID of the domain associated with this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        freeformTags:
          additionalProperties:
            type: string
          description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

            Example: `{"bar-key": "value"}`

            '
          type: object
        id:
          description: The OCID of the domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        isGovernanceEnabled:
          description: Indicates whether governance is enabled for this domain.
          example: true
          type: boolean
        lifecycleState:
          description: The lifecycle state of the domain governance entity.
          example: ACTIVE
          type: string
          x-obmcs-enumref: '#/definitions/DomainGovernance/lifecycleState'
        onsSubscriptionId:
          description: The ONS subscription associated with this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        onsTopicId:
          description: The ONS topic associated with this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        ownerId:
          description: The OCID of the tenancy that owns this domain governance entity.
          maxLength: 255
          minLength: 1
          type: string
        subscriptionEmail:
          description: Email address to be used to notify the user, and that the ONS subscription will be created with.
          maxLength: 255
          minLength: 6
          type: string
        systemTags:
          additionalProperties:
            additionalProperties:
              description: 'The value of the tag. Only string, integer, and boolean types are supported.

                '
              type: object
            description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

              Example: `{"bar-key": "value"}`

              '
            type: object
          description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

            Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

            '
          type: object
        timeCreated:
          description: Date-time when this domain governance was created. An RFC 3339-formatted date and time string.
          example: 2016-08-29 09:12:33.001000+00:00
          format: date-time
          type: string
        timeUpdated:
          description: Date-time when this domain governance was last updated. An RFC 3339-formatted date and time string.
          example: 2016-08-29 09:12:33.001000+00:00
          format: date-time
          type: string
      required:
      - id
      - ownerId
      - compartmentId
      - domainId
      - lifecycleState
      - isGovernanceEnabled
      - onsTopicId
      - onsSubscriptionId
  responses:
    default:
      description: Unknown Error
      headers:
        opc-request-id:
          description: 'Unique Oracle-assigned identifier for the request. If you need to contact

            Oracle about a particular request, please provide the request ID.

            '
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
x-refined-from:
- oci-organizations-1-openapi.yaml
- oci-organizations-openapi.yaml
x-anchors:
  x-headers:
    etag:
      description: 'For optimistic concurrency control. See `if-match`.

        '
      type: string
    opc-next-page:
      description: 'For pagination of a list of items. When paging through a list, if this header appears in the response,

        then a partial list might have been returned. Include this value as the `page` parameter for the

        subsequent GET request to get the next batch of items.

        '
      type: string
    opc-request-id:
      description: 'Unique Oracle-assigned identifier for the request. If you need to contact

        Oracle about a particular request, please provide the request ID.

        '
      type: string
    opc-work-request-id:
      description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use this to query status of the asynchronous operation.

        '
      type: string
    retry-after:
      description: A decimal number representing the number of seconds the client should wait before polling this endpoint again.
      type: integer
  x-properties:
    definedTags:
      additionalProperties:
        additionalProperties:
          description: 'The value of the tag. Only string, integer and boolean types are supported.

            '
          type: object
        description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

          Example: `{"bar-key": "value"}`

          '
        type: object
      description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

        Example: `{"foo-namespace": {"bar-key": "value"}}`

        '
      type: object
    freeformTags:
      additionalProperties:
        type: string
      description: 'Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.

        Example: `{"bar-key": "value"}`

        '
      type: object
    systemTags:
      additionalProperties:
        additionalProperties:
          description: 'The value of the tag. Only string, integer and boolean types are supported.

            '
          type: object
        description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

          Example: `{"bar-key": "value"}`

          '
        type: object
      description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

        Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

        '
      type: object
x-obmcs-client-retries-enabled: true
x-oracle-package: com.oracle.idm.agcs.cp
