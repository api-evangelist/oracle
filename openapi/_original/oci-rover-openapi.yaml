swagger: '2.0'
info:
  description: A description of the RoverCloudService API.
  title: RoverCloudService API
  version: '20201210'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/bc82a6be3188c7b03e57bdc1f0d7bc345fff101db5cc63bbd3e9a61d9baea4a3.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the RoverCloudService API OCI service and stored verbatim;
      API Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/bc82a6be3188c7b03e57bdc1f0d7bc345fff101db5cc63bbd3e9a61d9baea4a3.yaml
    what: the harvested document for RoverCloudService API
host: 127.0.0.1
basePath: /20201210
schemes:
- https
produces:
- application/json
paths:
  /roverClusters:
    get:
      description: 'Returns a list of RoverClusters.

        '
      operationId: ListRoverClusters
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/ClusterTypeQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of RoverClusterSummary objects.
          headers:
            opc-next-page: &id003
              description: 'For pagination of a list of items. When paging through a list, if this header
                appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter
                for the

                subsequent GET request to get the next batch of items.

                '
              type: string
            opc-prev-page: &id004
              description: 'For pagination of a list of items. When paging through a list, if this header
                appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter
                for the

                subsequent GET request to get the previous batch of items.

                '
              type: string
            opc-request-id: &id001
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/RoverClusterCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of all RoverClusters in a compartment.
      tags:
      - roverCluster
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RoverCluster'
    post:
      description: 'Creates a new RoverCluster.

        '
      operationId: CreateRoverCluster
      parameters:
      - description: Details for the new RoverCluster.
        in: body
        name: CreateRoverClusterDetails
        required: true
        schema:
          $ref: '#/definitions/CreateRoverClusterDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The RoverCluster was successfully created.
          headers:
            etag: &id002
              description: 'For optimistic concurrency control. See `if-match`.

                '
              type: string
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverCluster'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new RoverCluster.
      tags:
      - roverCluster
  /roverClusters/{roverClusterId}:
    delete:
      description: Deletes a RoverCluster resource by identifier
      operationId: DeleteRoverCluster
      parameters:
      - $ref: '#/parameters/RoverClusterIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        204:
          description: The RoverCluster was successfully deleted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete a provisioned RoverCluster
      tags:
      - roverCluster
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RoverCluster'
    get:
      description: Gets a RoverCluster by identifier
      operationId: GetRoverCluster
      parameters:
      - $ref: '#/parameters/RoverClusterIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Retrieves the RoverCluster with the given id
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverCluster'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get RoverCluster
      tags:
      - roverCluster
      x-obmcs-client-retries-enabled: true
    put:
      description: Updates the RoverCluster
      operationId: UpdateRoverCluster
      parameters:
      - $ref: '#/parameters/RoverClusterIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateRoverClusterDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateRoverClusterDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The RoverCluster was successfully updated.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverCluster'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the RoverCluster identified by the id
      tags:
      - roverCluster
  /roverClusters/{roverClusterId}/actions/changeCompartment:
    post:
      description: Moves a cluster into a different compartment.
      operationId: ChangeRoverClusterCompartment
      parameters:
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RoverClusterIdentifierPathParam'
      - description: CompartmentId of the destination compartment
        in: body
        name: ChangeRoverClusterCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeRoverClusterCompartmentDetails'
      responses:
        204:
          description: Moves the cluster resource to new compartment
          headers:
            opc-request-id:
              type: string
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Move a cluster into a different compartment. When provided, if-Match is matched against
        ETag values of the resource.
      tags:
      - roverCluster
      x-related-resource: '#/definitions/RoverCluster'
  /roverClusters/{roverClusterId}/certificate:
    get:
      description: Get the certificate for a rover cluster
      operationId: GetRoverClusterCertificate
      parameters:
      - $ref: '#/parameters/RoverClusterIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Retrieves the certificate for the rover cluster
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverClusterCertificate'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the certificate for a rover cluster
      tags:
      - roverCluster
      x-obmcs-client-retries-enabled: true
  /roverEntitlements:
    get:
      description: Returns a list of RoverEntitlements.
      operationId: ListRoverEntitlements
      parameters:
      - description: filtering by Rover Device Entitlement id
        in: query
        name: id
        required: false
        type: string
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: List of Rover Device Entitlements
          headers:
            opc-next-page: *id003
            opc-prev-page: *id004
            opc-request-id:
              type: string
          schema:
            $ref: '#/definitions/RoverEntitlementCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Gets a list of all RoverEntitlements in a compartment.
      tags:
      - roverEntitlement
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RoverEntitlementCollection'
    post:
      description: Create the Entitlement to use a Rover Device. It requires some offline process of review
        and signatures before request is granted.
      operationId: CreateRoverEntitlement
      parameters:
      - description: Creates a Rover Device Entitlement
        in: body
        name: CreateRoverEntitlementDetails
        required: true
        schema:
          $ref: '#/definitions/CreateRoverEntitlementDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Creates a New Rover Device Entitlement
          headers:
            etag:
              type: string
            opc-request-id:
              type: string
          schema:
            $ref: '#/definitions/RoverEntitlement'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          description: Internal error while creating Rover Device Entitlement
          headers:
            opc-request-id:
              type: string
          schema:
            $ref: '#/definitions/Error'
        default:
          $ref: '#/responses/default'
      summary: Create the Rover Device Entitlement
      tags:
      - roverEntitlement
      x-related-resource: '#/definitions/RoverEntitlement'
  /roverEntitlements/{roverEntitlementId}:
    delete:
      description: Deletes a rover entitlement
      operationId: DeleteRoverEntitlement
      parameters:
      - $ref: '#/parameters/RoverEntitlementIdPathParam'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      responses:
        204:
          description: Rover entitlement deleted
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        500:
          $ref: '#/responses/500'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        default:
          $ref: '#/responses/default'
      summary: Deletes a rover entitlement
      tags:
      - roverEntitlement
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RoverEntitlement'
    get:
      description: Describes the Rover Device Entitlement in detail
      operationId: GetRoverEntitlement
      parameters:
      - $ref: '#/parameters/RoverEntitlementIdPathParam'
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Rover Device Entitlement
          headers:
            etag:
              type: string
            opc-request-id:
              type: string
          schema:
            $ref: '#/definitions/RoverEntitlement'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Describes the Rover Device Entitlement in detail
      tags:
      - roverEntitlement
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RoverEntitlement'
    put:
      description: Updates the RoverEntitlement
      operationId: UpdateRoverEntitlement
      parameters:
      - $ref: '#/parameters/RoverEntitlementIdPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateRoverEntitlementDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateRoverEntitlementDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The Rover Entitlement was successfully updated.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverEntitlement'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the Rover Entitlement identified by the id
      tags:
      - roverEntitlement
      x-related-resource: '#/definitions/RoverEntitlement'
  /roverEntitlements/{roverEntitlementId}/actions/changeCompartment:
    post:
      description: Moves an entitlement into a different compartment.
      operationId: ChangeRoverEntitlementCompartment
      parameters:
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RoverEntitlementIdPathParam'
      - description: CompartmentId of the destination compartment
        in: body
        name: ChangeRoverEntitlementCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeRoverEntitlementCompartmentDetails'
      responses:
        204:
          description: Moves the entitlement resource to new compartment
          headers:
            opc-request-id:
              type: string
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Move an entitlement into a different compartment. When provided, if-Match is matched against
        ETag values of the resource.
      tags:
      - roverEntitlement
      x-related-resource: '#/definitions/RoverEntitlement'
  /roverNodes:
    get:
      description: 'Returns a list of RoverNodes.

        '
      operationId: ListRoverNodes
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/NodeTypeQueryParam'
      - $ref: '#/parameters/ShapeQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of RoverNodeSummary objects.
          headers:
            opc-next-page: *id003
            opc-prev-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverNodeCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of all RoverNodes in a compartment.
      tags:
      - roverNode
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RoverNode'
    post:
      description: 'Creates a new RoverNode.

        '
      operationId: CreateRoverNode
      parameters:
      - description: Details for the new RoverNode.
        in: body
        name: CreateRoverNodeDetails
        required: true
        schema:
          $ref: '#/definitions/CreateRoverNodeDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The RoverNode was successfully created.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverNode'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new RoverNode.
      tags:
      - roverNode
  /roverNodes/{roverNodeId}:
    delete:
      description: Deletes a RoverNode resource by identifier
      operationId: DeleteRoverNode
      parameters:
      - $ref: '#/parameters/RoverNodeIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        204:
          description: The RoverNode was successfully deleted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete a provisioned RoverNode
      tags:
      - roverNode
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RoverNode'
    get:
      description: Gets a RoverNode by identifier.
      operationId: GetRoverNode
      parameters:
      - $ref: '#/parameters/RoverNodeIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Retrieves the RoverNode with the given id
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverNode'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get RoverNode
      tags:
      - roverNode
      x-obmcs-client-retries-enabled: true
    put:
      description: Updates the RoverNode
      operationId: UpdateRoverNode
      parameters:
      - $ref: '#/parameters/RoverNodeIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateRoverNodeDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateRoverNodeDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The RoverNode was successfully updated.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverNode'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the RoverNode identified by the id
      tags:
      - roverNode
  /roverNodes/{roverNodeId}/actions/changeCompartment:
    post:
      description: Moves a rover node into a different compartment.
      operationId: ChangeRoverNodeCompartment
      parameters:
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RoverNodeIdentifierPathParam'
      - description: CompartmentId of the destination compartment
        in: body
        name: ChangeRoverNodeCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeRoverNodeCompartmentDetails'
      responses:
        204:
          description: Moves the node resource to new compartment
          headers:
            opc-request-id:
              type: string
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Move a rover node into a different compartment. When provided, if-Match is matched against
        ETag values of the resource.
      tags:
      - roverNode
      x-related-resource: '#/definitions/RoverNode'
  /roverNodes/{roverNodeId}/actions/setKey:
    post:
      description: Get the resource principal public key for a rover node
      operationId: RoverNodeActionSetKey
      parameters:
      - $ref: '#/parameters/RoverNodeIdentifierPathParam'
      - $ref: '#/parameters/SetRoverNodeRptToken'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: The information to be updated.
        in: body
        name: RoverNodeActionSetKeyDetails
        required: true
        schema:
          $ref: '#/definitions/RoverNodeActionSetKeyDetails'
      produces:
      - application/json
      responses:
        200:
          description: Result of setting the resource principal public key for the rover node
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverNodeSetKey'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Set the resource principal public key for a rover node
      tags:
      - roverNode
      x-related-resource: '#/definitions/RoverNodeSetKey'
  /roverNodes/{roverNodeId}/certificate:
    get:
      description: Get the certificate for a rover node
      operationId: GetRoverNodeCertificate
      parameters:
      - $ref: '#/parameters/RoverNodeIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Retrieves the certificate for the rover node
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverNodeCertificate'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the certificate for a rover node
      tags:
      - roverNode
      x-obmcs-client-retries-enabled: true
  /roverNodes/{roverNodeId}/encryptionKey:
    get:
      description: Get the data encryption key for a rover node.
      operationId: GetRoverNodeEncryptionKey
      parameters:
      - description: Serial number of the rover node.
        in: path
        name: roverNodeId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Retrieves the data encryption key for rover node.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverNodeEncryptionKey'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the data encryption key for a rover node.
      tags:
      - roverNode
      x-obmcs-client-retries-enabled: true
  /roverNodes/{roverNodeId}/getRpt:
    get:
      description: Get the resource principal token for a rover node
      operationId: GetRoverNodeGetRpt
      parameters:
      - $ref: '#/parameters/RoverNodeIdentifierPathParam'
      - $ref: '#/parameters/GetRoverNodeRptToken'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Retrieves the resource principal token for the rover node
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RoverNodeGetRpt'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the resource principal token for a rover node
      tags:
      - roverNode
      x-obmcs-client-retries-enabled: true
  /shapes:
    get:
      description: 'Returns a list of Shapes.

        '
      operationId: ListShapes
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of ShapeSummary objects.
          headers:
            opc-next-page: *id003
            opc-prev-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/ShapeCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of all Shapes in a compartment.
      tags:
      - shape
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/ShapeSummary'
definitions:
  ChangeRoverClusterCompartmentDetails:
    description: Object for moving a cluster to a different compartment.
    properties:
      compartmentId:
        description: 'The [OCID] (/Content/General/Concepts/identifiers.htm) of the compartment into which
          the resources should be moved.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    type: object
  ChangeRoverEntitlementCompartmentDetails:
    description: Object for moving an entitlement to a different compartment.
    properties:
      compartmentId:
        description: 'The [OCID] (/Content/General/Concepts/identifiers.htm) of the compartment into which
          the resources should be moved.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    type: object
  ChangeRoverNodeCompartmentDetails:
    description: Object for moving a node to a different compartment.
    properties:
      compartmentId:
        description: 'The [OCID] (/Content/General/Concepts/identifiers.htm) of the compartment into which
          the resources should be moved.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    type: object
  ClusterType:
    description: Possible rover node types.
    enum: &id005
    - STANDALONE
    - STATION
    type: string
  CreateRoverClusterDetails:
    description: The information required to create a RoverCluster.
    properties:
      clusterSize:
        description: Number of nodes desired in the cluster, in standalone clusters, between 5 and 15
          inclusive. In station clusters, between 15 and 30 inclusive.
        format: int32
        maximum: 30
        minimum: 5
        type: integer
      clusterType:
        description: Type of cluster.
        enum: *id005
        type: string
        x-obmcs-top-level-enum: '#/definitions/ClusterType'
      clusterWorkloads:
        description: List of existing workloads that should be provisioned on the nodes.
        items:
          $ref: '#/definitions/RoverWorkload'
        type: array
      compartmentId:
        description: The OCID of the compartment containing the RoverCluster.
        maxLength: 255
        minLength: 1
        type: string
      customerShippingAddress:
        $ref: '#/definitions/ShippingAddress'
      dataValidationCode:
        description: Validation code returned by data validation tool. Required for return shipping label
          generation if data import was requested.
        type: string
        x-default-description: 'null'
      definedTags: &id006
        additionalProperties:
          additionalProperties:
            type: object
          type: object
        description: 'The defined tags associated with this resource, if any. Each key is predefined and
          scoped to namespaces.

          For more information, see [Resource Tags](https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).

          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      enclosureType:
        description: The type of enclosure rover nodes in this cluster are shipped in.
        enum: &id010
        - RUGGADIZED
        - NON_RUGGADIZED
        type: string
        x-obmcs-top-level-enum: '#/definitions/EnclosureType'
      freeformTags: &id007
        additionalProperties:
          type: string
        description: 'The freeform tags associated with this resource, if any. Each tag is a simple key-value
          pair with no

          predefined name, type, or namespace. For more information, see [Resource Tags](https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).

          Example: `{"Department": "Finance"}`

          '
        type: object
      importCompartmentId:
        description: An OCID of a compartment where data will be imported to upon Rover cluster return.
        type: string
        x-default-description: 'null'
      importFileBucket:
        description: Name of a bucket where files from NFS share will be imported to upon Rover cluster
          return.
        type: string
        x-default-description: 'null'
      isImportRequested:
        description: The flag indicating that customer requests data to be imported to OCI upon Rover
          cluster return.
        type: boolean
        x-default-description: 'null'
      lifecycleState:
        description: The current state of the RoverCluster.
        enum: &id008
        - CREATING
        - UPDATING
        - ACTIVE
        - DELETING
        - DELETED
        - FAILED
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      oracleShippingTrackingUrl:
        description: Tracking Url for the shipped Rover Cluster.
        format: uri
        type: string
      pointOfContact:
        description: Name of point of contact for this order if customer is picking up.
        type: string
      pointOfContactPhoneNumber:
        description: Phone number of point of contact for this order if customer is picking up.
        type: string
      shippingPreference:
        default: ORACLE_SHIPPED
        description: Preference for device delivery.
        enum:
        - ORACLE_SHIPPED
        - CUSTOMER_PICKUP
        type: string
      shippingVendor:
        description: Shipping vendor of choice for orace to customer shipping.
        type: string
      subscriptionId:
        description: ID provided to customer after successful subscription to Rover Stations.
        type: string
      superUserPassword:
        description: Root password for the rover cluster.
        type: string
      systemTags: &id009
        additionalProperties:
          additionalProperties:
            type: object
          type: object
        description: 'The system tags associated with this resource, if any. The system tags are set by
          Oracle cloud infrastructure services. Each key is predefined and scoped to namespaces.

          For more information, see [Resource Tags](https://docs.cloud.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).

          Example: `{orcl-cloud: {free-tier-retain: true}}`

          '
        type: object
      timePickupExpected:
        description: Expected date when customer wants to pickup the cluster if they chose customer pickup.
        format: date-time
        type: string
      unlockPassphrase:
        description: Password to unlock the rover cluster.
        type: string
    required:
    - displayName
    - compartmentId
    - clusterSize
    type: object
  CreateRoverEntitlementDetails:
    description: Information required to create a RoverEntitlement.
    properties:
      compartmentId:
        description: The OCID of the compartment containing the RoverEntitlement.
        type: string
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      entitlementDetails:
        description: Details about the entitlement.
        type: string
      freeformTags: *id007
      lifecycleState:
        description: The current state of the RoverNode.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      requestorEmail:
        description: Requestor email for the entitlement.
        type: string
      requestorName:
        description: Requestor name for the entitlement.
        type: string
      systemTags: *id009
      tenantId:
        description: tenant Id.
        type: string
    required:
    - compartmentId
    - requestorName
    - requestorEmail
    type: object
  CreateRoverNodeDetails:
    description: The information required to create a RoverNode.
    properties:
      compartmentId:
        description: The OCID of the compartment containing the RoverNode.
        maxLength: 255
        minLength: 1
        type: string
      customerShippingAddress:
        $ref: '#/definitions/ShippingAddress'
      dataValidationCode:
        description: Validation code returned by data validation tool. Required for return shipping label
          generation if data import was requested.
        type: string
        x-default-description: 'null'
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      enclosureType:
        description: The type of enclosure rover nodes in this cluster are shipped in.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/EnclosureType'
      freeformTags: *id007
      importCompartmentId:
        description: An OCID of a compartment where data will be imported to upon Rover node return.
        type: string
        x-default-description: 'null'
      importFileBucket:
        description: Name of a bucket where files from NFS share will be imported to upon Rover node return.
        type: string
        x-default-description: 'null'
      isImportRequested:
        description: The flag indicating that customer requests data to be imported to OCI upon Rover
          node return.
        type: boolean
        x-default-description: 'null'
      lifecycleState:
        description: The current state of the RoverNode.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      nodeWorkloads:
        description: List of existing workloads that should be provisioned on the node.
        items:
          $ref: '#/definitions/RoverWorkload'
        type: array
      oracleShippingTrackingUrl:
        description: Tracking Url for the shipped FmsRoverNode.
        format: uri
        type: string
      pointOfContact:
        description: Name of point of contact for this order if customer is picking up.
        type: string
      pointOfContactPhoneNumber:
        description: Phone number of point of contact for this order if customer is picking up.
        type: string
      publicKey:
        description: The public key of the resource principal
        type: string
      serialNumber:
        description: Serial number of the node.
        type: string
      shape:
        description: The shape of the node.
        type: string
      shippingPreference:
        default: ORACLE_SHIPPED
        description: Preference for device delivery.
        enum:
        - ORACLE_SHIPPED
        - CUSTOMER_PICKUP
        type: string
      shippingVendor:
        description: Shipping vendor of choice for orace to customer shipping.
        type: string
      superUserPassword:
        description: Root password for the rover node.
        type: string
      systemTags: *id009
      timePickupExpected:
        description: Expected date when customer wants to pickup the device if they chose customer pickup.
        format: date-time
        type: string
      timeReturnWindowEnds:
        description: End time for the window to pickup the device from customer.
        format: date-time
        type: string
      timeReturnWindowStarts:
        description: Start time for the window to pickup the device from customer.
        format: date-time
        type: string
      unlockPassphrase:
        description: Passphrase to unlock the rover node.
        type: string
    required:
    - displayName
    - compartmentId
    type: object
  EnclosureType:
    description: Type of enclosure the node is shipped in.
    enum: *id010
    type: string
  Error:
    description: Error Information.
    properties:
      code:
        description: A short error code that defines the error, meant for programmatic parsing.
        type: string
      message:
        description: A human-readable error string.
        type: string
    required:
    - code
    - message
  LifecycleState:
    description: Possible lifecycle states.
    enum: *id008
    type: string
  NodeType:
    description: Possible rover node types.
    enum: &id011
    - STANDALONE
    - CLUSTERED
    - STATION
    type: string
  RoverCluster:
    description: Description of RoverCluster.
    properties:
      clusterSize:
        description: Size of the cluster.
        format: int32
        maximum: 30
        minimum: 5
        type: integer
      clusterType:
        default: STANDALONE
        description: Type of cluster.
        enum: *id005
        type: string
        x-obmcs-top-level-enum: '#/definitions/ClusterType'
      clusterWorkloads:
        description: List of existing workloads that should be provisioned on the nodes.
        items:
          $ref: '#/definitions/RoverWorkload'
        type: array
      compartmentId:
        description: The OCID of the compartment containing the RoverCluster.
        type: string
      customerShippingAddress:
        $ref: '#/definitions/ShippingAddress'
      dataValidationCode:
        description: Validation code returned by data validation tool. Required for return shipping label
          generation if data import was requested.
        type: string
      definedTags: *id006
      deliveryTrackingInfo:
        description: Tracking information for device shipping.
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      enclosureType:
        description: The type of enclosure rover nodes in this cluster are shipped in.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/EnclosureType'
      exteriorDoorCode:
        description: Service generated code for the exterior trailer door of the trailer.
        type: string
        x-default-description: 'null'
      freeformTags: *id007
      id:
        description: The OCID of RoverCluster.
        type: string
      imageExportPar:
        description: The link to pre-authenticated request for a bucket where image workloads are moved.
        type: string
      importCompartmentId:
        description: An OCID of a compartment where data will be imported to upon Rover cluster return.
        type: string
      importFileBucket:
        description: Name of a bucket where files from NFS share will be imported to upon Rover cluster
          return.
        type: string
      interiorAlarmDisarmCode:
        description: Service generated code to disarm the interior alarm of the trailer.
        type: string
        x-default-description: 'null'
      isImportRequested:
        description: The flag indicating that customer requests data to be imported to OCI upon Rover
          cluster return.
        type: boolean
      lifecycleState:
        description: The current state of the RoverCluster.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      nodes:
        description: The summary of nodes that are part of this cluster.
        items:
          $ref: '#/definitions/RoverNodeSummary'
        type: array
      oracleShippingTrackingUrl:
        description: Tracking Url for the shipped Rover Cluster.
        format: uri
        type: string
      pointOfContact:
        description: Name of point of contact for this order if customer is picking up.
        type: string
      pointOfContactPhoneNumber:
        description: Phone number of point of contact for this order if customer is picking up.
        type: string
      returnShippingLabelUri:
        description: Uri to download return shipping label.
        format: uri
        type: string
      shippingPreference:
        default: ORACLE_SHIPPED
        description: Preference for device delivery.
        enum:
        - ORACLE_SHIPPED
        - CUSTOMER_PICKUP
        type: string
      shippingVendor:
        description: Shipping vendor of choice for orace to customer shipping.
        type: string
      subscriptionId:
        description: ID provided to customer after successful subscription to Rover Stations.
        type: string
      superUserPassword:
        description: Root password for the rover cluster.
        type: string
      systemTags: *id009
      tags: &id012
        description: 'The tags associated with tagSlug.

          '
        type: string
      timeCreated:
        description: The time the the RoverCluster was created. An RFC3339 formatted datetime string
        format: date-time
        type: string
      timeCustomerReceived:
        description: Time when customer received the cluster.
        format: date-time
        type: string
      timeCustomerReturned:
        description: Time when customer returned the cluster.
        format: date-time
        type: string
      timePickupExpected:
        description: Expected date when customer wants to pickup the device if they chose customer pickup.
        format: date-time
        type: string
      timeReturnWindowEnds:
        description: End time for the window to pickup the device from customer.
        format: date-time
        type: string
      timeReturnWindowStarts:
        description: Start time for the window to pickup the device from customer.
        format: date-time
        type: string
      unlockPassphrase:
        description: Password to unlock the rover cluster.
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - clusterSize
    - displayName
    type: object
  RoverClusterCertificate:
    description: The certificate response
    properties:
      certificate:
        description: The certificate that can be installed on a client to do TLS communication to the
          cluster
        type: string
    required:
    - certificate
    type: object
  RoverClusterCollection:
    description: Results of a roverCluster search. Contains both RoverClusterSummary items and other data.
    properties:
      items:
        description: List of roverClusterSummary.
        items:
          $ref: '#/definitions/RoverClusterSummary'
        type: array
    required:
    - items
    type: object
  RoverClusterSummary:
    description: Summary of the RoverCluster.
    properties:
      clusterSize:
        description: Size of the cluster.
        format: int32
        type: integer
      clusterType:
        description: Type of cluster.
        enum: *id005
        type: string
        x-obmcs-top-level-enum: '#/definitions/ClusterType'
      compartmentId:
        description: The OCID of the compartment containing the RoverCluster.
        type: string
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      freeformTags: *id007
      id:
        description: The OCID of RoverCluster.
        type: string
      lifecycleState:
        description: The current state of the RoverCluster.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      nodes:
        description: The nodes that are part of this cluster.
        items:
          type: string
        type: array
      systemTags: *id009
      timeCreated:
        description: The time the the RoverCluster was created. An RFC3339 formatted datetime string
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    type: object
  RoverEntitlement:
    description: Information about a RoverEntitlement.
    properties:
      compartmentId:
        description: The compartment Id for the entitlement.
        type: string
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      entitlementDetails:
        description: Details about the entitlement.
        type: string
      freeformTags: *id007
      id:
        description: A property that can uniquely identify the rover entitlement.
        type: string
      lifecycleState:
        description: Lifecyclestate for the entitlement.
        enum:
        - CREATING
        - ACTIVE
        - INACTIVE
        - DELETED
        type: string
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      requestorEmail:
        description: Requestor email for the entitlement.
        type: string
      requestorName:
        description: Requestor name for the entitlement.
        type: string
      systemTags: *id009
      tenantId:
        description: tenant Id.
        type: string
      timeCreated:
        description: Time of creation for the entitlement.
        format: date-time
        type: string
      timeUpdated:
        description: Time when the entitlement was last updated.
        format: date-time
        type: string
    required:
    - compartmentId
    - lifecycleState
    - requestorName
    - requestorEmail
    - id
    type: object
  RoverEntitlementCollection:
    description: Results of a roverEntitlement search. Contains RoverEntitlementSummary.
    properties:
      items:
        description: List of RoverEntitlementSummary.
        items:
          $ref: '#/definitions/RoverEntitlementSummary'
        type: array
    required:
    - items
    type: object
  RoverEntitlementSummary:
    description: Rover entitlement summary.
    properties:
      compartmentId:
        description: The compartment Id.
        type: string
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      freeformTags: *id007
      id:
        description: Id of the entitlement.
        type: string
      lifecycleState:
        description: Lifecyclestate for the entitlement.
        type: string
        x-obmcs-enumref: '#/definitions/RoverEntitlement/lifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      requestorEmail:
        description: Email id of the requestor for entitlement.
        type: string
      requestorName:
        description: Requestor name for the entitlement.
        type: string
      systemTags: *id009
    required:
    - compartmentId
    - lifecycleState
    type: object
  RoverNode:
    description: Information about a RoverNode.
    properties:
      clusterId:
        description: The cluster ID if the node is part of a cluster.
        type: string
      compartmentId:
        description: The OCID of the compartment containing the RoverNode.
        type: string
      customerShippingAddress:
        $ref: '#/definitions/ShippingAddress'
      dataValidationCode:
        description: Validation code returned by data validation tool. Required for return shipping label
          generation if data import was requested.
        type: string
      definedTags: *id006
      deliveryTrackingInfo:
        description: Tracking information for device shipping.
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      enclosureType:
        description: The type of enclosure rover node is shipped in.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/EnclosureType'
      freeformTags: *id007
      id:
        description: The OCID of RoverNode.
        type: string
      imageExportPar:
        description: The link to pre-authenticated request for a bucket where image workloads are moved.
        type: string
      importCompartmentId:
        description: An OCID of a compartment where data will be imported to upon Rover node return.
        type: string
      importFileBucket:
        description: Name of a bucket where files from NFS share will be imported to upon Rover node return.
        type: string
      isImportRequested:
        description: The flag indicating that customer requests data to be imported to OCI upon Rover
          node return.
        type: boolean
      lifecycleState:
        description: The current state of the RoverNode.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      nodeType:
        description: The type of node indicating if it belongs to a cluster
        enum: *id011
        type: string
        x-obmcs-top-level-enum: '#/definitions/NodeType'
      nodeWorkloads:
        description: List of existing workloads that should be provisioned on the node.
        items:
          $ref: '#/definitions/RoverWorkload'
        type: array
      oracleShippingTrackingUrl:
        description: Tracking Url for the shipped RoverNode.
        format: uri
        type: string
      pointOfContact:
        description: Name of point of contact for this order if customer is picking up.
        type: string
      pointOfContactPhoneNumber:
        description: Phone number of point of contact for this order if customer is picking up.
        type: string
      publicKey:
        description: The public key of the resource principal
        type: string
      returnShippingLabelUri:
        description: Uri to download return shipping label.
        format: uri
        type: string
      serialNumber:
        description: Serial number of the node.
        type: string
      shape:
        description: The shape of the node.
        type: string
      shippingPreference:
        default: ORACLE_SHIPPED
        description: Preference for device delivery.
        enum:
        - ORACLE_SHIPPED
        - CUSTOMER_PICKUP
        type: string
      shippingVendor:
        description: Shipping vendor of choice for orace to customer shipping.
        type: string
      superUserPassword:
        description: Root password for the rover node.
        type: string
      systemTags: *id009
      tags: *id012
      timeCreated:
        description: The time the the RoverNode was created. An RFC3339 formatted datetime string
        format: date-time
        type: string
      timeCustomerReceieved:
        description: Date and time when customer received tne node.
        format: date-time
        type: string
      timeCustomerReturned:
        description: Date and time when customer returned the node.
        format: date-time
        type: string
      timePickupExpected:
        description: Expected date when customer wants to pickup the device if they chose customer pickup.
        format: date-time
        type: string
      timeReturnWindowEnds:
        description: End time for the window to pickup the device from customer.
        format: date-time
        type: string
      timeReturnWindowStarts:
        description: Start time for the window to pickup the device from customer.
        format: date-time
        type: string
      unlockPassphrase:
        description: Password to unlock the rover node.
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - displayName
    type: object
  RoverNodeActionSetKeyDetails:
    description: The information required to update a rover node's set key details.
    properties:
      publicKey:
        description: The public key of the resource principal
        type: string
    type: object
  RoverNodeCertificate:
    description: The certificate response
    properties:
      certificate:
        description: The certificate that can be installed on a client to do TLS communication to the
          node
        type: string
    required:
    - certificate
    type: object
  RoverNodeCollection:
    description: Results of a roverNode search. Contains both RoverNodeSummary items and other data.
    properties:
      items:
        description: List of roverNodes.
        items:
          $ref: '#/definitions/RoverNodeSummary'
        type: array
    required:
    - items
    type: object
  RoverNodeEncryptionKey:
    description: The response containing encryption key for a rover node.
    properties:
      encryptionKey:
        description: The encryption key key for a rover node.
        type: string
    required:
    - encryptionKey
    type: object
  RoverNodeGetRpt:
    description: The resource principal token response.
    properties:
      resourcePrincipalToken:
        description: The resource principal token blob that contains claims about the resource.
        type: string
      servicePrincipalSessionToken:
        description: The service principal session token
        type: string
    required:
    - resourcePrincipalToken
    type: object
  RoverNodeSetKey:
    description: Information about the success of setting a rover node's resource principal public key.
    properties:
      isSuccessful:
        description: Whether the node's resource principal public key was set correctly
        type: boolean
    required:
    - isSuccessful
    type: object
  RoverNodeSummary:
    description: Summary of the RoverNode.
    properties:
      clusterId:
        description: The cluster ID if the node is part of a cluster.
        type: string
      compartmentId:
        description: The OCID of the compartment containing the RoverNode.
        type: string
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      freeformTags: *id007
      id:
        description: The OCID of the RoverNode.
        type: string
      lifecycleState:
        description: The current state of the RoverNode.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      nodeType:
        description: The type of node indicating if it belongs to a cluster
        enum: *id011
        type: string
        x-obmcs-top-level-enum: '#/definitions/NodeType'
      serialNumber:
        description: Serial number of the node.
        type: string
      shape:
        description: The shape of the node.
        type: string
      systemTags: *id009
      timeCreated:
        description: The time the the RoverNode was created. An RFC3339 formatted datetime string
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    type: object
  RoverWorkload:
    description: Information about a RoverWorkload.
    properties:
      compartmentId:
        description: The OCID of the compartment containing the workload.
        type: string
      id:
        description: The Unique Oracle ID (OCID) that is immutable on creation.
        type: string
      name:
        description: Name of the Rover Workload
        type: string
      objectCount:
        description: Number of objects in a workload.
        type: string
      prefix:
        description: Prefix to filter objects in case it is a bucket.
        type: string
      rangeEnd:
        description: End of the range in a bucket.
        type: string
      rangeStart:
        description: Start of the range in a bucket.
        type: string
      size:
        description: Size of the workload.
        type: string
      workRequestId:
        description: The compute work request id to track progress of custom image exports.
        type: string
      workloadType:
        description: The type of workload
        type: string
    required:
    - id
    - compartmentId
    - workloadType
    type: object
  ShapeCollection:
    description: Results of a listShape search. Contains both Shape items and other data.
    properties:
      items:
        description: List of shapeSummary objects.
        items:
          $ref: '#/definitions/ShapeSummary'
        type: array
    required:
    - items
    type: object
  ShapeSummary:
    description: A shape of a node on a Rover device.
    properties:
      definedTags: *id006
      freeformTags: *id007
      gpuDescription:
        default: null
        description: A short description of the graphics processing unit (GPU) available for this shape.
        type: string
      gpus:
        description: The number of GPUs available for this shape.
        minimum: 0
        type: integer
      memoryInGBs:
        description: The default amount of memory available for this shape, in gigabytes.
        format: float
        minimum: 0
        type: number
      networkingBandwidthInGbps:
        description: The networking bandwidth available for this shape, in gigabits per second.
        format: float
        minimum: 0
        type: number
      numberOfUsbControllers:
        description: The number of USB controllers available for this shape.
        minimum: 0
        type: integer
      ocpus:
        description: The default number of OCPUs available for this shape.
        minimum: 0
        type: integer
      processorDescription:
        description: A short description of the shape's processor (CPU).
        maxLength: 255
        minLength: 1
        type: string
      shape:
        description: The name of the shape.
        maxLength: 255
        minLength: 1
        type: string
      systemTags: *id009
      tags: *id012
      usbControllerDescription:
        default: 'null'
        description: A short description of the USB controller available for this shape.
        type: string
    required:
    - shape
    type: object
  ShippingAddress:
    description: Shipping address for rover devices.
    properties:
      address1:
        description: Address line 1.
        maxLength: 128
        minLength: 1
        type: string
      address2:
        description: Address line 2.
        maxLength: 128
        minLength: 1
        type: string
      address3:
        description: Address line 3.
        maxLength: 128
        minLength: 1
        type: string
      address4:
        description: Address line 4.
        maxLength: 128
        minLength: 1
        type: string
      addressee:
        description: Addressee in shipping address.
        maxLength: 64
        minLength: 1
        type: string
      careOf:
        description: CareOf for shipping address.
        maxLength: 64
        minLength: 1
        type: string
      cityOrLocality:
        description: city or locality for shipping address.
        maxLength: 64
        minLength: 1
        type: string
      country:
        description: country for shipping address.
        maxLength: 64
        minLength: 1
        type: string
      email:
        description: recipient email address.
        format: email
        type: string
      phoneNumber:
        description: recipient phone number.
        type: string
      stateOrRegion:
        description: state or region for shipping address.
        maxLength: 64
        minLength: 1
        type: string
      zipcode:
        description: zipcode for shipping address.
        maxLength: 64
        minLength: 1
        type: string
    required:
    - addressee
    - address1
    - cityOrLocality
    - stateOrRegion
    - zipcode
    - country
    - phoneNumber
    type: object
  SortOrders:
    description: Sort orders.
    enum: &id013
    - ASC
    - DESC
    type: string
  UpdateRoverClusterDetails:
    description: The information required to update a RoverCluster.
    properties:
      clusterSize:
        description: Number of nodes desired in the cluster, in standalone clusters, between 5 and 15
          inclusive. In station clusters, between 15 and 30 inclusive.
        format: int32
        maximum: 30
        minimum: 5
        type: integer
      clusterWorkloads:
        description: List of existing workloads that should be provisioned on the nodes.
        items:
          $ref: '#/definitions/RoverWorkload'
        type: array
      customerShippingAddress:
        $ref: '#/definitions/ShippingAddress'
      dataValidationCode:
        description: Validation code returned by data validation tool. Required for return shipping label
          generation if data import was requested.
        type: string
        x-default-description: 'null'
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      enclosureType:
        description: The type of enclosure rover nodes in this cluster are shipped in.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/EnclosureType'
      freeformTags: *id007
      importCompartmentId:
        description: An OCID of a compartment where data will be imported to upon Rover cluster return.
        type: string
        x-default-description: 'null'
      importFileBucket:
        description: Name of a bucket where files from NFS share will be imported to upon Rover cluster
          return.
        type: string
        x-default-description: 'null'
      isImportRequested:
        description: The flag indicating that customer requests data to be imported to OCI upon Rover
          cluster return.
        type: boolean
        x-default-description: 'null'
      lifecycleState:
        description: The current state of the RoverCluster.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      oracleShippingTrackingUrl:
        description: Tracking Url for the shipped Rover Cluster.
        format: uri
        type: string
      pointOfContact:
        description: Name of point of contact for this order if customer is picking up.
        type: string
      pointOfContactPhoneNumber:
        description: Phone number of point of contact for this order if customer is picking up.
        type: string
      shippingPreference:
        default: ORACLE_SHIPPED
        description: Preference for device delivery.
        enum:
        - ORACLE_SHIPPED
        - CUSTOMER_PICKUP
        type: string
      shippingVendor:
        description: Shipping vendor of choice for orace to customer shipping.
        type: string
      subscriptionId:
        description: ID provided to customer after successful subscription to Rover Stations.
        type: string
      superUserPassword:
        description: Root password for the rover cluster.
        type: string
      systemTags: *id009
      timePickupExpected:
        description: Expected date when customer wants to pickup the device if they chose customer pickup.
        format: date-time
        type: string
      unlockPassphrase:
        description: Password to unlock the rover cluster.
        type: string
    type: object
  UpdateRoverEntitlementDetails:
    description: Information required to update a RoverEntitlement.
    properties:
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      entitlementDetails:
        description: Details about the entitlement.
        type: string
      freeformTags: *id007
      lifecycleState:
        description: The current state of the RoverNode.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      requestorEmail:
        description: Requestor email for the entitlement.
        type: string
      requestorName:
        description: Requestor name for the entitlement.
        type: string
      systemTags: *id009
      tenantId:
        description: tenant Id.
        type: string
    type: object
  UpdateRoverNodeDetails:
    description: The information required to update a RoverNode.
    properties:
      customerShippingAddress:
        $ref: '#/definitions/ShippingAddress'
      dataValidationCode:
        description: Validation code returned by data validation tool. Required for return shipping label
          generation if data import was requested.
        type: string
        x-default-description: 'null'
      definedTags: *id006
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      enclosureType:
        description: The type of enclosure rover nodes in this cluster are shipped in.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/EnclosureType'
      freeformTags: *id007
      importCompartmentId:
        description: An OCID of a compartment where data will be imported to upon Rover node return.
        type: string
        x-default-description: 'null'
      importFileBucket:
        description: Name of a bucket where files from NFS share will be imported to upon Rover node return.
        type: string
        x-default-description: 'null'
      isImportRequested:
        description: The flag indicating that customer requests data to be imported to OCI upon Rover
          node return.
        type: boolean
        x-default-description: 'null'
      lifecycleState:
        description: The current state of the RoverNode.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      lifecycleStateDetails:
        description: A property that can contain details on the lifecycle.
        type: string
      nodeWorkloads:
        description: List of existing workloads that should be provisioned on the node.
        items:
          $ref: '#/definitions/RoverWorkload'
        type: array
      oracleShippingTrackingUrl:
        description: Tracking Url for the shipped FmsRoverNode.
        format: uri
        type: string
      pointOfContact:
        description: Name of point of contact for this order if customer is picking up.
        type: string
      pointOfContactPhoneNumber:
        description: Phone number of point of contact for this order if customer is picking up.
        type: string
      publicKey:
        description: The public key of the resource principal
        type: string
      serialNumber:
        description: Serial number of the node.
        type: string
      shape:
        description: The shape of workloads in the node.
        type: string
      shippingPreference:
        default: ORACLE_SHIPPED
        description: Preference for device delivery.
        enum:
        - ORACLE_SHIPPED
        - CUSTOMER_PICKUP
        type: string
      shippingVendor:
        description: Shipping vendor of choice for orace to customer shipping.
        type: string
      superUserPassword:
        description: Root password for the rover node.
        type: string
      systemTags: *id009
      timePickupExpected:
        description: Expected date when customer wants to pickup the device if they chose customer pickup.
        format: date-time
        type: string
      timeReturnWindowEnds:
        description: End time for the window to pickup the device from customer.
        format: date-time
        type: string
      timeReturnWindowStarts:
        description: Start time for the window to pickup the device from customer.
        format: date-time
        type: string
      unlockPassphrase:
        description: Password to unlock the rover node.
        type: string
    type: object
parameters:
  AvailabilityDomainQueryParam:
    description: The name of the Availability domain.
    in: query
    name: availabilityDomain
    required: false
    type: string
  ClusterTypeQueryParam:
    description: A filter to return only Clusters of type matched with the given cluster type.
    enum: *id005
    in: query
    name: clusterType
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-top-level-enum: '#/definitions/ClusterType'
  CompartmentIdQueryParam:
    description: The OCID of the compartment in which to list resources.
    in: query
    name: compartmentId
    required: true
    type: string
  DisplayNameQueryParam:
    description: A filter to return only resources that match the entire display name given.
    in: query
    maxLength: 255
    minLength: 1
    name: displayName
    type: string
    x-default-description: 'null'
  GetRoverNodeRptToken:
    description: 'The Java Web Token which is a signature of the request that is signed with the resource''s
      private key

      This is meant solely in the context of getRpt

      '
    in: header
    name: jwt
    required: true
    type: string
  IfMatchHeader:
    description: 'For optimistic concurrency control. In the PUT or DELETE call

      for a resource, set the `if-match` parameter to the value of the

      etag from a previous GET or POST response for that resource.

      The resource will be updated or deleted only if the etag you

      provide matches the resource''s current etag value.

      '
    in: header
    name: if-match
    required: false
    type: string
  LifecycleStateQueryParam:
    description: A filter to return only resources their lifecycleState matches the given lifecycleState.
    enum: *id008
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-top-level-enum: '#/definitions/LifecycleState'
  NodeTypeQueryParam:
    description: A filter to return only Nodes of type matched with the given node type.
    enum: *id011
    in: query
    name: nodeType
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-top-level-enum: '#/definitions/NodeType'
  PaginationLimitQueryParam:
    default: 10
    description: The maximum number of items to return.
    in: query
    maximum: 100
    minimum: 1
    name: limit
    type: integer
  PaginationTokenQueryParam:
    description: The page token representing the page at which to start retrieving results. This is usually
      retrieved from a previous list call.
    in: query
    minLength: 1
    name: page
    type: string
    x-default-description: 'null'
  RequestIdHeader:
    description: The client request ID for tracing.
    in: header
    name: opc-request-id
    type: string
  RetryTokenHeader:
    description: 'A token that uniquely identifies a request so it can be retried in case of a timeout
      or

      server error without risk of executing that same action again. Retry tokens expire after 24

      hours, but can be invalidated before then due to conflicting operations. For example, if a resource

      has been deleted and purged from the system, then a retry of the original creation request

      might be rejected.

      '
    in: header
    maxLength: 64
    minLength: 1
    name: opc-retry-token
    required: false
    type: string
  RoverClusterIdentifierPathParam:
    description: Unique RoverCluster identifier
    in: path
    name: roverClusterId
    required: true
    type: string
  RoverEntitlementIdPathParam:
    description: ID of the rover node or cluster entitlement
    in: path
    name: roverEntitlementId
    required: true
    type: string
  RoverNodeIdentifierPathParam:
    description: Unique RoverNode identifier
    in: path
    name: roverNodeId
    required: true
    type: string
  SetRoverNodeRptToken:
    description: 'The Java Web Token which is a signature of the request that is signed with the resource''s
      private key

      This is meant solely in the context of getRpt

      '
    in: header
    name: jwt
    required: true
    type: string
  ShapeQueryParam:
    description: A filter to return only Nodes of type matched with the given node shape.
    in: query
    name: shape
    required: false
    type: string
    x-default-description: 'null'
  SortByQueryParam:
    default: timeCreated
    description: 'The field to sort by. Only one sort order may be provided. Default order for timeCreated
      is descending. Default order for displayName is ascending. If no value is specified timeCreated
      is default.

      '
    enum:
    - timeCreated
    - displayName
    in: query
    name: sortBy
    type: string
  SortOrderQueryParam:
    default: ASC
    description: The sort order to use, either 'asc' or 'desc'.
    enum: *id013
    in: query
    name: sortOrder
    type: string
    x-obmcs-top-level-enum: '#/definitions/SortOrders'
responses:
  400:
    description: Bad Request
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  401:
    description: Unauthorized
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  404:
    description: Not Found
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  409:
    description: Conflict
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  412:
    description: Precondition failed
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  429:
    description: Too Many Requests
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  500:
    description: Internal Server Error
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  default:
    description: Unknown Error
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
x-anchors:
  x-headers:
    etag: *id002
    opc-next-page: *id003
    opc-prev-page: *id004
    opc-request-id: *id001
  x-properties:
    definedTags: *id006
    freeformTags: *id007
    systemTags: *id009
    tags: *id012
x-oracle-package: com.oracle.pic.rover
