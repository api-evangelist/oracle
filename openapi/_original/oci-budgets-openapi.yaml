swagger: '2.0'
info:
  description: Use the Budgets API to manage budgets and budget alerts. For more information, see [Budgets
    Overview](/iaas/Content/Billing/Concepts/budgetsoverview.htm).
  title: Budgets API
  version: '20190111'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/5206052d079d5c8ca6b161abffcdbcb07a30a59e95bc818bd6f4e9f87c304d17.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the Budgets API OCI service and stored verbatim; API
      Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/5206052d079d5c8ca6b161abffcdbcb07a30a59e95bc818bd6f4e9f87c304d17.yaml
    what: the harvested document for Budgets API
host: 127.0.0.1
basePath: /20190111
schemes:
- http
- https
produces:
- application/json
paths:
  /budgets:
    get:
      description: 'Gets a list of budgets in a compartment.


        By default, ListBudgets returns budgets of the ''COMPARTMENT'' target type, and the budget records
        with only one target compartment OCID.


        To list all budgets, set the targetType query parameter to ALL (for example: ''targetType=ALL'').


        Clients should ignore new targetTypes, or upgrade to the latest version of the client SDK to handle
        new targetTypes.

        '
      operationId: ListBudgets
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/TargetTypeQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of BudgetSummary objects.
          headers:
            opc-next-page:
              description: 'For pagination of a list of `Budget`s. If this header appears in the response,
                then this

                is a partial list of budgets. Include this value as the `page` parameter in a subsequent

                GET request to get the next batch of budgets.

                '
              type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
          schema:
            items:
              $ref: '#/definitions/BudgetSummary'
            type: array
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a list of Budgets
      tags:
      - budget
    post:
      description: 'Creates a new budget.

        '
      operationId: CreateBudget
      parameters:
      - description: Details for the new budget.
        in: body
        name: CreateBudgetDetails
        required: true
        schema:
          $ref: '#/definitions/CreateBudgetDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The budget was successfully created.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/Budget'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new Budget
      tags:
      - budget
      x-example: "POST /20190111/budgets\nHost: usage.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization\
        \ and other headers&gt;</var>\n{\n  \"compartmentId\": \"ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrvur7xq\"\
        ,\n  \"targetType\" : \"COMPARTMENT\",\n  \"targets\" : [\"ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrvur7xq\"\
        ],\n  \"amount\": \"100.00\",\n  \"resetPeriod\": \"Monthly\"\n}\n"
  /budgets/{budgetId}:
    delete:
      description: Deletes a specified budget resource.
      operationId: DeleteBudget
      parameters:
      - $ref: '#/parameters/BudgetIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: The budget was successfully deleted.
          headers:
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete a Budget
      tags:
      - budget
      x-related-resource: '#/definitions/Budget'
    get:
      description: Gets a budget by the identifier.
      operationId: GetBudget
      parameters:
      - $ref: '#/parameters/BudgetIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Retrieves the budget with the given OCID.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/Budget'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a Budget
      tags:
      - budget
    put:
      description: Update a budget identified by the OCID.
      operationId: UpdateBudget
      parameters:
      - $ref: '#/parameters/BudgetIdPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateBudgetDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateBudgetDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The budget was successfully updated.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/Budget'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates a Budget
      tags:
      - budget
  /budgets/{budgetId}/alertRules:
    get:
      description: 'Returns a list of Alert Rules for a specified budget.

        '
      operationId: ListAlertRules
      parameters:
      - $ref: '#/parameters/BudgetIdPathParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of AlertRuleSummary objects.
          headers:
            opc-next-page:
              description: 'For pagination of a list of `AlertRuleSummary`. If this header appears in
                the response, then this

                is a partial list of AlertRuleSummaries. Include this value as the `page` parameter in
                a subsequent

                GET request to get the next batch of AlertRuleSummaries.

                '
              type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
          schema:
            items:
              $ref: '#/definitions/AlertRuleSummary'
            type: array
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of all Alert Rules for a Budget
      tags:
      - budget
    post:
      description: 'Creates a new Alert Rule.

        '
      operationId: CreateAlertRule
      parameters:
      - $ref: '#/parameters/BudgetIdPathParam'
      - description: Details for the new Alert Rule.
        in: body
        name: CreateAlertRuleDetails
        required: true
        schema:
          $ref: '#/definitions/CreateAlertRuleDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The Alert Rule was successfully created.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/AlertRule'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new Alert Rule
      tags:
      - budget
  /budgets/{budgetId}/alertRules/{alertRuleId}:
    delete:
      description: Deletes a specified Alert Rule resource.
      operationId: DeleteAlertRule
      parameters:
      - $ref: '#/parameters/BudgetIdPathParam'
      - $ref: '#/parameters/AlertRuleIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: The Alert Rule was successfully deleted.
          headers:
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete an Alert Rule
      tags:
      - budget
      x-related-resource: '#/definitions/AlertRule'
    get:
      description: Gets an Alert Rule for a specified budget.
      operationId: GetAlertRule
      parameters:
      - $ref: '#/parameters/BudgetIdPathParam'
      - $ref: '#/parameters/AlertRuleIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Retrieves the Alert Rule with the given OCID.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/AlertRule'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get an Alert Rule for a specified budget
      tags:
      - budget
    put:
      description: Update an Alert Rule for the budget identified by the OCID.
      operationId: UpdateAlertRule
      parameters:
      - $ref: '#/parameters/BudgetIdPathParam'
      - $ref: '#/parameters/AlertRuleIdPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateAlertRuleDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateAlertRuleDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The Alert Rule was successfully updated.
          headers:
            etag:
              description: For optimistic concurrency control. See `if-match`.
              type: string
            opc-request-id:
              description: 'Unique Oracle-assigned identifier for the request. If

                you need to contact Oracle about a particular request,

                please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/AlertRule'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates an Alert Rule
      tags:
      - budget
  /costAlertSubscriptions:
    get:
      description: 'Gets a list of Cost Alert Subscription in a compartment.

        '
      operationId: ListCostAlertSubscriptions
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/NameQueryParam'
      - $ref: '#/parameters/CostAlertSubscriptionLifecycleStateQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of CostAlertSubscriptionSummary objects.
          headers:
            opc-next-page: &id003
              description: 'For list pagination. When this header appears in the response, additional
                pages

                of results remain. For important details about how pagination works, see

                [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

                '
              type: string
            opc-request-id: &id001
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/CostAlertSubscriptionCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a list of Cost Alert Subscriptions
      tags:
      - costAd
    post:
      description: 'Creates a new CostAlert Subscription.

        '
      operationId: CreateCostAlertSubscription
      parameters:
      - description: Details for the new subscription.
        in: body
        name: CreateCostAlertSubscriptionDetails
        required: true
        schema:
          $ref: '#/definitions/CreateCostAlertSubscriptionDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The costAlertSubscription was successfully created.
          headers:
            etag: &id002
              description: 'For optimistic concurrency control. See `if-match`.

                '
              type: string
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAlertSubscription'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new Cost Alert Subscription
      tags:
      - costAd
      x-example: "POST /20190111/costAlertSubscription\nHost: usage.us-phoenix-1.oci.oraclecloud.com\n\
        <var>&lt;authorization and other headers&gt;</var>\n{\n  \"compartmentId\": \"ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrvur7xq\"\
        ,\n  \"name\": \"Department A email list\",\n  \"channels\" : {\"email\": [\"abc123@example.com\"\
        ,\"example-user@domain-ex.com\"]}\n}\n"
  /costAlertSubscriptions/{costAlertSubscriptionId}:
    delete:
      description: Deletes a specified CostAlertSubscription resource.
      operationId: DeleteCostAlertSubscription
      parameters:
      - $ref: '#/parameters/CostAlertSubscriptionIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: CostAlertSubscription is successfully deleted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete a CostAlertSubscription
      tags:
      - costAd
      x-related-resource: '#/definitions/CostAlertSubscription'
    get:
      description: Gets a CostAlertSubscription by the identifier.
      operationId: GetCostAlertSubscription
      parameters:
      - $ref: '#/parameters/CostAlertSubscriptionIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Retrieves the CostAlertSubscription with the given OCID.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAlertSubscription'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a CostAlertSubscription
      tags:
      - costAd
    put:
      description: Update a CostAlertSubscription identified by the OCID.
      operationId: UpdateCostAlertSubscription
      parameters:
      - $ref: '#/parameters/CostAlertSubscriptionIdPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateCostAlertSubscriptionDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateCostAlertSubscriptionDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The CostAlertSubscription was successfully updated.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAlertSubscription'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates a CostAlertSubscription
      tags:
      - costAd
  /costAnomalyEventAnalytics:
    get:
      description: 'Gets a list of Cost Anomaly Events analytics summary - aggregated metrics for a given
        time period.

        '
      operationId: SummarizeCostAnomalyEventAnalytics
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/NameQueryParam'
      - $ref: '#/parameters/CostMonitorIdQueryParam'
      - $ref: '#/parameters/TargetTenantIdQueryParam'
      - $ref: '#/parameters/StartDateQueryParam'
      - $ref: '#/parameters/EndDateQueryParam'
      - $ref: '#/parameters/RegionQueryParam'
      - $ref: '#/parameters/CostImpactQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of CostAnomalyEventAnalyticSummary objects.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyEventAnalyticCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a list of Cost Anomaly Events analytics summary.
      tags:
      - costAd
      x-related-resource: '#/definitions/CostAnomalyEvent'
  /costAnomalyEvents:
    get:
      description: 'Gets a list of Cost Anomaly Event in a compartment.

        '
      operationId: ListCostAnomalyEvents
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/NameQueryParam'
      - $ref: '#/parameters/CostMonitorIdQueryParam'
      - $ref: '#/parameters/TargetTenantIdQueryParam'
      - $ref: '#/parameters/StartDateQueryParam'
      - $ref: '#/parameters/EndDateQueryParam'
      - $ref: '#/parameters/RegionQueryParam'
      - $ref: '#/parameters/CostImpactQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of CostAnomalyEventSummary objects.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyEventCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a list of Cost Anomaly Event
      tags:
      - costAd
  /costAnomalyEvents/{costAnomalyEventId}:
    get:
      description: Gets a CostAnomalyEvent by the identifier.
      operationId: GetCostAnomalyEvent
      parameters:
      - $ref: '#/parameters/CostAnomalyEventIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Retrieves the CostAnomalyEvent with the given OCID.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyEvent'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a CostAnomalyEvent
      tags:
      - costAd
    put:
      description: Update a CostAnomalyEvent identified by the OCID.
      operationId: UpdateCostAnomalyEvent
      parameters:
      - $ref: '#/parameters/CostAnomalyEventIdPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateCostAnomalyEventDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateCostAnomalyEventDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The CostAnomalyEvent was successfully updated.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyEvent'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates a CostAnomalyEvent
      tags:
      - costAd
  /costAnomalyMonitors:
    get:
      description: 'Gets a list of Cost Anomaly Monitors in a compartment.

        '
      operationId: ListCostAnomalyMonitors
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/CostAnomalyMonitorLifecycleStateQueryParam'
      - $ref: '#/parameters/NameQueryParam'
      - $ref: '#/parameters/TargetTenantIdQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of CostAnomalyMonitorSummary objects.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyMonitorCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a list of Cost Anomaly Monitors
      tags:
      - costAd
    post:
      description: 'Creates a new costAnomaly Monitor.

        '
      operationId: CreateCostAnomalyMonitor
      parameters:
      - description: Details for the new monitor.
        in: body
        name: CreateCostAnomalyMonitorDetails
        required: true
        schema:
          $ref: '#/definitions/CreateCostAnomalyMonitorDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The cost anomaly monitor is successfully created.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyMonitor'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new costAnomaly Monitor
      tags:
      - costAd
      x-example: "POST /20250520/costAnomalyMonitor\nHost: usage.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization\
        \ and other headers&gt;</var>\n{\n  compartmentId: \"ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrvur7xq\"\
        ,\n  name: \"sample cost monitor\",\n  targetResourceFilter: {\n    operator: \"AND\",\n    filters:\
        \ [\n      {\n        operator: \"OR\",\n        dimensions: [\n          {key: \"compartment\"\
        ,value: \"ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrtge6tg\"\
        },\n          {key: \"compartment\",value: \"ocid1.compartment.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrtbefww\"\
        }\n        ],\n        tags: [],\n        filters: []\n      },\n      {\n        operator: \"\
        OR\",\n        dimensions: [\n          {key: \"service\",value: \"compute\"},\n          {key:\
        \ \"service\",value: \"network\"}\n        ],\n        tags: [],\n        filters: []\n      },\n\
        \      {\n        operator: \"OR\",\n        dimensions: [],\n        tags: [\n          {namespace:\
        \ \"Oracle-Tags\", key: \"CreatedBy\", value: \"ocid1.user.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrfffebde\"\
        },\n          {namespace: \"Oracle-Tags\", key: \"CreatedBy\", value: \"ocid1.user.oc1..aaaaaaaayzfqeibduyox6iib3olcmdar3ugly4fmameq4h7lcdlihrpounnhr\"\
        }\n        ],\n        filters: []\n      }\n    ]\n    dimensions: [],\n    tags: []\n  }\n}\n"
  /costAnomalyMonitors/{costAnomalyMonitorId}:
    delete:
      description: Deletes a specified CostAnomalyMonitor resource.
      operationId: DeleteCostAnomalyMonitor
      parameters:
      - $ref: '#/parameters/CostAnomalyMonitorIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: The CostAnomalyMonitor is successfully deleted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete a CostAnomalyMonitor
      tags:
      - costAd
      x-related-resource: '#/definitions/CostAnomalyMonitor'
    get:
      description: Gets a CostAnomalyMonitor by the identifier.
      operationId: GetCostAnomalyMonitor
      parameters:
      - $ref: '#/parameters/CostAnomalyMonitorIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Retrieves the CostAnomalyMonitor with the given OCID.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyMonitor'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns a CostAnomalyMonitor
      tags:
      - costAd
    put:
      description: Update a CostAnomalyMonitor identified by the OCID.
      operationId: UpdateCostAnomalyMonitor
      parameters:
      - $ref: '#/parameters/CostAnomalyMonitorIdPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateCostAnomalyMonitorDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateCostAnomalyMonitorDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The Cost Anomaly Monitor is successfully updated.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyMonitor'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates a Cost Anomaly Monitor
      tags:
      - costAd
  /costAnomalyMonitors/{costAnomalyMonitorId}/actions/disable:
    post:
      description: Disables the cost anomaly monitor. This stops cost anomaly detection for targeted resource(s).
      operationId: DisableCostAnomalyMonitor
      parameters:
      - $ref: '#/parameters/CostAnomalyMonitorIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        200:
          description: The cost anomaly monitor has been disabled.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyMonitor'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Disables the cost anomaly monitor.
      tags:
      - costAd
      x-obmcs-terraform:
        actionType: DISABLE
        identifier: CostAnomalyMonitorEnableToggle
      x-related-resource: '#/definitions/CostAnomalyMonitor'
  /costAnomalyMonitors/{costAnomalyMonitorId}/actions/enable:
    post:
      description: Enables the cost anomaly monitor. This (re)starts the cost anomaly detection for targeted
        resource(s).
      operationId: EnableCostAnomalyMonitor
      parameters:
      - $ref: '#/parameters/CostAnomalyMonitorIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        200:
          description: The cost anomaly monitor has been enabled.
          headers:
            etag: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CostAnomalyMonitor'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Enables the cost anomaly monitor.
      tags:
      - costAd
      x-obmcs-terraform:
        actionType: ENABLE
        identifier: CostAnomalyMonitorEnableToggle
      x-related-resource: '#/definitions/CostAnomalyMonitor'
definitions:
  AlertRule:
    description: The alert rule.
    properties:
      budgetId:
        description: The OCID of the budget.
        type: string
      definedTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the alert rule.
        type: string
      displayName:
        description: The name of the alert rule. Avoid entering confidential information.
        type: string
      freeformTags:
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      id:
        description: The OCID of the alert rule.
        type: string
      lifecycleState:
        description: The current state of the alert rule.
        enum: &id004
        - ACTIVE
        - INACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      message:
        description: Custom message sent when an alert is triggered.
        type: string
      recipients:
        description: 'The delimited list of email addresses to receive the alert when it triggers.

          Delimiter characters can be a comma, space, TAB, or semicolon.

          '
        type: string
      threshold:
        description: 'The threshold for triggering the alert. If the thresholdType is PERCENTAGE, the
          maximum value is 10000.

          '
        exclusiveMinimum: true
        minimum: 0
        type: number
      thresholdType:
        description: The type of threshold.
        enum: &id005
        - PERCENTAGE
        - ABSOLUTE
        type: string
        x-obmcs-top-level-enum: '#/definitions/ThresholdType'
      timeCreated:
        description: The time the budget was created.
        format: date-time
        type: string
      timeUpdated:
        description: The time the budget was updated.
        format: date-time
        type: string
      type:
        description: 'The type of the alert. Valid values are ACTUAL (the alert triggers based on actual
          usage), or

          FORECAST (the alert triggers based on predicted usage).

          '
        enum: &id006
        - ACTUAL
        - FORECAST
        type: string
        x-obmcs-top-level-enum: '#/definitions/AlertType'
      version:
        description: The version of the alert rule. Starts from 1 and increments by 1.
        type: integer
    required:
    - id
    - budgetId
    - displayName
    - type
    - threshold
    - thresholdType
    - recipients
    - timeCreated
    - timeUpdated
    - lifecycleState
  AlertRuleSummary:
    description: The alert rule.
    properties:
      budgetId:
        description: The OCID of the budget.
        type: string
      definedTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the alert rule.
        type: string
      displayName:
        description: The name of the alert rule. Avoid entering confidential information.
        type: string
      freeformTags:
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      id:
        description: The OCID of the alert rule.
        type: string
      lifecycleState:
        description: The current state of the alert rule.
        enum: *id004
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      message:
        description: The custom message that will be sent when the alert is triggered.
        type: string
      recipients:
        description: The audience that receives the alert when it triggers.
        type: string
      threshold:
        description: 'The threshold for triggering the alert. If the thresholdType is PERCENTAGE, the
          maximum value is 10000.

          '
        exclusiveMinimum: true
        minimum: 0
        type: number
      thresholdType:
        description: The type of threshold.
        enum: *id005
        type: string
        x-obmcs-top-level-enum: '#/definitions/ThresholdType'
      timeCreated:
        description: The time when the budget was created.
        format: date-time
        type: string
      timeUpdated:
        description: The time when the budget was updated.
        format: date-time
        type: string
      type:
        description: 'ACTUAL means the alert triggers based on actual usage.

          FORECAST means the alert triggers based on predicted usage.

          '
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/AlertType'
      version:
        description: The version of the alert rule. Starts from 1 and increments by 1.
        type: integer
    required:
    - id
    - budgetId
    - displayName
    - type
    - threshold
    - thresholdType
    - recipients
    - timeCreated
    - timeUpdated
    - lifecycleState
  AlertType:
    description: 'Valid values are ACTUAL (the alert triggers based on actual usage), or

      FORECAST (the alert triggers based on predicted usage).

      '
    enum: *id006
    type: string
  Budget:
    description: A budget.
    properties:
      actualSpend:
        description: The actual spend in currency for the current budget cycle.
        minimum: 0
        type: number
      alertRuleCount:
        description: The total number of alert rules in the budget.
        minimum: 0
        type: integer
      amount:
        description: 'The amount of the budget expressed in the currency of the customer''s rate card.

          '
        exclusiveMinimum: true
        minimum: 0
        type: number
      budgetProcessingPeriodStartOffset:
        default: 1
        description: The number of days offset from the first day of the month, at which the budget processing
          period starts. In months that have fewer days than this value, processing will begin on the
          last day of that month. For example, for a value of 12, processing starts every month on the
          12th at midnight.
        exclusiveMaximum: false
        exclusiveMinimum: false
        maximum: 31
        minimum: 1
        type: integer
      compartmentId:
        description: The OCID of the compartment.
        type: string
      definedTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the budget.
        type: string
      displayName:
        description: The display name of the budget. Avoid entering confidential information.
        type: string
      endDate:
        description: The time when the one-time budget concludes. For example, `2023-08-12T16:01:19.847222+05:30`.
          The date-time format conforms to RFC 3339, and will be truncated to the starting point of the
          date provided after being converted to UTC time.
        format: date-time
        type: string
      forecastedSpend:
        description: The forecasted spend in currency by the end of the current budget cycle.
        minimum: 0
        type: number
      freeformTags:
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      id:
        description: The OCID of the budget.
        type: string
      lifecycleState:
        description: The current state of the budget.
        enum: *id004
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      processingPeriodType:
        description: 'The budget processing period type. Valid values are INVOICE, MONTH, and SINGLE_USE.

          '
        enum: &id007
        - INVOICE
        - MONTH
        - SINGLE_USE
        type: string
        x-obmcs-top-level-enum: '#/definitions/ProcessingPeriodType'
      resetPeriod:
        description: 'The reset period for the budget.

          '
        enum: &id008
        - MONTHLY
        type: string
        x-obmcs-top-level-enum: '#/definitions/ResetPeriod'
      startDate:
        description: The date when the one-time budget begins. For example, `2023-07-12T16:01:19.847222+05:30`.
          The date-time format conforms to RFC 3339, and will be truncated to the starting point of the
          date provided after being converted to UTC time.
        format: date-time
        type: string
      systemTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
      targetCompartmentId:
        description: 'This is DEPRECATED. For backwards compatability, the property is populated when

          the targetType is "COMPARTMENT", and targets contain the specific target compartment OCID.

          For all other scenarios, this property will be left empty.

          '
        type: string
      targetType:
        description: 'The type of target on which the budget is applied.

          '
        enum: &id009
        - COMPARTMENT
        - TAG
        type: string
        x-obmcs-top-level-enum: '#/definitions/TargetType'
      targets:
        description: "The list of targets on which the budget is applied.\n  If the targetType is \"COMPARTMENT\"\
          , the targets contain the list of compartment OCIDs.\n  If the targetType is \"TAG\", the targets\
          \ contain the list of cost tracking tag identifiers in the form of \"{tagNamespace}.{tagKey}.{tagValue}\"\
          .\n"
        items:
          type: string
        type: array
      timeCreated:
        description: The time that the budget was created.
        format: date-time
        type: string
      timeSpendComputed:
        description: The time that the budget spend was last computed.
        format: date-time
        type: string
      timeUpdated:
        description: The time that the budget was updated.
        format: date-time
        type: string
      version:
        description: The version of the budget. Starts from 1 and increments by 1.
        type: integer
    required:
    - id
    - compartmentId
    - displayName
    - amount
    - resetPeriod
    - timeCreated
    - timeUpdated
    - alertRuleCount
    - lifecycleState
  BudgetSummary:
    description: A budget.
    properties:
      actualSpend:
        description: The actual spend in currency for the current budget cycle.
        minimum: 0
        type: number
      alertRuleCount:
        description: The total number of alert rules in the budget.
        minimum: 0
        type: integer
      amount:
        description: 'The amount of the budget, expressed in the currency of the customer''s rate card.

          '
        exclusiveMinimum: true
        minimum: 0
        type: number
      budgetProcessingPeriodStartOffset:
        default: 1
        description: The number of days offset from the first day of the month, at which the budget processing
          period starts. In months that have fewer days than this value, processing will begin on the
          last day of that month. For example, for a value of 12, processing starts every month on the
          12th at midnight.
        exclusiveMaximum: false
        exclusiveMinimum: false
        maximum: 31
        minimum: 1
        type: integer
      compartmentId:
        description: The OCID of the compartment.
        type: string
      definedTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the budget.
        type: string
      displayName:
        description: The display name of the budget. Avoid entering confidential information.
        type: string
      endDate:
        description: The time when the one-time budget concludes. For example, - `2023-08-12T16:01:19.847222+05:30`.
          The date-time format conforms to RFC 3339, and will be truncated to the starting point of the
          date provided after being converted to UTC time.
        format: date-time
        type: string
      forecastedSpend:
        description: The forecasted spend in currency by the end of the current budget cycle.
        minimum: 0
        type: number
      freeformTags:
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      id:
        description: The OCID of the budget.
        type: string
      lifecycleState:
        description: The current state of the budget.
        enum: *id004
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      processingPeriodType:
        description: 'The type of the budget processing period. Valid values are INVOICE, MONTH, and SINGLE_USE.

          '
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/ProcessingPeriodType'
      resetPeriod:
        description: 'The reset period for the budget.

          '
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/ResetPeriod'
      startDate:
        description: The date when the one-time budget begins. For example, `2023-07-12T16:01:19.847222+05:30`.
          The date-time format conforms to RFC 3339, and will be truncated to the starting point of the
          date provided after being converted to UTC time.
        format: date-time
        type: string
      systemTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
      targetCompartmentId:
        description: 'This is DEPRECATED. For backwards compatability, the property is populated when

          the targetType is "COMPARTMENT", and the targets contain the specific target compartment OCID.

          For all other scenarios, this property is left empty.

          '
        type: string
      targetType:
        description: 'The type of target on which the budget is applied.

          '
        enum: *id009
        type: string
        x-obmcs-top-level-enum: '#/definitions/TargetType'
      targets:
        description: "The list of targets on which the budget is applied.\n  If the targetType is \"COMPARTMENT\"\
          , the targets contain the list of compartment OCIDs.\n  If the targetType is \"TAG\", the targets\
          \ contain the list of cost tracking tag identifiers in the form of \"{tagNamespace}.{tagKey}.{tagValue}\"\
          .\n"
        items:
          type: string
        type: array
      timeCreated:
        description: The time the budget was created.
        format: date-time
        type: string
      timeSpendComputed:
        description: The time the budget spend was last computed.
        format: date-time
        type: string
      timeUpdated:
        description: The time the budget was updated.
        format: date-time
        type: string
      version:
        description: The version of the budget. Starts from 1 and increments by 1.
        type: integer
    required:
    - id
    - compartmentId
    - displayName
    - amount
    - resetPeriod
    - timeCreated
    - timeUpdated
    - alertRuleCount
    - lifecycleState
  CostAlertSubscription:
    description: A CostAlertSubscription.
    properties:
      channels:
        description: The notification channels string.
        maxLength: 1000
        minLength: 1
        type: string
      compartmentId:
        description: The OCID of the compartment which hold the cost alert subscription resource.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: &id010
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.


            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a

          namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the cost alert subscription.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: &id011
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no

          predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      id:
        description: The OCID of the Cost Alert Subscription.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the cost alert subscription.
        enum:
        - ACTIVE
        - INACTIVE
        type: string
      name:
        description: The name of the cost alert subscription. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      systemTags: &id012
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"free-tier-retained": "true"}`

            '
          type: object
        description: 'System tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).

          System tags can be viewed by users, but can only be created by the system.


          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
      timeCreated:
        description: The time that the cost alert subscription was created.
        format: date-time
        type: string
      timeUpdated:
        description: The time that the cost alert subscription was updated.
        format: date-time
        type: string
      type:
        description: Type of alert subscription
        enum: &id013
        - DEFAULT
        - CUSTOM
        type: string
        x-obmcs-top-level-enum: '#/definitions/MonitorType'
    required:
    - id
    - compartmentId
    - name
    - lifecycleState
    - channels
    - timeCreated
    - timeUpdated
  CostAlertSubscriptionCollection:
    description: Results of a costAlertSubscription search. Contains both CostAlertSubscriptionSummary
      items and other data.
    properties:
      items:
        description: A page of CostAnomalyMonitorSummary objects.
        items:
          $ref: '#/definitions/CostAlertSubscriptionSummary'
        type: array
    required:
    - items
    type: object
  CostAlertSubscriptionMap:
    description: The mapping of cost monitor to alert subscription along with thresholds.
    properties:
      costAlertSubscriptionId:
        description: The costAlertSubscription ocid which the cost monitor alert maps to.
        type: string
      operator:
        description: 'The filter operator. Example: ''AND'', ''OR'', ''NOT''.'
        enum:
        - AND
        - NOT
        - OR
        type: string
      thresholdAbsoluteValue:
        description: The absolute threshold value.
        minimum: 0
        type: integer
      thresholdRelativePercent:
        description: The relative percentage threshold value.
        minimum: 0
        type: integer
    type: object
  CostAlertSubscriptionSummary:
    description: A CostAlertSubscription.
    properties:
      compartmentId:
        description: The OCID of the compartment which hold the cost alert subscription resource.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id010
      freeformTags: *id011
      id:
        description: The OCID of the Cost Alert Subscription.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the cost alert subscription.
        type: string
        x-obmcs-enumref: '#/definitions/CostAlertSubscription/lifecycleState'
      name:
        description: The name of the cost alert subscription. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      systemTags: *id012
    required:
    - id
    - compartmentId
    - name
    - lifecycleState
  CostAnomalyEvent:
    description: A CostAnomalyEvent.
    properties:
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      costImpact:
        description: The cost impact of the detected anomaly.
        format: double
        type: number
      costMonitorId:
        description: The OCID of the associated cost monitor.
        maxLength: 255
        minLength: 1
        type: string
      costMonitorName:
        description: The name of the associated cost monitor.
        maxLength: 255
        minLength: 1
        type: string
      costVariancePercentage:
        description: The cost variance percentage of the detected anomaly.
        format: double
        type: number
      definedTags: *id010
      feedbackResponse:
        description: The feedback response for the cost anomaly event.
        enum:
        - ACCURATE_ANOMALY
        - EXPECTED_ANOMALY
        - NOT_AN_ANOMALY
        type: string
      freeformTags: *id011
      id:
        description: The OCID of the Cost Anomaly Event.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the cost anomaly event.
        enum:
        - ACTIVE
        - INACTIVE
        type: string
      rootCauseDetail:
        $ref: '#/definitions/RootCauseDetail'
      systemTags: *id012
      targetResourceFilter:
        $ref: '#/definitions/TargetResourceFilter'
      timeAnomalyEventDate:
        description: The event date of the anomaly event.
        format: date-time
        type: string
      timeCreated:
        description: The created time of the cost anomaly event.
        format: date-time
        type: string
      timeUpdated:
        description: The updated time of the cost anomaly event.
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - costMonitorId
    - lifecycleState
    - costMonitorName
    - targetResourceFilter
    - timeAnomalyEventDate
    - timeCreated
    - timeUpdated
  CostAnomalyEventAnalyticCollection:
    description: Results of a CostAnomalyEventAnalytics search.
    properties:
      items:
        description: The list of CostAnomalyEvent Analytic summary.
        items:
          $ref: '#/definitions/CostAnomalyEventAnalyticSummary'
        type: array
    required:
    - items
  CostAnomalyEventAnalyticSummary:
    description: A CostAnomalyEventAnalyticSummary.
    properties:
      averageCostImpact:
        description: The average cost impact of the anomaly events in the given time period.
        format: double
        type: number
      averageCostVariance:
        description: The average cost variance of the anomaly events in the given time period.
        format: double
        type: number
      count:
        description: The number of cost anomaly events in the given time period.
        type: integer
      timeAnomalyEventEndDate:
        description: startDate for anomaly event date.
        format: date-time
        type: string
      timeAnomalyEventStartDate:
        description: startDate for anomaly event date.
        format: date-time
        type: string
    required:
    - timeAnomalyEventStartDate
    - timeAnomalyEventEndDate
    - count
    - averageCostImpact
    - averageCostVariance
  CostAnomalyEventCollection:
    description: Results of a costAnomalyEvent search. Contains both CostAnomalyEventSummary items and
      other data.
    properties:
      items:
        description: A page of CostAnomalyMonitorSummary objects.
        items:
          $ref: '#/definitions/CostAnomalyEventSummary'
        type: array
    required:
    - items
    type: object
  CostAnomalyEventSummary:
    description: A CostAnomalyEvent.
    properties:
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      costImpact:
        description: The cost impact of the detected anomaly.
        format: double
        type: number
      costMonitorId:
        description: The OCID of the associated cost monitor.
        maxLength: 255
        minLength: 1
        type: string
      costMonitorName:
        description: The name of the associated cost monitor.
        maxLength: 255
        minLength: 1
        type: string
      costVariancePercentage:
        description: The cost variance percentage of the detected anomaly.
        format: double
        type: number
      definedTags: *id010
      freeformTags: *id011
      id:
        description: The OCID of the Cost Anomaly Event.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the cost anomaly event.
        type: string
        x-obmcs-enumref: '#/definitions/CostAnomalyEvent/lifecycleState'
      systemTags: *id012
      targetResourceFilter:
        $ref: '#/definitions/TargetResourceFilter'
      timeAnomalyEventDate:
        description: The event date of the anomaly event.
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - costMonitorId
    - costMonitorName
    - targetResourceFilter
    - timeAnomalyEventDate
  CostAnomalyMonitor:
    description: A CostAnomalyMonitor.
    properties:
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      costAlertSubscriptionMap:
        $ref: '#/definitions/CostAlertSubscriptionMap'
      definedTags: *id010
      description:
        description: The description of the budget.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id011
      id:
        description: The OCID of the Cost Anomaly Monitor.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleDetails:
        description: 'The current state details of the cost monitor.

          '
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the cost monitor.
        enum:
        - ACTIVE
        - INACTIVE
        - DELETED
        type: string
      name:
        description: The name of the cost anomaly monitor. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      systemTags: *id012
      targetResourceFilter:
        $ref: '#/definitions/TargetResourceFilter'
      timeCreated:
        description: The time that the cost monitor was created.
        format: date-time
        type: string
      timeUpdated:
        description: The time that the cost monitor was last updated.
        format: date-time
        type: string
      type:
        description: Type of cost monitor
        enum: *id013
        type: string
        x-obmcs-top-level-enum: '#/definitions/MonitorType'
    required:
    - id
    - compartmentId
    - name
    - lifecycleState
    - targetResourceFilter
    - timeCreated
    - timeUpdated
  CostAnomalyMonitorCollection:
    description: Results of a costAnomalyMonitor search. Contains both CostAnomalyMonitorSummary items
      and other data.
    properties:
      items:
        description: A page of CostAnomalyMonitorSummary objects.
        items:
          $ref: '#/definitions/CostAnomalyMonitorSummary'
        type: array
    required:
    - items
    type: object
  CostAnomalyMonitorSummary:
    description: A CostAnomalyMonitor.
    properties:
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id010
      description:
        description: The description of the budget.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id011
      id:
        description: The OCID of the Cost Anomaly Monitor.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the cost monitor.
        type: string
        x-obmcs-enumref: '#/definitions/CostAnomalyMonitor/lifecycleState'
      name:
        description: The name of the cost anomaly monitor. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      systemTags: *id012
      targetResourceFilter:
        $ref: '#/definitions/TargetResourceFilter'
      timeCreated:
        description: The time that the cost monitor was created.
        format: date-time
        type: string
      timeUpdated:
        description: The time that the cost monitor was last updated.
        format: date-time
        type: string
      type:
        description: Type of cost monitor
        enum: *id013
        type: string
        x-obmcs-top-level-enum: '#/definitions/MonitorType'
    required:
    - id
    - compartmentId
    - name
    - lifecycleState
    - targetResourceFilter
    - timeCreated
    - timeUpdated
  CreateAlertRuleDetails:
    description: The create alert rule details. This is a batch-create.
    properties:
      definedTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the alert rule.
        maxLength: 200
        type: string
      displayName:
        description: The name of the alert rule. Avoid entering confidential information.
        maxLength: 200
        type: string
      freeformTags:
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      message:
        description: The message to be sent to the recipients when the alert rule is triggered.
        maxLength: 1000
        type: string
      recipients:
        description: The audience that receives the alert when it triggers. An empty string is interpreted
          as null.
        type: string
      threshold:
        description: 'The threshold for triggering the alert, expressed as a whole number or decimal value.

          If the thresholdType is ABSOLUTE, the threshold can have at most 12 digits before the decimal
          point, and up to two digits after the decimal point.

          If the thresholdType is PERCENTAGE, the maximum value is 10000 and can have up to two digits
          after the decimal point.

          '
        exclusiveMaximum: true
        exclusiveMinimum: true
        maximum: 1.0E12
        minimum: 0.0
        type: number
      thresholdType:
        description: The type of threshold.
        enum: *id005
        type: string
        x-obmcs-top-level-enum: '#/definitions/ThresholdType'
      type:
        description: 'The type of the alert. Valid values are ACTUAL (the alert triggers based on actual
          usage), or

          FORECAST (the alert triggers based on predicted usage).

          '
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/AlertType'
    required:
    - type
    - threshold
    - thresholdType
  CreateBudgetDetails:
    description: 'The create budget details.


      Clients should use ''targetType'' and ''targets'' to specify the target type and list of targets
      on which the budget is applied.


      For backwards compatibility, ''targetCompartmentId'' is still supported for all existing clients.

      This is considered deprecated, however, and all clients are upgraded to use ''targetType'' and ''targets''.


      Specifying both ''targetCompartmentId'' and ''targets'' causes a Bad Request.

      '
    properties:
      amount:
        description: 'The amount of the budget expressed as a whole number in the currency of the customer''s
          rate card.

          '
        exclusiveMaximum: true
        exclusiveMinimum: true
        maximum: 1.0E12
        minimum: 0.0
        type: number
      budgetProcessingPeriodStartOffset:
        default: 1
        description: The number of days offset from the first day of the month, at which the budget processing
          period starts. In months that have fewer days than this value, processing will begin on the
          last day of that month. For example, for a value of 12, processing starts every month on the
          12th at midnight.
        exclusiveMaximum: false
        exclusiveMinimum: false
        maximum: 31
        minimum: 1
        type: integer
      compartmentId:
        description: The OCID of the compartment.
        type: string
      definedTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the budget.
        maxLength: 200
        type: string
      displayName:
        description: The displayName of the budget. Avoid entering confidential information.
        maxLength: 200
        type: string
      endDate:
        description: The date when the one-time budget concludes. For example, `2023-08-12T16:01:19.847222+05:30`.
          The date-time format conforms to RFC 3339, and will be truncated to the starting point of the
          date provided after being converted to UTC time.
        format: date-time
        type: string
      freeformTags:
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      processingPeriodType:
        description: 'The type of the budget processing period. Valid values are INVOICE, MONTH, and SINGLE_USE.

          '
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/ProcessingPeriodType'
      resetPeriod:
        description: 'The reset period for the budget.

          '
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/ResetPeriod'
      startDate:
        description: The date when the one-time budget begins. For example, `2023-07-12T16:01:19.847222+05:30`.
          The date-time format conforms to RFC 3339, and will be truncated to the starting point of the
          date provided after being converted to UTC time.
        format: date-time
        type: string
      systemTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
      targetCompartmentId:
        description: 'This is DEPRECATED. Set the target compartment ID in targets instead.

          '
        type: string
      targetType:
        default: COMPARTMENT
        description: 'The type of target on which the budget is applied.

          '
        enum: *id009
        type: string
        x-obmcs-top-level-enum: '#/definitions/TargetType'
      targets:
        description: "The list of targets on which the budget is applied.\n  If targetType is \"COMPARTMENT\"\
          , the targets contain the list of compartment OCIDs.\n  If targetType is \"TAG\", the targets\
          \ contain the list of cost tracking tag identifiers in the form of \"{tagNamespace}.{tagKey}.{tagValue}\"\
          .\nCurerntly, the array should contain exactly one item.\n"
        items:
          type: string
        type: array
    required:
    - compartmentId
    - amount
    - resetPeriod
  CreateCostAlertSubscriptionDetails:
    description: 'The create CostAlertSubscriptionDetails details.

      The clients should pass compartmentId where the resource should belong, description and channels.

      '
    properties:
      channels:
        description: The notification channels string.
        maxLength: 1000
        minLength: 1
        type: string
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id010
      description:
        description: The description of the cost alert subscription.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id011
      id:
        description: The OCID of the Cost Anomaly Subscription.
        maxLength: 255
        minLength: 1
        type: string
      name:
        description: The name of the cost alert subscription. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      type:
        description: Type of alert subscription
        enum: *id013
        type: string
        x-obmcs-top-level-enum: '#/definitions/MonitorType'
    required:
    - compartmentId
    - name
    - type
    - channels
  CreateCostAnomalyMonitorDetails:
    description: 'The create CostAnomalyMonitor details.

      The clients should pass ocid of the compartmentId for the monitor resource to be created. tenantIdFilter
      and resourceFilter

      targets resources to monitor in a given tenancy.

      '
    properties:
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      costAlertSubscriptionMap:
        $ref: '#/definitions/CostAlertSubscriptionMap'
      definedTags: *id010
      description:
        description: The description of the cost anomaly monitor.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id011
      name:
        description: The name of the cost anomaly monitor. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      targetResourceFilter:
        $ref: '#/definitions/TargetResourceFilter'
    required:
    - compartmentId
    - name
    - targetResourceFilter
  Dimension:
    description: 'The dimension used for filtering. Available dimensions are: "service", "skuName", "skuPartNumber",
      "unit", "compartmentName", "compartmentPath", "compartmentId", "tenantName, "tenantId", "platform",
      "region", "logicalAd", "resourceId".

      For example:

      `[{value: "COMPUTE", key: "service"}]`

      '
    properties:
      key:
        description: The dimension key.
        maxLength: 255
        minLength: 1
        type: string
      value:
        description: The dimension value.
        maxLength: 255
        minLength: 1
        type: string
    required:
    - key
    - value
    type: object
  Error:
    description: Error Information.
    properties:
      code:
        description: A short error code that defines the error, meant for programmatic parsing.
        type: string
      message:
        description: A human-readable error string.
        type: string
    required:
    - code
    - message
  LifecycleState:
    description: The current state of the resource.
    enum: *id004
    type: string
  MonitorType:
    description: 'The reset period for the budget. The valid value is DEFAULT,CUSTOM

      '
    enum: *id013
    type: string
  ProcessingPeriodType:
    description: The budget processing period type. Valid values are INVOICE, MONTH, and SINGLE_USE.
    enum: *id007
    type: string
  ResetPeriod:
    description: 'The reset period for the budget. The valid value is MONTHLY.

      '
    enum: *id008
    type: string
  RootCauseDetail:
    description: The root cause details of the cost anomaly event.
    properties:
      value:
        description: The json object representing the root cause details of the anomaly event.
        type: object
    type: object
  SortBy:
    description: 'The field to sort by. If not specified, the default is timeCreated.

      The default sort order for timeCreated is DESC.

      The default sort order for displayName is ASC in alphanumeric order.

      '
    enum: &id014
    - timeCreated
    - displayName
    type: string
  SortOrder:
    description: The sort order to use. Valid values are 'asc' or 'desc'.
    enum: &id015
    - ASC
    - DESC
    type: string
  Tag:
    description: The tag used for filtering.
    properties:
      key:
        description: The tag key.
        maxLength: 255
        minLength: 1
        type: string
      namespace:
        description: The tag namespace.
        maxLength: 255
        minLength: 1
        type: string
      value:
        description: The tag value.
        maxLength: 100
        minLength: 1
        type: string
    type: object
  TargetResourceFilter:
    description: "The filter object to target resources for cost monitor. \nCost generated by one or more\
      \ resources identified by this \nfilter is monitored for anomalous deviations.\n"
    properties:
      dimensions:
        description: The dimensions to filter on.
        items:
          $ref: '#/definitions/Dimension'
        maxItems: 100
        type: array
      filters:
        description: The nested filter object.
        items:
          $ref: '#/definitions/TargetResourceFilter'
        maxItems: 100
        type: array
      operator:
        description: 'The filter operator. Example: ''AND'', ''OR'', ''NOT''.'
        enum:
        - AND
        - NOT
        - OR
        type: string
      tags:
        description: The tags to filter on.
        items:
          $ref: '#/definitions/Tag'
        maxItems: 100
        type: array
    type: object
  TargetType:
    description: The type of target on which the budget is applied. Valid values are COMPARTMENT or TAG.
    enum: *id009
    type: string
  ThresholdType:
    description: The type of threshold. Valid values are PERCENTAGE or ABSOLUTE.
    enum: *id005
    type: string
  UpdateAlertRuleDetails:
    description: The update alert rule details.
    properties:
      definedTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the alert rule.
        maxLength: 200
        type: string
      displayName:
        description: The name of the alert rule. Avoid entering confidential information.
        maxLength: 200
        type: string
      freeformTags:
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      message:
        description: The message to be delivered to the recipients when an alert is triggered.
        maxLength: 1000
        type: string
      recipients:
        description: The audience that receives the alert when it triggers. If you need to clear out this
          value, pass in an empty string instead of a null value.
        type: string
      threshold:
        description: 'The threshold for triggering the alert, expressed as a whole number or decimal value.

          If the thresholdType is ABSOLUTE, the threshold can have at most 12 digits before the decimal
          point, and up to two digits after the decimal point.

          If the thresholdType is PERCENTAGE, the maximum value is 10000 and can have up to two digits
          after the decimal point.

          '
        exclusiveMaximum: true
        exclusiveMinimum: true
        maximum: 1.0E12
        minimum: 0.0
        type: number
      thresholdType:
        description: The type of threshold.
        enum: *id005
        type: string
        x-obmcs-top-level-enum: '#/definitions/ThresholdType'
      type:
        description: 'The type of the alert. Valid values are ACTUAL (the alert triggers based on actual
          usage), or

          FORECAST (the alert triggers based on predicted usage).

          '
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/AlertType'
  UpdateBudgetDetails:
    description: The update budget details.
    properties:
      amount:
        description: 'The amount of the budget expressed as a whole number in the currency of the customer''s
          rate card.

          '
        exclusiveMaximum: true
        exclusiveMinimum: true
        maximum: 1.0E12
        minimum: 0.0
        type: number
      budgetProcessingPeriodStartOffset:
        default: 1
        description: The number of days offset from the first day of the month, at which the budget processing
          period starts. In months that have fewer days than this value, processing will begin on the
          last day of that month. For example, for a value of 12, processing starts every month on the
          12th at midnight.
        exclusiveMaximum: false
        exclusiveMinimum: false
        maximum: 31
        minimum: 1
        type: integer
      definedTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.

            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description:
        description: The description of the budget.
        maxLength: 200
        type: string
      displayName:
        description: The displayName of the budget. Avoid entering confidential information.
        maxLength: 200
        type: string
      endDate:
        description: The time when the one-time budget concludes. For example, `2023-08-12T16:01:19.847222+05:30`.
          The date-time format conforms to RFC 3339, and will be truncated to the starting point of the
          date provided after being converted to UTC time.
        format: date-time
        type: string
      freeformTags:
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      processingPeriodType:
        description: 'The type of the budget processing period. Valid values are INVOICE, MONTH, and SINGLE_USE.

          '
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/ProcessingPeriodType'
      resetPeriod:
        description: 'The reset period for the budget.

          '
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/ResetPeriod'
      startDate:
        description: The date when the one-time budget begins. For example, `2023-07-12T16:01:19.847222+05:30`.
          The date-time format conforms to RFC 3339, and will be truncated to the starting point of the
          date provided after being converted to UTC time.
        format: date-time
        type: string
      systemTags:
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
  UpdateCostAlertSubscriptionDetails:
    description: 'The update CostAlertSubscriptionDetails details.

      '
    properties:
      channels:
        description: The notification channels string.
        maxLength: 1000
        minLength: 1
        type: string
      definedTags: *id010
      description:
        description: The description of the cost alert subscription.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id011
  UpdateCostAnomalyEventDetails:
    description: 'The update CostAnomalyEvent details.

      '
    properties:
      definedTags: *id010
      feedbackResponse:
        description: The feedback response for the cost anomaly event.
        type: string
        x-obmcs-enumref: '#/definitions/CostAnomalyEvent/feedbackResponse'
      freeformTags: *id011
  UpdateCostAnomalyMonitorDetails:
    description: Update CostAnomalyMonitor details.
    properties:
      costAlertSubscriptionMap:
        $ref: '#/definitions/CostAlertSubscriptionMap'
      definedTags: *id010
      description:
        description: The description of the cost anomaly monitor.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id011
parameters:
  AlertRuleIdPathParam:
    description: The unique Alert Rule OCID.
    in: path
    name: alertRuleId
    required: true
    type: string
    x-default-description: ''
  AlertRuleIdQueryParam:
    description: The unique Alert Rule OCID.
    in: query
    name: alertRuleId
    required: true
    type: string
  BudgetIdPathParam:
    description: The unique budget OCID.
    in: path
    name: budgetId
    required: true
    type: string
    x-default-description: ''
  BudgetIdQueryParam:
    description: The unique budget OCID.
    in: query
    name: budgetId
    required: true
    type: string
  CompartmentIdQueryParam:
    description: The ID of the compartment in which to list resources.
    in: query
    name: compartmentId
    required: true
    type: string
  CostAlertSubscriptionIdPathParam:
    description: The unique costAlertSubscription OCID.
    in: path
    maxLength: 255
    minLength: 1
    name: costAlertSubscriptionId
    required: true
    type: string
    x-default-description: ''
  CostAlertSubscriptionLifecycleStateQueryParam:
    description: The current state of the cost alert subscription.
    in: query
    name: lifecycleState
    type: string
    x-obmcs-enumref: '#/definitions/CostAlertSubscription/lifecycleState'
  CostAnomalyEventIdPathParam:
    description: The unique costAnomalyEventId OCID.
    in: path
    maxLength: 255
    minLength: 1
    name: costAnomalyEventId
    required: true
    type: string
    x-default-description: ''
  CostAnomalyMonitorIdPathParam:
    description: The unique costAnomalyMonitor OCID.
    in: path
    maxLength: 255
    minLength: 1
    name: costAnomalyMonitorId
    required: true
    type: string
    x-default-description: ''
  CostAnomalyMonitorLifecycleStateQueryParam:
    description: The current state of the cost monitor.
    in: query
    name: lifecycleState
    type: string
    x-obmcs-enumref: '#/definitions/CostAnomalyMonitor/lifecycleState'
  CostImpactQueryParam:
    description: cost impact of the anomaly event.
    format: double
    in: query
    name: costImpact
    required: true
    type: number
    x-default-description: ''
  CostMonitorIdQueryParam:
    description: The cost monitor ocid.
    in: query
    maxLength: 255
    minLength: 1
    name: costAnomalyMonitorId
    required: true
    type: string
    x-default-description: ''
  DisplayNameQueryParam:
    description: 'A user-friendly name. This does not have to be unique, and it''s changeable.


      Example: `My new resource`

      '
    in: query
    maxLength: 255
    minLength: 1
    name: displayName
    type: string
    x-default-description: ''
  EndDateQueryParam:
    description: endDate for anomaly event date.
    format: date-time
    in: query
    name: timeAnomalyEventEndDate
    required: true
    type: string
    x-default-description: ''
  IfMatchHeader:
    description: 'For optimistic concurrency control. In the PUT or DELETE call

      for a resource, set the `if-match` parameter to the value of the

      etag from a previous GET or POST response for that resource.

      The resource will be updated or deleted only if the etag you

      provide matches the resource''s current etag value.

      '
    in: header
    name: if-match
    required: false
    type: string
  LifecycleStateQueryParam:
    description: The current state of the resource to filter by.
    enum: *id004
    in: query
    name: lifecycleState
    type: string
    x-default-description: ''
    x-obmcs-top-level-enum: '#/definitions/LifecycleState'
  NameQueryParam:
    description: 'Unique, non-changeable resource name.

      '
    in: query
    maxLength: 255
    minLength: 1
    name: name
    type: string
    x-default-description: ''
  PaginationLimitQueryParam:
    default: 25
    description: The maximum number of items to return.
    in: query
    maximum: 1000
    minimum: 1
    name: limit
    type: integer
  PaginationTokenQueryParam:
    description: The page token representing the page at which to start retrieving results. This is usually
      retrieved from a previous list call.
    in: query
    minLength: 1
    name: page
    type: string
    x-default-description: ''
  RegionQueryParam:
    description: region of the anomaly event.
    in: query
    maxLength: 255
    minLength: 1
    name: region
    required: true
    type: string
    x-default-description: ''
  RequestIdHeader:
    description: The client request ID for tracing.
    in: header
    name: opc-request-id
    type: string
  RetryTokenHeader:
    description: 'A token that uniquely identifies a request so it can be retried, in case of a timeout
      or

      server error, without risk of executing that same action again. Retry tokens expire after 24

      hours, but can be invalidated before then due to conflicting operations. For example, if a resource

      has been deleted and purged from the system, then a retry of the original creation request

      might be rejected.

      '
    in: header
    maxLength: 64
    minLength: 1
    name: opc-retry-token
    required: false
    type: string
  SortByQueryParam:
    default: timeCreated
    description: 'The field to sort by. If not specified, the default is timeCreated.

      The default sort order for timeCreated is DESC.

      The default sort order for displayName is ASC in alphanumeric order.

      '
    enum: *id014
    in: query
    name: sortBy
    type: string
    x-obmcs-top-level-enum: '#/definitions/SortBy'
  SortOrderQueryParam:
    description: The sort order to use, either 'asc' or 'desc'.
    enum: *id015
    in: query
    name: sortOrder
    type: string
    x-default-description: ''
    x-obmcs-top-level-enum: '#/definitions/SortOrder'
  StartDateQueryParam:
    description: startDate for anomaly event date.
    format: date-time
    in: query
    name: timeAnomalyEventStartDate
    required: true
    type: string
    x-default-description: ''
  TargetTenantIdQueryParam:
    description: The target tenantId ocid filter param.
    in: query
    maxLength: 255
    minLength: 1
    name: targetTenantId
    type: string
    x-default-description: ''
  TargetTypeQueryParam:
    description: "The type of target to filter by:\n  * ALL - List all budgets\n  * COMPARTMENT - List\
      \ all budgets with targetType == \"COMPARTMENT\"\n  * TAG - List all budgets with targetType ==\
      \ \"TAG\"\n"
    enum:
    - ALL
    - COMPARTMENT
    - TAG
    in: query
    name: targetType
    type: string
    x-default-description: ''
responses:
  400:
    description: Bad Request
    headers:
      opc-request-id:
        description: 'Unique Oracle-assigned identifier for the request. If

          you need to contact Oracle about a particular request,

          please provide the request ID.

          '
        type: string
    schema:
      $ref: '#/definitions/Error'
  401:
    description: Unauthorized
    headers:
      opc-request-id:
        description: 'Unique Oracle-assigned identifier for the request. If

          you need to contact Oracle about a particular request,

          please provide the request ID.

          '
        type: string
    schema:
      $ref: '#/definitions/Error'
  404:
    description: Not Found
    headers:
      opc-request-id:
        description: 'Unique Oracle-assigned identifier for the request. If

          you need to contact Oracle about a particular request,

          please provide the request ID.

          '
        type: string
    schema:
      $ref: '#/definitions/Error'
  409:
    description: Conflict
    headers:
      opc-request-id:
        description: 'Unique Oracle-assigned identifier for the request. If you need to contact Oracle
          about a

          particular request, please provide the request ID.

          '
        type: string
    schema:
      $ref: '#/definitions/Error'
  412:
    description: Precondition failed
    headers:
      opc-request-id:
        description: 'Unique Oracle-assigned identifier for the request. If

          you need to contact Oracle about a particular request,

          please provide the request ID.

          '
        type: string
    schema:
      $ref: '#/definitions/Error'
  429:
    description: Too Many Requests
    headers:
      opc-request-id:
        description: 'Unique Oracle-assigned identifier for the request. If

          you need to contact Oracle about a particular request,

          please provide the request ID.

          '
        type: string
    schema:
      $ref: '#/definitions/Error'
  500:
    description: Internal Server Error
    headers:
      opc-request-id:
        description: 'Unique Oracle-assigned identifier for the request. If

          you need to contact Oracle about a particular request,

          please provide the request ID.

          '
        type: string
    schema:
      $ref: '#/definitions/Error'
  default:
    description: Unknown Error
    headers:
      opc-request-id:
        description: 'Unique Oracle-assigned identifier for the request. If

          you need to contact Oracle about a particular request,

          please provide the request ID.

          '
        type: string
    schema:
      $ref: '#/definitions/Error'
x-anchors:
  x-headers:
    etag: *id002
    opc-next-page: *id003
    opc-request-id: *id001
    opc-work-request-id:
      description: 'The OCID of the work request. Use [GetWorkRequest](#/en/iaas/20160918/WorkRequests/GetWorkRequest)
        with this ID to track the status

        of the request.

        '
      type: string
x-obmcs-client-retries-enabled: true
x-oracle-package: com.oracle.oci.usage.budgets
x-properties:
  definedTags: *id010
  freeformTags: *id011
  systemTags: *id012
