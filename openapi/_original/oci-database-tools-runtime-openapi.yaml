swagger: '2.0'
info:
  description: Use the Database Tools Runtime API to connect to databases through Database Tools Connections.
  title: Database Tools Runtime API
  version: '20230222'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/6813b69eeb0b14920a5a0d51829957383dfca32ea6f54edf4feba6d1eea66e56.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the Database Tools Runtime API OCI service and stored
      verbatim; API Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/6813b69eeb0b14920a5a0d51829957383dfca32ea6f54edf4feba6d1eea66e56.yaml
    what: the harvested document for Database Tools Runtime API
host: 127.0.0.1
basePath: /20230222
schemes:
- http
- https
produces:
- application/json
paths:
  /databaseToolsConnections/{databaseToolsConnectionId}/actions/executeSql:
    post:
      description: 'Execute statements on a database tools connection.

        '
      operationId: ExecuteSqlDatabaseToolsConnection
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - description: Script to execute on a DatabaseToolsConnection.
        in: body
        name: ExecuteSqlDatabaseToolsConnectionDetails
        required: true
        schema:
          $ref: '#/definitions/ExecuteSqlDatabaseToolsConnectionDetails'
      responses:
        200:
          description: The script has been executed synchronously and the response contains the result
          headers:
            opc-request-id: &id001
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              type: string
            opc-work-request-id: &id002
              description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use
                this to query status of the asynchronous operation.

                '
              type: string
          schema:
            $ref: '#/definitions/ExecuteSqlResponse'
        202:
          description: The script statements are being executed asynchronously and the results can be
            monitored by polling the work request
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Execute statements on a database tools connection.
      tags:
      - databaseToolsRuntime
      x-example: "POST /...\nHost: dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and\
        \ other headers&gt;</var>\n\n{\n  ...\n}\n"
      x-related-resource: '#/definitions/DatabaseToolsConnection'
  /databaseToolsConnections/{databaseToolsConnectionId}/actions/validateConnection:
    post:
      description: Validates the specified Database Tools connection.
      operationId: ValidateDatabaseToolsConnection
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - description: Request to validate a DatabaseToolsConnection.
        in: body
        name: ValidateDatabaseToolsConnectionDetails
        required: true
        schema:
          $ref: '#/definitions/ValidateDatabaseToolsConnectionDetails'
      responses:
        200:
          description: The Database Tools connection validation details.
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/ValidateDatabaseToolsConnectionResult'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Post DatabaseToolsConnectionValidate
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/DatabaseToolsConnection'
  /databaseToolsConnections/{databaseToolsConnectionId}/credentials:
    get:
      description: Returns a paginated list of `CredentialSummary` for the specified request.
      operationId: ListCredentials
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of `CredentialSummary`.
          headers:
            opc-next-page: &id003
              description: 'For pagination of a list of items. When paging through a list, if this header
                appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter
                for the

                subsequent GET request to get the next batch of items.

                '
              type: string
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CredentialCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: List credentials
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/Credential'
    post:
      description: Creates a credential for the user specified by the key.
      operationId: CreateCredential
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - description: The payload to create credential.
        in: body
        name: CreateCredentialDetails
        required: true
        schema:
          $ref: '#/definitions/CreateCredentialDetails'
      responses:
        200:
          description: The credential information.
          headers:
            location: &id004
              description: URI of the new resource which was created by the request.
              type: string
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Credential'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Creates a credential for the user specified by the key.
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/Credential'
  /databaseToolsConnections/{databaseToolsConnectionId}/credentials/{credentialKey}:
    delete:
      description: Delete credential
      operationId: DeleteCredential
      parameters:
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      responses:
        204:
          description: Request has run successfully. The credential is deleted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Delete credential
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/Credential'
    get:
      description: Get a credential
      operationId: GetCredential
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      responses:
        200:
          description: The credential information
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Credential'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Get a credential
      tags:
      - databaseToolsRuntime
    put:
      description: Update a credential
      operationId: UpdateCredential
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateCredentialDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateCredentialDetails'
      responses:
        200:
          description: The updated credential information
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Credential'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        405:
          $ref: '#/responses/405'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Update a credential
      tags:
      - databaseToolsRuntime
  /databaseToolsConnections/{databaseToolsConnectionId}/credentials/{credentialKey}/executeGrantees:
    get:
      description: Get a list of all execute grantees
      operationId: ListCredentialExecuteGrantees
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: A page of `CredentialExecuteGranteeSummary` objects.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CredentialExecuteGranteeCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Get a list of all execute grantees
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/CredentialExecuteGrantee'
    post:
      description: Grants the EXECUTE privilege on the credential to the user specified by the key.
      operationId: CreateCredentialExecuteGrantee
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - description: The payload to create an execute grantee.
        in: body
        name: CreateCredentialExecuteGranteeDetails
        required: true
        schema:
          $ref: '#/definitions/CreateCredentialExecuteGranteeDetails'
      responses:
        200:
          description: Specifies the database user, the grantee, who is granted the execute privilege
            on the credential.
          headers:
            location: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CredentialExecuteGrantee'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Grants the EXECUTE privilege on the credential to the user specified by the key.
      tags:
      - databaseToolsRuntime
  /databaseToolsConnections/{databaseToolsConnectionId}/credentials/{credentialKey}/executeGrantees/{executeGranteeKey}:
    delete:
      description: Delete execute grantee
      operationId: DeleteCredentialExecuteGrantee
      parameters:
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - $ref: '#/parameters/CredentialExecuteGranteeKeyPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      responses:
        204:
          description: Request has run successfully. The execute grantee is deleted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Delete execute grantee
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/CredentialExecuteGrantee'
    get:
      description: Get a credential execute grantee
      operationId: GetCredentialExecuteGrantee
      parameters:
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - $ref: '#/parameters/CredentialExecuteGranteeKeyPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      responses:
        200:
          description: The execute grantee information
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CredentialExecuteGrantee'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Get a credential execute grantee
      tags:
      - databaseToolsRuntime
  /databaseToolsConnections/{databaseToolsConnectionId}/credentials/{credentialKey}/publicSynonyms:
    get:
      description: Get a list of all public synonyms for the given credential
      operationId: ListCredentialPublicSynonyms
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: A page of `CredentialPublicSynonymSummary` objects.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CredentialPublicSynonymCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Get a list of all public synonyms for the given credential
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/CredentialPublicSynonym'
    post:
      description: Creates a public synonym for the given credentials
      operationId: CreateCredentialPublicSynonym
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - description: The payload to create a public synonym for the given credentials.
        in: body
        name: CreateCredentialPublicSynonymDetails
        required: true
        schema:
          $ref: '#/definitions/CreateCredentialPublicSynonymDetails'
      responses:
        200:
          description: The public synonym information
          headers:
            location: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CredentialPublicSynonym'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Creates a public synonym for the given credentials
      tags:
      - databaseToolsRuntime
  /databaseToolsConnections/{databaseToolsConnectionId}/credentials/{credentialKey}/publicSynonyms/{publicSynonymKey}:
    delete:
      description: Deletes the public synonym
      operationId: DeleteCredentialPublicSynonym
      parameters:
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - $ref: '#/parameters/PublicSynonymKeyPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      responses:
        204:
          description: Request has run successfully. The public synonym is deleted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Delete a public synonym
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/CredentialPublicSynonym'
    get:
      description: Get a public synonym
      operationId: GetCredentialPublicSynonym
      parameters:
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      - $ref: '#/parameters/PublicSynonymKeyPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      responses:
        200:
          description: The public synonym information
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/CredentialPublicSynonym'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Get a public synonym
      tags:
      - databaseToolsRuntime
  /databaseToolsConnections/{databaseToolsConnectionId}/propertySets/{propertySetKey}:
    get:
      description: Get a property set
      operationId: GetPropertySet
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/PropertySetKeyPathParam'
      responses:
        200:
          description: The property set information
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/PropertySet'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Get a property set
      tags:
      - databaseToolsRuntime
    put:
      description: Update a property set
      operationId: UpdatePropertySet
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/PropertySetKeyPathParam'
      - description: The information to be updated.
        in: body
        name: UpdatePropertySetDetails
        required: true
        schema:
          $ref: '#/definitions/UpdatePropertySetDetails'
      responses:
        200:
          description: The property set information
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/PropertySet'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        405:
          $ref: '#/responses/405'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Update a property set
      tags:
      - databaseToolsRuntime
  /databaseToolsConnections/{databaseToolsConnectionId}/users/{userKey}/credentials:
    get:
      description: Returns a paginated list of user `UserCredentialSummary` for the specified request.
      operationId: ListUserCredentials
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/UserKeyPathParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of `UserCredentialSummary`.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/UserCredentialCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: List user credentials
      tags:
      - databaseToolsRuntime
      x-related-resource: '#/definitions/UserCredential'
  /databaseToolsConnections/{databaseToolsConnectionId}/users/{userKey}/credentials/{credentialKey}:
    get:
      description: Get a user credential
      operationId: GetUserCredential
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsConnectionIdPathParam'
      - $ref: '#/parameters/UserKeyPathParam'
      - $ref: '#/parameters/CredentialKeyPathParam'
      responses:
        200:
          description: The user credential information
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/UserCredential'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Get a user credential
      tags:
      - databaseToolsRuntime
  /databaseToolsDatabaseApiGatewayConfigAdvancedProperties:
    get:
      description: Returns list of database API gateway config setting descriptions to be provided as
        advanced properties.
      operationId: ListDatabaseToolsDatabaseApiGatewayConfigAdvancedProperties
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: List of database API gateway config setting descriptions to be provided as advanced
            properties.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigAdvancedPropertySummaryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      tags:
      - databaseToolsRuntime
      x-example: "GET /20230222/databaseToolsDatabaseApiGatewayConfigAdvancedProperties HTTP/1.1\nHost:\
        \ dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and other headers&gt;</var>\n\
        \n{\n  items: [{\n    \"key\": \"apex.security.administrator.roles\",\n    \"configTypes\": [\
        \ \"GLOBAL\", \"POOL\" ],\n    \"databaseToolsConnectionType\": \"ORACLE_DATABASE\",\n    \"displayName\"\
        : \"APEX Security Administrator Roles\",\n    \"documentationUrl\": \"https://docs.oracle.com/en/database/oracle/oracle-rest-data-services/latest/ordig/about-REST-configuration-files.html\"\
        ,\n    \"description\": \"Specifies the comma delimited list of additional roles to assign authenticated\
        \ APEX administrator type users\",\n    \"categoryKey\": \"APEX\",\n    \"categoryDisplayName\"\
        : \"Oracle APEX\",\n    \"defaultValue\": null,\n    \"minValue\": null,\n    \"maxValue\": null,\n\
        \    \"hintText\": \"Enter comma delimited list of roles.\",\n    \"dataType\": \"STRING\"\n \
        \ }]\n}\n"
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigAdvancedPropertySummary'
  /databaseToolsDatabaseApiGatewayConfigs/{databaseToolsDatabaseApiGatewayConfigId}/content:
    get:
      description: Get the content of a Database Tools database API gateway config
      operationId: GetDatabaseToolsDatabaseApiGatewayConfigContent
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/IfNoneMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      produces:
      - application/json
      responses:
        200:
          description: The Database Tools database API gateway config content
          headers:
            etag: &id005
              description: 'For optimistic concurrency control. See `if-match`.

                '
              type: string
            opc-request-id: *id001
          schema:
            format: binary
            type: string
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the content of a Database Tools database API gateway config
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/content

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfig'
  /databaseToolsDatabaseApiGatewayConfigs/{databaseToolsDatabaseApiGatewayConfigId}/globals/{globalKey}:
    get:
      description: Get a Database Tools database API gateway config global resource
      operationId: GetDatabaseToolsDatabaseApiGatewayConfigGlobal
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigGlobalKeyPathParam'
      responses:
        200:
          description: The Database Tools database API gateway config global information
          headers:
            etag: *id005
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigGlobal'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get a Database Tools database API gateway config global resource
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/globals/SETTINGS
        HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigGlobal'
    put:
      description: Update a Database Tools database API gateway config global resource
      operationId: UpdateDatabaseToolsDatabaseApiGatewayConfigGlobal
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigGlobalKeyPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateDatabaseToolsDatabaseApiGatewayConfigGlobalDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateDatabaseToolsDatabaseApiGatewayConfigGlobalDetails'
      responses:
        200:
          description: The updated Database Tools database API gateway config global information
          headers:
            etag: *id005
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigGlobal'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update a Database Tools database API gateway config global resource
      tags:
      - databaseToolsRuntime
      x-example: "PUT /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/globals/SETTINGS\
        \ HTTP/1.1\nHost: dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and other headers&gt;</var>\n\
        \n{\n  \"type\": \"DEFAULT\",\n  \"databaseApi\": \"ENABLED\",\n  \"advancedProperties\": {\n\
        \    \"configRefreshInterval\": \"1h\"\n  }\n}\n"
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigGlobal'
  /databaseToolsDatabaseApiGatewayConfigs/{databaseToolsDatabaseApiGatewayConfigId}/pools:
    get:
      description: Returns a list of Database Tools database API gateway config pool resources
      operationId: ListDatabaseToolsDatabaseApiGatewayConfigPools
      parameters:
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      responses:
        200:
          description: A page of Database Tools database API gateway config pool summary objects.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List Database Tools database API gateway config pool resources
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools
        HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
    post:
      description: Create a Database Tools database API gateway config pool resource
      operationId: CreateDatabaseToolsDatabaseApiGatewayConfigPool
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - description: Details for the new Database Tools database API gateway config pool resource.
        in: body
        name: CreateDatabaseToolsDatabaseApiGatewayConfigPoolDetails
        required: true
        schema:
          $ref: '#/definitions/CreateDatabaseToolsDatabaseApiGatewayConfigPoolDetails'
      responses:
        200:
          description: The created Database Tools database API gateway config pool information
          headers:
            etag: *id005
            location: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Create a Database Tools database API gateway config pool resource
      tags:
      - databaseToolsRuntime
      x-example: "POST /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools\
        \ HTTP/1.1\nHost: dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and other headers&gt;</var>\n\
        \n{\n  \"type\": \"DEFAULT\",\n  \"displayName\": \"APIs for HR database\",\n  \"databaseToolsConnectionId\"\
        : \"ocid1.databasetoolsconnection.oc1.phx.exampleaaaaaaaaaaaa\",\n  \"advancedProperties\" : {\n\
        \    \"misc.pagination.maxRows\" : \"100\"\n  }\n}\n"
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
  /databaseToolsDatabaseApiGatewayConfigs/{databaseToolsDatabaseApiGatewayConfigId}/pools/{poolKey}:
    delete:
      description: Deletes the specified Database Tools database API gateway config pool resource.
      operationId: DeleteDatabaseToolsDatabaseApiGatewayConfigPool
      parameters:
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: Request to delete the Database Tools database API gateway config pool has been
            accepted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Deletes a Database Tools database API gateway config pool resource.
      tags:
      - databaseToolsRuntime
      x-example: 'DELETE /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
    get:
      description: Get a Database Tools database API gateway config pool resource
      operationId: GetDatabaseToolsDatabaseApiGatewayConfigPool
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      responses:
        200:
          description: The Database Tools database API gateway config pool information
          headers:
            etag: *id005
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get a Database Tools database API gateway config pool resource
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
    put:
      description: Update a Database Tools database API gateway config pool resource
      operationId: UpdateDatabaseToolsDatabaseApiGatewayConfigPool
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateDatabaseToolsDatabaseApiGatewayConfigPoolDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateDatabaseToolsDatabaseApiGatewayConfigPoolDetails'
      responses:
        200:
          description: The updated Database Tools database API gateway config pool information
          headers:
            etag: *id005
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update a Database Tools database API gateway config pool resource
      tags:
      - databaseToolsRuntime
      x-example: "PUT /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>\n\
        Host: dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and other headers&gt;</var>\n\
        \n{\n  \"type\": \"DEFAULT\",\n  \"displayName\": \"New Display Name\"\n}\n"
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
  /databaseToolsDatabaseApiGatewayConfigs/{databaseToolsDatabaseApiGatewayConfigId}/pools/{poolKey}/apiSpecs:
    get:
      description: Returns a list of Database Tools database API gateway config API spec resources
      operationId: ListDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecs
      parameters:
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      responses:
        200:
          description: A page of Database Tools database API gateway config API spec summary objects.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpecCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List Database Tools database API gateway config API spec resources
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/apiSpecs
        HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
    post:
      description: Create a Database Tools database API gateway config API spec resource
      operationId: CreateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpec
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - description: Details for the new Database Tools database API gateway config API spec resource.
        in: body
        name: CreateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDetails
        required: true
        schema:
          $ref: '#/definitions/CreateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDetails'
      responses:
        200:
          description: The created Database Tools database API gateway config API spec information
          headers:
            etag: *id005
            location: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Create a Database Tools database API gateway config API spec resource
      tags:
      - databaseToolsRuntime
      x-example: "POST /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/apiSpecs\
        \ HTTP/1.1\nHost: dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and other headers&gt;</var>\n\
        \n{\n  \"type\": \"DEFAULT\",\n  \"displayName\": \"Employees API\",\n  \"content\": \"{\\\"...\\\
        \": \\\"...\\\"}\"\n}\n"
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
  /databaseToolsDatabaseApiGatewayConfigs/{databaseToolsDatabaseApiGatewayConfigId}/pools/{poolKey}/apiSpecs/{apiSpecKey}:
    delete:
      description: Deletes the specified Database Tools database API gateway config API spec resource.
      operationId: DeleteDatabaseToolsDatabaseApiGatewayConfigPoolApiSpec
      parameters:
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigApiSpecKeyPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: Request to delete the Database Tools database API gateway config API spec has been
            accepted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Deletes a Database Tools database API gateway config API spec resource.
      tags:
      - databaseToolsRuntime
      x-example: 'DELETE /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/apiSpecs/<var>&lt;apiSpecKey&gt;</var>

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
    get:
      description: Get a Database Tools database API gateway config API spec resource
      operationId: GetDatabaseToolsDatabaseApiGatewayConfigPoolApiSpec
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigApiSpecKeyPathParam'
      responses:
        200:
          description: The Database Tools database API gateway config API spec information
          headers:
            etag: *id005
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get a Database Tools database API gateway config API spec resource
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/apiSpecs/<var>&lt;apiSpecKey&gt;</var>

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
    put:
      description: Update a Database Tools database API gateway config API spec resource
      operationId: UpdateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpec
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigApiSpecKeyPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDetails'
      responses:
        200:
          description: The updated Database Tools database API gateway config API spec information
          headers:
            etag: *id005
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update a Database Tools database API gateway config API spec resource
      tags:
      - databaseToolsRuntime
      x-example: "PUT /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/apiSpecs/<var>&lt;apiSpecKey&gt;</var>\n\
        Host: dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and other headers&gt;</var>\n\
        \n{\n  \"type\": \"DEFAULT\",\n  \"displayName\": \"New Display Name\",\n}\n"
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
  /databaseToolsDatabaseApiGatewayConfigs/{databaseToolsDatabaseApiGatewayConfigId}/pools/{poolKey}/autoApiSpecs:
    get:
      description: Returns a list of Database Tools database API gateway config auto API spec resources
      operationId: ListDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecs
      parameters:
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      responses:
        200:
          description: A page of Database Tools database API gateway config auto API spec summary objects.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List Database Tools database API gateway config auto API spec resources
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/autoApiSpecs
        HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
    post:
      description: Create a Database Tools database API gateway config auto API spec resource
      operationId: CreateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - description: Details for the new Database Tools database API gateway config auto API spec resource.
        in: body
        name: CreateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDetails
        required: true
        schema:
          $ref: '#/definitions/CreateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDetails'
      responses:
        200:
          description: The created Database Tools database API gateway config auto API spec information
          headers:
            etag: *id005
            location: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Create a Database Tools database API gateway config auto API spec resource
      tags:
      - databaseToolsRuntime
      x-example: "POST /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/autoApiSpecs\
        \ HTTP/1.1\nHost: dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and other headers&gt;</var>\n\
        \n{\n  \"type\": \"DEFAULT\",\n  \"displayName\": \"Employees Auto API\",\n  \"objectName\": \"\
        EMPLOYEES\",\n  \"objectType\": \"TABLE\",\n  \"alias\": \"emp\",\n  \"operation\": [\"READ\"\
        ],\n  \"securitySchemes\": [\"BASIC\"]\n}\n"
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
  ? /databaseToolsDatabaseApiGatewayConfigs/{databaseToolsDatabaseApiGatewayConfigId}/pools/{poolKey}/autoApiSpecs/{autoApiSpecKey}
  : delete:
      description: Deletes the specified Database Tools database API gateway config auto API spec resource.
      operationId: DeleteDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec
      parameters:
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigAutoApiSpecKeyPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: Request to delete the Database Tools database API gateway config auto API spec
            has been accepted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Deletes a Database Tools database API gateway config auto API spec resource.
      tags:
      - databaseToolsRuntime
      x-example: 'DELETE /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/autoApiSpecs/<var>&lt;autoApiSpecKey&gt;</var>

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
    get:
      description: Get a Database Tools database API gateway config auto API spec resource
      operationId: GetDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigAutoApiSpecKeyPathParam'
      responses:
        200:
          description: The Database Tools database API gateway config auto API spec information
          headers:
            etag: *id005
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get a Database Tools database API gateway config auto API spec resource
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/autoApiSpecs/<var>&lt;autoApiSpecKey&gt;</var>

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
    put:
      description: Update a Database Tools database API gateway config auto API spec resource
      operationId: UpdateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigIdPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam'
      - $ref: '#/parameters/DatabaseToolsDatabaseApiGatewayConfigAutoApiSpecKeyPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDetails'
      responses:
        200:
          description: The updated Database Tools database API gateway config auto API spec information
          headers:
            etag: *id005
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update a Database Tools database API gateway config auto API spec resource
      tags:
      - databaseToolsRuntime
      x-example: "PUT /20230222/databaseToolsDatabaseApiGatewayConfigs/<var>&lt;databaseToolsDatabaseApiGatewayConfigId&gt;</var>/pools/<var>&lt;poolKey&gt;</var>/autoApiSpecs/<var>&lt;autoApiSpecKey&gt;</var>\n\
        Host: dbtools.us-phoenix-1.oci.oraclecloud.com\n<var>&lt;authorization and other headers&gt;</var>\n\
        \n{\n  \"type\": \"DEFAULT\",\n  \"displayName\": \"New Display Name\",\n}\n"
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
  /databaseToolsIdentities/{databaseToolsIdentityId}/actions/validateCredential:
    post:
      description: 'Validates the Database Tools identity credentials by establishing a connection to
        the customer database

        and executing the dbms_cloud.send_request to validate the credential.

        '
      operationId: ValidateDatabaseToolsIdentityCredential
      parameters:
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/DatabaseToolsIdentityIdPathParam'
      - description: Request to validate a Database Tools identity Credential.
        in: body
        name: ValidateDatabaseToolsIdentityCredentialDetails
        required: true
        schema:
          $ref: '#/definitions/ValidateDatabaseToolsIdentityCredentialDetails'
      responses:
        200:
          description: The Database Tools identity credential validation details.
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/ValidateDatabaseToolsIdentityCredentialResult'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Validates a Database Tools identity credential.
      tags:
      - databaseToolsRuntime
      x-example: 'POST /20230222/databaseToolsIdentities/<var>&lt;databaseToolsIdentityId&gt;</var>/actions/validateCredential

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/DatabaseToolsIdentity'
  /workRequests:
    get:
      description: 'Lists the work requests in a compartment.

        '
      operationId: ListWorkRequests
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/ResourceIdentifierQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/WorkRequestSortByQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/OperationTypeQueryParam'
      - $ref: '#/parameters/CreatedByPrincipalIdQueryParam'
      - $ref: '#/parameters/WorkRequestStatusQueryParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List Work Requests
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/workRequests?compartmentId=<var>&lt;compartmentId&gt;</var> HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}:
    delete:
      description: Attempts to cancel the specified work request.
      operationId: CancelWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      responses:
        202:
          description: Request to cancel work request is accepted.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        503:
          $ref: '#/responses/503'
        default:
          $ref: '#/responses/default'
      summary: Attempts to cancel the specified work request
      tags:
      - databaseToolsRuntime
      x-example: 'DELETE /20230222/workRequests/<var>&lt;workRequestId&gt;</var> HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/WorkRequest'
    get:
      description: Gets the status of the specified work request.
      operationId: GetWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The detail of the work request.
          headers:
            etag: *id005
            opc-request-id: *id001
            retry-after: &id029
              description: A decimal number representing the number of seconds the client should wait
                before polling this endpoint again.
              format: float
              type: number
          schema:
            $ref: '#/definitions/WorkRequest'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: GET Work Request Status
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/workRequests/<var>&lt;workRequestId&gt;</var> HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
  /workRequests/{workRequestId}/errors:
    get:
      description: 'Returns a paginated list of errors for the specified work request.

        '
      operationId: ListWorkRequestErrors
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      responses:
        200:
          description: List of work request errors.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestErrorCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists work request errors
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/workRequests/<var>&lt;workRequestId&gt;</var>/errors HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/WorkRequestError'
  /workRequests/{workRequestId}/logs:
    get:
      description: 'Returns a paginated list of logs for the specified work request.

        '
      operationId: ListWorkRequestLogs
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      responses:
        200:
          description: List of work request logs.
          headers:
            opc-next-page: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestLogEntryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists work request logs
      tags:
      - databaseToolsRuntime
      x-example: 'GET /20230222/workRequests/<var>&lt;workRequestId&gt;</var>/logs HTTP/1.1

        Host: dbtools.us-phoenix-1.oci.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/WorkRequestLogEntry'
definitions:
  ActionType:
    description: The type of action.
    enum: &id026
    - CREATED
    - UPDATED
    - DELETED
    - IN_PROGRESS
    - RELATED
    - FAILED
    - CANCELED
    type: string
  ApexFaIntegrationPrerequisitesCheck:
    description: The results of a prerequisites check for APEX FA integration
    properties:
      status:
        description: Status indicating the outcome of the prerequisites check.
        enum:
        - PASS
        - FAIL
        - ERROR
        - UNKNOWN
        type: string
      statusDetails:
        description: Messages describing the prerequisites check outcome. Messages can provide actionable
          information when the status indicates a failure.
        items:
          maxLength: 255
          minLength: 0
          type: string
        maxItems: 20
        minItems: 0
        type: array
  ConnectionType:
    description: The connection type.
    enum: &id010
    - ORACLE_DATABASE
    - MYSQL
    - POSTGRESQL
    type: string
  CreateCredentialBasicDetails:
    allOf:
    - $ref: '#/definitions/CreateCredentialDetails'
    - discriminator: BASIC
      properties:
        password:
          description: The password for the new credential.
          format: password
          maxLength: 24000
          minLength: 1
          type: string
        userName:
          description: The username for the new credential.
          maxLength: 24000
          minLength: 1
          type: string
      required:
      - userName
      - password
    description: Details for the Credential for the Basic type.
  CreateCredentialDetails:
    description: Details for the new credential.
    discriminator: type
    properties:
      key:
        description: The credential_name to be created
        maxLength: 255
        minLength: 1
        type: string
        x-obmcs-cli-param: credential-key
      type:
        description: The type of credential.
        enum: &id009
        - BASIC
        type: string
        x-obmcs-top-level-enum: '#/definitions/CredentialType'
    required:
    - type
    - key
    type: object
  CreateCredentialExecuteGranteeDetails:
    description: Specifies the database user, the grantee, who is granted the execute privilege on the
      credential.
    properties:
      key:
        description: The name of the user to grant the EXECUTE privilege on the credential.
        maxLength: 128
        minLength: 1
        type: string
        x-obmcs-cli-param: execute-grantee-key
    required:
    - key
    type: object
  CreateCredentialPublicSynonymDetails:
    description: The public synonym information
    properties:
      key:
        description: The name of the public synonym for the credential
        maxLength: 128
        minLength: 1
        type: string
        x-obmcs-cli-param: public-synonym-key
    required:
    - key
    type: object
  CreateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDefaultDetails:
    allOf:
    - $ref: '#/definitions/CreateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDetails'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config API spec sub resource to
      be created.
  CreateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDetails:
    description: The content of a Database Tools database API gateway config API spec sub resource to
      be created.
    discriminator: type
    properties:
      content:
        description: The content of a string-escaped Open API spec in JSON format.
        maxLength: 102400
        minLength: 2
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      type:
        description: The type of the Database Tools database API gateway config API spec sub resource.
        enum: &id006
        - DEFAULT
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - type
    - displayName
    - content
    type: object
  CreateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDefaultDetails:
    allOf:
    - $ref: '#/definitions/CreateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDetails'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config auto API spec sub resource
      to be created.
  CreateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDetails:
    description: The content of a Database Tools database API gateway config auto API spec sub resource
      to be created.
    discriminator: type
    properties:
      alias:
        description: Used as the URI path element for this object. When not specified the objectName lowercase
          is the default value.
        maxLength: 255
        minLength: 0
        type: string
      databaseObjectName:
        description: The name of the database object.
        maxLength: 255
        minLength: 1
        type: string
      databaseObjectType:
        description: The type of the database object.
        enum:
        - FUNCTION
        - MVIEW
        - PACKAGE
        - PROCEDURE
        - TABLE
        - VIEW
        - DUALITYVIEW
        type: string
      description:
        description: Description of the autoApiSpec.
        maxLength: 4000
        minLength: 0
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      operations:
        description: The operations to limit access to this resource. If not specified then the default
          is ["READ","WRITE"].
        items:
          enum:
          - READ
          - WRITE
          type: string
        maxItems: 2
        minItems: 0
        type: array
      roles:
        description: The name of the database API gateway config roles protecting the resource. Only valid
          for RBAC JWT Profile pools and BEARER securitySchemes.
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 20
        minItems: 0
        type: array
      scope:
        description: The name of the database API gateway config privilege protecting the resource. Only
          valid for SCOPE JWT Profile pools and BEARER securitySchemes.
        maxLength: 255
        minLength: 0
        type: string
      securitySchemes:
        description: The security schemes that can access this resource. If not specified then the resource
          is public.
        items:
          enum:
          - BASIC
          - BEARER
          type: string
        maxItems: 2
        minItems: 0
        type: array
      type:
        description: The type of the Database Tools database API gateway config auto API spec sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - type
    - displayName
    - databaseObjectName
    - databaseObjectType
    type: object
  CreateDatabaseToolsDatabaseApiGatewayConfigPoolDefaultDetails:
    allOf:
    - $ref: '#/definitions/CreateDatabaseToolsDatabaseApiGatewayConfigPoolDetails'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config pool sub resource to be created.
  CreateDatabaseToolsDatabaseApiGatewayConfigPoolDetails:
    description: The content of a Database Tools database API gateway config pool sub resource to be created.
    discriminator: type
    properties:
      advancedProperties:
        additionalProperties:
          type: string
        description: Advanced pool properties.
        type: object
      databaseActionsStatus:
        default: DISABLED
        description: Specifies to enable the Database Actions feature.
        enum:
        - ENABLED
        - DISABLED
        type: string
      databaseToolsConnectionId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the related Database Tools
          connection. Specifies the OCI database tools connection ocid to build the connection pool from.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      initialPoolSize:
        default: 0
        description: Specifies the initial size for the number of database connections that will be created
          for the pool.
        maximum: 100000
        minimum: 0
        type: integer
      jwtProfileAudience:
        description: Specifies the expected audience for the JWT token. This value is used to validate
          the aud claim in the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileIssuer:
        description: Specifies the issuer of the JWT token. This value is used to validate the iss claim
          in the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileJwkUrl:
        description: Specifies the URL of the JSON Web Key (JWK) that is used to verify the signature
          of the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileRoleClaimName:
        description: Specifies the JSON pointer to the claim in the JWT token that contains the roles
          of the users.
        maxLength: 4000
        minLength: 0
        type: string
      maxPoolSize:
        default: 100
        description: Specifies the maximum number of database connections allowed for the pool.
        maximum: 100000
        minimum: 1
        type: integer
      minPoolSize:
        default: 0
        description: Specifies the minimum number of database connections allowed for the pool.
        maximum: 1000
        minimum: 0
        type: integer
      poolRouteValue:
        description: The pool route value provided in requests to target this pool.
        maxLength: 255
        minLength: 1
        type: string
      restEnabledSqlStatus:
        default: DISABLED
        description: Specifies whether the REST-Enabled SQL service is active.
        enum:
        - ENABLED
        - DISABLED
        type: string
      type:
        description: The type of the Database Tools database API gateway config pool sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - type
    - displayName
    - databaseToolsConnectionId
    - poolRouteValue
    type: object
  Credential:
    description: Credential information
    properties:
      enabled:
        description: Indicates whether this credential is enabled (TRUE) or not (FALSE)
        type: string
      key:
        description: Name of the credential
        type: string
      keyType:
        description: Indicates whether this refers to a public synonym or not.
        enum: &id007
        - CREDENTIAL_NAME
        - PUBLIC_SYNONYM
        type: string
        x-obmcs-top-level-enum: '#/definitions/CredentialKeyType'
      owner:
        description: Owner of the credential
        type: string
      relatedResource:
        $ref: '#/definitions/CredentialRelatedResource'
      userName:
        description: Name of the user that will be used to log in to the remote database or the remote
          or local operating system
        type: string
      windowsDomain:
        description: For a Windows target, the Windows domain to use when logging in
        type: string
    required:
    - key
    - userName
    - enabled
    - owner
    type: object
  CredentialCollection:
    description: List of `CredentialSummary` items.
    properties:
      items:
        description: Array of `CredentialSummary` items.
        items:
          $ref: '#/definitions/CredentialSummary'
        type: array
    required:
    - items
    type: object
  CredentialExecuteGrantee:
    description: Specifies the database user, the grantee, who is granted the execute privilege on the
      credential.
    properties:
      key:
        description: The name of the user to grant the EXECUTE privilege on the credential.
        maxLength: 128
        minLength: 1
        type: string
    required:
    - key
    type: object
  CredentialExecuteGranteeCollection:
    description: List of `CredentialExecuteGranteeSummary` items.
    properties:
      items:
        description: Array of `CredentialExecuteGranteeSummary` items.
        items:
          $ref: '#/definitions/CredentialExecuteGranteeSummary'
        type: array
    required:
    - items
    type: object
  CredentialExecuteGranteeSummary:
    description: Specifies the database user, the grantee, who is granted the execute privilege on the
      credential.
    properties:
      key:
        description: The name of the user to grant the EXECUTE privilege on the credential.
        maxLength: 128
        minLength: 1
        type: string
    required:
    - key
    type: object
  CredentialKeyType:
    description: The credential key type.
    enum: *id007
    type: string
  CredentialPublicSynonym:
    description: The public synonym information
    properties:
      key:
        description: The name of the public synonym for the credential
        maxLength: 128
        minLength: 1
        type: string
    required:
    - key
    type: object
  CredentialPublicSynonymCollection:
    description: List of `CredentialPublicSynonymSummary` items.
    properties:
      items:
        description: Array of `CredentialPublicSynonymSummary` items.
        items:
          $ref: '#/definitions/CredentialPublicSynonymSummary'
        type: array
    required:
    - items
    type: object
  CredentialPublicSynonymSummary:
    description: The public synonym information
    properties:
      key:
        description: The name of the public synonym for the credential
        maxLength: 128
        minLength: 1
        type: string
    required:
    - key
    type: object
  CredentialRelatedResource:
    description: A related resource for a credential.
    properties:
      identifier:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the related resource.
        type: string
      type:
        description: The related resource type.
        enum: &id008
        - DATABASE_TOOLS_IDENTITY
        type: string
        x-obmcs-top-level-enum: '#/definitions/CredentialRelatedResourceType'
    required:
    - type
    - identifier
    type: object
  CredentialRelatedResourceType:
    description: The related resource type.
    enum: *id008
    type: string
  CredentialSummary:
    description: Credential information
    properties:
      enabled:
        description: Indicates whether this credential is enabled (TRUE) or not (FALSE)
        type: string
      key:
        description: Name of the credential
        type: string
      keyType:
        description: Indicates whether this refers to a public synonym or not.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/CredentialKeyType'
      owner:
        description: Owner of the credential
        type: string
      relatedResource:
        $ref: '#/definitions/CredentialRelatedResource'
      userName:
        description: Name of the user that will be used to log in to the remote database or the remote
          or local operating system
        type: string
      windowsDomain:
        description: For a Windows target, the Windows domain to use when logging in
        type: string
    required:
    - key
    - userName
    - enabled
    - owner
    type: object
  CredentialType:
    description: The credential type.
    enum: *id009
    type: string
  DatabaseApiGatewayConfigAdvancedPropertyConfigType:
    description: The database API gateway config advanced property config type.
    enum: &id014
    - GLOBAL
    - POOL
    type: string
  DatabaseApiGatewayConfigCertificateBundle:
    description: The certificate bundle that describes the SSL certicicate. Ignored if the httpsPort is
      0.
    discriminator: type
    properties:
      type:
        description: The type of the certificate.
        enum:
        - FILENAME
        - SELF_SIGNED
        type: string
    required:
    - type
    type: object
  DatabaseApiGatewayConfigCertificateBundleFileName:
    allOf:
    - $ref: '#/definitions/DatabaseApiGatewayConfigCertificateBundle'
    - discriminator: FILENAME
      properties:
        certificatePrivateKey:
          $ref: '#/definitions/DatabaseApiGatewayConfigCertificatePrivateKeyFileName'
        certificatePublic:
          $ref: '#/definitions/DatabaseApiGatewayConfigCertificatePublicFileName'
    description: The certificate bundle that describes the SSL certicicate files.
  DatabaseApiGatewayConfigCertificateBundleSelfSigned:
    allOf:
    - $ref: '#/definitions/DatabaseApiGatewayConfigCertificateBundle'
    - discriminator: SELF_SIGNED
    description: The certificate bundle that denotes the use of a self signed certificate.
  DatabaseApiGatewayConfigCertificatePrivateKeyFileName:
    description: Describes a certificate private key file to be used with SSL
    properties:
      format:
        description: The format of the file
        enum:
        - DER
        - PEM
        type: string
      path:
        description: The path to the file
        maxLength: 4096
        minLength: 0
        type: string
    type: object
  DatabaseApiGatewayConfigCertificatePublicFileName:
    description: Describes a certificate file to be used with SSL. Ignored if the httpsPort is 0.
    properties:
      format:
        description: The format of the file
        enum:
        - PEM
        type: string
      path:
        description: The path to the file
        maxLength: 4096
        minLength: 0
        type: string
    type: object
  DatabaseApiGatewayConfigGlobalKey:
    description: The Database Tools database API gateway config global key.
    enum: &id027
    - SETTINGS
    type: string
  DatabaseApiGatewayConfigType:
    description: The Database Tools database API gateway config type.
    enum: *id006
    type: string
  DatabaseToolsConnection:
    description: Simplified DatabaseToolsConnection representation for splat usage only
    properties:
      compartmentId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment containing
          the Database Tools connection.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: &id011
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          Example: `{"foo-namespace": {"bar-key": "value"}}`

          '
        type: object
      displayName:
        description: A user-friendly name. Does not have to be unique and can be updated. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: &id012
        additionalProperties:
          type: string
        description: 'Simple key-value pair that is applied without any predefined name, type or scope.
          Exists for cross-compatibility only.

          Example: `{"bar-key": "value"}`

          '
        type: object
      id:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the Database Tools connection.
        maxLength: 255
        minLength: 1
        type: string
        x-obmcs-cli-param: connection-id
      lifecycleDetails:
        description: A message describing the current state in more detail. For example, this message
          can be used to provide actionable information for a resource in the Failed state.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the Database Tools connection.
        enum:
        - CREATING
        - UPDATING
        - ACTIVE
        - INACTIVE
        - DELETING
        - DELETED
        - FAILED
        type: string
      systemTags: &id013
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
      timeCreated:
        description: The time the Database Tools connection was created. An RFC3339 formatted datetime
          string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the DatabaseToolsConnection was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The Database Tools connection type.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/ConnectionType'
    required:
    - id
    - displayName
    - compartmentId
    - lifecycleState
    - timeCreated
    - timeUpdated
    - type
    type: object
  DatabaseToolsDatabaseApiGatewayConfig:
    description: Simplified DatabaseToolsDatabaseApiGatewayConfig representation for splat usage only.
    properties:
      compartmentId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment containing
          the Database Tools database API gateway config.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id011
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id012
      id:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the Database Tools database
          API gateway config.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleDetails:
        description: A message describing the current state in more detail. For example, this message
          can be used to provide actionable information for a resource in the Failed state.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the Database Tools database API gateway config.
        enum:
        - ACTIVE
        - DELETED
        type: string
      systemTags: *id013
      timeCreated:
        description: The time the Database Tools database API gateway config was created. An RFC3339 formatted
          datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the Database Tools database API gateway config was updated. An RFC3339 formatted
          datetime string.
        format: date-time
        type: string
      type:
        description: The Database Tools database API gateway config type.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - id
    - displayName
    - compartmentId
    - lifecycleState
    - timeCreated
    - timeUpdated
    - type
    type: object
  DatabaseToolsDatabaseApiGatewayConfigAdvancedPropertySummary:
    description: Description of a database API gateway config setting to be provided as an advanced property.
    properties:
      categoryDisplayName:
        description: A user-friendly name of a category.
        maxLength: 255
        minLength: 1
        type: string
      categoryKey:
        description: The category of the Database Tools database API gateway config global setting.
        maxLength: 255
        minLength: 1
        type: string
      configTypes:
        description: 'The config types that support this advanced property. The supported types are GLOBAL
          and POOL.

          '
        items:
          enum: *id014
          type: string
          x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigAdvancedPropertyConfigType'
        type: array
      dataType:
        description: The data type of a database API gateway config setting.
        enum:
        - STRING
        - NUMBER
        - DURATION
        - BOOLEAN
        type: string
      databaseToolsConnectionTypes:
        description: "The type of database (as determined by a type of Database Tools connection) to which\
          \ this setting applies. \nThe advancedProperty applies to all types of Database Tools connection\
          \ when null. This is only applicable\nwhen configTypes includes POOL.\n"
        items:
          enum:
          - ORACLE_DATABASE
          - MYSQL
          type: string
        type: array
      defaultValue:
        description: The default value (if applicable) of a database API gateway config setting.
        maxLength: 255
        minLength: 1
        type: string
      description:
        description: A user-friendly description of a database API gateway config setting.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: A user-friendly name.
        maxLength: 255
        minLength: 1
        type: string
      documentationUrl:
        description: Uniform resource locator (URL) of documentation related to this setting.
        maxLength: 255
        minLength: 1
        type: string
      hintText:
        description: Hint text for a database API gateway config setting.
        maxLength: 255
        minLength: 1
        type: string
      key:
        description: A string that uniquely identifies a Database Tools database API gateway config global
          settings resource.
        maxLength: 255
        minLength: 1
        type: string
      listOfValues:
        description: A list of string values (if applicable) supported by this database API gateway config
          setting.
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 100
        minItems: 0
        type: array
      maxValue:
        description: A maximum numeric value (if applicable) of a database API gateway config setting.
        type: integer
      minValue:
        description: A minimum numeric value (if applicable) of a database API gateway config setting.
        type: integer
    required:
    - key
    - displayName
    - dataType
    type: object
  DatabaseToolsDatabaseApiGatewayConfigAdvancedPropertySummaryCollection:
    description: List of database API gateway config setting descriptions to be provided as advanced properties.
    properties:
      items:
        description: List of database API gateway config setting descriptions to be provided as advanced
          properties.
        items:
          $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigAdvancedPropertySummary'
        type: array
    required:
    - items
    type: object
  DatabaseToolsDatabaseApiGatewayConfigContent:
    description: The content of a Database Tools database API gateway config global resource.
    properties:
      global:
        $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigGlobal'
      pools:
        description: The content of the Database Tools database API gateway config pools each with API
          spec and auto API spec definitions.
        items:
          $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolContent'
        maxItems: 1000
        minItems: 0
        type: array
    required:
    - global
    type: object
  DatabaseToolsDatabaseApiGatewayConfigGlobal:
    description: The content of a Database Tools database API gateway config global resource.
    discriminator: type
    properties:
      advancedProperties:
        additionalProperties:
          type: string
        description: Advanced global properties.
        type: object
      certificateBundle:
        $ref: '#/definitions/DatabaseApiGatewayConfigCertificateBundle'
      databaseApiStatus:
        default: ENABLED
        description: ORDS database API is a database management and monitoring REST API. Database Actions
          requires this feature.
        enum:
        - ENABLED
        - DISABLED
        type: string
      documentRoot:
        description: The location of the static resources to be served under the / root server path.
        maxLength: 4096
        minLength: 0
        type: string
      httpPort:
        default: 0
        description: Specifies the HTTP listen port. 0 disables HTTP. Use of ports below 1024 requires
          elevated (root) privileges and is generally discouraged; deployment on non-privileged ports
          (1024–65535) is recommended.
        maximum: 65535
        minimum: 0
        type: integer
      httpsPort:
        default: 8443
        description: Specifies the HTTPS listen port. 0 disables HTTPS. Use of ports below 1024 requires
          elevated (root) privileges and is generally discouraged; deployment on non-privileged ports
          (1024–65535) is recommended. ORDS will use a self-signed certificate if a certificate bundle
          is not provided.
        maximum: 65535
        minimum: 0
        type: integer
      key:
        description: A string that uniquely identifies a Database Tools database API gateway config global
          settings resource.
        maxLength: 255
        minLength: 1
        type: string
      metadataSource:
        description: The RESTful service definition location.
        enum:
        - DATABASE
        - CLOUD
        type: string
      poolRoute:
        default: PATH
        description: How the target pool route value is determined for a HTTP request.
        enum:
        - PATH
        - HEADER
        type: string
      poolRoutingHeader:
        description: The request header name providing the pool route value.
        maxLength: 255
        minLength: 0
        type: string
      timeCreated:
        description: The time the resource was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the resource was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The type of the Database Tools database API gateway config global settings resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - key
    - type
    - timeCreated
    - timeUpdated
    type: object
  DatabaseToolsDatabaseApiGatewayConfigGlobalDefault:
    allOf:
    - $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigGlobal'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config global resource.
  DatabaseToolsDatabaseApiGatewayConfigPool:
    description: The content of a Database Tools database API gateway config pool sub resource.
    discriminator: type
    properties:
      advancedProperties:
        additionalProperties:
          type: string
        description: Advanced pool properties.
        type: object
      databaseActionsStatus:
        default: DISABLED
        description: Specifies to enable the Database Actions feature.
        enum:
        - ENABLED
        - DISABLED
        type: string
      databaseToolsConnectionId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the related Database Tools
          connection. Specifies the OCI database tools connection ocid to build the connection pool from.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      initialPoolSize:
        default: 0
        description: Specifies the initial size for the number of database connections that will be created
          for the pool.
        maximum: 100000
        minimum: 0
        type: integer
      jwtProfileAudience:
        description: Specifies the expected audience for the JWT token. This value is used to validate
          the aud claim in the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileIssuer:
        description: Specifies the issuer of the JWT token. This value is used to validate the iss claim
          in the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileJwkUrl:
        description: Specifies the URL of the JSON Web Key (JWK) that is used to verify the signature
          of the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileRoleClaimName:
        description: Specifies the JSON pointer to the claim in the JWT token that contains the roles
          of the users.
        maxLength: 4000
        minLength: 0
        type: string
      key:
        description: A system generated string that uniquely identifies a pool sub resource.
        maxLength: 255
        minLength: 1
        type: string
      maxPoolSize:
        default: 100
        description: Specifies the maximum number of database connections allowed for the pool.
        maximum: 100000
        minimum: 1
        type: integer
      minPoolSize:
        default: 0
        description: Specifies the minimum number of database connections allowed for the pool.
        maximum: 1000
        minimum: 0
        type: integer
      poolRouteValue:
        description: The pool route value provided in requests to target this pool.
        maxLength: 255
        minLength: 1
        type: string
      restEnabledSqlStatus:
        default: DISABLED
        description: Specifies whether the REST-Enabled SQL service is active.
        enum:
        - ENABLED
        - DISABLED
        type: string
      timeCreated:
        description: The time the resource was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the resource was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The type of the Database Tools database API gateway config pool sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - key
    - type
    - timeCreated
    - timeUpdated
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec:
    description: The content of a Database Tools database API gateway config API spec sub resource defined
      within a pool.
    discriminator: type
    properties:
      content:
        description: The content of a string-escaped Open API spec in JSON format.
        maxLength: 102400
        minLength: 2
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      key:
        description: A system generated string that uniquely identifies an API spec sub resource within
          a given pool.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: The time the resource was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the resource was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The type of the Database Tools database API gateway config API spec sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - key
    - type
    - displayName
    - content
    - timeCreated
    - timeUpdated
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolApiSpecCollection:
    description: List of Database Tools database API gateway config API spec summary items.
    properties:
      items:
        description: Array of Database Tools database API gateway config API spec summary items.
        items:
          $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpecSummary'
        type: array
    required:
    - items
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDefault:
    allOf:
    - $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config API spec sub resource defined
      within a pool.
  DatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDefaultSummary:
    allOf:
    - $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpecSummary'
    - discriminator: DEFAULT
    description: Summary of the Database Tools database API gateway config API spec resource.
  DatabaseToolsDatabaseApiGatewayConfigPoolApiSpecSummary:
    description: Summary of the Database Tools database API gateway config API spec resource.
    discriminator: type
    properties:
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      key:
        description: A system generated string that uniquely identifies an API spec sub resource within
          a pool.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: The time the resource was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the resource was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The type of the Database Tools database API gateway config API spec sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - key
    - type
    - displayName
    - timeCreated
    - timeUpdated
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec:
    description: The content of a Database Tools database API gateway config auto API spec sub resource
      defined within a pool.
    discriminator: type
    properties:
      alias:
        description: Used as the URI path element for this object. When not specified the objectName lowercase
          is the default value.
        maxLength: 255
        minLength: 0
        type: string
      databaseObjectName:
        description: The name of the database object.
        maxLength: 255
        minLength: 1
        type: string
      databaseObjectType:
        description: The type of the database object.
        enum:
        - FUNCTION
        - MVIEW
        - PACKAGE
        - PROCEDURE
        - TABLE
        - VIEW
        - DUALITYVIEW
        type: string
      description:
        description: Description of the autoApiSpec.
        maxLength: 4000
        minLength: 0
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      key:
        description: A system generated string that uniquely identifies an auto API spec sub resource
          within a given pool.
        maxLength: 255
        minLength: 1
        type: string
      operations:
        description: The operations to limit access to this resource. If not specified then the default
          is ["READ","WRITE"].
        items:
          enum:
          - READ
          - WRITE
          type: string
        maxItems: 2
        minItems: 0
        type: array
      roles:
        description: The name of the database API gateway config roles protecting the resource. Only valid
          for RBAC JWT Profile pools and BEARER securitySchemes.
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 20
        minItems: 0
        type: array
      scope:
        description: The name of the database API gateway config privilege protecting the resource. Only
          valid for SCOPE JWT Profile pools and BEARER securitySchemes.
        maxLength: 255
        minLength: 0
        type: string
      securitySchemes:
        description: The security schemes that can access this resource. If not specified then the resource
          is public.
        items:
          enum:
          - BASIC
          - BEARER
          type: string
        maxItems: 2
        minItems: 0
        type: array
      timeCreated:
        description: The time the resource was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the resource was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The type of the Database Tools database API gateway config auto API spec sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - key
    - type
    - displayName
    - timeCreated
    - timeUpdated
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecCollection:
    description: List of Database Tools database API gateway config auto API spec summary items.
    properties:
      items:
        description: Array of Database Tools database API gateway config auto API spec summary items.
        items:
          $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecSummary'
        type: array
    required:
    - items
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDefault:
    allOf:
    - $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config auto API spec sub resource
      defined within a pool.
  DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDefaultSummary:
    allOf:
    - $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecSummary'
    - discriminator: DEFAULT
    description: Summary of the Database Tools database API gateway config auto API spec resource.
  DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecSummary:
    description: Summary of the Database Tools database API gateway config auto API spec resource.
    discriminator: type
    properties:
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      key:
        description: A system generated string that uniquely identifies an auto API spec sub resource
          within a pool.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: The time the resource was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the resource was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The type of the Database Tools database API gateway config auto API spec sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - key
    - type
    - displayName
    - timeCreated
    - timeUpdated
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolCollection:
    description: List of Database Tools database API gateway config pool summary items.
    properties:
      items:
        description: Array of Database Tools database API gateway config pool summary items.
        items:
          $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolSummary'
        type: array
    required:
    - items
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolContent:
    description: The content of a Database Tools database API gateway config pool sub resource.
    discriminator: type
    properties:
      advancedProperties:
        additionalProperties:
          type: string
        description: Advanced pool properties.
        type: object
      apiSpecs:
        description: Array of Database Tools database API gateway config API spec items.
        items:
          $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolApiSpec'
        maxItems: 10
        minItems: 0
        type: array
      autoApiSpecs:
        description: Array of Database Tools database API gateway config auto API spec items.
        items:
          $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpec'
        maxItems: 1000
        minItems: 0
        type: array
      databaseActionsStatus:
        default: DISABLED
        description: Specifies to enable the Database Actions feature.
        enum:
        - ENABLED
        - DISABLED
        type: string
      databaseToolsConnectionId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the related Database Tools
          connection. Specifies the OCI database tools connection ocid to build the connection pool from.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      initialPoolSize:
        default: 0
        description: Specifies the initial size for the number of database connections that will be created
          for the pool.
        maximum: 100000
        minimum: 0
        type: integer
      jwtProfileAudience:
        description: Specifies the expected audience for the JWT token. This value is used to validate
          the aud claim in the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileIssuer:
        description: Specifies the issuer of the JWT token. This value is used to validate the iss claim
          in the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileJwkUrl:
        description: Specifies the URL of the JSON Web Key (JWK) that is used to verify the signature
          of the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileRoleClaimName:
        description: Specifies the JSON pointer to the claim in the JWT token that contains the roles
          of the users.
        maxLength: 4000
        minLength: 0
        type: string
      key:
        description: A system generated string that uniquely identifies a pool sub resource.
        maxLength: 255
        minLength: 1
        type: string
      maxPoolSize:
        default: 100
        description: Specifies the maximum number of database connections allowed for the pool.
        maximum: 100000
        minimum: 1
        type: integer
      minPoolSize:
        default: 0
        description: Specifies the minimum number of database connections allowed for the pool.
        maximum: 1000
        minimum: 0
        type: integer
      poolRouteValue:
        description: The pool route value provided in requests to target this pool.
        maxLength: 255
        minLength: 1
        type: string
      restEnabledSqlStatus:
        default: DISABLED
        description: Specifies whether the REST-Enabled SQL service is active.
        enum:
        - ENABLED
        - DISABLED
        type: string
      timeCreated:
        description: The time the resource was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the resource was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The type of the Database Tools database API gateway config pool sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - key
    - type
    type: object
  DatabaseToolsDatabaseApiGatewayConfigPoolContentDefault:
    allOf:
    - $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolContent'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config pool sub resource.
  DatabaseToolsDatabaseApiGatewayConfigPoolDefault:
    allOf:
    - $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPool'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config pool sub resource.
  DatabaseToolsDatabaseApiGatewayConfigPoolDefaultSummary:
    allOf:
    - $ref: '#/definitions/DatabaseToolsDatabaseApiGatewayConfigPoolSummary'
    - discriminator: DEFAULT
    description: Summary of the Database Tools database API gateway config pool resource.
  DatabaseToolsDatabaseApiGatewayConfigPoolSummary:
    description: Summary of the Database Tools database API gateway config pool resource.
    discriminator: type
    properties:
      databaseToolsConnectionId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the related Database Tools
          connection.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      key:
        description: A system generated string that uniquely identifies a pool sub resource.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: The time the resource was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the resource was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The type of the Database Tools database API gateway config pool sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - key
    - type
    - displayName
    - databaseToolsConnectionId
    - timeCreated
    - timeUpdated
    type: object
  DatabaseToolsExternalAuthenticationStatus:
    description: 'The status of the external authentication. AVAILABLE if the external authentication
      can be used with this connection. UNAVAILABLE if not.

      ENABLED if the external authentication is enabled. UNKNOWN if the user has insufficient privilege.

      '
    enum: &id022
    - AVAILABLE
    - UNAVAILABLE
    - ENABLED
    - UNKNOWN
    type: string
  DatabaseToolsIdentity:
    description: Manages credentials in a database to access service resources.
    discriminator: type
    properties:
      compartmentId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment containing
          the Database Tools identity.
        maxLength: 255
        minLength: 1
        type: string
      databaseToolsConnectionId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the related Database Tools
          connection.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id011
      displayName:
        description: A user-friendly name. Does not have to be unique and can be updated. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id012
      id:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the Database Tools identity.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleDetails:
        description: A message describing the current state in more detail. For example, this message
          can be used to provide actionable information for a resource in the Failed state.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The current state of the Database Tools identity.
        enum: &id015
        - CREATING
        - UPDATING
        - ACTIVE
        - DELETING
        - DELETED
        - FAILED
        - NEEDS_ATTENTION
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseToolsIdentityLifecycleState'
      systemTags: *id013
      timeCreated:
        description: The time the Database Tools identity was created. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The time the Database Tools identity was updated. An RFC3339 formatted datetime string.
        format: date-time
        type: string
      type:
        description: The Database Tools identity type.
        enum: &id018
        - ORACLE_DATABASE_RESOURCE_PRINCIPAL
        type: string
        x-obmcs-top-level-enum: '#/definitions/IdentityType'
    required:
    - id
    - type
    - compartmentId
    - displayName
    - databaseToolsConnectionId
    - lifecycleState
    - timeCreated
    - timeUpdated
    type: object
  DatabaseToolsIdentityLifecycleState:
    description: The lifecycle state.
    enum: *id015
    type: string
  DatabaseToolsIdentityOracleDatabaseResourcePrincipal:
    allOf:
    - $ref: '#/definitions/DatabaseToolsIdentity'
    - discriminator: ORACLE_DATABASE_RESOURCE_PRINCIPAL
      properties:
        credentialKey:
          description: The name of the credential object created in the Oracle Database.
          maxLength: 128
          minLength: 1
          type: string
      required:
      - credentialKey
    description: Manages credentials in a database to access service resources.
  DatabaseToolsIdentityStatus:
    description: The status of the identity. AVAILABLE if the identity type can be used with this connection.
      UNAVAILABLE if not.
    enum: &id023
    - AVAILABLE
    - UNAVAILABLE
    type: string
  DbmsCloudStatus:
    description: The DBMS_CLOUD package installation status.
    enum: &id021
    - AVAILABLE
    - UNAVAILABLE
    type: string
  Error:
    description: Error Information.
    properties:
      code:
        description: A short error code that defines the error, meant for programmatic parsing.
        type: string
      message:
        description: A human-readable error string.
        type: string
    required:
    - code
    - message
  ExecuteSqlAsynchronousInputDetails:
    description: Async request script input details
    discriminator: type
    properties:
      type:
        description: The request input type.
        enum:
        - OBJECT_STORAGE
        - INLINE
        type: string
    required:
    - type
    type: object
  ExecuteSqlAsynchronousInputInlineDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlAsynchronousInputDetails'
    - discriminator: INLINE
      properties:
        content:
          $ref: '#/definitions/ExecuteSqlInputDetails'
      required:
      - content
    description: Inline scipt input.
    type: object
  ExecuteSqlAsynchronousInputObjectStorageDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlAsynchronousInputDetails'
    - discriminator: OBJECT_STORAGE
      properties:
        object:
          $ref: '#/definitions/ExecuteSqlObjectStorageLocation'
      required:
      - object
    description: Object Storage script input.
    type: object
  ExecuteSqlBind:
    description: Bind information.
    properties:
      dataType:
        description: The data type.
        type: string
      index:
        description: Index of the bind. Index of the first bind is 1.
        minimum: 1
        type: integer
      mode:
        description: The mode in which the bind is used.
        enum:
        - IN
        - INOUT
        - OUT
        type: string
      name:
        description: Name of the bind.
        type: string
      plsqlTableTypeDetails:
        $ref: '#/definitions/ExecuteSqlBindPlsqlTable'
      results:
        description: results
        items:
          type: object
        type: array
      values:
        description: values
        items:
          type: object
        type: array
    required:
    - dataType
    type: object
  ExecuteSqlBindPlsqlTable:
    description: PL/SQL table type details
    properties:
      typeComponents:
        description: Specifies an array of data types in the PL/SQL table.
        items:
          $ref: '#/definitions/ExecuteSqlBindTypeComponent'
        type: array
      typeName:
        description: Only an empty string is accepted as the value.
        type: string
      typeSubname:
        description: Only an empty string is accepted as the value.
        type: string
    type: object
  ExecuteSqlBindTypeComponent:
    description: Data type component.
    properties:
      dataType:
        description: Oracle Database data type.
        type: string
    type: object
  ExecuteSqlDatabaseToolsConnectionAsynchronousDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlDatabaseToolsConnectionDetails'
    - discriminator: ASYNCHRONOUS
      properties:
        input:
          $ref: '#/definitions/ExecuteSqlAsynchronousInputDetails'
        timeoutInSeconds:
          description: Maximum time in seconds allowed for the request to complete, measured from submission.
          maximum: 86400
          minimum: 30
          type: integer
      required:
      - input
    description: Asynchronous request.
    type: object
  ExecuteSqlDatabaseToolsConnectionDetails:
    description: A request
    discriminator: type
    properties:
      output:
        $ref: '#/definitions/ExecuteSqlOutputDetails'
      type:
        description: The request execution type.
        enum:
        - SYNCHRONOUS
        - ASYNCHRONOUS
        type: string
    required:
    - type
    type: object
  ExecuteSqlDatabaseToolsConnectionSynchronousDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlDatabaseToolsConnectionDetails'
    - discriminator: SYNCHRONOUS
      properties:
        input:
          $ref: '#/definitions/ExecuteSqlInputDetails'
      required:
      - input
    description: Synchronous request.
    type: object
  ExecuteSqlInputBatchDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlInputDetails'
    - discriminator: BATCH
      properties:
        batchStatementTexts:
          description: DML statements to execute in jdbc batch mode
          items:
            type: string
          type: array
        properties:
          description: Client properties returned as-is in the response
          type: object
        responseFormat:
          $ref: '#/definitions/ExecuteSqlResponseFormat'
      required:
      - batchStatementTexts
    description: Contains the details for the Batch SQL statements to execute on the database connection.
    type: object
  ExecuteSqlInputDetails:
    description: Contains the details for the SQL statements to execute on the database connection.
    discriminator: type
    properties:
      type:
        description: The Request type.
        enum: &id016
        - STANDARD
        - BATCH
        - SCRIPT
        type: string
        x-obmcs-top-level-enum: '#/definitions/PayloadType'
    required:
    - type
    type: object
  ExecuteSqlInputScriptDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlInputDetails'
    - discriminator: SCRIPT
      properties:
        properties:
          description: Client properties returned as-is in the response
          items:
            additionalProperties:
              type: object
            type: object
          type: array
        scripts:
          description: The collection of scripts to execute.
          items:
            $ref: '#/definitions/ExecuteSqlInputScriptSqlRequestDetails'
          type: array
        version:
          description: Request payload version, returned as-is in the response
          type: string
      required:
      - scripts
    description: Contains the details for the Script SQL statements to execute on the database connection.
    type: object
  ExecuteSqlInputScriptSqlRequestDetails:
    description: Contains the details for the Script SQL statements to execute on the database connection.
    properties:
      properties:
        description: Client properties returned as-is in the response
        type: object
      sqlRequests:
        description: The SQL statements to execute.
        items:
          $ref: '#/definitions/ExecuteSqlInputDetails'
        type: array
    required:
    - sqlRequests
    type: object
  ExecuteSqlInputStandardDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlInputDetails'
    - discriminator: STANDARD
      properties:
        binds:
          description: Array of objects specifying the bind information.
          items:
            $ref: '#/definitions/ExecuteSqlBind'
          type: array
        limit:
          description: The maximum number of rows to return from the query (-1 disables pagination).
          maximum: 10000
          minimum: -1
          type: integer
        offset:
          description: The first row to return in the result set.
          minimum: 0
          type: integer
        properties:
          description: Client properties returned as-is in the response
          type: object
        responseFormat:
          $ref: '#/definitions/ExecuteSqlResponseFormat'
        statementText:
          description: Statements to execute (Can be more than one).
          type: string
      required:
      - statementText
    description: Contains the details for the Standard SQL statements to execute on the database connection.
    type: object
  ExecuteSqlObjectStorageLocation:
    description: Object Storage location.
    properties:
      bucketName:
        description: The name of the object storage bucket
        type: string
      namespace:
        description: The name of the object storage namespace
        type: string
      objectName:
        description: The name of the object
        type: string
    required:
    - namespace
    - bucketName
    - objectName
    type: object
  ExecuteSqlOutputDetails:
    description: Defines how the script result should be stored
    discriminator: type
    properties:
      resultDispositionTemplates:
        description: "Defines how the result of commands in a script should be stored. \nIf the command\
          \ does not match any template filter, the result will be inline.\n"
        items:
          $ref: '#/definitions/ExecuteSqlOutputResultDispositionTemplate'
        type: array
      type:
        description: The response disposition type.
        enum:
        - OBJECT_STORAGE
        type: string
    required:
    - type
    type: object
  ExecuteSqlOutputDispositionDetails:
    description: Describes how the response of a command is to be stored
    discriminator: type
    properties:
      type:
        description: The location resource type.
        enum:
        - OBJECT_STORAGE
        type: string
    required:
    - type
    type: object
  ExecuteSqlOutputDispositionObjectStorageDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlOutputDispositionDetails'
    - discriminator: OBJECT_STORAGE
      properties:
        bucketName:
          description: The name of the object storage bucket
          type: string
        cacheControl:
          description: The Cache-Control header
          type: string
        contentDisposition:
          description: The content disposition
          type: string
        contentEncoding:
          description: The content encoding
          type: string
        contentLanguage:
          description: The content language
          type: string
        contentType:
          description: The content type
          type: string
        namespace:
          description: The name of the object storage namespace
          type: string
        objectName:
          description: The name of the object template (can contain statementId placeholder, for example;
            query_{statementId}.csv )
          type: string
        opcMeta:
          additionalProperties:
            type: string
          description: The object metadata
          type: object
        opcSseCustomerAlgorithm:
          description: The Encryption Algorithm
          type: string
        opcSseCustomerKey:
          description: The base64-encoded 256-bit encryption key to use
          type: string
        opcSseCustomerKeySha256:
          description: The base64-encoded SHA256 hash of the encryption key
          type: string
        opcSseKmsKeyId:
          description: The OCID of a master encryption key
          type: string
        storageTier:
          description: The storage tier that the object should be stored in
          enum:
          - STANDARD
          - INFREQUENT_ACCESS
          - ARCHIVE
          type: string
      required:
      - namespace
      - bucketName
      - objectName
    description: Describes how the result of a statement is stored in Object Storage
    type: object
  ExecuteSqlOutputObjectStorageDetails:
    allOf:
    - $ref: '#/definitions/ExecuteSqlOutputDetails'
    - discriminator: OBJECT_STORAGE
      properties:
        object:
          $ref: '#/definitions/ExecuteSqlObjectStorageLocation'
        timeOfDeletion:
          description: The time when the object becomes eligible for deletion, expressed as an RFC 3339
            date-time string.
          format: date-time
          type: string
      required:
      - object
    description: Object Storage resource output details.
    type: object
  ExecuteSqlOutputResultDispositionTemplate:
    description: Template defining how the result of a command should be stored.
    properties:
      objectTemplate:
        $ref: '#/definitions/ExecuteSqlOutputDispositionDetails'
      statementType:
        description: Commands matching this statement type will use this result disposition.
        enum: &id017
        - QUERY
        - DML
        - DDL
        - PLSQL
        - SQLPLUS
        - IGNORE
        - TRANSACTION_CONTROL
        - SESSION_CONTROL
        - SYSTEM_CONTROL
        - JDBC
        - OTHER
        type: string
        x-obmcs-top-level-enum: '#/definitions/StatementType'
    required:
    - statementType
    - objectTemplate
    type: object
  ExecuteSqlResponse:
    description: Contains the details for the SQL statements to execute on the database connection.
    discriminator: type
    properties:
      env:
        $ref: '#/definitions/ExecuteSqlResponseEnv'
      type:
        description: The response payload type
        enum: *id016
        type: string
        x-obmcs-top-level-enum: '#/definitions/PayloadType'
      version:
        description: Script version
        type: string
    required:
    - type
    type: object
  ExecuteSqlResponseBatch:
    allOf:
    - $ref: '#/definitions/ExecuteSqlResponse'
    - discriminator: BATCH
      properties:
        items:
          description: The execution result of a statement.
          items:
            $ref: '#/definitions/ExecuteSqlResponseItemBatch'
          type: array
      required:
      - items
    description: Contains the details for the Batch SQL statements to execute on the database connection.
    type: object
  ExecuteSqlResponseEnv:
    description: Database session environment.
    properties:
      defaultTimeZone:
        description: The database default time zone.
        format: x-obmcs-time-zone
        type: string
    required:
    - defaultTimeZone
    type: object
  ExecuteSqlResponseFormat:
    description: Specifies which major elements of the response are returned
    properties:
      binds:
        description: Set to false to exclude binds from response
        type: boolean
      response:
        description: Set to false to exclude response from response
        type: boolean
      result:
        description: Set to false to exclude result from response
        type: boolean
      resultSetMetaData:
        description: Set to false to exclude result set metadata from response
        type: boolean
      statementInformation:
        description: Set to false to exclude statement information from response
        type: boolean
      statementText:
        description: Set to false to exclude statement text from response
        type: boolean
    type: object
  ExecuteSqlResponseItemBatch:
    description: A statement execution response for batched type requests.
    properties:
      batchStatementTexts:
        description: DML statements to execute in jdbc batch mode
        items:
          type: string
        type: array
      dbmsOutput:
        description: Output from DBMS_OUTPUT package. Server output must be enabled (e.g., SET SERVEROUTPUT
          ON).
        type: string
      error:
        $ref: '#/definitions/ExecuteSqlResponseItemError'
      properties:
        $ref: '#/definitions/ExecuteSqlResponseItemProperties'
      responses:
        description: Responses generated when executing the statements.
        items:
          type: string
        type: array
      results:
        description: Results generated when executing the statements.
        items:
          type: integer
        type: array
      statementId:
        description: The sequence number of the statement. Sequence number of the first statement is 1.
        minimum: 1
        type: integer
      statementPos:
        $ref: '#/definitions/ExecuteSqlResponseItemStatementPos'
      statementType:
        description: The Statement type.
        enum: *id017
        type: string
        x-obmcs-top-level-enum: '#/definitions/StatementType'
    type: object
  ExecuteSqlResponseItemError:
    description: Error details.
    properties:
      errorCode:
        description: Error code.
        type: integer
      errorColumn:
        description: Error column number.
        type: integer
      errorDetails:
        description: Error details.
        type: string
      errorLine:
        description: Error line number.
        type: integer
      errorMessage:
        description: Error message.
        type: string
    type: object
  ExecuteSqlResponseItemProperties:
    description: Property information
    properties:
      displayName:
        description: "Client properties returned as-is in the response. \nFor asynchronous requests, the\
          \ \"displayName\" property can be set to provide a user-friendly name that will propagate to\
          \ the resulting Work Request to allow for easier identification and tracking of the operation's\
          \ progress and outcome.\n"
        type: object
      query:
        description: Client properties returned as-is in the response
        type: object
      statement:
        description: Client properties returned as-is in the response
        type: object
    type: object
  ExecuteSqlResponseItemResultSet:
    description: Result set generated from the query.
    properties:
      count:
        description: The number of rows returned.
        minimum: 0
        type: integer
      hasMore:
        description: Specifies whether the result set has more rows.
        type: boolean
      items:
        description: All rows in the result set.
        items:
          additionalProperties:
            type: object
          type: object
        type: array
      limit:
        description: Maximum number of rows returned from the query.
        maximum: 10000
        type: integer
      metadata:
        description: Metadata of the column.
        items:
          $ref: '#/definitions/ExecuteSqlResponseItemResultSetMetadata'
        type: array
      offset:
        description: The first row returned in the result set.
        minimum: 0
        type: integer
    type: object
  ExecuteSqlResponseItemResultSetMetadata:
    description: Metadata of the column
    properties:
      columnTypeName:
        description: Oracle Database data type of the column.
        type: string
      databaseColumnName:
        description: Name of the column in the Oracle Database.
        type: string
      isNullable:
        description: Specifies if the column is nullable (0 if the column is not nullable)
        type: boolean
      precision:
        description: Precision of the column.
        minimum: 0
        type: integer
      scale:
        description: Scale of the column.
        minimum: 0
        type: integer
      uniqueColumnName:
        description: Name of the column in the response Result Set.
        type: string
    type: object
  ExecuteSqlResponseItemScript:
    description: A statement execution response for script type requests.
    properties:
      binds:
        description: Array of objects specifying the bind information.
        items:
          $ref: '#/definitions/ExecuteSqlBind'
        type: array
      dbmsOutput:
        description: Output from DBMS_OUTPUT package. Server output must be enabled (e.g., SET SERVEROUTPUT
          ON).
        type: string
      error:
        $ref: '#/definitions/ExecuteSqlResponseItemError'
      properties:
        $ref: '#/definitions/ExecuteSqlResponseItemProperties'
      responses:
        description: Responses generated when executing the statements.
        items:
          type: string
        type: array
      resultSet:
        $ref: '#/definitions/ExecuteSqlResponseItemResultSet'
      resultSetObject:
        $ref: '#/definitions/ExecuteSqlOutputDispositionDetails'
      results:
        description: Results generated when executing the statements.
        items:
          type: integer
        type: array
      statementId:
        description: The sequence number of the statement. Sequence number of the first statement is 1.
        minimum: 1
        type: integer
      statementPos:
        $ref: '#/definitions/ExecuteSqlResponseItemStatementPos'
      statementText:
        description: Text of statements executed.
        type: string
      statementType:
        description: The Statement type.
        enum: *id017
        type: string
        x-obmcs-top-level-enum: '#/definitions/StatementType'
    type: object
  ExecuteSqlResponseItemStandard:
    description: A statement execution response for Standard type requests.
    properties:
      binds:
        description: Array of objects specifying the bind information.
        items:
          $ref: '#/definitions/ExecuteSqlBind'
        type: array
      dbmsOutput:
        description: Output from DBMS_OUTPUT package. Server output must be enabled (e.g., SET SERVEROUTPUT
          ON).
        type: string
      error:
        $ref: '#/definitions/ExecuteSqlResponseItemError'
      properties:
        $ref: '#/definitions/ExecuteSqlResponseItemProperties'
      responses:
        description: Responses generated when executing the statements.
        items:
          type: string
        type: array
      resultSet:
        $ref: '#/definitions/ExecuteSqlResponseItemResultSet'
      resultSetObject:
        $ref: '#/definitions/ExecuteSqlOutputDispositionDetails'
      results:
        description: Results generated when executing the statements.
        items:
          type: integer
        type: array
      statementId:
        description: The sequence number of the statement. Sequence number of the first statement is 1.
        minimum: 1
        type: integer
      statementPos:
        $ref: '#/definitions/ExecuteSqlResponseItemStatementPos'
      statementText:
        description: Text of statements executed.
        type: string
      statementType:
        description: The Statement type.
        enum: *id017
        type: string
        x-obmcs-top-level-enum: '#/definitions/StatementType'
    type: object
  ExecuteSqlResponseItemStatementPos:
    description: Position of the statement.
    properties:
      endLine:
        description: End line of the statement.
        type: integer
      startLine:
        description: Start line of the statement. First start line is 1.
        minimum: 1
        type: integer
    type: object
  ExecuteSqlResponseScript:
    allOf:
    - $ref: '#/definitions/ExecuteSqlResponse'
    - discriminator: SCRIPT
      properties:
        items:
          description: The execution result of a statement.
          items:
            $ref: '#/definitions/ExecuteSqlResponseItemScript'
          type: array
      required:
      - items
    description: Contains the details for the Script SQL statements to execute on the database connection.
    type: object
  ExecuteSqlResponseStandard:
    allOf:
    - $ref: '#/definitions/ExecuteSqlResponse'
    - discriminator: STANDARD
      properties:
        items:
          description: The execution result of a statement.
          items:
            $ref: '#/definitions/ExecuteSqlResponseItemStandard'
          type: array
      required:
      - items
    description: Contains the details for the Standard SQL statements to execute on the database connection.
    type: object
  IdentityProviderType:
    description: The identity provider type
    enum: &id020
    - OCI_IAM
    - AZURE_AD
    - NONE
    type: string
  IdentityType:
    description: The Database Tools identity type.
    enum: *id018
    type: string
  OperationStatus:
    description: The state of the work request operation.
    enum: &id025
    - ACCEPTED
    - IN_PROGRESS
    - FAILED
    - SUCCEEDED
    - CANCELING
    - CANCELED
    - WAITING
    type: string
  OperationType:
    description: The work request operation type.
    enum: &id024
    - EXECUTE_SQL
    - EXECUTE_SQL_SCHEDULED_DELETION
    type: string
  PayloadType:
    description: The payload type.
    enum: *id016
    type: string
  PropertySet:
    description: The base definition for a property set
    discriminator: key
    properties:
      isMutable:
        description: Indicates whether the property set is mutable or not
        type: boolean
      key:
        description: The name of the property set
        enum: &id019
        - APEX_DOCUMENT_GENERATOR
        - APEX
        - APEX_FA_INTEGRATION
        - ORACLE_DATABASE_EXTERNAL_AUTHENTICATION
        type: string
        x-obmcs-top-level-enum: '#/definitions/PropertySetKey'
    required:
    - key
    - isMutable
    type: object
  PropertySetApex:
    allOf:
    - $ref: '#/definitions/PropertySet'
    - discriminator: APEX
      properties:
        userKey:
          description: The APEX engine schema name
          type: string
        version:
          description: The version of APEX
          type: string
    description: Contains the details of an APEX property set
  PropertySetApexDocumentGenerator:
    allOf:
    - $ref: '#/definitions/PropertySet'
    - discriminator: APEX_DOCUMENT_GENERATOR
      properties:
        autonomousDatabaseResourcePrincipalStatus:
          description: The status of the Autonomous Database Serverless Resource Principal (OCI$RESOURCE_PRINCIPAL)
          enum:
          - ENABLED
          - DISABLED
          type: string
        credentialKey:
          description: The name of the credential used by APEX to manage Object Storage Buckets and Objects
            as well as invoke the Document Generator function
          type: string
        functionId:
          description: The [OCID](/Content/General/Concepts/identifiers.htm) of the Document Generator
            function
          maxLength: 255
          minLength: 0
          type: string
        invokeEndpoint:
          description: The base endpoint URL to use to invoke the Document Generator function
          type: string
        objectStorageBucketCompartmentId:
          description: The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment containing
            the Object Storage Buckets managed by APEX
          maxLength: 255
          minLength: 0
          type: string
        objectStorageEndpoint:
          description: Object Storage Endpoint
          type: string
        objectStorageNamespace:
          description: The Object Storage Namespace containing the Object Storage Buckets managed by APEX
          type: string
        printServerType:
          description: The print server type
          enum:
          - DOCUMENT_GENERATOR
          - NONE
          - STANDARD
          - ADVANCED
          - AOP
          - OTHER
          type: string
    description: Contains the details of an APEX Document Generator property set
  PropertySetApexFaIntegration:
    allOf:
    - $ref: '#/definitions/PropertySet'
    - discriminator: APEX_FA_INTEGRATION
      properties:
        authenticationSubstitutions:
          additionalProperties:
            type: string
          description: APEX FA Integration key-value pairs.
          type: object
        instanceDbmsCredentialEnabled:
          description: 'Specifies whether database credentials can be used in all workspaces on the APEX
            instance. Supported values include: "Y", "N" and empty string.'
          type: string
        prerequisitesCheck:
          $ref: '#/definitions/ApexFaIntegrationPrerequisitesCheck'
    description: Contains the details of an APEX FA Integration property set
  PropertySetKey:
    description: The property set key
    enum: *id019
    type: string
  PropertySetOracleDatabaseExternalAuthentication:
    allOf:
    - $ref: '#/definitions/PropertySet'
    - discriminator: ORACLE_DATABASE_EXTERNAL_AUTHENTICATION
      properties:
        identityProvider:
          $ref: '#/definitions/PropertySetOracleDatabaseExternalAuthenticationIdentityProvider'
    description: Contains the details of Oracle Database External Authentication property set
  PropertySetOracleDatabaseExternalAuthenticationIdentityProvider:
    description: External identity type provider
    discriminator: type
    properties:
      type:
        description: External identity type provider.  Supported values - OCI_IAM, AZURE_AD, NONE.
        enum: *id020
        type: string
        x-obmcs-top-level-enum: '#/definitions/IdentityProviderType'
    required:
    - type
    type: object
  PropertySetOracleDatabaseExternalAuthenticationIdentityProviderAzureAd:
    allOf:
    - $ref: '#/definitions/PropertySetOracleDatabaseExternalAuthenticationIdentityProvider'
    - discriminator: AZURE_AD
      properties:
        configs:
          additionalProperties:
            type: string
          description: 'External identity provider configuration parameters. Simple key-value pair

            Example: { "tenant_id": "...", "application_id_uri": "...", ... }

            '
          type: object
      required:
      - configs
    description: External identity provider for AZURE_AD
  PropertySetOracleDatabaseExternalAuthenticationIdentityProviderNone:
    allOf:
    - $ref: '#/definitions/PropertySetOracleDatabaseExternalAuthenticationIdentityProvider'
    - discriminator: NONE
    description: External identity provider value used to disable external authentication
  PropertySetOracleDatabaseExternalAuthenticationIdentityProviderOciIam:
    allOf:
    - $ref: '#/definitions/PropertySetOracleDatabaseExternalAuthenticationIdentityProvider'
    - discriminator: OCI_IAM
    description: External identity provider for OCI_IAM
  SortOrder:
    description: The sort order to use, either ascending (`ASC`) or descending (`DESC`).
    enum: &id028
    - ASC
    - DESC
    type: string
  StatementType:
    description: The statement type.
    enum: *id017
    type: string
  UpdateCredentialBasicDetails:
    allOf:
    - $ref: '#/definitions/UpdateCredentialDetails'
    - discriminator: BASIC
      properties:
        password:
          description: The password for the new credential.
          format: password
          maxLength: 24000
          minLength: 1
          type: string
        userName:
          description: The username for the new credential.
          maxLength: 24000
          minLength: 1
          type: string
    description: Credential information to be updated for the BASIC type.
  UpdateCredentialDetails:
    description: Credential information to be updated.
    discriminator: type
    properties:
      type:
        description: The type of credential.
        enum: *id009
        type: string
        x-obmcs-top-level-enum: '#/definitions/CredentialType'
    required:
    - type
    type: object
  UpdateDatabaseToolsDatabaseApiGatewayConfigGlobalDefaultDetails:
    allOf:
    - $ref: '#/definitions/UpdateDatabaseToolsDatabaseApiGatewayConfigGlobalDetails'
    - discriminator: DEFAULT
    description: The content of the Database Tools database API gateway config global sub resource to
      be updated.
  UpdateDatabaseToolsDatabaseApiGatewayConfigGlobalDetails:
    description: The content of the Database Tools database API gateway config global sub resource to
      be updated.
    discriminator: type
    properties:
      advancedProperties:
        additionalProperties:
          type: string
        description: Advanced global properties.
        type: object
      certificateBundle:
        $ref: '#/definitions/DatabaseApiGatewayConfigCertificateBundle'
      databaseApiStatus:
        description: ORDS database API is a database management and monitoring REST API. Database Actions
          requires this feature.
        enum:
        - ENABLED
        - DISABLED
        type: string
      documentRoot:
        description: The location of the static resources to be served under the / root server path.
        maxLength: 4096
        minLength: 0
        type: string
      httpPort:
        description: Specifies the HTTP listen port. 0 disables HTTP. Use of ports below 1024 requires
          elevated (root) privileges and is generally discouraged; deployment on non-privileged ports
          (1024–65535) is recommended.
        maximum: 65535
        minimum: 0
        type: integer
      httpsPort:
        description: Specifies the HTTPS listen port. 0 disables HTTPS. Use of ports below 1024 requires
          elevated (root) privileges and is generally discouraged; deployment on non-privileged ports
          (1024–65535) is recommended. ORDS will use a self-signed certificate if a certificate bundle
          is not provided.
        maximum: 65535
        minimum: 0
        type: integer
      poolRoute:
        description: How the target pool route value is determined for a HTTP request.
        enum:
        - PATH
        - HEADER
        type: string
      poolRoutingHeader:
        description: The request header name providing the pool route value.
        maxLength: 255
        minLength: 0
        type: string
      type:
        description: The type of the Database Tools database API gateway config global settings resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - type
    type: object
  UpdateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDefaultDetails:
    allOf:
    - $ref: '#/definitions/UpdateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDetails'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config API spec sub resource to
      be updated.
  UpdateDatabaseToolsDatabaseApiGatewayConfigPoolApiSpecDetails:
    description: The content of a Database Tools database API gateway config API spec sub resource to
      be updated.
    discriminator: type
    properties:
      content:
        description: The content of a string-escaped Open API spec in JSON format.
        maxLength: 102400
        minLength: 2
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      type:
        description: The type of the Database Tools database API gateway config API spec sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - type
    type: object
  UpdateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDefaultDetails:
    allOf:
    - $ref: '#/definitions/UpdateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDetails'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config auto API spec sub resource
      to be updated.
  UpdateDatabaseToolsDatabaseApiGatewayConfigPoolAutoApiSpecDetails:
    description: The content of a Database Tools database API gateway config auto API spec sub resource
      to be updated.
    discriminator: type
    properties:
      alias:
        description: Used as the URI path element for this object. When not specified the objectName lowercase
          is the default value.
        maxLength: 255
        minLength: 0
        type: string
      databaseObjectName:
        description: The name of the database object.
        maxLength: 255
        minLength: 1
        type: string
      databaseObjectType:
        description: The type of the database object.
        enum:
        - FUNCTION
        - MVIEW
        - PACKAGE
        - PROCEDURE
        - TABLE
        - VIEW
        - DUALITYVIEW
        type: string
      description:
        description: Description of the autoApiSpec.
        maxLength: 4000
        minLength: 0
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      operations:
        description: The operations to limit access to this resource. If not specified then the default
          is ["READ","WRITE"].
        items:
          enum:
          - READ
          - WRITE
          type: string
        maxItems: 2
        minItems: 0
        type: array
      roles:
        description: The name of the database API gateway config roles protecting the resource. Only valid
          for RBAC JWT Profile pools and BEARER securitySchemes.
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 20
        minItems: 0
        type: array
      scope:
        description: The name of the database API gateway config privilege protecting the resource. Only
          valid for SCOPE JWT Profile pools and BEARER securitySchemes.
        maxLength: 255
        minLength: 0
        type: string
      securitySchemes:
        description: The security schemes that can access this resource. If not specified then the resource
          is public.
        items:
          enum:
          - BASIC
          - BEARER
          type: string
        maxItems: 2
        minItems: 0
        type: array
      type:
        description: The type of the Database Tools database API gateway config auto API spec sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - type
    type: object
  UpdateDatabaseToolsDatabaseApiGatewayConfigPoolDefaultDetails:
    allOf:
    - $ref: '#/definitions/UpdateDatabaseToolsDatabaseApiGatewayConfigPoolDetails'
    - discriminator: DEFAULT
    description: The content of a Database Tools database API gateway config pool sub resource to be updated.
  UpdateDatabaseToolsDatabaseApiGatewayConfigPoolDetails:
    description: The content of a Database Tools database API gateway config pool sub resource to be updated.
    discriminator: type
    properties:
      advancedProperties:
        additionalProperties:
          type: string
        description: Advanced pool properties.
        type: object
      databaseActionsStatus:
        default: DISABLED
        description: Specifies to enable the Database Actions feature.
        enum:
        - ENABLED
        - DISABLED
        type: string
      databaseToolsConnectionId:
        description: The [OCID](/Content/General/Concepts/identifiers.htm) of the related Database Tools
          connection. Specifies the OCI database tools connection ocid to build the connection pool from.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it’s changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      initialPoolSize:
        default: 0
        description: Specifies the initial size for the number of database connections that will be created
          for the pool.
        maximum: 100000
        minimum: 0
        type: integer
      jwtProfileAudience:
        description: Specifies the expected audience for the JWT token. This value is used to validate
          the aud claim in the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileIssuer:
        description: Specifies the issuer of the JWT token. This value is used to validate the iss claim
          in the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileJwkUrl:
        description: Specifies the URL of the JSON Web Key (JWK) that is used to verify the signature
          of the JWT token.
        maxLength: 4000
        minLength: 0
        type: string
      jwtProfileRoleClaimName:
        description: Specifies the JSON pointer to the claim in the JWT token that contains the roles
          of the users.
        maxLength: 4000
        minLength: 0
        type: string
      maxPoolSize:
        default: 100
        description: Specifies the maximum number of database connections allowed for the pool.
        maximum: 100000
        minimum: 1
        type: integer
      minPoolSize:
        default: 0
        description: Specifies the minimum number of database connections allowed for the pool.
        maximum: 1000
        minimum: 0
        type: integer
      poolRouteValue:
        description: The pool route value provided in requests to target this pool.
        maxLength: 255
        minLength: 1
        type: string
      restEnabledSqlStatus:
        default: DISABLED
        description: Specifies whether the REST-Enabled SQL service is active.
        enum:
        - ENABLED
        - DISABLED
        type: string
      type:
        description: The type of the Database Tools database API gateway config pool sub resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigType'
    required:
    - type
    type: object
  UpdatePropertySetApexDocumentGeneratorDetails:
    allOf:
    - $ref: '#/definitions/UpdatePropertySetDetails'
    - discriminator: APEX_DOCUMENT_GENERATOR
      properties:
        autonomousDatabaseResourcePrincipalStatus:
          description: The status of the Autonomous Database Serverless Resource Principal (OCI$RESOURCE_PRINCIPAL)
          type: string
          x-obmcs-enumref: '#/definitions/PropertySetApexDocumentGenerator/autonomousDatabaseResourcePrincipalStatus'
        credentialKey:
          description: The name of the credential used by APEX to manage Object Storage Buckets and Objects
            as well as invoke the Document Generator function.
          type: string
        functionId:
          description: The [OCID](/Content/General/Concepts/identifiers.htm) of the Document Generator
            function
          maxLength: 255
          minLength: 0
          type: string
        invokeEndpoint:
          description: The base endpoint URL to use to invoke the Document Generator function
          type: string
        objectStorageBucketCompartmentId:
          description: The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment containing
            the Object Storage Buckets managed by APEX
          maxLength: 255
          minLength: 0
          type: string
        objectStorageEndpoint:
          description: Object Storage Endpoint
          type: string
        objectStorageNamespace:
          description: The Object Storage Namespace containing the Object Storage Buckets managed by APEX
          type: string
        printServerType:
          description: The print server type
          type: string
          x-obmcs-enumref: '#/definitions/PropertySetApexDocumentGenerator/printServerType'
    description: Contains the update details of an APEX document generator property set
  UpdatePropertySetApexFaIntegrationDetails:
    allOf:
    - $ref: '#/definitions/UpdatePropertySetDetails'
    - discriminator: APEX_FA_INTEGRATION
      properties:
        authenticationSubstitutions:
          additionalProperties:
            type: string
          description: APEX FA Integration key-value pairs.
          type: object
        instanceDbmsCredentialEnabled:
          description: 'Specifies whether database credentials can be used in all workspaces on the APEX
            instance. Supported values include: "Y", "N" and empty string.'
          type: string
    description: Contains the update details of an APEX FA Integration property set
  UpdatePropertySetDetails:
    description: The base definition for a property set update
    discriminator: key
    properties:
      key:
        description: The name of the property set
        enum: *id019
        type: string
        x-obmcs-top-level-enum: '#/definitions/PropertySetKey'
    required:
    - key
    type: object
  UpdatePropertySetOracleDatabaseExternalAuthenticationDetails:
    allOf:
    - $ref: '#/definitions/UpdatePropertySetDetails'
    - discriminator: ORACLE_DATABASE_EXTERNAL_AUTHENTICATION
      properties:
        identityProvider:
          $ref: '#/definitions/PropertySetOracleDatabaseExternalAuthenticationIdentityProvider'
      required:
      - identityProvider
    description: Contains the update details of an Oracle Database External Authentication property set
  UserCredential:
    description: Credential information
    properties:
      enabled:
        description: Indicates whether this credential is enabled (TRUE) or not (FALSE)
        type: string
      key:
        description: Name of the credential
        type: string
      keyType:
        description: Indicates whether this refers to a public synonym or not.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/CredentialKeyType'
      owner:
        description: Owner of the credential
        type: string
      relatedResource:
        $ref: '#/definitions/CredentialRelatedResource'
      userName:
        description: Name of the user that will be used to log in to the remote database or the remote
          or local operating system
        type: string
      windowsDomain:
        description: For a Windows target, the Windows domain to use when logging in
        type: string
    required:
    - key
    - userName
    - enabled
    - owner
    type: object
  UserCredentialCollection:
    description: List of `UserCredentialSummary` items.
    properties:
      items:
        description: Array of `UserCredentialSummary` items.
        items:
          $ref: '#/definitions/UserCredentialSummary'
        type: array
    required:
    - items
    type: object
  UserCredentialSummary:
    description: User Credential information
    properties:
      enabled:
        description: Indicates whether this credential is enabled (TRUE) or not (FALSE)
        type: string
      key:
        description: Name of the credential
        type: string
      keyType:
        description: Indicates whether this refers to a public synonym or not.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/CredentialKeyType'
      owner:
        description: Owner of the credential
        type: string
      relatedResource:
        $ref: '#/definitions/CredentialRelatedResource'
      userName:
        description: Name of the user that will be used to log in to the remote database or the remote
          or local operating system
        type: string
      windowsDomain:
        description: For a Windows target, the Windows domain to use when logging in
        type: string
    required:
    - key
    - userName
    - enabled
    - owner
    type: object
  ValidateDatabaseToolsConnectionDetails:
    description: Connection validation details.
    discriminator: type
    properties:
      type:
        description: The `DatabaseToolsConnection` type.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/ConnectionType'
    required:
    - type
    type: object
  ValidateDatabaseToolsConnectionMySqlDetails:
    allOf:
    - $ref: '#/definitions/ValidateDatabaseToolsConnectionDetails'
    - discriminator: MYSQL
    description: Connection validation details for the MySQL Server.
  ValidateDatabaseToolsConnectionMySqlResult:
    allOf:
    - $ref: '#/definitions/ValidateDatabaseToolsConnectionResult'
    - discriminator: MYSQL
      properties:
        databaseName:
          description: The database name.
          maxLength: 14
          minLength: 1
          type: string
        databaseVersion:
          description: The database version.
          maxLength: 255
          minLength: 1
          type: string
      required:
      - databaseVersion
    description: Connection validation result for the MySQL Server.
  ValidateDatabaseToolsConnectionOracleDatabaseDetails:
    allOf:
    - $ref: '#/definitions/ValidateDatabaseToolsConnectionDetails'
    - discriminator: ORACLE_DATABASE
    description: Connection validation details for the Oracle Database.
  ValidateDatabaseToolsConnectionOracleDatabaseResult:
    allOf:
    - $ref: '#/definitions/ValidateDatabaseToolsConnectionResult'
    - discriminator: ORACLE_DATABASE
      properties:
        authenticatedIdentity:
          description: Returns the identity used in authentication.
          maxLength: 255
          minLength: 1
          type: string
        authenticationMethod:
          description: Returns the method of authentication.
          maxLength: 255
          minLength: 1
          type: string
        cloudService:
          description: Displays the CLOUD_SERVICE value within the USERENV context
          maxLength: 255
          minLength: 1
          type: string
        currentSchema:
          description: The name of the currently active default schema.
          maxLength: 255
          minLength: 1
          type: string
        databaseName:
          description: The database name.
          maxLength: 14
          minLength: 1
          type: string
        databaseToolsIdentityStatuses:
          description: Status on whether an identity type can be used with this connection or not.
          items:
            $ref: '#/definitions/ValidationIdentityStatus'
          maxItems: 1
          minItems: 1
          type: array
        databaseVersion:
          description: The database version.
          maxLength: 255
          minLength: 1
          type: string
        dbmsCloudStatus:
          description: The DBMS_CLOUD package installation status.
          enum: *id021
          type: string
          x-obmcs-top-level-enum: '#/definitions/DbmsCloudStatus'
        enterpriseIdentity:
          description: Displays the enterprise identity value within the USERENV context
          maxLength: 255
          minLength: 1
          type: string
        externalAuthenticationStatuses:
          description: Status on whether an external authentication can be used with this connection or
            not.
          items:
            $ref: '#/definitions/ValidationExternalAuthenticationStatus'
          maxItems: 2
          minItems: 2
          type: array
        proxyEnterpriseIdentity:
          description: Displays the proxy enterprise identity value within the USERENV context
          maxLength: 255
          minLength: 1
          type: string
        proxyUser:
          description: Name of the database user who opened the current session on behalf of sessionUser.
          maxLength: 255
          minLength: 1
          type: string
        sessionUser:
          description: The name of the session user (the user who logged on).
          maxLength: 255
          minLength: 1
          type: string
      required:
      - databaseVersion
      - dbmsCloudStatus
    description: Connection validation result for the Oracle Database.
  ValidateDatabaseToolsConnectionPostgresqlDetails:
    allOf:
    - $ref: '#/definitions/ValidateDatabaseToolsConnectionDetails'
    - discriminator: POSTGRESQL
    description: Connection validation details for the Postgresql Server.
  ValidateDatabaseToolsConnectionPostgresqlResult:
    allOf:
    - $ref: '#/definitions/ValidateDatabaseToolsConnectionResult'
    - discriminator: POSTGRESQL
      properties:
        databaseName:
          description: The database name.
          maxLength: 14
          minLength: 1
          type: string
        databaseVersion:
          description: The database version.
          maxLength: 255
          minLength: 1
          type: string
      required:
      - databaseVersion
    description: Connection validation result for the PostgreSQL Server.
  ValidateDatabaseToolsConnectionResult:
    description: Connection validation result.
    discriminator: type
    properties:
      action:
        description: A human-readable message that suggests a remedial action to resolve the validation
          error.
        type: string
      cause:
        description: A human-readable message that describes possible causes for the validation error.
        type: string
      code:
        description: A short code that defines the result of the validation, meant for programmatic parsing.
          The value OK indicates that the validation was successful.
        type: string
      message:
        description: A human-readable message that describes the result of the validation.
        type: string
      type:
        description: The Database Tools connection type.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/ConnectionType'
    required:
    - type
    - code
    - message
    type: object
  ValidateDatabaseToolsIdentityCredentialDetails:
    description: Identity Credential validation details.
    discriminator: type
    properties:
      type:
        description: The Database Tools identity type.
        enum: *id018
        type: string
        x-obmcs-top-level-enum: '#/definitions/IdentityType'
    required:
    - type
    type: object
  ValidateDatabaseToolsIdentityCredentialOracleDatabaseResourcePrincipalDetails:
    allOf:
    - $ref: '#/definitions/ValidateDatabaseToolsIdentityCredentialDetails'
    - discriminator: ORACLE_DATABASE_RESOURCE_PRINCIPAL
    description: The Database Tools identity validate credential details for the Oracle Database resource
      principal identity type.
  ValidateDatabaseToolsIdentityCredentialOracleDatabaseResourcePrincipalResult:
    allOf:
    - $ref: '#/definitions/ValidateDatabaseToolsIdentityCredentialResult'
    - discriminator: ORACLE_DATABASE_RESOURCE_PRINCIPAL
    description: The Database Tools identity validate credentials result for the Oracle Database resource
      principal identity type.
  ValidateDatabaseToolsIdentityCredentialResult:
    description: Identity validation result.
    discriminator: type
    properties:
      action:
        description: A human-readable message that suggests a remedial action to resolve the validation
          error.
        type: string
      cause:
        description: A human-readable message that describes possible causes for the validation error.
        type: string
      code:
        description: A short code that defines the result of the validation, meant for programmatic parsing.
          The value OK indicates that the validation was successful.
        type: string
      message:
        description: A human-readable message that describes the result of the validation.
        type: string
      type:
        description: The Database Tools identity type.
        enum: *id018
        type: string
        x-obmcs-top-level-enum: '#/definitions/IdentityType'
    required:
    - type
    - code
    - message
    type: object
  ValidationExternalAuthenticationStatus:
    description: Status details for an external authentication
    properties:
      status:
        description: 'The status of the external authentication. AVAILABLE if the external authentication
          can be used with this connection. UNAVAILABLE if not.

          ENABLED if the external authentication is enabled.

          '
        enum: *id022
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseToolsExternalAuthenticationStatus'
      statusDetails:
        description: If the status is UNAVAILABLE this displays the cause.
        type: string
      type:
        description: The Database Tools identity provider type.
        enum: *id020
        type: string
        x-obmcs-top-level-enum: '#/definitions/IdentityProviderType'
    type: object
  ValidationIdentityStatus:
    description: Status details for an identity type
    properties:
      status:
        description: The status of the identity. AVAILABLE if the identity type can be used with this
          connection. UNAVAILABLE if not.
        enum: *id023
        type: string
        x-obmcs-top-level-enum: '#/definitions/DatabaseToolsIdentityStatus'
      statusDetails:
        description: If the status is UNAVAILABLE this displays the cause.
        type: string
      type:
        description: The Database Tools identity type.
        enum: *id018
        type: string
        x-obmcs-top-level-enum: '#/definitions/IdentityType'
    type: object
  WorkRequest:
    description: An asynchronous work request.
    properties:
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment that
          contains the work request.
        maxLength: 255
        minLength: 1
        type: string
      createdByPrincipalDomainId:
        description: Identity domain OCID for the user that submitted the asynchronous request.
        maxLength: 255
        minLength: 1
        type: string
      createdByPrincipalId:
        description: Principal id of the user or resource that submitted the asynchrounous request.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: "A user-friendly name for the operation. \nIf provided for an asynchronous operation,\
          \ this value will be propagated to the displayName property of the resulting Work Request, allowing\
          \ for easier identification and tracking of the operation's progress and outcome. This name\
          \ is purely for descriptive purposes and does not affect the functionality of the operation\
          \ itself.\n"
        maxLength: 255
        minLength: 1
        type: string
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
        maxLength: 255
        minLength: 1
        type: string
      operationType:
        description: The asynchronous operation tracked by this work request.
        enum: *id024
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: Percentage of the request completed.
        format: float
        type: number
      resources:
        description: The resources affected by this work request.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: The status of the work request.
        enum: *id025
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: The date and time the work request was created, in the format defined by RFC3339.
        format: date-time
        type: string
      timeFinished:
        description: 'The date and time the work request reached a terminal state, either `FAILED` or
          `SUCCEEDED`.

          Format is defined by RFC3339.

          '
        format: date-time
        type: string
      timeScheduled:
        description: 'The date and time the work request is eligible to be launched,

          in the format defined by RFC3339.

          '
        format: date-time
        type: string
      timeStarted:
        description: 'The date and time the work request transitioned from `ACCEPTED` to `IN_PROGRESS`,

          in the format defined by RFC3339.

          '
        format: date-time
        type: string
      timeUpdated:
        description: 'The date and time the work request was last updated,

          in the format defined by RFC3339.

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
  WorkRequestCollection:
    description: List of `WorkRequestSummary` items.
    properties:
      items:
        description: Array of `WorkRequestSummary` items.
        items:
          $ref: '#/definitions/WorkRequestSummary'
        type: array
    required:
    - items
    type: object
  WorkRequestError:
    description: An error encountered while executing a work request.
    properties:
      code:
        description: 'A machine-usable code for the error that occured. Error codes are listed on

          (https://docs.cloud.oracle.com/Content/API/References/apierrors.htm)

          '
        type: string
      message:
        description: A human readable description of the issue encountered.
        type: string
      timestamp:
        description: The time the error occured. An RFC3339 formatted datetime string.
        format: date-time
        type: string
    required:
    - code
    - message
    - timestamp
  WorkRequestErrorCollection:
    description: List of WorkRequestError items.
    properties:
      items:
        description: Array of Work Request Error items.
        items:
          $ref: '#/definitions/WorkRequestError'
        type: array
    required:
    - items
    type: object
  WorkRequestLogEntry:
    description: A log message from the execution of a work request.
    properties:
      message:
        description: Human-readable log message.
        type: string
      timestamp:
        description: The time the log message was written. An RFC3339 formatted datetime string.
        format: date-time
        type: string
    required:
    - message
    - timestamp
  WorkRequestLogEntryCollection:
    description: List of work request log items.
    properties:
      items:
        description: Array of work request log items.
        items:
          $ref: '#/definitions/WorkRequestLogEntry'
        type: array
    required:
    - items
    type: object
  WorkRequestResource:
    description: A resource created or operated on by a work request.
    properties:
      actionType:
        description: 'The way in which this resource is affected by the work tracked in the work request.

          A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

          work is complete for that resource at which point it will transition to CREATED, UPDATED,

          or DELETED, respectively.

          '
        enum: *id026
        type: string
        x-obmcs-top-level-enum: '#/definitions/ActionType'
      entityType:
        description: The resource type the work request affects.
        type: string
      entityUri:
        description: The URI path that the user can use for a GET operation to access the resource metadata.
        type: string
      identifier:
        description: The identifier of the resource the work request affects.
        type: string
      metadata:
        additionalProperties:
          description: 'Key-value pair representing Object Storage namespace, bucketName, and/or objectName.

            Example: `{"bucketName": "myBucket", "namespace": "myNamespace", "objectName": "myObject"}`

            '
          type: string
        description: Additional information explaining the resource.
        type: object
    required:
    - actionType
    - entityType
    - identifier
  WorkRequestSummary:
    description: A summary of the status of a work request.
    properties:
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment that
          contains the work request.
        maxLength: 255
        minLength: 1
        type: string
      createdByPrincipalDomainId:
        description: Identity domain OCID for the user that submitted the asynchronous request.
        maxLength: 255
        minLength: 1
        type: string
      createdByPrincipalId:
        description: Principal id of the user or resource that submitted the asynchrounous request.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: 'A user-friendly name for the operation.

          If provided for an asynchronous operation, this value will be propagated to the displayName
          property of the resulting Work Request, allowing for easier identification and tracking of the
          operation''s progress and outcome. This name is purely for descriptive purposes and does not
          affect the functionality of the operation itself.

          '
        maxLength: 255
        minLength: 1
        type: string
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
        maxLength: 255
        minLength: 1
        type: string
      operationType:
        description: The asynchronous operation tracked by this work request.
        enum: *id024
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: Percentage of the request completed.
        format: float
        type: number
      resources:
        description: The resources affected by this work request.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: The status of the work request.
        enum: *id025
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: The date and time the work request was created, in the format defined by RFC3339.
        format: date-time
        type: string
      timeFinished:
        description: 'The date and time the work request reached a terminal state, either `FAILED` or
          `SUCCEEDED`.

          Format is defined by RFC3339.

          '
        format: date-time
        type: string
      timeScheduled:
        description: 'The date and time the work request is eligible to be launched,

          in the format defined by RFC3339.

          '
        format: date-time
        type: string
      timeStarted:
        description: 'The date and time the work request transitioned from `ACCEPTED` to `IN_PROGRESS`,

          in the format defined by RFC3339.

          '
        format: date-time
        type: string
      timeUpdated:
        description: 'The date and time the work request was last updated,

          in the format defined by RFC3339.

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - percentComplete
    - timeAccepted
parameters:
  CompartmentIdQueryParam:
    description: The ID of the compartment in which to list resources.
    in: query
    name: compartmentId
    required: true
    type: string
  CreatedByPrincipalIdQueryParam:
    description: A filter to return only work requests initiated by the specified principal OCID.
    in: query
    name: createdByPrincipalId
    type: string
  CredentialExecuteGranteeKeyPathParam:
    description: The name of the user granted the EXECUTE privilege on the credential.
    in: path
    name: executeGranteeKey
    required: true
    type: string
  CredentialKeyPathParam:
    description: The name of the credential
    in: path
    name: credentialKey
    required: true
    type: string
  DatabaseApiGatewayConfigAdvancedPropertyConfigTypeQueryParam:
    collectionFormat: multi
    description: A filter to return only advanced properties with one of the specified config type values.
    in: query
    items:
      enum: *id014
      type: string
      x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigAdvancedPropertyConfigType'
    name: configType
    required: false
    type: array
    x-default-description: 'null'
  DatabaseToolsConnectionIdPathParam:
    description: The [OCID](/Content/General/Concepts/identifiers.htm) of a Database Tools connection.
    in: path
    name: databaseToolsConnectionId
    required: true
    type: string
    x-obmcs-cli-param: connection-id
  DatabaseToolsDatabaseApiGatewayConfigApiSpecKeyPathParam:
    description: The key of the API spec config.
    in: path
    name: apiSpecKey
    required: true
    type: string
  DatabaseToolsDatabaseApiGatewayConfigAutoApiSpecKeyPathParam:
    description: The key of the auto API spec config.
    in: path
    name: autoApiSpecKey
    required: true
    type: string
  DatabaseToolsDatabaseApiGatewayConfigGlobalKeyPathParam:
    description: The key of the global config.
    enum: *id027
    in: path
    name: globalKey
    required: true
    type: string
    x-obmcs-top-level-enum: '#/definitions/DatabaseApiGatewayConfigGlobalKey'
  DatabaseToolsDatabaseApiGatewayConfigIdPathParam:
    description: The [OCID](/Content/General/Concepts/identifiers.htm) of a Database Tools database API
      gateway config.
    in: path
    name: databaseToolsDatabaseApiGatewayConfigId
    required: true
    type: string
    x-obmcs-cli-param: database-api-gateway-config-id
  DatabaseToolsDatabaseApiGatewayConfigPoolKeyPathParam:
    description: The key of the pool config.
    in: path
    name: poolKey
    required: true
    type: string
  DatabaseToolsIdentityIdPathParam:
    description: The [OCID](/Content/General/Concepts/identifiers.htm) of a Database Tools identity.
    in: path
    name: databaseToolsIdentityId
    required: true
    type: string
    x-obmcs-cli-param: identity-id
  DisplayNameQueryParam:
    description: A filter to return only resources that match the entire specified display name.
    in: query
    maxLength: 255
    minLength: 1
    name: displayName
    type: string
    x-default-description: 'null'
  IfMatchHeader:
    description: "If-Match is most often used with state-changing methods (e.g., POST, PUT, DELETE) to\
      \ prevent \naccidental overwrites when multiple user agentss might be acting in parallel on the\
      \ same \nresource (i.e., to prevent the \"lost update\" problem). In general, it can be used with\
      \ any \nmethod that involves the selection or modification of a representation to abort the request\
      \ \nif the selected representation's current entity tag is not a member within the If-Match field\
      \ value.  \nWhen specified on an action-specific subresource, the ETag value of the resource on\
      \ which the \naction is requested should be provided.\n"
    in: header
    name: if-match
    required: false
    type: string
  IfNoneMatchHeader:
    description: 'For conditional requests. In the GET call for a resource, set the

      `If-None-Match` header to the value of the ETag from a previous GET (or

      POST or PUT) response for that resource. The server will return with

      either a 304 Not Modified response if the resource has not changed, or a

      200 OK response with the updated representation.

      '
    in: header
    name: if-none-match
    required: false
    type: string
    x-default-description: 'null'
  OperationTypeQueryParam:
    collectionFormat: multi
    description: The work request operation type.
    in: query
    items:
      enum: *id024
      type: string
      x-obmcs-top-level-enum: '#/definitions/OperationType'
    name: operationType
    required: false
    type: array
    x-default-description: 'null'
  PaginationLimitQueryParam:
    default: 10
    description: The maximum number of items to return.
    in: query
    maximum: 1000
    minimum: 1
    name: limit
    type: integer
  PaginationTokenQueryParam:
    description: The page token representing the page at which to start retrieving results. This is usually
      retrieved from a previous list call.
    in: query
    minLength: 1
    name: page
    type: string
  PropertySetKeyPathParam:
    description: The name of the property set
    enum: *id019
    in: path
    name: propertySetKey
    required: true
    type: string
    x-obmcs-top-level-enum: '#/definitions/PropertySetKey'
  PublicSynonymKeyPathParam:
    description: The name of the public synonym for the credential
    in: path
    name: publicSynonymKey
    required: true
    type: string
  RequestIdHeader:
    description: The client request ID for tracing.
    in: header
    name: opc-request-id
    type: string
  ResourceIdentifierQueryParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the resource.
    in: query
    maxLength: 255
    minLength: 1
    name: resourceIdentifier
    type: string
    x-default-description: 'null'
  RetryTokenHeader:
    description: 'A token that uniquely identifies a request so it can be retried in case of a timeout
      or

      server error without risk of executing that same action again. Retry tokens expire after 24

      hours, but can be invalidated before then due to conflicting operations. For example, if a resource

      has been deleted and purged from the system, then a retry of the original creation request

      might be rejected.


      Accepted characters: ASCII alphanumerics plus underscore (U+005F LOW LINE "_") and dash (U+002D
      HYPHEN-MINUS "-")

      '
    in: header
    maxLength: 64
    minLength: 1
    name: opc-retry-token
    pattern: ^[a-zA-Z0-9_\-]{1,64}$
    required: false
    type: string
  SortByQueryParam:
    default: timeCreated
    description: 'The field to sort by. Only one sort order may be provided. Default order for timeCreated
      is descending. Default order for displayName is ascending. If no value is specified timeCreated
      is default.

      '
    enum:
    - timeCreated
    - displayName
    in: query
    name: sortBy
    type: string
  SortOrderQueryParam:
    default: ASC
    description: The sort order to use, either 'asc' or 'desc'.
    enum: *id028
    in: query
    name: sortOrder
    type: string
    x-obmcs-top-level-enum: '#/definitions/SortOrder'
  UserKeyPathParam:
    description: The name of the database user.
    in: path
    name: userKey
    required: true
    type: string
  WorkRequestIdPathParam:
    description: The ID of the asynchronous request.
    in: path
    name: workRequestId
    required: true
    type: string
  WorkRequestSortByQueryParam:
    default: timeAccepted
    description: 'The field to sort by. Only one sort order may be provided. Default order for timeAccepted
      is descending. If no value is specified timeAccepted is default.

      '
    enum:
    - timeAccepted
    - timeUpdated
    - timeFinished
    in: query
    name: sortBy
    type: string
  WorkRequestStatusQueryParam:
    collectionFormat: multi
    description: A filter to return only work requests with one of the specified status values.
    in: query
    items:
      enum: *id025
      type: string
      x-obmcs-top-level-enum: '#/definitions/OperationStatus'
    name: status
    required: false
    type: array
    x-default-description: 'null'
responses:
  400:
    description: Bad Request
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  401:
    description: Unauthorized
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  404:
    description: Not Found
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  405:
    description: Method not allowed
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  409:
    description: Conflict
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  412:
    description: Precondition failed
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  429:
    description: Too Many Requests
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  500:
    description: Internal Server Error
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  503:
    description: Service Unavailable
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  default:
    description: Unknown Error
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
x-anchors:
  x-headers:
    etag: *id005
    location: *id004
    opc-next-page: *id003
    opc-request-id: *id001
    opc-work-request-id: *id002
    retry-after: *id029
  x-properties:
    definedTags: *id011
    freeformTags: *id012
    systemTags: *id013
x-obmcs-client-retries-enabled: true
x-oracle-package: com.oracle.pic.dbtools.runtime
