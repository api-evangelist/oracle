swagger: '2.0'
info:
  description: Use the Organizations API to consolidate multiple OCI tenancies into an organization, and
    centrally manage your tenancies and organization resources. For more information, see [Organization
    Management Overview](/iaas/Content/General/Concepts/organization_management_overview.htm).
  title: Organizations API
  version: '20200801'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/a57a0f220d4c7c60a7e03892a88d732aec5aa8fc26de7188f6dc188ce1b13f06.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the Organizations API OCI service and stored verbatim;
      API Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/a57a0f220d4c7c60a7e03892a88d732aec5aa8fc26de7188f6dc188ce1b13f06.yaml
    what: the harvested document for Organizations API
host: 127.0.0.1
basePath: /20200801
schemes:
- http
- https
produces:
- application/json
paths:
  /assignedSubscriptions:
    get:
      deprecated: true
      description: Lists subscriptions that are consumed by the compartment. Only the root compartment
        is allowed.
      operationId: ListAssignedSubscriptions
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/SubscriptionIdOptionalQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      produces:
      - application/json
      responses:
        200:
          description: Retrieved the list of subscriptions.
          headers:
            opc-next-page: &id002
              description: 'For pagination of a list of items. When paging through a list, if this header
                appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter
                for the

                subsequent GET request to get the next batch of items.

                '
              type: string
            opc-request-id: &id001
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/AssignedSubscriptionCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists subscriptions that are consumed by the compartment. Only root compartment is allowed.
      tags:
      - subscription
      x-related-resource: '#/definitions/AssignedSubscription'
  /assignedSubscriptions/{assignedSubscriptionId}:
    get:
      deprecated: true
      description: Get the assigned subscription details by assigned subscription ID.
      operationId: GetAssignedSubscription
      parameters:
      - description: OCID of the assigned subscription.
        in: path
        maxLength: 255
        name: assignedSubscriptionId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/CompartmentIdOptionalQueryParam'
      produces:
      - application/json
      responses:
        200:
          description: Assigned subscription information retrieved successfully.
          headers:
            etag: &id003
              description: 'For optimistic concurrency control. See `if-match`.

                '
              type: string
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/AssignedSubscription'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get assigned subscription details by assigned subscription ID.
      tags:
      - subscription
      x-related-resource: '#/definitions/AssignedSubscription'
  /childTenancies:
    post:
      consumes:
      - application/json
      deprecated: true
      description: Creates a child tenancy asynchronously.
      operationId: CreateChildTenancy
      parameters:
      - description: Parameters to create a child tenancy.
        in: body
        name: CreateChildTenancyDetails
        required: true
        schema:
          $ref: '#/definitions/CreateChildTenancyDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Child tenancy creation accepted.
          headers:
            opc-request-id: *id001
            opc-work-request-id: &id004
              description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use
                this to query the status of the asynchronous operation.

                '
              type: string
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a child tenancy asynchronously.
      tags:
      - organization
  /domainGovernances:
    get:
      deprecated: true
      description: 'Return a (paginated) list of domain governance entities.

        '
      operationId: ListDomainGovernances
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/DomainIdQueryParam'
      - $ref: '#/parameters/DomainGovernanceIdQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/NameQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of domain governance entities.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DomainGovernanceCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists domain governance entities
      tags:
      - domainGovernance
      x-related-resource: '#/definitions/DomainGovernance'
    post:
      consumes:
      - application/json
      deprecated: true
      description: Adds domain governance to a claimed domain.
      operationId: CreateDomainGovernance
      parameters:
      - description: Parameters for adding domain governance to a claimed domain.
        in: body
        name: CreateDomainGovernanceDetails
        required: true
        schema:
          $ref: '#/definitions/CreateDomainGovernanceDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Domain governance entity created.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DomainGovernance'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Adds domain governance to a claimed domain.
      tags:
      - domainGovernance
  /domainGovernances/{domainGovernanceId}:
    delete:
      deprecated: true
      description: Removes domain governance from a claimed domain.
      operationId: DeleteDomainGovernance
      parameters:
      - $ref: '#/parameters/DomainGovernanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        204:
          description: Domain governance removed from domain.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Removes domain governance from a claimed domain.
      tags:
      - domainGovernance
      x-related-resource: '#/definitions/DomainGovernance'
    get:
      deprecated: true
      description: Gets information about the domain governance entity.
      operationId: GetDomainGovernance
      parameters:
      - $ref: '#/parameters/DomainGovernanceIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Domain governance entity retrieved.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DomainGovernance'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets information about the domain governance entity.
      tags:
      - domainGovernance
    put:
      deprecated: true
      description: Updates the domain governance entity.
      operationId: UpdateDomainGovernance
      parameters:
      - $ref: '#/parameters/DomainGovernanceIdPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateDomainGovernanceDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateDomainGovernanceDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Domain governance entity updated.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DomainGovernance'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the domain governance entity identified by the ID.
      tags:
      - domainGovernance
      x-related-resource: '#/definitions/DomainGovernance'
  /domains:
    get:
      deprecated: true
      description: 'Return a (paginated) list of domains.

        '
      operationId: ListDomains
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/DomainIdQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/DomainStatusQueryParam'
      - $ref: '#/parameters/NameQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of domains.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/DomainCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists domains
      tags:
      - domain
      x-related-resource: '#/definitions/Domain'
    post:
      consumes:
      - application/json
      deprecated: true
      description: Begins the registration process for claiming a domain.
      operationId: CreateDomain
      parameters:
      - description: Parameters for domain creation.
        in: body
        name: CreateDomainDetails
        required: true
        schema:
          $ref: '#/definitions/CreateDomainDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        201:
          description: Domain creation accepted.
          headers:
            etag: *id003
            opc-request-id: *id001
            opc-work-request-id: *id004
          schema:
            $ref: '#/definitions/Domain'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Begins the registration process for claiming a domain.
      tags:
      - domain
  /domains/{domainId}:
    delete:
      deprecated: true
      description: Releases the domain, making it available to be claimed again by another tenancy.
      operationId: DeleteDomain
      parameters:
      - $ref: '#/parameters/DomainIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Domain release process started.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Releases the domain, making it available to be claimed again by another tenancy.
      tags:
      - domain
      x-related-resource: '#/definitions/Domain'
    get:
      deprecated: true
      description: Gets information about the domain.
      operationId: GetDomain
      parameters:
      - $ref: '#/parameters/DomainIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Domain retrieved.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Domain'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets information about the domain.
      tags:
      - domain
    put:
      deprecated: true
      description: Updates the domain.
      operationId: UpdateDomain
      parameters:
      - $ref: '#/parameters/DomainIdPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateDomainDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateDomainDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Domain updated.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Domain'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the domain identified by the ID.
      tags:
      - domain
      x-related-resource: '#/definitions/Domain'
  /links:
    get:
      deprecated: true
      description: Return a (paginated) list of links.
      operationId: ListLinks
      parameters:
      - $ref: '#/parameters/ParentTenancyIdQueryParam'
      - $ref: '#/parameters/ChildTenancyIdQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of links.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/LinkCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists links
      tags:
      - link
      x-related-resource: '#/definitions/Link'
  /links/{linkId}:
    delete:
      deprecated: true
      description: Starts the link termination workflow.
      operationId: DeleteLink
      parameters:
      - description: OCID of the link to terminate.
        in: path
        maxLength: 255
        name: linkId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Link termination workflow started.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Starts the link termination workflow.
      tags:
      - link
      x-related-resource: '#/definitions/Link'
    get:
      deprecated: true
      description: Gets information about the link.
      operationId: GetLink
      parameters:
      - description: OCID of the link to retrieve.
        in: path
        maxLength: 255
        name: linkId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Link retrieved.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Link'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets information about the link.
      tags:
      - link
  /links/{linkId}/tenancyNames:
    get:
      description: Gets information about the link along with the parent and child tenancy names.
      operationId: GetLinkWithTenancyNames
      parameters:
      - description: OCID of the link to retrieve.
        in: path
        maxLength: 255
        name: linkId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Link with tenancy names retrieved.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/LinkWithTenancyNames'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets information about the link along with tenancy names.
      tags:
      - link
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/LinkWithTenancyNames'
  /orders/{activationToken}:
    get:
      deprecated: true
      description: 'Returns the order details given by the order ID in the JWT.

        '
      operationId: GetOrder
      parameters:
      - $ref: '#/parameters/OrderActivationToken'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: OrderActivationToken is valid and the requested subscription information has been
            found and was returned.
          headers:
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Order'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns order details with subscription information.
      tags:
      - orders
      x-obmcs-authz-declarations:
        authorizationRequired: false
      x-related-resource: '#/definitions/Order'
  /orders/{activationToken}/actions/activate:
    post:
      deprecated: true
      description: Triggers an order activation workflow on behalf of the tenant, given by compartment
        ID in the body.
      operationId: ActivateOrder
      parameters:
      - description: The information needed to activate an order in a tenancy.
        in: body
        name: ActivateOrderDetails
        required: true
        schema:
          $ref: '#/definitions/ActivateOrderDetails'
      - $ref: '#/parameters/OrderActivationToken'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: Order activation accepted and activation has begun.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Order activation workflow creation.
      tags:
      - orders
      x-related-resource: '#/definitions/Order'
  /organizationTenancies/{organizationTenancyId}:
    delete:
      consumes:
      - application/json
      deprecated: true
      description: If certain validations are successful, initiate tenancy termination.
      operationId: DeleteOrganizationTenancy
      parameters:
      - description: OCID of the tenancy to be terminated.
        in: path
        maxLength: 255
        name: organizationTenancyId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      produces:
      - application/json
      responses:
        202:
          description: Tenancy termination initiated.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Initiate tenancy termination.
      tags:
      - organization
      x-related-resource: '#/definitions/OrganizationTenancy'
  /organizationTenancies/{organizationTenancyId}/actions/approveForTransfer:
    post:
      consumes:
      - application/json
      deprecated: true
      description: Approve an organization's child tenancy for transfer.
      operationId: ApproveOrganizationTenancyForTransfer
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - description: OCID of the child tenancy.
        in: path
        maxLength: 255
        name: organizationTenancyId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Organization's child tenancy was successfully approved for transfer.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OrganizationTenancy'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Approve an organization's child tenancy for transfer.
      tags:
      - organization
      x-related-resource: '#/definitions/OrganizationTenancy'
  /organizationTenancies/{organizationTenancyId}/actions/restore:
    post:
      consumes:
      - application/json
      deprecated: true
      description: An asynchronous API to restore a tenancy.
      operationId: RestoreOrganizationTenancy
      parameters:
      - description: OCID of the tenancy to be restored.
        in: path
        maxLength: 255
        name: organizationTenancyId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Request to restore the tenancy is accepted.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Restore tenancy.
      tags:
      - organization
      x-related-resource: '#/definitions/OrganizationTenancy'
  /organizationTenancies/{organizationTenancyId}/actions/unapproveForTransfer:
    post:
      consumes:
      - application/json
      deprecated: true
      description: Cancel an organization's child tenancy for transfer.
      operationId: UnapproveOrganizationTenancyForTransfer
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - description: OCID of the child tenancy.
        in: path
        maxLength: 255
        name: organizationTenancyId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Organization's child tenancy was successfully canceled for transfer.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OrganizationTenancy'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Cancel an organization's child tenancy for transfer.
      tags:
      - organization
      x-related-resource: '#/definitions/OrganizationTenancy'
  /organizations:
    get:
      deprecated: true
      description: Lists organizations associated with the caller.
      operationId: ListOrganizations
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      responses:
        200:
          description: The operation succeeded.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OrganizationCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists organizations associated with the caller.
      tags:
      - organization
      x-related-resource: '#/definitions/Organization'
  /organizations/{organizationId}:
    get:
      deprecated: true
      description: Gets information about the organization.
      operationId: GetOrganization
      parameters:
      - description: OCID of the organization to retrieve.
        in: path
        maxLength: 255
        name: organizationId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: The operation succeeded.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Organization'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets information about the organization.
      tags:
      - organization
      x-related-resource: '#/definitions/Organization'
    put:
      deprecated: true
      description: Map the default subscription to the organization.
      operationId: UpdateOrganization
      parameters:
      - description: OCID of the organization.
        in: path
        maxLength: 255
        name: organizationId
        required: true
        type: string
      - description: The information to be updated.
        in: body
        name: UpdateOrganizationDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateOrganizationDetails'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      produces:
      - application/json
      responses:
        202:
          description: Default subscription mapping to the organization accepted.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Assign the subscription to the organization.
      tags:
      - organization
      x-related-resource: '#/definitions/Organization'
  /organizations/{organizationId}/tenancies:
    get:
      deprecated: true
      description: Gets a list of tenancies in the organization.
      operationId: ListOrganizationTenancies
      parameters:
      - description: OCID of the organization.
        in: path
        maxLength: 255
        name: organizationId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      produces:
      - application/json
      responses:
        200:
          description: OrganizationTenancyCollection successfully retrieved.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OrganizationTenancyCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of tenancies in the organization.
      tags:
      - organization
      x-related-resource: '#/definitions/Organization'
  /organizations/{organizationId}/tenancies/{organizationTenancyId}/actions/addGovernance:
    post:
      consumes:
      - application/json
      deprecated: true
      description: Starts a work request to opt the tenancy in to governance rules.
      operationId: AddGovernance
      parameters:
      - description: OCID of the organization.
        in: path
        maxLength: 255
        name: organizationId
        required: true
        type: string
      - description: OCID of tenancy that is opting in to governance rules.
        in: path
        maxLength: 255
        name: organizationTenancyId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Work request for opting the tenancy in to governance rule initiation.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Starts a work request to opt the tenancy in to governance rules.
      tags:
      - governance
      x-related-resource: '#/definitions/OrganizationTenancy'
  /organizations/{organizationId}/tenancies/{organizationTenancyId}/actions/removeGovernance:
    post:
      consumes:
      - application/json
      deprecated: true
      description: Starts a work request to opt the tenancy out of governance rules.
      operationId: RemoveGovernance
      parameters:
      - description: OCID of the organization.
        in: path
        maxLength: 255
        name: organizationId
        required: true
        type: string
      - description: OCID of tenancy that is opting out of governance rules.
        in: path
        maxLength: 255
        name: organizationTenancyId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Work request for opting the tenancy out of governance rules initiation.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Starts a work request to opt the tenancy out of governance rules.
      tags:
      - governance
      x-related-resource: '#/definitions/OrganizationTenancy'
  /organizations/{organizationId}/tenancies/{tenancyId}:
    get:
      deprecated: true
      description: Gets information about the organization's tenancy.
      operationId: GetOrganizationTenancy
      parameters:
      - description: OCID of the organization.
        in: path
        maxLength: 255
        name: organizationId
        required: true
        type: string
      - description: OCID of the tenancy to retrieve.
        in: path
        maxLength: 255
        name: tenancyId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: OrganizationTenancy successfully retrieved.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OrganizationTenancy'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets information about the organization's tenancy.
      tags:
      - organization
      x-related-resource: '#/definitions/OrganizationTenancy'
  /recipientInvitations:
    get:
      deprecated: true
      description: 'Return a (paginated) list of recipient invitations.

        '
      operationId: ListRecipientInvitations
      parameters:
      - $ref: '#/parameters/SenderTenancyIdQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/RecipientInvitationStatusQueryParam'
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      responses:
        200:
          description: List of recipient invitations.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RecipientInvitationCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists recipient invitations
      tags:
      - recipientInvitation
      x-related-resource: '#/definitions/RecipientInvitation'
  /recipientInvitations/{recipientInvitationId}:
    get:
      deprecated: true
      description: Gets information about the recipient invitation.
      operationId: GetRecipientInvitation
      parameters:
      - description: OCID of the recipient invitation to retrieve.
        in: path
        maxLength: 255
        name: recipientInvitationId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Recipient invitation retrieved.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RecipientInvitation'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets information about the recipient invitation.
      tags:
      - recipientInvitation
    put:
      deprecated: true
      description: Updates the recipient invitation.
      operationId: UpdateRecipientInvitation
      parameters:
      - description: OCID of the recipient invitation to update.
        in: path
        maxLength: 255
        name: recipientInvitationId
        required: true
        type: string
      - description: The information to be updated.
        in: body
        name: UpdateRecipientInvitationDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateRecipientInvitationDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Recipient invitation updated.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RecipientInvitation'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the RecipientInvitation identified by the id.
      tags:
      - recipientInvitation
      x-related-resource: '#/definitions/RecipientInvitation'
  /recipientInvitations/{recipientInvitationId}/actions/accept:
    post:
      consumes:
      - application/json
      deprecated: true
      description: Accepts a recipient invitation.
      operationId: AcceptRecipientInvitation
      parameters:
      - description: OCID of recipient invitation to accept.
        in: path
        maxLength: 255
        name: recipientInvitationId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Recipient invitation accepted.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Accepts a recipient invitation.
      tags:
      - recipientInvitation
      x-related-resource: '#/definitions/RecipientInvitation'
  /recipientInvitations/{recipientInvitationId}/actions/ignore:
    post:
      consumes:
      - application/json
      deprecated: true
      description: Ignores a recipient invitation.
      operationId: IgnoreRecipientInvitation
      parameters:
      - description: OCID of recipient invitation to ignore.
        in: path
        maxLength: 255
        name: recipientInvitationId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Recipient invitation ignored.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RecipientInvitation'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Ignores a recipient invitation.
      tags:
      - recipientInvitation
      x-related-resource: '#/definitions/RecipientInvitation'
  /senderInvitations:
    get:
      deprecated: true
      description: 'Return a (paginated) list of sender invitations.

        '
      operationId: ListSenderInvitations
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/RecipientTenancyIdQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/SenderInvitationStatusQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of sender invitations.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/SenderInvitationCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists sender invitations
      tags:
      - senderInvitation
      x-related-resource: '#/definitions/SenderInvitation'
    post:
      consumes:
      - application/json
      deprecated: true
      description: Creates a sender invitation and asynchronously sends the invitation to the recipient.
      operationId: CreateSenderInvitation
      parameters:
      - description: Parameters for sender invitation creation.
        in: body
        name: CreateSenderInvitationDetails
        required: true
        schema:
          $ref: '#/definitions/CreateSenderInvitationDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Sender invitation creation accepted.
          headers:
            etag: *id003
            opc-request-id: *id001
            opc-work-request-id: *id004
          schema:
            $ref: '#/definitions/SenderInvitation'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a sender invitation and asynchronously sends the invitation to recipient.
      tags:
      - senderInvitation
      x-related-resource: '#/definitions/SenderInvitation'
  /senderInvitations/{senderInvitationId}:
    get:
      deprecated: true
      description: Gets information about the sender invitation.
      operationId: GetSenderInvitation
      parameters:
      - description: OCID of the sender invitation to retrieve.
        in: path
        maxLength: 255
        name: senderInvitationId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Sender invitation retrieved.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/SenderInvitation'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets information about the sender invitation.
      tags:
      - senderInvitation
    put:
      deprecated: true
      description: Updates the sender invitation.
      operationId: UpdateSenderInvitation
      parameters:
      - description: OCID of the sender invitation to update.
        in: path
        maxLength: 255
        name: senderInvitationId
        required: true
        type: string
      - description: The information to be updated.
        in: body
        name: UpdateSenderInvitationDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateSenderInvitationDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Sender invitation updated.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/SenderInvitation'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the SenderInvitation identified by the id.
      tags:
      - senderInvitation
      x-related-resource: '#/definitions/SenderInvitation'
  /senderInvitations/{senderInvitationId}/actions/cancel:
    post:
      consumes:
      - application/json
      deprecated: true
      description: Cancels a sender invitation.
      operationId: CancelSenderInvitation
      parameters:
      - description: OCID of the sender invitation to cancel.
        in: path
        maxLength: 255
        name: senderInvitationId
        required: true
        type: string
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        202:
          description: Sender invitation cancellation request accepted.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id004
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Cancels a sender invitation.
      tags:
      - senderInvitation
      x-related-resource: '#/definitions/SenderInvitation'
  /subscriptionMappings:
    get:
      deprecated: true
      description: Lists the subscription mappings for all the subscriptions owned by a given compartmentId.
        Only the root compartment is allowed.
      operationId: ListSubscriptionMappings
      parameters:
      - $ref: '#/parameters/SubscriptionIdQueryParam'
      - $ref: '#/parameters/SubscriptionMappingIdOptionalQueryParam'
      - $ref: '#/parameters/CompartmentIdOptionalQueryParam'
      - $ref: '#/parameters/SubscriptionMappingLifecycleStateQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      produces:
      - application/json
      responses:
        200:
          description: Retrieved list of subscriptionMapping.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/SubscriptionMappingCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List subscription mappings for all the subscriptions owned by a given compartmentId. Only
        root compartment is allowed.
      tags:
      - subscription
      x-related-resource: '#/definitions/SubscriptionMapping'
    post:
      consumes:
      - application/json
      deprecated: true
      description: Assign the tenancy record identified by the compartment ID to the given subscription
        ID.
      operationId: CreateSubscriptionMapping
      parameters:
      - description: Compartment ID and Subscription ID details to create a subscription mapping.
        in: body
        name: CreateSubscriptionMappingDetails
        required: true
        schema:
          $ref: '#/definitions/CreateSubscriptionMappingDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Tenancy assigned to subscription ID successfully.
          headers:
            etag: *id003
            opc-request-id: *id001
            opc-work-request-id: *id004
          schema:
            $ref: '#/definitions/SubscriptionMapping'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Assign the tenancy record identified by the compartment ID to the given subscription ID.
      tags:
      - subscription
      x-related-resource: '#/definitions/SubscriptionMapping'
  /subscriptionMappings/{subscriptionMappingId}:
    delete:
      deprecated: true
      description: Delete the subscription mapping details by subscription mapping ID.
      operationId: DeleteSubscriptionMapping
      parameters:
      - description: OCID of the subscription mapping ID.
        in: path
        maxLength: 255
        name: subscriptionMappingId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      produces:
      - application/json
      responses:
        204:
          description: Subscription mapping information deleted successfully.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete subscription mapping details by subscription mapping ID.
      tags:
      - subscription
      x-related-resource: '#/definitions/SubscriptionMapping'
    get:
      deprecated: true
      description: Get the subscription mapping details by subscription mapping ID.
      operationId: GetSubscriptionMapping
      parameters:
      - description: OCID of the subscriptionMappingId.
        in: path
        maxLength: 255
        name: subscriptionMappingId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Subscription mapping information retrieved successfully.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/SubscriptionMapping'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the subscription mapping details by subscription mapping ID.
      tags:
      - subscription
      x-related-resource: '#/definitions/SubscriptionMapping'
  /subscriptions:
    get:
      deprecated: true
      description: List the subscriptions that a compartment owns. Only the root compartment is allowed.
      operationId: ListSubscriptions
      parameters:
      - $ref: '#/parameters/CompartmentIdOptionalQueryParam'
      - $ref: '#/parameters/SubscriptionIdOptionalQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      produces:
      - application/json
      responses:
        200:
          description: Retrieved list of subscriptions.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/SubscriptionCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List subscriptions that a compartment owns. Only root compartment is allowed.
      tags:
      - subscription
      x-related-resource: '#/definitions/Subscription'
  /subscriptions/{subscriptionId}:
    get:
      deprecated: true
      description: Gets the subscription details by subscriptionId.
      operationId: GetSubscription
      parameters:
      - description: OCID of the subscription.
        in: path
        maxLength: 255
        name: subscriptionId
        required: true
        type: string
      - $ref: '#/parameters/RequestIdHeader'
      produces:
      - application/json
      responses:
        200:
          description: Subscription information retrieved successfully.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/Subscription'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets subscription details by subscriptionId.
      tags:
      - subscription
  /subscriptions/{subscriptionId}/availableRegions:
    get:
      deprecated: true
      description: List the available regions based on subscription ID.
      operationId: ListAvailableRegions
      parameters:
      - $ref: '#/parameters/SubscriptionIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      produces:
      - application/json
      responses:
        200:
          description: Retrieved the list of regions where the subscription is used.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/AvailableRegionCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List available regions based on subscription ID.
      tags:
      - subscription
      x-related-resource: '#/definitions/Subscription'
  /workRequests:
    get:
      deprecated: true
      description: 'Lists the work requests in a compartment.

        '
      operationId: ListWorkRequests
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List Work Requests
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}:
    get:
      deprecated: true
      description: Gets the status of the work request with the given ID.
      operationId: GetWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The detail of the work request.
          headers:
            opc-request-id: *id001
            retry-after: &id019
              description: A decimal number representing the number of seconds the client should wait
                before polling this endpoint again.
              format: float
              type: number
          schema:
            $ref: '#/definitions/WorkRequest'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: GET Work Request Status
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}/errors:
    get:
      deprecated: true
      description: 'Return a (paginated) list of errors for a given work request.

        '
      operationId: ListWorkRequestErrors
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of work request errors.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestErrorCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists work request errors
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequestError'
  /workRequests/{workRequestId}/logs:
    get:
      deprecated: true
      description: 'Return a (paginated) list of logs for a given work request.

        '
      operationId: ListWorkRequestLogs
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of work request logs.
          headers:
            opc-next-page: *id002
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestLogEntryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists work request logs
      tags:
      - workRequest
      x-related-resource: '#/definitions/WorkRequestLogEntry'
definitions:
  ActionType:
    description: Possible types of actions.
    enum: &id017
    - CREATED
    - UPDATED
    - DELETED
    - IN_PROGRESS
    - RELATED
    type: string
  ActivateOrderDetails:
    description: The parameters for activating an order subscription in a tenancy.
    properties:
      compartmentId:
        description: Tenant ID to activate the order.
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
  AssignedSubscription:
    description: Assigned subscription information.
    properties:
      classicSubscriptionId:
        description: Subscription ID.
        example: '7971096'
        maxLength: 31
        minLength: 1
        type: string
      cloudAmountCurrency:
        description: The currency code for the customer associated with the subscription.
        example: USD
        maxLength: 63
        type: string
      compartmentId:
        description: OCID of the compartment. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      csiNumber:
        description: Customer service identifier for the customer associated with the subscription.
        example: '12345678'
        maxLength: 63
        type: string
      customerCountryCode:
        description: The country code for the customer associated with the subscription.
        example: US
        maxLength: 3
        type: string
      endDate:
        description: Subscription end time.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      id:
        description: OCID of the subscription.
        maxLength: 255
        minLength: 1
        type: string
      isClassicSubscription:
        default: false
        description: Denotes if the subscription is legacy or not.
        example: true
        type: boolean
      isGovernmentSubscription:
        default: false
        description: Denotes whether or not the subscription is a government subscription.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the subscription.
        enum: &id005
        - CREATING
        - ACTIVE
        - INACTIVE
        - UPDATING
        - DELETING
        - DELETED
        - FAILED
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/SubscriptionLifecycleState'
      orderIds:
        description: List of subscription order OCIDs that contributed to this subscription.
        items:
          type: string
        type: array
      programType:
        description: Denotes any program that is associated with the subscription.
        example: SE_TRIAL
        maxLength: 63
        type: string
      promotion:
        description: List of promotions related to the subscription.
        items:
          $ref: '#/definitions/Promotion'
        type: array
      purchaseEntitlementId:
        description: Purchase entitlement ID associated with the subscription.
        example: entitlementId
        maxLength: 63
        type: string
      regionAssignment:
        description: Region for the subscription.
        example: us-ashburn-1
        maxLength: 100
        minLength: 1
        type: string
      serviceName:
        description: The type of subscription, such as 'CLOUDCM', 'SAAS', 'ERP', or 'CRM'.
        example: CLOUDCM
        maxLength: 127
        minLength: 1
        type: string
      skus:
        description: List of SKUs linked to the subscription.
        items:
          $ref: '#/definitions/SubscriptionSku'
        type: array
      startDate:
        description: Subscription start time.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      subscriptionTier:
        description: Tier for the subscription, whether it is a free promotion subscription or a paid
          subscription.
        example: FREE
        maxLength: 63
        type: string
      timeCreated:
        description: Date-time when subscription is created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when subscription is updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - classicSubscriptionId
    - serviceName
  AssignedSubscriptionCollection:
    description: List of AssignedSubscription.
    properties:
      items:
        description: Array containing AssignedSubscription items.
        items:
          $ref: '#/definitions/AssignedSubscriptionSummary'
        type: array
    required:
    - items
    type: object
  AssignedSubscriptionSummary:
    description: Summary of AssignedSubscription information.
    properties:
      classicSubscriptionId:
        description: Subscription ID.
        example: '7971096'
        maxLength: 31
        minLength: 1
        type: string
      compartmentId:
        description: OCID of the compartment. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      csiNumber:
        description: Customer service identifier for the customer associated with the subscription.
        example: '12345678'
        maxLength: 63
        type: string
      endDate:
        description: Subscription end time.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      id:
        description: OCID of the subscription.
        maxLength: 255
        minLength: 1
        type: string
      isClassicSubscription:
        default: false
        description: Denotes if the subscription is legacy or not.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the subscription.
        enum: *id005
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/SubscriptionLifecycleState'
      regionAssignment:
        description: Region for the subscription.
        example: us-ashburn-1
        type: string
      serviceName:
        description: The type of subscription, such as 'CLOUDCM', 'SAAS', 'ERP', or 'CRM'.
        example: CLOUDCM
        maxLength: 127
        minLength: 1
        type: string
      startDate:
        description: Subscription start time.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeCreated:
        description: Date-time when subscription is created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when subscription is updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - classicSubscriptionId
    - serviceName
  AvailableRegionCollection:
    description: List of available regions.
    properties:
      items:
        description: Array containing available region items.
        items:
          $ref: '#/definitions/AvailableRegionSummary'
        type: array
    required:
    - items
    type: object
  AvailableRegionSummary:
    description: The summary of region availability for a subscription.
    properties:
      regionName:
        description: Region availability for the subscription.
        example: us-ashburn-1
        maxLength: 127
        minLength: 1
        type: string
    required:
    - regionName
  CreateChildTenancyDetails:
    description: The parameters for creating a child tenancy.
    properties:
      adminEmail:
        description: Email address of the child tenancy administrator.
        maxLength: 255
        minLength: 6
        type: string
      compartmentId:
        description: The tenancy ID of the parent tenancy.
        maxLength: 255
        minLength: 1
        type: string
      features:
        description: List of features that the child and parent will have links for. Link of type CORE
          will always be created.
        example:
        - CORE
        items:
          maxLength: 64
          minLength: 1
          type: string
        maxItems: 10
        type: array
        uniqueItems: true
      governanceStatus:
        default: OPTED_OUT
        description: The governance status of the child tenancy.
        enum: &id009
        - OPTED_IN
        - OPTED_OUT
        example: OPTED_IN
        type: string
        x-obmcs-top-level-enum: '#/definitions/GovernanceStatus'
      homeRegion:
        description: The home region to use for the child tenancy. This must be a region where the parent
          tenancy is subscribed.
        example: us-ashburn-1
        maxLength: 24
        minLength: 1
        type: string
      policyName:
        description: The name to use for the administrator policy in the child tenancy. Must contain only
          letters and underscores.
        example: admin_policy
        maxLength: 255
        minLength: 1
        type: string
        x-default-description: <tenancyName>_admin_policy
      tenancyName:
        description: The tenancy name to use for the child tenancy.
        example: oracle-account
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    - tenancyName
    - homeRegion
    - adminEmail
  CreateDomainDetails:
    description: The parameters for creating a domain.
    properties:
      compartmentId:
        description: OCID of the tenancy.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: &id006
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer, and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          Example: `{"foo-namespace": {"bar-key": "value"}}`

          '
        type: object
      domainName:
        description: The domain name.
        example: example.com
        maxLength: 253
        minLength: 1
        type: string
      freeformTags: &id007
        additionalProperties:
          type: string
        description: 'Simple key-value pair that is applied without any predefined name, type or scope.
          Exists for cross-compatibility only.

          Example: `{"bar-key": "value"}`

          '
        type: object
      isGovernanceEnabled:
        description: Indicates whether governance should be enabled for this domain. Defaults to false.
        type: boolean
      subscriptionEmail:
        description: Email address to be used to notify the user, and that the ONS subscription will be
          created with.
        maxLength: 255
        minLength: 6
        type: string
    required:
    - compartmentId
    - domainName
  CreateDomainGovernanceDetails:
    description: The parameters for creating a domain governance entity.
    properties:
      compartmentId:
        description: OCID of the tenancy.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      domainId:
        description: OCID of the domain.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id007
      onsSubscriptionId:
        description: The ONS subscription associated with this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      onsTopicId:
        description: The ONS topic associated with this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      subscriptionEmail:
        description: Email address to be used to notify the user, and that the ONS subscription will be
          created with.
        maxLength: 255
        minLength: 6
        type: string
    required:
    - compartmentId
    - domainId
    - subscriptionEmail
    - onsTopicId
    - onsSubscriptionId
  CreateSenderInvitationDetails:
    description: The parameters for creating a sender invitation.
    properties:
      compartmentId:
        description: OCID of the sender tenancy.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      displayName:
        description: A user-created name to describe the invitation. Avoid entering confidential information.
        maxLength: 255
        type: string
      features:
        description: List of features that the invitation is being sent for. Each feature would create
          one link, of that type.
        example:
        - CORE
        items:
          maxLength: 64
          minLength: 1
          type: string
        maxItems: 10
        type: array
        uniqueItems: true
      freeformTags: *id007
      recipientEmailAddress:
        description: Email address of the recipient.
        maxLength: 255
        minLength: 6
        type: string
      recipientTenancyId:
        description: OCID of the recipient tenancy.
        maxLength: 255
        minLength: 1
        type: string
      subjects:
        default:
        - LINK
        description: The list of subjects this invitation contains.
        items:
          $ref: '#/definitions/InvitationSubject'
        minItems: 1
        type: array
        uniqueItems: true
    required:
    - compartmentId
    - recipientTenancyId
  CreateSubscriptionMappingDetails:
    description: CreateSubscriptionMappingDetails contains subscription and compartment identified by
      the tenancy, and OCID information.
    properties:
      compartmentId:
        description: OCID of the compartment. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      subscriptionId:
        description: OCID of subscription.
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    - subscriptionId
  Domain:
    description: The domain model that is associated with a tenancy.
    properties:
      compartmentId:
        description: The OCID of the tenancy that owns this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      domainName:
        description: The domain name.
        example: example.com
        maxLength: 253
        minLength: 1
        type: string
      freeformTags: *id007
      id:
        description: The OCID of the domain.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: Lifecycle state of the domain.
        enum:
        - ACTIVE
        - DELETED
        - FAILED
        example: ACTIVE
        type: string
      ownerId:
        description: The OCID of the tenancy that has started the registration process for this domain.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: Status of the domain.
        enum:
        - PENDING
        - RELEASING
        - RELEASED
        - EXPIRING
        - REVOKING
        - REVOKED
        - ACTIVE
        - FAILED
        example: PENDING
        type: string
      systemTags: &id008
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer, and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
      timeCreated:
        description: Date-time when this domain was created. An RFC 3339-formatted date and time string.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when this domain was last updated. An RFC 3339-formatted date and time
          string.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      txtRecord:
        description: The code that the owner of the domain will need to add as a TXT record to their domain.
        example: oci-domain-verification=exampleEncodedToken
        maxLength: 45
        minLength: 40
        type: string
    required:
    - id
    - domainName
    - ownerId
    - compartmentId
    - lifecycleState
    - status
    - txtRecord
  DomainCollection:
    description: Result of a query request for a list of domains. Contains DomainSummary items.
    properties:
      items:
        description: Array containing DomainSummary items.
        items:
          $ref: '#/definitions/DomainSummary'
        type: array
    required:
    - items
    type: object
  DomainGovernance:
    description: The model for a domain governance entity.
    properties:
      compartmentId:
        description: The OCID of the tenancy that owns this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      domainId:
        description: The OCID of the domain associated with this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id007
      id:
        description: The OCID of the domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      isGovernanceEnabled:
        description: Indicates whether governance is enabled for this domain.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the domain governance entity.
        enum:
        - ACTIVE
        - INACTIVE
        example: ACTIVE
        type: string
      onsSubscriptionId:
        description: The ONS subscription associated with this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      onsTopicId:
        description: The ONS topic associated with this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      ownerId:
        description: The OCID of the tenancy that owns this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      subscriptionEmail:
        description: Email address to be used to notify the user, and that the ONS subscription will be
          created with.
        maxLength: 255
        minLength: 6
        type: string
      systemTags: *id008
      timeCreated:
        description: Date-time when this domain governance was created. An RFC 3339-formatted date and
          time string.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when this domain governance was last updated. An RFC 3339-formatted date
          and time string.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - ownerId
    - compartmentId
    - domainId
    - lifecycleState
    - onsTopicId
    - onsSubscriptionId
  DomainGovernanceCollection:
    description: Result of a query request for a list of domain governance entities. Contains DomainGovernanceSummary
      items.
    properties:
      items:
        description: Array containing DomainGovernanceSummary items.
        items:
          $ref: '#/definitions/DomainGovernanceSummary'
        type: array
    required:
    - items
    type: object
  DomainGovernanceSummary:
    description: The summary of a domain govenance entity owned by a tenancy.
    properties:
      compartmentId:
        description: The OCID of the tenancy that has started the registration process for this domain.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      domainId:
        description: The OCID of the domain associated with this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id007
      id:
        description: The OCID of the domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      isGovernanceEnabled:
        description: Indicates whether governance is enabled for this domain.
        example: true
        type: boolean
      lifecycleState:
        description: The lifecycle state of the domain governance entity.
        example: ACTIVE
        type: string
        x-obmcs-enumref: '#/definitions/DomainGovernance/lifecycleState'
      onsSubscriptionId:
        description: The ONS subscription associated with this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      onsTopicId:
        description: The ONS topic associated with this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      ownerId:
        description: The OCID of the tenancy that owns this domain governance entity.
        maxLength: 255
        minLength: 1
        type: string
      subscriptionEmail:
        description: Email address to be used to notify the user, and that the ONS subscription will be
          created with.
        maxLength: 255
        minLength: 6
        type: string
      systemTags: *id008
      timeCreated:
        description: Date-time when this domain governance was created. An RFC 3339-formatted date and
          time string.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when this domain governance was last updated. An RFC 3339-formatted date
          and time string.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - ownerId
    - compartmentId
    - domainId
    - lifecycleState
    - isGovernanceEnabled
    - onsTopicId
    - onsSubscriptionId
  DomainSummary:
    description: The summary of a domain owned by a tenancy.
    properties:
      compartmentId:
        description: The OCID of the tenancy that has started the registration process for this domain.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      domainName:
        description: The domain name.
        example: example.com
        maxLength: 253
        minLength: 1
        type: string
      freeformTags: *id007
      id:
        description: OCID of the domain.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The lifecycle state of the domain.
        example: ACTIVE
        type: string
        x-obmcs-enumref: '#/definitions/Domain/lifecycleState'
      ownerId:
        description: The OCID of the tenancy that has started the registration process for this domain.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: Status of the domain.
        example: PENDING
        type: string
        x-obmcs-enumref: '#/definitions/Domain/status'
      systemTags: *id008
      timeCreated:
        description: Date-time when this domain was created. An RFC 3339-formatted date and time string.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when this domain was last updated. An RFC 3339-formatted date and time
          string.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      txtRecord:
        description: The code that the owner of the domain will need to add as a TXT record to their domain.
        example: oci-domain-verification=exampleEncodedToken
        maxLength: 45
        minLength: 40
        type: string
    required:
    - id
    - domainName
    - ownerId
    - compartmentId
    - lifecycleState
    - status
    - txtRecord
  Error:
    description: Error information.
    properties:
      code:
        description: A short error code that defines the error, meant for programmatic parsing.
        type: string
      message:
        description: A human-readable error string.
        type: string
    required:
    - code
    - message
  GovernanceStatus:
    description: Possible governance statuses.
    enum: *id009
    type: string
  InvitationFeature:
    description: The feature model for sender invitation.
    properties:
      name:
        description: Name of the feature.
        maxLength: 255
        minLength: 1
        type: string
      recipientInvitationId:
        description: recipientInvitationId for this feature.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: Status of the sender invitation.
        enum: &id014
        - PENDING
        - CANCELED
        - ACCEPTED
        - EXPIRED
        - FAILED
        - NOT_APPLICABLE
        example: ACCEPTED
        type: string
        x-obmcs-top-level-enum: '#/definitions/SenderInvitationStatus'
    type: object
  InvitationSubject:
    description: A subject an invitation can contain.
    enum:
    - LINK
    - GOVERNANCE
    - EXTENDED_FEATURES
    type: string
  LifecycleState:
    description: Possible lifecycle states for a resource.
    enum: &id010
    - CREATING
    - ACTIVE
    - INACTIVE
    - UPDATING
    - FAILED
    - TERMINATED
    type: string
  Link:
    description: A link between a parent tenancy and a child tenancy.
    properties:
      childTenancyId:
        description: OCID of the child tenancy.
        maxLength: 255
        minLength: 1
        type: string
      feature:
        description: Type of the feature that the link is supporting. Default value is CORE.
        example: CORE
        maxLength: 64
        minLength: 1
        type: string
      id:
        description: OCID of the link.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: Lifecycle state of the link.
        enum: *id010
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      parentTenancyId:
        description: OCID of the parent tenancy.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: Date-time when this link was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeTerminated:
        description: Date-time when this link was terminated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when this link was last updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - parentTenancyId
    - childTenancyId
    - timeCreated
  LinkCollection:
    description: Result of a query request for a list of links. Contains Link items.
    properties:
      items:
        description: Array containing LinkSummary items.
        items:
          $ref: '#/definitions/LinkSummary'
        type: array
    required:
    - items
    type: object
  LinkSummary:
    description: The summary of a link between a parent tenancy and a child tenancy.
    properties:
      childTenancyId:
        description: OCID of the child tenancy.
        maxLength: 255
        minLength: 1
        type: string
      feature:
        description: Type of the feature that the link is supporting. Default value is CORE.
        example: CORE
        maxLength: 64
        minLength: 1
        type: string
      id:
        description: OCID of the link.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: Lifecycle state of the link.
        enum: *id010
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      parentTenancyId:
        description: OCID of the parent tenancy.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: Date-time when this link was created
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeTerminated:
        description: Date-time when this link was terminated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when this link was last updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - parentTenancyId
    - childTenancyId
    - timeCreated
  LinkWithTenancyNames:
    allOf:
    - $ref: '#/definitions/Link'
    - properties:
        childTenancyName:
          description: Name of the child tenancy.
          maxLength: 255
          minLength: 1
          type: string
        parentTenancyName:
          description: Name of the parent tenancy.
          maxLength: 255
          minLength: 1
          type: string
      required:
      - parentTenancyName
      - childTenancyName
    description: A link between a parent tenancy and a child tenancy with tenancy names.
  OperationStatus:
    description: Possible operation status.
    enum: &id016
    - ACCEPTED
    - IN_PROGRESS
    - FAILED
    - SUCCEEDED
    - CANCELING
    - CANCELED
    type: string
  OperationType:
    description: Possible operation types.
    enum: &id015
    - CREATE_SENDER_INVITATION
    - ACCEPT_RECIPIENT_INVITATION
    - CANCEL_SENDER_INVITATION
    - COMPLETE_ORDER_ACTIVATION
    - ACTIVATE_ORDER_EXISTING_TENANCY
    - REGISTER_DOMAIN
    - RELEASE_DOMAIN
    - CREATE_CHILD_TENANCY
    - ASSIGN_DEFAULT_SUBSCRIPTION
    - MANUAL_LINK_CREATION
    - TERMINATE_ORGANIZATION_TENANCY
    - UPDATE_SAAS_CAPABILITY
    - SOFT_TERMINATE_TENANCY
    - HARD_TERMINATE_TENANCY
    - RESTORE_TENANCY
    - LOG_TENANCY_TERMINATION_REQUEST
    - STANDALONE_TENANCY_TERMINATION_REQUEST
    - SELF_OPT_IN
    - SELF_OPT_OUT
    - ORDER_PAYLOADS_BACKFILL_ETL
    - PUBLISH_TO_TOPIC
    - TERMINATE_LINK
    type: string
  Order:
    description: Order details.
    properties:
      adminEmail:
        description: Email address of the administrator who owns the subscription.
        maxLength: 255
        minLength: 6
        type: string
      dataCenterRegion:
        description: Order's data center region.
        example: global
        type: string
      orderNumber:
        description: Immutable and unique order number holding customer subscription information.
        type: string
      orderState:
        description: State of the order.
        type: string
      subscriptions:
        description: Array of subscriptions associated with the order.
        items:
          $ref: '#/definitions/SubscriptionInfo'
        type: array
    required:
    - orderNumber
    - adminEmail
    - orderState
    - subscriptions
  Organization:
    description: An organization entity.
    properties:
      compartmentId:
        description: OCID of the compartment containing the organization. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      defaultUcmSubscriptionId:
        description: OCID of the default Universal Credits Model subscription. Any tenancy joining the
          organization will automatically get assigned this subscription, if a subscription is not explictly
          assigned.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: A display name for the organization. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      id:
        description: OCID of the organization.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: Lifecycle state of the organization.
        enum:
        - CREATING
        - ACTIVE
        - UPDATING
        - DELETING
        - DELETED
        - FAILED
        example: ACTIVE
        type: string
      parentName:
        description: The name of the tenancy that is the organization parent.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: Date and time when the organization was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date and time when the organization was last updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - defaultUcmSubscriptionId
    - timeCreated
    - lifecycleState
  OrganizationCollection:
    description: Result of a query request for a list of organizations. Contains OrganizationSummary items.
    properties:
      items:
        description: Array containing OrganizationSummary items.
        items:
          $ref: '#/definitions/OrganizationSummary'
        type: array
    required:
    - items
    type: object
  OrganizationSummary:
    description: An organization entity.
    properties:
      compartmentId:
        description: OCID of the compartment containing the organization. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      defaultUcmSubscriptionId:
        description: OCID of the default Universal Credits Model subscription. Any tenancy joining the
          organization will automatically get assigned this subscription, if a subscription is not explictly
          assigned.
        maxLength: 255
        minLength: 1
        type: string
      displayName:
        description: A display name for the organization. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      id:
        description: OCID of the organization.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: Lifecycle state of the organization.
        example: ACTIVE
        type: string
        x-obmcs-enumref: '#/definitions/Organization/lifecycleState'
      parentName:
        description: The name of the tenancy that is the organization parent.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: Date and time when the organization was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date and time when the organization was last updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - defaultUcmSubscriptionId
    - timeCreated
    - lifecycleState
  OrganizationTenancy:
    description: The information about the organization tenancy.
    properties:
      governanceStatus:
        description: The governance status of the tenancy.
        enum: *id009
        example: OPTED_IN
        type: string
        x-obmcs-top-level-enum: '#/definitions/GovernanceStatus'
      isApprovedForTransfer:
        description: Parameter to indicate the tenancy is approved for transfer to another organization.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the organization tenancy.
        enum: &id011
        - CREATING
        - ACTIVE
        - INACTIVE
        - DELETED
        - FAILED
        - DELETING
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/OrganizationTenancyLifecycleState'
      name:
        description: Name of the tenancy.
        example: someTenancy123
        type: string
      role:
        description: Role of the organization tenancy.
        enum: &id012
        - PARENT
        - CHILD
        - NONE
        example: PARENT
        type: string
        x-obmcs-top-level-enum: '#/definitions/OrganizationTenancyRole'
      tenancyId:
        description: OCID of the tenancy.
        maxLength: 255
        minLength: 1
        type: string
      timeJoined:
        description: Date and time when the tenancy joined the organization.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeLeft:
        description: Date and time when the tenancy left the organization.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - tenancyId
    - governanceStatus
  OrganizationTenancyCollection:
    description: Result of a query request for a list of organization tenancies. Contains OrganizationTenancySummary
      items.
    properties:
      items:
        description: Array containing OrganizationTenancySummary items.
        items:
          $ref: '#/definitions/OrganizationTenancySummary'
        type: array
    required:
    - items
    type: object
  OrganizationTenancyLifecycleState:
    description: Possible lifecycle states for the organization tenancy.
    enum: *id011
    type: string
  OrganizationTenancyRole:
    description: Possible roles for the organization tenancy.
    enum: *id012
    type: string
  OrganizationTenancySummary:
    description: An organization tenancy summary entity.
    properties:
      governanceStatus:
        description: The governance status of the tenancy.
        enum: *id009
        example: OPTED_IN
        type: string
        x-obmcs-top-level-enum: '#/definitions/GovernanceStatus'
      isApprovedForTransfer:
        description: Parameter to indicate the tenancy is approved for transfer to another organization.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the organization tenancy.
        enum: *id011
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/OrganizationTenancyLifecycleState'
      name:
        description: Name of the tenancy.
        example: someTenancy123
        type: string
      role:
        description: Role of the organization tenancy.
        enum: *id012
        example: PARENT
        type: string
        x-obmcs-top-level-enum: '#/definitions/OrganizationTenancyRole'
      tenancyId:
        description: OCID of the tenancy.
        maxLength: 255
        minLength: 1
        type: string
      timeJoined:
        description: Date and time when the tenancy joined the organization.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeLeft:
        description: Date and time when the tenancy left the organization.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - tenancyId
    - governanceStatus
  Promotion:
    description: Promotion information for a subscription.
    properties:
      amount:
        description: If a subscription is present, indicates the total amount of promotional subscription
          credit.
        example: 10020.0
        format: float
        maxLength: 63
        type: number
      currencyUnit:
        description: Currency unit associated with the promotion.
        example: USD
        maxLength: 63
        type: string
      duration:
        description: Specifies how long the promotion related to the subscription, if any, is valid in
          duration units.
        example: 5021
        maxLength: 63
        type: integer
      durationUnit:
        description: Unit for the duration.
        example: day
        maxLength: 63
        type: string
      isIntentToPay:
        description: Speficies whether or not the customer intends to pay after the promotion has expired.
        example: true
        maxLength: 63
        type: boolean
      status:
        description: If a subscription is present, indicates the current status of the subscription promotion.
        enum:
        - INITIALIZED
        - ACTIVE
        - EXPIRED
        example: ACTIVE
        maxLength: 63
        type: string
      timeExpired:
        description: Date and time when the promotion ends.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeStarted:
        description: Date and time when the promotion starts.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
  RecipientInvitation:
    description: The invitation model that the recipient owns.
    properties:
      compartmentId:
        description: OCID of the recipient tenancy.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      displayName:
        description: A user-created name to describe the invitation. Avoid entering confidential information.
        example: test_name
        type: string
      features:
        description: List of features that the invitation is being sent for. Each feature would create
          one link, of that type.
        example:
        - CORE
        items:
          maxLength: 64
          minLength: 1
          type: string
        maxItems: 10
        type: array
        uniqueItems: true
      freeformTags: *id007
      id:
        description: OCID of the recipient invitation.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: Lifecycle state of the recipient invitation.
        enum: *id010
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      recipientEmailAddress:
        description: Email address of the recipient.
        maxLength: 255
        minLength: 6
        type: string
      senderInvitationId:
        description: OCID of the corresponding sender invitation.
        maxLength: 255
        minLength: 1
        type: string
      senderTenancyId:
        description: OCID of the sender tenancy.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: Status of the recipient invitation.
        enum: &id013
        - PENDING
        - CANCELED
        - ACCEPTED
        - IGNORED
        - EXPIRED
        - FAILED
        example: ACCEPTED
        type: string
        x-obmcs-top-level-enum: '#/definitions/RecipientInvitationStatus'
      subjects:
        description: The list of subjects the invitation contains.
        items:
          $ref: '#/definitions/InvitationSubject'
        minItems: 1
        type: array
        uniqueItems: true
      systemTags: *id008
      timeCreated:
        description: Date and time when the recipient invitation was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date and time when the recipient invitation was last updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - senderInvitationId
    - senderTenancyId
    - lifecycleState
    - status
    - timeCreated
    - subjects
  RecipientInvitationCollection:
    description: Result of a query request for a list of recipient invitations. Contains RecipientInvitationSummary
      items.
    properties:
      items:
        description: Array containing RecipientInvitationSummary items.
        items:
          $ref: '#/definitions/RecipientInvitationSummary'
        type: array
    required:
    - items
    type: object
  RecipientInvitationStatus:
    description: Possible statuses for a recipient invitation.
    enum: *id013
    type: string
  RecipientInvitationSummary:
    description: The summary of the invitation model that the recipient owns.
    properties:
      compartmentId:
        description: OCID of the recipient tenancy.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      displayName:
        description: A user-created name to describe the invitation. Avoid entering confidential information.
        type: string
      features:
        description: List of features that the invitation is being sent for. Each feature would create
          one link, of that type.
        example:
        - CORE
        items:
          maxLength: 64
          minLength: 1
          type: string
        maxItems: 10
        type: array
        uniqueItems: true
      freeformTags: *id007
      id:
        description: OCID of the recipient invitation.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: Lifecycle state of the recipient invitation.
        enum: *id010
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      recipientEmailAddress:
        description: Email address of the recipient.
        maxLength: 255
        minLength: 6
        type: string
      senderInvitationId:
        description: OCID of the corresponding sender invitation.
        maxLength: 255
        minLength: 1
        type: string
      senderTenancyId:
        description: OCID of the sender tenancy.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: Status of the recipient invitation.
        enum: *id013
        example: ACCEPTED
        type: string
        x-obmcs-top-level-enum: '#/definitions/RecipientInvitationStatus'
      subjects:
        description: The list of subjects the invitation contains.
        items:
          $ref: '#/definitions/InvitationSubject'
        minItems: 1
        type: array
        uniqueItems: true
      systemTags: *id008
      timeCreated:
        description: Date and time when the recipient invitation was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date and time when the recipient invitation was last updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - displayName
    - senderInvitationId
    - senderTenancyId
    - lifecycleState
    - status
    - timeCreated
    - subjects
  SenderInvitation:
    description: The invitation model that the sender owns.
    properties:
      compartmentId:
        description: OCID of the sender tenancy.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      displayName:
        description: A user-created name to describe the invitation. Avoid entering confidential information.
        example: test_name
        type: string
      freeformTags: *id007
      id:
        description: OCID of the sender invitation.
        maxLength: 255
        minLength: 1
        type: string
      invitationFeatures:
        description: List of features that the invitation is being sent for. Each feature would create
          one link, of that type.
        items:
          $ref: '#/definitions/InvitationFeature'
        maxItems: 10
        type: array
        uniqueItems: true
      lifecycleState:
        description: Lifecycle state of the sender invitation.
        enum: *id010
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      recipientEmailAddress:
        description: Email address of the recipient.
        maxLength: 255
        minLength: 6
        type: string
      recipientInvitationId:
        description: OCID of the corresponding recipient invitation.
        maxLength: 255
        minLength: 1
        type: string
      recipientTenancyId:
        description: OCID of the recipient tenancy.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: Status of the sender invitation.
        enum: *id014
        example: ACCEPTED
        type: string
        x-obmcs-top-level-enum: '#/definitions/SenderInvitationStatus'
      subjects:
        description: The list of subjects the invitation contains.
        items:
          $ref: '#/definitions/InvitationSubject'
        minItems: 1
        type: array
        uniqueItems: true
      systemTags: *id008
      timeCreated:
        description: Date and time when the sender invitation was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date and time when the sender invitation was last updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - recipientTenancyId
    - lifecycleState
    - status
    - timeCreated
    - subjects
  SenderInvitationCollection:
    description: Result of a query request for a list of sender invitations. Contains SenderInvitationSummary
      items.
    properties:
      items:
        description: Array containing SenderInvitationSummary items.
        items:
          $ref: '#/definitions/SenderInvitationSummary'
        type: array
    required:
    - items
    type: object
  SenderInvitationStatus:
    description: Possible statuses for a sender invitation.
    enum: *id014
    type: string
  SenderInvitationSummary:
    description: The summary of the invitation model that the sender owns.
    properties:
      compartmentId:
        description: OCID of the sender tenancy.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id006
      displayName:
        description: A user-created name to describe the invitation. Avoid entering confidential information.
        type: string
      freeformTags: *id007
      id:
        description: OCID of the sender invitation.
        maxLength: 255
        minLength: 1
        type: string
      invitationFeatures:
        description: List of features that the invitation is being sent for. Each feature would create
          one link, of that type.
        items:
          $ref: '#/definitions/InvitationFeature'
        maxItems: 10
        type: array
        uniqueItems: true
      lifecycleState:
        description: Lifecycle state of the sender invitation.
        enum: *id010
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      recipientEmailAddress:
        description: Email address of the recipient.
        maxLength: 255
        minLength: 6
        type: string
      recipientInvitationId:
        description: OCID of the corresponding recipient invitation.
        maxLength: 255
        minLength: 1
        type: string
      recipientTenancyId:
        description: OCID of the recipient tenancy.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: Status of the sender invitation.
        enum: *id014
        example: ACCEPTED
        type: string
        x-obmcs-top-level-enum: '#/definitions/SenderInvitationStatus'
      subjects:
        description: The list of subjects the invitation contains.
        items:
          $ref: '#/definitions/InvitationSubject'
        minItems: 1
        type: array
        uniqueItems: true
      systemTags: *id008
      timeCreated:
        description: Date and time when the sender invitation was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date and time when the sender invitation was last updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - displayName
    - recipientTenancyId
    - lifecycleState
    - status
    - timeCreated
    - subjects
  Sku:
    description: A single subscription SKU.
    properties:
      name:
        description: SKU name.
        type: string
      number:
        description: SKU number.
        type: string
      quantity:
        description: SKU quantity.
        type: integer
  SortOrder:
    description: Sort order.
    enum: &id018
    - ASC
    - DESC
    type: string
  Subscription:
    description: Subscription information for compartmentId. Only root compartments are allowed.
    properties:
      classicSubscriptionId:
        description: Classic subscription ID.
        example: '7971096'
        maxLength: 31
        minLength: 1
        type: string
      cloudAmountCurrency:
        description: The currency code for the customer associated with the subscription.
        example: USD
        maxLength: 63
        type: string
      compartmentId:
        description: OCID of the compartment. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      csiNumber:
        description: Customer service identifier for the customer associated with the subscription.
        example: '12345678'
        maxLength: 63
        type: string
      customerCountryCode:
        description: The country code for the customer associated with the subscription.
        example: US
        maxLength: 3
        type: string
      endDate:
        description: Subscription end time.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      id:
        description: OCID of the subscription details for the particular root compartment or tenancy.
        maxLength: 255
        minLength: 1
        type: string
      isClassicSubscription:
        default: false
        description: Denotes if the subscription is from classic systems or not.
        example: true
        type: boolean
      isGovernmentSubscription:
        default: false
        description: Denotes whether or not the subscription is a government subscription.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the subscription.
        enum: *id005
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/SubscriptionLifecycleState'
      paymentModel:
        description: The pay model of the subscription, such as 'Pay as you go' or 'Monthly'.
        example: Monthly
        maxLength: 63
        type: string
      programType:
        description: Denotes any program that is associated with the subscription.
        example: SE_TRIAL
        maxLength: 63
        type: string
      promotion:
        description: List of promotions related to the subscription.
        items:
          $ref: '#/definitions/Promotion'
        type: array
      purchaseEntitlementId:
        description: Purchase entitlement ID associated with the subscription.
        example: entitlementId
        maxLength: 63
        type: string
      regionAssignment:
        description: Region for the subscription.
        example: us-ashburn-1
        maxLength: 63
        minLength: 1
        type: string
      serviceName:
        description: The type of subscription, such as 'CLOUDCM', 'SAAS', 'ERP', or 'CRM'.
        example: CLOUDCM
        maxLength: 127
        minLength: 1
        type: string
      skus:
        description: List of SKUs linked to this subscription.
        items:
          $ref: '#/definitions/SubscriptionSku'
        type: array
      startDate:
        description: Subscription start time.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      subscriptionTier:
        description: Tier for the subscription, whether it is a free promotion subscription or a paid
          subscription.
        example: FREE
        maxLength: 63
        type: string
      timeCreated:
        description: Date-time when subscription is created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when subscription is updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - classicSubscriptionId
    - serviceName
  SubscriptionCollection:
    description: List of subscriptions.
    properties:
      items:
        description: Array containing subscription items.
        items:
          $ref: '#/definitions/SubscriptionSummary'
        type: array
    required:
    - items
    type: object
  SubscriptionInfo:
    description: A single subscription's details.
    properties:
      endDate:
        description: Subscription end date. An RFC 3339-formatted date and time string.
        format: date-time
        type: string
      service:
        description: Subscription service name.
        example: CLOUDCM
        type: string
      skus:
        description: List of SKUs the subscription contains.
        items:
          $ref: '#/definitions/Sku'
        type: array
      spmSubscriptionId:
        description: Subscription ID.
        type: string
      startDate:
        description: Subscription start date. An RFC 3339-formatted date and time string.
        format: date-time
        type: string
    required:
    - spmSubscriptionId
    - service
    - startDate
    - endDate
    - skus
  SubscriptionLifecycleState:
    description: Lifecycle state of the subscription.
    enum: *id005
    example: ACTIVE
    type: string
  SubscriptionMapping:
    description: Subscription mapping information.
    properties:
      compartmentId:
        description: OCID of the compartment. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      id:
        description: OCID of the mapping between subscription and compartment identified by the tenancy.
        maxLength: 255
        minLength: 1
        type: string
      isExplicitlyAssigned:
        default: false
        description: Denotes if the subscription is explicity assigned to the root compartment or tenancy.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the subscriptionMapping.
        enum:
        - CREATING
        - ACTIVE
        - INACTIVE
        - UPDATING
        - DELETING
        - DELETED
        - FAILED
        example: ACTIVE
        type: string
      subscriptionId:
        description: OCID of the subscription.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: Date-time when subscription mapping was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeTerminated:
        description: Date-time when subscription mapping was terminated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when subscription mapping was updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - subscriptionId
    - compartmentId
    - isExplicitlyAssigned
    - lifecycleState
    - timeCreated
    - timeUpdated
  SubscriptionMappingCollection:
    description: List of subscription mappings.
    properties:
      items:
        description: Array containing subscription mapping items.
        items:
          $ref: '#/definitions/SubscriptionMappingSummary'
        type: array
    required:
    - items
    type: object
  SubscriptionMappingSummary:
    description: Subscription mapping information.
    properties:
      compartmentId:
        description: OCID of the compartment. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      id:
        description: OCID of the mapping between subscription and compartment identified by the tenancy.
        maxLength: 255
        minLength: 1
        type: string
      isExplicitlyAssigned:
        default: false
        description: Denotes if the subscription is explicity assigned to the root compartment or tenancy.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the subscription mapping.
        example: ACTIVE
        type: string
        x-obmcs-enumref: '#/definitions/SubscriptionMapping/lifecycleState'
      subscriptionId:
        description: OCID of the subscription.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: Date-time when subscription mapping was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeTerminated:
        description: Date-time when subscription mapping was terminated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when subscription mapping was updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - subscriptionId
    - compartmentId
    - isExplicitlyAssigned
    - lifecycleState
    - timeCreated
    - timeUpdated
  SubscriptionSku:
    description: SKU information.
    properties:
      description:
        description: Description of the stock units.
        example: Oracle Database Exadata Cloud Service - Half Rack - X6 - BYOL - Hosted Environment Per
          Hour
        maxLength: 2000
        type: string
      endDate:
        description: Date-time when the SKU ended.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      gsiOrderLineId:
        description: Sales order line identifier.
        example: 233155
        type: string
      isAdditionalInstance:
        default: false
        description: Denotes if an additional test instance can be provisioned by the SAAS application.
        example: true
        type: boolean
      isBaseServiceComponent:
        default: false
        description: Denotes if the SKU is considered as a parent or child.
        example: true
        type: boolean
      licensePartDescription:
        description: Description of the covered product belonging to this SKU.
        example: Additional Bandwidth - Megabyte BUNDLED-BW-1MB
        maxLength: 240
        type: string
      metricName:
        description: Base metric for billing the service.
        example: HOSTED EXPENSE REPORT
        maxLength: 60
        type: string
      quantity:
        description: Quantity of the stock units.
        example: 1
        type: integer
      sku:
        description: Stock keeping unit ID.
        example: '7971096'
        maxLength: 40
        type: string
      startDate:
        description: Date-time when the SKU was created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - sku
  SubscriptionSummary:
    description: Summary of subscription.
    properties:
      classicSubscriptionId:
        description: Classic subscription ID.
        example: '7971096'
        maxLength: 31
        minLength: 1
        type: string
      compartmentId:
        description: OCID of the compartment. Always a tenancy OCID.
        maxLength: 255
        minLength: 1
        type: string
      endDate:
        description: Subscription end time.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      id:
        description: OCID of the subscription details for a particular root compartment or tenancy.
        maxLength: 255
        minLength: 1
        type: string
      isClassicSubscription:
        default: false
        description: Denotes if the subscription is from classic systems or not.
        example: true
        type: boolean
      lifecycleState:
        description: Lifecycle state of the subscription.
        enum: *id005
        example: ACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/SubscriptionLifecycleState'
      paymentModel:
        description: The pay model of the subscription, such as 'Pay as you go' or 'Monthly'.
        example: Monthly
        maxLength: 63
        type: string
      regionAssignment:
        description: Region for the subscription.
        example: us-ashburn-1
        maxLength: 63
        minLength: 1
        type: string
      serviceName:
        description: The type of subscription, such as 'CLOUDCM', 'SAAS', 'ERP', or 'CRM'.
        example: CLOUDCM
        maxLength: 127
        minLength: 1
        type: string
      startDate:
        description: Subscription start time.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeCreated:
        description: Date-time when subscription is created.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
      timeUpdated:
        description: Date-time when subscription is updated.
        example: 2016-08-29 09:12:33.001000+00:00
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - classicSubscriptionId
    - serviceName
  UpdateDomainDetails:
    description: The parameters for updating a domain.
    properties:
      definedTags: *id006
      freeformTags: *id007
  UpdateDomainGovernanceDetails:
    description: The parameters for updating a domain govenance entity.
    properties:
      definedTags: *id006
      freeformTags: *id007
      isGovernanceEnabled:
        description: Indicates whether governance is enabled for this domain.
        example: true
        type: boolean
      subscriptionEmail:
        description: Email address to be used to notify the user, and that the ONS subscription will be
          created with. The ONS subscription for the previous email will also be deleted.
        maxLength: 255
        minLength: 6
        type: string
  UpdateOrganizationDetails:
    description: The parameters for updating an organization.
    properties:
      defaultUcmSubscriptionId:
        description: OCID of the default Universal Credits Model subscription. Any tenancy joining the
          organization will automatically get assigned this subscription, if a subscription is not explictly
          assigned.
        maxLength: 255
        minLength: 1
        type: string
    required:
    - defaultUcmSubscriptionId
  UpdateRecipientInvitationDetails:
    description: The parameters for updating a recipient invitation.
    properties:
      definedTags: *id006
      displayName:
        description: A user-created name to describe the invitation. Avoid entering confidential information.
        maxLength: 255
        type: string
      freeformTags: *id007
  UpdateSenderInvitationDetails:
    description: The parameters for updating a sender invitation.
    properties:
      definedTags: *id006
      displayName:
        description: A user-created name to describe the invitation. Avoid entering confidential information.
        maxLength: 255
        type: string
      freeformTags: *id007
  WorkRequest:
    description: A description of the work request status.
    properties:
      compartmentId:
        description: 'The OCID of the compartment that contains the work request.

          '
        type: string
      id:
        description: The ID of the work request.
        type: string
      operationType:
        description: Type of the work request.
        enum: *id015
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: Percentage of the request completed.
        format: float
        type: number
      resources:
        description: The resources affected by this work request.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: Status of the current work request.
        enum: *id016
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: 'The date and time the request was created, as described in

          [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

          '
        format: date-time
        type: string
      timeFinished:
        description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
      timeStarted:
        description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

          section 14.29.

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
  WorkRequestCollection:
    description: Result of a query request for a list of work requests. Contains WorkRequestSummary items.
    properties:
      items:
        description: Array containing WorkRequestSummary items.
        items:
          $ref: '#/definitions/WorkRequestSummary'
        type: array
    required:
    - items
    type: object
  WorkRequestError:
    description: An error encountered while executing a work request.
    properties:
      code:
        description: 'A machine-usable code for the error that occured. Error codes are listed in

          [API Errors](/iaas/Content/API/References/apierrors.htm).

          '
        type: string
      message:
        description: A human-readable description of the issue encountered.
        type: string
      timestamp:
        description: The time the error occured. An RFC 3339-formatted date and time string.
        format: date-time
        type: string
    required:
    - code
    - message
    - timestamp
  WorkRequestErrorCollection:
    description: Result of a query request for a list of work request errors. Contains WorkRequestError
      items.
    properties:
      items:
        description: Array containing WorkRequestError items.
        items:
          $ref: '#/definitions/WorkRequestError'
        type: array
    required:
    - items
    type: object
  WorkRequestLogEntry:
    description: A log message from the execution of a work request.
    properties:
      message:
        description: Human-readable log message.
        type: string
      timestamp:
        description: The time the log message was written. An RFC 3339-formatted date and time string.
        format: date-time
        type: string
    required:
    - message
    - timestamp
  WorkRequestLogEntryCollection:
    description: Result of a query request for a list of work request log entries. Contains WorkRequestLogEntry
      items.
    properties:
      items:
        description: Array containing WorkRequestLogEntry items.
        items:
          $ref: '#/definitions/WorkRequestLogEntry'
        type: array
    required:
    - items
    type: object
  WorkRequestResource:
    description: A resource created or operated on by a work request.
    properties:
      actionType:
        description: 'The way in which this resource is affected by the work tracked in the work request.

          A resource being created, updated, or deleted will remain in the IN_PROGRESS state until

          work is complete for that resource, at which point it will transition to CREATED, UPDATED,

          or DELETED, respectively.

          '
        enum: *id017
        type: string
        x-obmcs-top-level-enum: '#/definitions/ActionType'
      entityType:
        description: The resource type the work request affects.
        type: string
      entityUri:
        description: The URI path that the user can do a GET on to access the resource metadata.
        type: string
      identifier:
        description: The identifier of the resource the work request affects.
        type: string
    required:
    - actionType
    - entityType
    - identifier
  WorkRequestSummary:
    description: A summary of work request status.
    properties:
      compartmentId:
        description: 'The OCID of the compartment that contains the work request.

          '
        type: string
      id:
        description: The ID of the work request.
        type: string
      operationType:
        description: Type of the work request.
        enum: *id015
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: Percentage of the request completed.
        format: float
        type: number
      resources:
        description: The resources affected by this work request.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: Status of current work request.
        enum: *id016
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: 'The date and time the request was created, as described in

          [RFC 3339](https://tools.ietf.org/rfc/rfc3339), section 14.29.

          '
        format: date-time
        type: string
      timeFinished:
        description: 'The date and time the object was finished, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
      timeStarted:
        description: 'The date and time the request was started, as described in [RFC 3339](https://tools.ietf.org/rfc/rfc3339),

          section 14.29.

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
parameters:
  ChildTenancyIdQueryParam:
    description: The ID of the child tenancy this link is associated with.
    in: query
    maxLength: 255
    name: childTenancyId
    required: false
    type: string
    x-default-description: 'null'
  ClassicSubscriptionIdOptionalQueryParam:
    description: The classic subscription ID.
    in: query
    maxLength: 255
    minLength: 1
    name: classicSubscriptionId
    required: false
    type: string
    x-default-description: 'null'
  CompartmentIdOptionalQueryParam:
    description: OCID of the compartment. Always a tenancy OCID.
    in: query
    maxLength: 255
    name: compartmentId
    required: false
    type: string
    x-default-description: 'null'
  CompartmentIdQueryParam:
    description: OCID of the compartment. Always a tenancy OCID.
    in: query
    maxLength: 255
    name: compartmentId
    required: true
    type: string
  DisplayNameQueryParam:
    description: A filter to return only resources that match the entire display name given.
    in: query
    maxLength: 255
    name: displayName
    type: string
    x-default-description: 'null'
  DomainGovernanceIdPathParam:
    description: The domain governance OCID.
    in: path
    maxLength: 255
    name: domainGovernanceId
    required: true
    type: string
  DomainGovernanceIdQueryParam:
    description: The domain governance OCID.
    in: query
    maxLength: 255
    name: domainGovernanceId
    required: false
    type: string
    x-default-description: 'null'
  DomainIdPathParam:
    description: The domain OCID.
    in: path
    maxLength: 255
    name: domainId
    required: true
    type: string
  DomainIdQueryParam:
    description: The domain OCID.
    in: query
    maxLength: 255
    name: domainId
    required: false
    type: string
    x-default-description: 'null'
  DomainStatusQueryParam:
    description: The status of the domain.
    in: query
    name: status
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/Domain/status'
  EntitlementIdQueryParam:
    description: Parameters for deleting an entitlement
    in: query
    name: entitlementId
    required: false
    type: string
    x-default-description: 'null'
  IfMatchHeader:
    description: 'For optimistic concurrency control. In the PUT or DELETE call

      for a resource, set the `if-match` parameter to the value of the

      etag from a previous GET or POST response for that resource.

      The resource will be updated or deleted only if the etag you

      provide matches the resource''s current etag value.

      '
    in: header
    name: if-match
    required: false
    type: string
  LifecycleStateQueryParam:
    description: The lifecycle state of the resource.
    enum: *id010
    in: query
    name: lifecycleState
    type: string
    x-default-description: 'null'
    x-obmcs-top-level-enum: '#/definitions/LifecycleState'
  NameQueryParam:
    description: A filter to return only resources that exactly match the name given.
    in: query
    maxLength: 255
    name: name
    required: false
    type: string
    x-default-description: 'null'
  OrderActivationToken:
    description: 'Activation token containing an order ID. A JWT RFC 7519-formatted string.

      '
    in: path
    name: activationToken
    required: true
    type: string
  PaginationLimitQueryParam:
    default: 100
    description: The maximum number of items to return.
    in: query
    maximum: 1000
    minimum: 1
    name: limit
    type: integer
  PaginationTokenQueryParam:
    description: The page token representing the page at which to start retrieving results. This is usually
      retrieved from a previous list call.
    in: query
    minLength: 1
    name: page
    type: string
    x-default-description: 'null'
  ParentTenancyIdQueryParam:
    description: The ID of the parent tenancy this link is associated with.
    in: query
    maxLength: 255
    name: parentTenancyId
    required: false
    type: string
    x-default-description: 'null'
  RecipientInvitationStatusQueryParam:
    description: The status of the recipient invitation.
    enum: *id013
    in: query
    name: status
    type: string
    x-default-description: 'null'
    x-obmcs-top-level-enum: '#/definitions/RecipientInvitationStatus'
  RecipientTenancyIdQueryParam:
    description: The tenancy that the invitation is addressed to.
    in: query
    maxLength: 255
    minLength: 1
    name: recipientTenancyId
    type: string
    x-default-description: 'null'
  RequestIdHeader:
    description: The client request ID for tracing.
    in: header
    name: opc-request-id
    type: string
  RetryTokenHeader:
    description: 'A token that uniquely identifies a request, so it can be retried in case of a timeout
      or

      server error, without risk of executing that same action again. Retry tokens expire after 24

      hours, but can be invalidated before then due to conflicting operations. For example, if a resource

      has been deleted and purged from the system, then a retry of the original creation request

      will be rejected.

      '
    in: header
    maxLength: 64
    minLength: 1
    name: opc-retry-token
    required: false
    type: string
  SenderInvitationStatusQueryParam:
    description: The status of the sender invitation.
    enum: *id014
    in: query
    name: status
    type: string
    x-default-description: 'null'
    x-obmcs-top-level-enum: '#/definitions/SenderInvitationStatus'
  SenderTenancyIdQueryParam:
    description: The tenancy that sent the invitation.
    in: query
    maxLength: 255
    name: senderTenancyId
    type: string
    x-default-description: 'null'
  SortByQueryParam:
    default: timeCreated
    description: 'The field to sort by. Only one sort order can be provided.

      * The default order for timeCreated is descending.

      * The default order for displayName is ascending.

      * If no value is specified, timeCreated is the default.

      '
    enum:
    - timeCreated
    - displayName
    in: query
    name: sortBy
    type: string
  SortOrderQueryParam:
    description: The sort order to use, whether 'asc' or 'desc'.
    enum: *id018
    in: query
    name: sortOrder
    type: string
    x-default-description: The default order depends on what parameter is being sorted by.
    x-obmcs-top-level-enum: '#/definitions/SortOrder'
  SubscriptionIdOptionalQueryParam:
    description: The ID of the subscription to which the tenancy is associated.
    in: query
    maxLength: 255
    minLength: 1
    name: subscriptionId
    required: false
    type: string
    x-default-description: 'null'
  SubscriptionIdPathParam:
    description: The ID of the subscription to which the tenancy is associated.
    in: path
    maxLength: 255
    minLength: 1
    name: subscriptionId
    required: true
    type: string
    x-default-description: 'null'
  SubscriptionIdQueryParam:
    description: The ID of the subscription to which the tenancy is associated.
    in: query
    maxLength: 255
    minLength: 1
    name: subscriptionId
    required: true
    type: string
    x-default-description: 'null'
  SubscriptionMappingIdOptionalQueryParam:
    description: A unique ID for subscription and tenancy mapping.
    in: query
    maxLength: 255
    minLength: 1
    name: subscriptionMappingId
    required: false
    type: string
    x-default-description: 'null'
  SubscriptionMappingLifecycleStateQueryParam:
    description: The lifecycle state of the resource.
    in: query
    name: lifecycleState
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/SubscriptionMapping/lifecycleState'
  WorkRequestIdPathParam:
    description: The ID of the asynchronous request.
    in: path
    name: workRequestId
    required: true
    type: string
responses:
  400:
    description: Bad Request
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  401:
    description: Unauthorized
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  404:
    description: Not Found
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  405:
    description: Method Not Allowed
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  409:
    description: Conflict
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  412:
    description: Precondition Failed
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  429:
    description: Too Many Requests
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  500:
    description: Internal Server Error
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  default:
    description: Unknown Error
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
x-anchors:
  x-headers:
    etag: *id003
    opc-next-page: *id002
    opc-request-id: *id001
    opc-work-request-id: *id004
    retry-after: *id019
  x-properties:
    definedTags: *id006
    freeformTags: *id007
    systemTags: *id008
x-obmcs-client-retries-enabled: false
x-oracle-package: com.oracle.accmgmt.tenantmgr.controlplane.v1
