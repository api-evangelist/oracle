swagger: '2.0'
info:
  description: Use Oracle Streaming with Apache Kafka Control Plane API to create/update/delete managed
    Kafka clusters.
  title: Oracle Streaming with Apache Kafka (OSAK) API
  version: '20240901'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/20737ba1cc15ca6941bfbbe2c9c672c4c8aac64b84d4892caf422bdd2f125081.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the Oracle Streaming with Apache Kafka (OSAK) API OCI
      service and stored verbatim; API Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/20737ba1cc15ca6941bfbbe2c9c672c4c8aac64b84d4892caf422bdd2f125081.yaml
    what: the harvested document for Oracle Streaming with Apache Kafka (OSAK) API
host: 127.0.0.1
basePath: /20240901
schemes:
- http
- https
produces:
- application/json
paths:
  /addonOptions:
    get:
      description: Gets a list of supported KafkaClusterAddons.
      operationId: ListAddonOptions
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByTimeCreatedQueryParam'
      responses:
        200:
          description: A page of AddonOptionSummary objects.
          headers:
            opc-next-page: &id001
              description: 'For list pagination. When this header appears in the response, additional
                pages of results remain. For

                important details about how pagination works, see [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

                '
              type: string
            opc-request-id: &id002
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/AddonOptionCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of supported KafkaClusterAddons.
      tags: &id017
      - kafkaCluster
  /kafkaClusterConfigs:
    get:
      description: 'Gets a list of KafkaClusterConfigs.

        '
      operationId: ListKafkaClusterConfigs
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/ConfigLifecycleStateQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/KafkaClusterConfigIdentifierQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of KafkaClusterConfigSummary objects.
          headers:
            opc-next-page: *id001
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaClusterConfigCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of KafkaClusterConfigs.
      tags: &id003
      - kafkaCluster
      x-related-resource: '#/definitions/KafkaClusterConfig'
    post:
      description: 'Creates a KafkaClusterConfig.

        '
      operationId: CreateKafkaClusterConfig
      parameters:
      - description: Details for the new KafkaClusterConfig.
        in: body
        name: CreateKafkaClusterConfigDetails
        required: true
        schema:
          $ref: '#/definitions/CreateKafkaClusterConfigDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The KafkaClusterConfig created.
          headers:
            content-location:
              description: Same as location.
              format: uri-reference
              type: string
            etag: &id004
              description: 'For optimistic concurrency control. See `if-match`.

                '
              type: string
            location:
              description: URL for the created kafkaCluster. The kafkaCluster OCID is generated after
                this request is sent.
              format: uri-reference
              type: string
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaClusterConfig'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a KafkaClusterConfig.
      tags: *id003
      x-related-resource: '#/definitions/KafkaClusterConfig'
  /kafkaClusterConfigs/{kafkaClusterConfigId}:
    delete:
      description: Deletes a KafkaClusterConfig.
      operationId: DeleteKafkaClusterConfig
      parameters:
      - $ref: '#/parameters/KafkaClusterConfigIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: The KafkaClusterConfig was deleted.
          headers:
            opc-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Deletes a KafkaClusterConfig.
      tags: *id003
      x-related-resource: '#/definitions/KafkaClusterConfig'
    get:
      description: Gets information about a KafkaClusterConfig.
      operationId: GetKafkaClusterConfig
      parameters:
      - $ref: '#/parameters/KafkaClusterConfigIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The KafkaClusterConfig was retrieved.
          headers:
            etag: *id004
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaClusterConfig'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get KafkaClusterConfig
      tags: *id003
    put:
      description: Updates a KafkaClusterConfig.
      operationId: UpdateKafkaClusterConfig
      parameters:
      - $ref: '#/parameters/KafkaClusterConfigIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateKafkaClusterConfigDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateKafkaClusterConfigDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The KafkaClusterConfig was updated.
          headers:
            etag: *id004
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaClusterConfig'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/405'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates a KafkaClusterConfig.
      tags: *id003
      x-related-resource: '#/definitions/KafkaClusterConfig'
  /kafkaClusterConfigs/{kafkaClusterConfigId}/actions/changeCompartment:
    post:
      description: 'Moves a KafkaClusterConfig into a different compartment within the same tenancy. For
        information about moving resources between

        compartments, see [Moving Resources to a Different Compartment](/iaas/Content/Identity/Tasks/managingcompartments.htm#moveRes).

        '
      operationId: ChangeKafkaClusterConfigCompartment
      parameters:
      - $ref: '#/parameters/KafkaClusterConfigIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: ChangeKafkaClusterConfigCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeKafkaClusterConfigCompartmentDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: The KafkaClusterConfig was moved into a different compartment.
          headers:
            opc-request-id: *id002
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Moves a KafkaClusterConfig into a different compartment.
      tags: *id003
      x-related-resource: '#/definitions/KafkaClusterConfig'
  /kafkaClusterConfigs/{kafkaClusterConfigId}/versions:
    get:
      description: 'Gets a list of KafkaClusterConfig Versions.

        '
      operationId: ListKafkaClusterConfigVersions
      parameters:
      - $ref: '#/parameters/KafkaClusterConfigIdentifierPathParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/KafkaClusterConfigVersionQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of kafka cluster config's version summary objects.
          headers:
            opc-next-page: *id001
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaClusterConfigVersionCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of KafkaClusterConfig Versions.
      tags: *id003
  /kafkaClusterConfigs/{kafkaClusterConfigId}/versions/{versionNumber}:
    delete:
      description: Deletes a KafkaClusterConfig version.
      operationId: DeleteKafkaClusterConfigVersion
      parameters:
      - $ref: '#/parameters/KafkaClusterConfigIdentifierPathParam'
      - $ref: '#/parameters/KafkaClusterConfigVersionPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: The KafkaCluster config version was deleted.
          headers:
            opc-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Deletes a KafkaClusterConfig version.
      tags: *id003
      x-related-resource: '#/definitions/KafkaClusterConfigVersion'
    get:
      description: Gets information about a KafkaClusterConfig version.
      operationId: GetKafkaClusterConfigVersion
      parameters:
      - $ref: '#/parameters/KafkaClusterConfigIdentifierPathParam'
      - $ref: '#/parameters/KafkaClusterConfigVersionPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The KafkaClusterConfig version was retrieved.
          headers:
            etag: *id004
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaClusterConfigVersion'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get KafkaClusterConfig version
      tags: *id003
  /kafkaClusters:
    get:
      description: 'Gets a list of KafkaClusters.

        '
      operationId: ListKafkaClusters
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/ClusterLifecycleStateQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/KafkaClusterIdentifierQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of KafkaClusterSummary objects.
          headers:
            opc-next-page: *id001
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaClusterCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of KafkaClusters.
      tags: &id005
      - kafkaCluster
      x-related-resource: '#/definitions/KafkaCluster'
    post:
      description: 'Creates a KafkaCluster.

        '
      operationId: CreateKafkaCluster
      parameters:
      - description: Details for the new KafkaCluster.
        in: body
        name: CreateKafkaClusterDetails
        required: true
        schema:
          $ref: '#/definitions/CreateKafkaClusterDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        201:
          description: Accepted the request. The KafkaCluster will be created.
          headers:
            content-location:
              description: Same as location.
              format: uri-reference
              type: string
            etag: *id004
            location:
              description: URL for the created kafkaCluster. The kafkaCluster OCID is generated after
                this request is sent.
              format: uri-reference
              type: string
            opc-request-id: *id002
            opc-work-request-id: &id006
              description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the asynchronous
                work request.

                Use GetWorkRequest with this ID to track the status of the request.

                '
              type: string
          schema:
            $ref: '#/definitions/KafkaCluster'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a KafkaCluster.
      tags: *id005
      x-related-resource: '#/definitions/KafkaCluster'
  /kafkaClusters/{kafkaClusterId}:
    delete:
      description: Deletes a KafkaCluster.
      operationId: DeleteKafkaCluster
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: Accepted the request. The KafkaCluster will be deleted.
          headers:
            opc-request-id: *id002
            opc-work-request-id: *id006
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Deletes a KafkaCluster.
      tags: *id005
      x-related-resource: '#/definitions/KafkaCluster'
    get:
      description: Gets information about a KafkaCluster.
      operationId: GetKafkaCluster
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The KafkaCluster was retrieved.
          headers:
            etag: *id004
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaCluster'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get KafkaCluster
      tags: *id005
    put:
      description: Updates a KafkaCluster.
      operationId: UpdateKafkaCluster
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateKafkaClusterDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateKafkaClusterDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: Accepted the request. The KafkaCluster will be updated.
          headers:
            etag: *id004
            opc-request-id: *id002
            opc-work-request-id: *id006
          schema:
            $ref: '#/definitions/KafkaCluster'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates a KafkaCluster.
      tags: *id005
      x-related-resource: '#/definitions/KafkaCluster'
  /kafkaClusters/{kafkaClusterId}/actions/changeCompartment:
    post:
      description: 'Moves a KafkaCluster into a different compartment within the same tenancy. For information
        about moving resources between

        compartments, see [Moving Resources to a Different Compartment](/iaas/Content/Identity/Tasks/managingcompartments.htm#moveRes).

        '
      operationId: ChangeKafkaClusterCompartment
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: ChangeKafkaClusterCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeKafkaClusterCompartmentDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: Accepted the request. The KafkaCluster will be moved into a different compartment.
          headers:
            opc-request-id: *id002
            opc-work-request-id: *id006
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Moves a KafkaCluster into a different compartment.
      tags: *id005
      x-related-resource: '#/definitions/KafkaCluster'
  /kafkaClusters/{kafkaClusterId}/actions/disableSuperuser:
    post:
      description: 'Removes the SASL superuser to the Kafka cluster.

        '
      operationId: DisableSuperuser
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        202:
          description: Accepted the request. The superuser will be removed from the cluster.
          headers:
            opc-request-id: *id002
            opc-work-request-id: *id006
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Disables the Kafka cluster SASL superuser
      tags: *id005
      x-obmcs-terraform:
        actionType: DISABLE
        identifier: Superuser
      x-related-resource: '#/definitions/KafkaCluster'
  /kafkaClusters/{kafkaClusterId}/actions/enableSuperuser:
    post:
      description: 'Adds a SASL superuser to the Kafka cluster. A generated password will be updated to
        the specified vault.

        '
      operationId: EnableSuperuser
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: EnableSuperuserDetails
        required: true
        schema:
          $ref: '#/definitions/EnableSuperuserDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        202:
          description: Accepted the request. A superuser will be added to the cluster.
          headers:
            opc-request-id: *id002
            opc-work-request-id: *id006
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Enables the Kafka cluster SASL superuser
      tags: *id005
      x-obmcs-terraform:
        actionType: ENABLE
        identifier: Superuser
      x-related-resource: '#/definitions/KafkaCluster'
  /kafkaClusters/{kafkaClusterId}/addons:
    get:
      description: Gets a list of KafkaClusterAddons.
      operationId: ListAddons
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/AddonLifecycleStateQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/NameFilterQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByTimeCreatedQueryParam'
      responses:
        200:
          description: A page of AddonSummary objects.
          headers:
            opc-next-page: *id001
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/AddonCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of KafkaClusterAddons.
      tags: *id005
      x-related-resource: '#/definitions/KafkaCluster'
    post:
      description: Installs a KafkaClusterAddon.
      operationId: InstallAddon
      parameters:
      - description: Details for the new KafkaClusterAddon.
        in: body
        name: InstallAddonDetails
        required: true
        schema:
          $ref: '#/definitions/InstallAddonDetails'
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/IfMatchHeader'
      responses:
        202:
          description: Accepted the request. The KafkaClusterAddon will be installed.
          headers:
            etag: *id004
            opc-request-id: *id002
            opc-work-request-id: *id006
          schema:
            $ref: '#/definitions/KafkaClusterAddon'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Installs a KafkaClusterAddon.
      tags: *id005
      x-obmcs-terraform:
        actionType: SPECIAL_UPDATE
      x-related-resource: '#/definitions/KafkaCluster'
  /kafkaClusters/{kafkaClusterId}/addons/{addonName}:
    delete:
      description: Uninstalls a KafkaClusterAddon in a provisioned cluster.
      operationId: UninstallAddon
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - $ref: '#/parameters/AddonNamePathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: The KafkaClusterAddon was uninstalled.
          headers:
            opc-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Uninstalls a KafkaClusterAddon.
      tags: *id005
      x-related-resource: '#/definitions/KafkaCluster'
    get:
      description: Gets information about a KafkaClusterAddon.
      operationId: GetAddon
      parameters:
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - $ref: '#/parameters/AddonNamePathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The KafkaClusterAddon was retrieved.
          headers:
            etag: *id004
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/KafkaClusterAddon'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get KafkaClusterAddon
      tags: *id005
      x-related-resource: '#/definitions/KafkaCluster'
    put:
      description: Installs updates on the existing KafkaClusterAddon.
      operationId: UpdateAddon
      parameters:
      - description: The information to be updated.
        in: body
        name: UpdateAddonDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateAddonDetails'
      - $ref: '#/parameters/KafkaClusterIdentifierPathParam'
      - $ref: '#/parameters/AddonNamePathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The KafkaClusterAddon was updated.
          headers:
            etag: *id004
            opc-request-id: *id002
            opc-work-request-id: *id006
          schema:
            $ref: '#/definitions/KafkaClusterAddon'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/405'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates a KafkaClusterAddon.
      tags: *id005
      x-related-resource: '#/definitions/KafkaCluster'
  /nodeShapes:
    get:
      description: Returns the list of shapes allowed in the region.
      operationId: ListNodeShapes
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/NameFilterQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      responses:
        200:
          description: A list of available shapes.
          headers:
            opc-next-page: *id001
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/NodeShapeCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of all possible node shapes
      tags: &id018
      - kafkaCluster
      x-related-resource: '#/definitions/KafkaCluster'
  /workRequests:
    get:
      description: 'Lists the work requests in a compartment.

        '
      operationId: ListWorkRequests
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/WorkRequestIdQueryParam'
      - $ref: '#/parameters/WorkRequestStatusQueryParam'
      - $ref: '#/parameters/ResourceIdentifierQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/WorkRequestSortByQueryParam'
      responses:
        200:
          description: The list of work requests was retrieved.
          headers:
            opc-next-page: *id001
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/WorkRequestSummaryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List work requests
      tags: &id007
      - kafkaCluster
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}:
    delete:
      description: Cancels a work request.
      operationId: CancelWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The work request will be canceled.
          headers:
            opc-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Cancel work request
      tags: *id007
      x-related-resource: '#/definitions/WorkRequest'
    get:
      description: Gets the details of a work request.
      operationId: GetWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The work request was retrieved.
          headers:
            etag: *id004
            opc-request-id: *id002
            retry-after: &id016
              description: A decimal number representing the number of seconds the client should wait
                before polling this endpoint again.
              type: integer
          schema:
            $ref: '#/definitions/WorkRequest'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get work request status
      tags: *id007
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}/errors:
    get:
      description: 'Lists the errors for a work request.

        '
      operationId: ListWorkRequestErrors
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/WorkRequestLogAndErrorSortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: The list of work request errors was retrieved.
          headers:
            opc-next-page: *id001
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/WorkRequestErrorCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List work request errors
      tags: *id007
      x-related-resource: '#/definitions/WorkRequestError'
  /workRequests/{workRequestId}/logs:
    get:
      description: 'Lists the logs for a work request.

        '
      operationId: ListWorkRequestLogs
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/WorkRequestLogAndErrorSortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: The list of work request logs.
          headers:
            opc-next-page: *id001
            opc-request-id: *id002
          schema:
            $ref: '#/definitions/WorkRequestLogEntryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List work request logs
      tags: *id007
      x-related-resource: '#/definitions/WorkRequestLogEntry'
definitions:
  ActionType:
    description: Possible types of actions.
    enum: &id013
    - CREATED
    - UPDATED
    - DELETED
    - IN_PROGRESS
    - RELATED
    - FAILED
    type: string
  AddonCollection:
    description: 'Results of a kafkaClusterAddon search. Contains both AddonSummary items and other information.

      '
    properties:
      items:
        description: List of kafkaClusterAddons.
        items:
          $ref: '#/definitions/AddonSummary'
        type: array
    required:
    - items
    type: object
  AddonOptionCollection:
    description: 'Results of a AddonOptions search. Contains AddonOptionSummary items

      '
    properties:
      items:
        description: List of kafka Cluster AddonOptions.
        items:
          $ref: '#/definitions/AddonOptionSummary'
        type: array
    required:
    - items
    type: object
  AddonOptionSummary:
    description: Summary information about a AddonOptions.
    properties:
      lifecycleState:
        description: The current state of the KafkaClusterAddon.
        enum:
        - ACTIVE
        - INACTIVE
        type: string
      name:
        description: A user-friendly name.
        type: string
    required:
    - name
    - lifecycleState
    type: object
  AddonSummary:
    description: Summary information about a KafkaClusterAddon.
    properties:
      addonType:
        description: Addon Type
        type: string
        x-obmcs-enumref: '#/definitions/KafkaClusterAddon/addonType'
      lifecycleDetails:
        description: 'A message that describes the current state of the KafkaClusterAddon in more detail.
          For example,

          can be used to provide actionable information for a resource in the Failed state.

          '
        type: string
      lifecycleState:
        description: The current state of the KafkaClusterAddon.
        type: string
        x-obmcs-enumref: '#/definitions/KafkaClusterAddon/lifecycleState'
      name:
        description: A user-friendly name.
        type: string
      timeCreated:
        description: "The date and time the KafkaClusterAddon was created, in the format defined by \n\
          [RFC 3339](https://tools.ietf.org/html/rfc3339).\n\nExample: `2016-08-25T21:10:29.600Z`\n"
        format: date-time
        type: string
      timeUpdated:
        description: "The date and time the KafkaClusterAddon was updated, in the format defined by \n\
          [RFC 3339](https://tools.ietf.org/html/rfc3339).\n\nExample: `2016-08-25T21:10:29.600Z`\n"
        format: date-time
        type: string
    required:
    - name
    - addonType
    - lifecycleState
    type: object
  BootstrapUrl:
    description: Kafka bootstrap URL
    properties:
      name:
        description: Name of the Kafka listener providing this bootstrap URL
        type: string
      url:
        description: Bootstrap URL
        type: string
    required:
    - name
    - url
    type: object
  BrokerShape:
    description: Configuration of the broker node.
    properties:
      nodeCount:
        description: Number of Kafka broker nodes
        minimum: 1
        type: integer
      nodeShape:
        description: Node shape for broker is passed as part of cluster creation, similar to VM.Standard.A1.Flex
        type: string
      ocpuCount:
        description: Number of OCPUs per nodes
        maximum: 80
        minimum: 1
        type: integer
      storageSizeInGbs:
        default: 50
        description: Size of the storage per nodes.
        maximum: 16000
        minimum: 50
        multipleOf: 50
        type: integer
    required:
    - nodeCount
    - ocpuCount
    type: object
  ChangeKafkaClusterCompartmentDetails:
    description: The configuration details for the move operation.
    properties:
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment to
          move the KafkaCluster to.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    type: object
  ChangeKafkaClusterConfigCompartmentDetails:
    description: The configuration details for the move operation.
    properties:
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment to
          move the KafkaClusterConfig to.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    type: object
  CreateKafkaClusterConfigDetails:
    description: The data to create a KafkaClusterConfig.
    properties:
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment to
          create the KafkaClusterConfig in.

          '
        maxLength: 255
        minLength: 1
        type: string
      definedTags: &id008
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.


            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/iaas/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: &id009
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no predefined
          name, type, or namespace.

          For more information, see [Resource Tags](/iaas/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      latestConfig:
        $ref: '#/definitions/KafkaClusterConfigVersion'
    required:
    - compartmentId
    - latestConfig
    type: object
  CreateKafkaClusterDetails:
    description: The data to create a KafkaCluster.
    properties:
      accessSubnets:
        description: Subnets where broker/coordinator VNICs will be created.
        items:
          $ref: '#/definitions/SubnetSet'
        type: array
      brokerShape:
        $ref: '#/definitions/BrokerShape'
      clientCertificateBundle:
        description: CA certificate bundle for mTLS broker authentication.
        type: string
      clusterConfigId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of Kafka Cluster configuration
          object
        type: string
      clusterConfigVersion:
        description: The version of configuration object
        type: integer
      clusterType:
        description: "Type of the cluster to spin up. \nDEVELOPMENT - setting that allows to sacrifice\
          \ HA and spin up cluster on single node\nPRODUCTION - Minimum allowed broker count is 3\n"
        type: string
        x-obmcs-enumref: '#/definitions/KafkaCluster/clusterType'
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment to
          create the KafkaCluster in.

          '
        maxLength: 255
        minLength: 1
        type: string
      coordinationType:
        description: Kafka coordination type. Set of available types depends on Kafka version
        type: string
        x-obmcs-enumref: '#/definitions/KafkaCluster/coordinationType'
      definedTags: *id008
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id009
      kafkaVersion:
        description: Version of Kafka to use to spin up the cluster
        type: string
    required:
    - compartmentId
    - accessSubnets
    - kafkaVersion
    - brokerShape
    - clusterType
    - clusterConfigId
    - clusterConfigVersion
    - coordinationType
    type: object
  EnableSuperuserDetails:
    description: The configuration details for the enable superuser operation.
    properties:
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment that
          contains the superuser secret.
        maxLength: 255
        minLength: 1
        type: string
      secretId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the secret that will
          be populated with the generated superuser password.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    - secretId
    type: object
  Error:
    description: Error information.
    properties:
      code:
        description: A short error code that defines the error, meant for programmatic parsing.
        type: string
      message:
        description: A human-readable error message.
        type: string
    required:
    - code
    - message
  InstallAddonDetails:
    description: The data to create a KafkaClusterAddon.
    discriminator: addonType
    properties:
      addonType:
        description: This is Addon Type of OCI kafka cluster
        type: string
        x-obmcs-enumref: '#/definitions/KafkaClusterAddon/addonType'
      description:
        description: A brief description of the add on being installed.
        type: string
      name:
        description: A unique user-friendly name. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
    required:
    - name
    - addonType
  InstallPublicConnectivityAddonDetails:
    allOf:
    - $ref: '#/definitions/InstallAddonDetails'
    - discriminator: PUBLICCONNECTIVITY
      properties:
        authenticationMechanism:
          description: Authentication mechanism.
          type: string
          x-obmcs-enumref: '#/definitions/PublicConnectivityAddon/authenticationMechanism'
        networkCidrs:
          description: A list of CIDR's for ingress/egress traffic.
          items:
            maxLength: 255
            minLength: 1
            type: string
          type: array
      required:
      - authenticationMechanism
      - networkCidrs
      type: object
    description: The data to install a PublicConnectivityAddon.
  KafkaCluster:
    description: 'A KafkaCluster is a description of a KafkaCluster.


      To use any of the API operations, you must be authorized in an IAM policy. If you''re not authorized,
      talk to

      an administrator. If you''re an administrator who needs to write policies to give users access,
      see

      [Getting Started with Policies](/iaas/Content/Identity/policiesgs/get-started-with-policies.htm).

      '
    properties:
      accessSubnets:
        description: Subnets where broker/coordinator VNICs will be created.
        items:
          $ref: '#/definitions/SubnetSet'
        type: array
      brokerShape:
        $ref: '#/definitions/BrokerShape'
      clientCertificateBundle:
        description: CA certificate bundle for mTLS broker authentication.
        type: string
      clusterConfigId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of Kafka Cluster configuration
          object
        type: string
      clusterConfigVersion:
        description: The version of configuration object
        type: integer
      clusterType:
        description: "Type of the cluster to spin up. \nDEVELOPMENT - setting that allows to sacrifice\
          \ HA and spin up cluster on a single node\nPRODUCTION - Minimum allowed broker count is 3\n"
        enum:
        - DEVELOPMENT
        - PRODUCTION
        type: string
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment.
        type: string
      coordinationType:
        description: Kafka coordination type. Set of available types depends on Kafka version
        enum:
        - ZOOKEEPER
        - KRAFT
        type: string
      definedTags: *id008
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable.
        type: string
      freeformTags: *id009
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaCluster.
        type: string
      kafkaBootstrapUrls:
        description: Bootstrap URL that can be used to connect to Kafka
        items:
          $ref: '#/definitions/BootstrapUrl'
        type: array
      kafkaVersion:
        description: Version of Kafka to use to spin up the cluster
        type: string
      lifecycleDetails:
        description: 'A message that describes the current state of the KafkaCluster in more detail. For
          example,

          can be used to provide actionable information for a resource in the Failed state.

          '
        type: string
      lifecycleState:
        description: The current state of the KafkaCluster.
        enum:
        - CREATING
        - UPDATING
        - ACTIVE
        - DELETING
        - DELETED
        - FAILED
        type: string
      secretId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the secret that contains
          superuser password.
        type: string
      systemTags: &id010
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a system tag key and value, scoped to a namespace.


            Example: `{"free-tier-retained": "true"}`

            '
          type: object
        description: 'System tags for this resource. Each key is predefined and scoped to a namespace.


          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
      timeCreated:
        description: 'The date and time the KafkaCluster was created, in the format defined by [RFC 3339](https://tools.ietf.org/html/rfc3339).


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeUpdated:
        description: 'The date and time the KafkaCluster was updated, in the format defined by [RFC 3339](https://tools.ietf.org/html/rfc3339).


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - timeCreated
    - displayName
    - freeformTags
    - definedTags
    - accessSubnets
    - kafkaVersion
    - brokerShape
    - clusterType
    - clusterConfigId
    - clusterConfigVersion
    type: object
  KafkaClusterAddon:
    description: The base data object to represent a KafkaClusterAddon.
    discriminator: addonType
    properties:
      addonType:
        description: The type of addon
        enum:
        - PUBLICCONNECTIVITY
        readOnly: true
        type: string
      description:
        description: Description of the add on
        type: string
      lifecycleState:
        description: The current state of the KafkaCluster.
        enum:
        - CREATING
        - UPDATING
        - ACTIVE
        - DELETING
        - DELETED
        - FAILED
        type: string
      name:
        description: A unique user-friendly name.
        type: string
      timeCreated:
        description: The time the addon was created.
        example: '2017-07-21T16:11:29Z'
        format: date-time
        type: string
      timeUpdated:
        description: The time the addon was updated.
        example: '2017-07-21T16:12:29Z'
        format: date-time
        type: string
    required:
    - name
    - addonType
    - lifecycleState
    type: object
  KafkaClusterCollection:
    description: Results of a kafkaCluster search. Contains both KafkaClusterSummary items and other information,
      such as metadata.
    properties:
      items:
        description: List of kafkaClusters.
        items:
          $ref: '#/definitions/KafkaClusterSummary'
        type: array
    required:
    - items
    type: object
  KafkaClusterConfig:
    description: 'A shared configuration object used by 0 or more kafka clusters.


      To use any of the API operations, you must be authorized in an IAM policy. If you''re not authorized,
      talk to

      an administrator. If you''re an administrator who needs to write policies to give users access,
      see

      [Getting Started with Policies](/iaas/Content/Identity/policiesgs/get-started-with-policies.htm).

      '
    properties:
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment.
        type: string
      definedTags: *id008
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable.
        type: string
      freeformTags: *id009
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaClusterConfig.
        type: string
      latestConfig:
        $ref: '#/definitions/KafkaClusterConfigVersion'
      lifecycleDetails:
        description: 'A message that describes the current state of the KafkaClusterConfig in more detail.
          For example,

          can be used to provide actionable information for a resource in the Failed state.

          '
        type: string
      lifecycleState:
        description: The current state of the KafkaClusterConfig.
        enum:
        - CREATING
        - ACTIVE
        - UPDATING
        - DELETED
        type: string
      systemTags: *id010
      timeCreated:
        description: 'The date and time the KafkaClusterConfig was created, in the format defined by [RFC
          3339](https://tools.ietf.org/html/rfc3339).


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeUpdated:
        description: 'The date and time the KafkaClusterConfig was updated, in the format defined by [RFC
          3339](https://tools.ietf.org/html/rfc3339).


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - timeCreated
    - displayName
    - freeformTags
    - definedTags
    - latestConfig
    type: object
  KafkaClusterConfigCollection:
    description: Results of a kafkaClusterConfig search. Contains both KafkaClusterConfigSummary items
      and other information, such as metadata.
    properties:
      items:
        description: List of kafkaClustersConfig.
        items:
          $ref: '#/definitions/KafkaClusterConfigSummary'
        type: array
    required:
    - items
    type: object
  KafkaClusterConfigSummary:
    description: Summary information about a KafkaClusterConfig.
    properties:
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment.
        type: string
      definedTags: *id008
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable.
        type: string
      freeformTags: *id009
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaClusterConfig.
        type: string
      lifecycleDetails:
        description: 'A message that describes the current state of the KafkaClusterConfig in more detail.
          For example,

          can be used to provide actionable information for a resource in the Failed state.

          '
        type: string
      lifecycleState:
        description: The current state of the KafkaClusterConfig.
        type: string
        x-obmcs-enumref: '#/definitions/KafkaClusterConfig/lifecycleState'
      systemTags: *id010
      timeCreated:
        description: 'The date and time the KafkaClusterConfig was created, in the format defined by [RFC
          3339](https://tools.ietf.org/html/rfc3339).


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeUpdated:
        description: 'The date and time the KafkaClusterConfig was updated, in the format defined by [RFC
          3339](https://tools.ietf.org/html/rfc3339).


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - timeCreated
    - displayName
    - freeformTags
    - definedTags
    type: object
  KafkaClusterConfigVersion:
    description: 'A shared configuration object used by 0 or more kafka clusters.

      '
    properties:
      configId:
        description: ID cluster configuration
        type: string
      properties:
        additionalProperties:
          type: string
        description: Cluster configuration key-value pairs
        maxProperties: 1000
        type: object
      timeCreated:
        description: 'The date and time the KafkaClusterConfigVersion was created, in the format defined
          by [RFC 3339](https://tools.ietf.org/html/rfc3339).

          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      versionNumber:
        description: Version of the cluster configuration
        type: integer
    required:
    - properties
    type: object
  KafkaClusterConfigVersionCollection:
    description: Results of a kafkaClusterConfigVersion search. Contains both KafkaClusterConfigVersionSummary
      items.
    properties:
      items:
        description: List of kafkaClustersConfigVersion.
        items:
          $ref: '#/definitions/KafkaClusterConfigVersionSummary'
        maxItems: 10
        minItems: 1
        type: array
    required:
    - items
    type: object
  KafkaClusterConfigVersionSummary:
    description: Summary information about a KafkaClusterConfigVersion.
    properties:
      configId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaClusterConfig.
        type: string
      timeCreated:
        description: 'The date and time the KafkaClusterConfigVersion was created, in the format defined
          by [RFC 3339](https://tools.ietf.org/html/rfc3339).

          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      versionNumber:
        description: Version of the cluster configuration
        type: integer
    required:
    - configId
    - versionNumber
    - timeCreated
    type: object
  KafkaClusterSummary:
    description: Summary information about a KafkaCluster.
    properties:
      accessSubnets:
        description: Subnets where broker/coordinator VNICs will be created.
        items:
          $ref: '#/definitions/SubnetSet'
        type: array
      brokerShape:
        $ref: '#/definitions/BrokerShape'
      clusterConfigId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of Kafka Cluster configuration
          object
        type: string
      clusterConfigVersion:
        description: The version of configuration object
        type: integer
      clusterType:
        description: "Type of the cluster to spin up. \nDEVELOPMENT - setting that allows to sacrifice\
          \ HA and spin up cluster on single node\nPRODUCTION - Minimum allowed broker count is 3\n"
        type: string
        x-obmcs-enumref: '#/definitions/KafkaCluster/clusterType'
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment.
        type: string
      coordinationType:
        description: Kafka coordination type. Set of available types depends on Kafka version
        type: string
        x-obmcs-enumref: '#/definitions/KafkaCluster/coordinationType'
      definedTags: *id008
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable.
        type: string
      freeformTags: *id009
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaCluster.
        type: string
      kafkaVersion:
        description: Version of Kafka to use to spin up the cluster
        type: string
      lifecycleDetails:
        description: 'A message that describes the current state of the KafkaCluster in more detail. For
          example,

          can be used to provide actionable information for a resource in the Failed state.

          '
        type: string
      lifecycleState:
        description: The current state of the KafkaCluster.
        type: string
        x-obmcs-enumref: '#/definitions/KafkaCluster/lifecycleState'
      systemTags: *id010
      timeCreated:
        description: 'The date and time the KafkaCluster was created, in the format defined by [RFC 3339](https://tools.ietf.org/html/rfc3339).


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeUpdated:
        description: 'The date and time the KafkaCluster was updated, in the format defined by [RFC 3339](https://tools.ietf.org/html/rfc3339).


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - timeCreated
    - displayName
    - freeformTags
    - definedTags
    - accessSubnets
    - kafkaVersion
    - brokerShape
    - clusterType
    - clusterConfigId
    - clusterConfigVersion
    type: object
  NodeShapeCollection:
    description: Results of a NodeShapeCollection search.
    properties:
      items:
        description: List of NodeShapeSummary.
        items:
          $ref: '#/definitions/NodeShapeSummary'
        type: array
    required:
    - items
    type: object
  NodeShapeSummary:
    description: Summary of the shape.
    properties:
      name:
        description: The name of the shape
        maxLength: 255
        minLength: 1
        type: string
    required:
    - name
    type: object
  OperationStatus:
    description: The status of the work request.
    enum: &id012
    - ACCEPTED
    - IN_PROGRESS
    - WAITING
    - NEEDS_ATTENTION
    - FAILED
    - SUCCEEDED
    - CANCELING
    - CANCELED
    type: string
  OperationType:
    description: The asynchronous operation tracked by the work request.
    enum: &id011
    - CREATE_KAFKA_CLUSTER
    - UPDATE_KAFKA_CLUSTER
    - DELETE_KAFKA_CLUSTER
    - MOVE_KAFKA_CLUSTER
    - ENABLE_SUPERUSER
    - DISABLE_SUPERUSER
    - INSTALL_KAFKA_CLUSTER_ADDON
    - UPDATE_KAFKA_CLUSTER_ADDON
    - UNINSTALL_KAFKA_CLUSTER_ADDON
    type: string
  PublicConnectivityAddon:
    allOf:
    - $ref: '#/definitions/KafkaClusterAddon'
    - discriminator: PUBLICCONNECTIVITY
      properties:
        authenticationMechanism:
          description: Authentication mechanism.
          enum:
          - SASL
          - MTLS
          type: string
        bootstrapUrl:
          description: The bootstrap url of the kafka cluster.
          type: string
        networkCidrs:
          description: A list of CIDR ranges for ingress/egress traffic.
          items:
            maxLength: 255
            minLength: 1
            type: string
          type: array
      required:
      - authenticationMechanism
      - bootstrapUrl
      - networkCidrs
      type: object
    description: The data that represents a Public Connectivity Addon
  SortOrder:
    description: 'The sort order to use, either ascending (`ASC`) or descending (`DESC`). The `displayName`

      sort order is case sensitive.

      '
    enum: &id015
    - ASC
    - DESC
    type: string
  SubnetSet:
    description: Summary information about a KafkaCluster.
    properties:
      subnets:
        description: Subnets OCIDs
        items:
          type: string
        maxItems: 10
        minItems: 1
        type: array
    required:
    - subnets
    type: object
  UpdateAddonDetails:
    description: The data to update a KafkaClusterAddon.
    discriminator: addonType
    properties:
      addonType:
        description: This is Addon Type of OCI kafka cluster
        readOnly: true
        type: string
        x-obmcs-enumref: '#/definitions/KafkaClusterAddon/addonType'
      description:
        description: A unique user-friendly name. Avoid entering confidential information.
        type: string
  UpdateKafkaClusterConfigDetails:
    description: The data to update a KafkaClusterConfig.
    properties:
      definedTags: *id008
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id009
      latestConfig:
        $ref: '#/definitions/KafkaClusterConfigVersion'
    type: object
  UpdateKafkaClusterDetails:
    description: The data to update a KafkaCluster.
    properties:
      accessSubnets:
        description: Subnets where broker/coordinator VNICs will be created.
        items:
          $ref: '#/definitions/SubnetSet'
        type: array
      brokerShape:
        $ref: '#/definitions/BrokerShape'
      clientCertificateBundle:
        description: CA certificate bundle for mTLS broker authentication.
        type: string
      clusterConfigId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of Kafka Cluster configuration
          object
        type: string
      clusterConfigVersion:
        description: The version of configuration object
        type: integer
      coordinationType:
        description: Kafka coordination type. Set of available types depends on Kafka version
        type: string
        x-obmcs-enumref: '#/definitions/KafkaCluster/coordinationType'
      definedTags: *id008
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id009
    type: object
  UpdatePublicConnectivityAddonDetails:
    allOf:
    - $ref: '#/definitions/UpdateAddonDetails'
    - discriminator: PUBLICCONNECTIVITY
      properties:
        networkCidrs:
          description: A list of CIDR ranges for ingress/egress traffic.
          items:
            maxLength: 255
            minLength: 1
            type: string
          type: array
      required:
      - networkCidrs
      type: object
    description: The data to update a KafkaClusterAddon.
  WorkRequest:
    description: 'An asynchronous work request. Work requests help you monitor long-running operations.
      When you start a long-running operation,

      the service creates a work request. A work request is an activity log that lets you track each step
      in the operation''s

      progress. Each work request has an OCID that lets you interact with it programmatically and use
      it for automation.

      '
    properties:
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment that
          contains the work request.

          '
        type: string
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
        type: string
      operationType:
        description: The asynchronous operation tracked by this work request.
        enum: *id011
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: 'Shows the progress of the operation tracked by the work request, as a percentage
          of the total work

          that must be performed.

          '
        format: float
        type: number
      resources:
        description: The resources that are affected by the work request.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: The status of the work request.
        enum: *id012
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: 'The date and time the work request was created, in the format defined by

          [RFC 3339](https://tools.ietf.org/html/rfc3339).

          '
        format: date-time
        type: string
      timeFinished:
        description: 'The date and time the work request was finished, in the format defined by

          [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
      timeStarted:
        description: 'The date and time the work request was started, in the format defined by

          [RFC 3339](https://tools.ietf.org/html/rfc3339).

          '
        format: date-time
        type: string
      timeUpdated:
        description: 'The date and time the work request was updated, in the format defined by

          [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
  WorkRequestError:
    description: An error encountered while performing an operation that is tracked by a work request.
    properties:
      code:
        description: 'A machine-usable code for the error that occurred. For a list of error codes, see

          [API Errors](/iaas/Content/API/References/apierrors.htm).

          '
        type: string
      message:
        description: A human-readable error message.
        type: string
      timestamp:
        description: 'The date and time the error occurred, in the format defined by

          [RFC 3339](https://tools.ietf.org/html/rfc3339).

          '
        format: date-time
        type: string
    required:
    - code
    - message
    - timestamp
  WorkRequestErrorCollection:
    description: A list of work request errors. Can contain both errors and other information, such as
      metadata.
    properties:
      items:
        description: A list of work request errors.
        items:
          $ref: '#/definitions/WorkRequestError'
        type: array
    required:
    - items
    type: object
  WorkRequestLogEntry:
    description: A log message from performing an operation that is tracked by a work request.
    properties:
      message:
        description: A human-readable log message.
        type: string
      timestamp:
        description: 'The date and time the log message was written, in the format defined by

          [RFC 3339](https://tools.ietf.org/html/rfc3339).

          '
        format: date-time
        type: string
    required:
    - message
    - timestamp
  WorkRequestLogEntryCollection:
    description: A list of work request logs. Can contain both logs and other information, such as metadata.
    properties:
      items:
        description: A list of work request log entries.
        items:
          $ref: '#/definitions/WorkRequestLogEntry'
        type: array
    required:
    - items
    type: object
  WorkRequestResource:
    description: A resource created or operated on by a work request.
    properties:
      actionType:
        description: 'The way in which this resource is affected by the operation tracked in the work
          request.

          A resource being created, updated, or deleted remains in the IN_PROGRESS state until

          work is complete for that resource, at which point it transitions to CREATED, UPDATED,

          or DELETED, respectively.

          '
        enum: *id013
        type: string
        x-obmcs-top-level-enum: '#/definitions/ActionType'
      entityType:
        description: The resource type that the work request affects.
        type: string
      entityUri:
        description: The URI path that you can use for a GET request to access the resource metadata.
        type: string
      identifier:
        description: An [OCID](/iaas/Content/General/Concepts/identifiers.htm) or other unique identifier
          for the resource.
        type: string
      metadata:
        additionalProperties:
          type: string
        description: Additional information that helps to explain the resource.
        enum: &id014
        - IS_DRY_RUN
        type: object
        x-obmcs-top-level-enum: '#/definitions/WorkRequestResourceMetadataKey'
    required:
    - actionType
    - entityType
    - identifier
  WorkRequestResourceMetadataKey:
    description: Possible metadata keys for work request resource metadata.
    enum: *id014
    type: string
  WorkRequestSummary:
    description: Summary information about an asynchronous work request.
    properties:
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment that
          contains the work request.

          '
        type: string
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the work request.
        type: string
      operationType:
        description: The asynchronous operation tracked by this work request.
        enum: *id011
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: 'Shows the progress of the operation tracked by the work request, as a percentage
          of the total work

          that must be performed.

          '
        format: float
        type: number
      resources:
        description: The resources that are affected by this work request.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: The status of the work request.
        enum: *id012
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: 'The date and time the work request was created, in the format defined by

          [RFC 3339](https://tools.ietf.org/html/rfc3339).

          '
        format: date-time
        type: string
      timeFinished:
        description: 'The date and time the work request was finished, in the format defined by

          [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
      timeStarted:
        description: 'The date and time the work request was started, in the format defined by

          [RFC 3339](https://tools.ietf.org/html/rfc3339).

          '
        format: date-time
        type: string
      timeUpdated:
        description: 'The date and time the work request was updated, in the format defined by

          [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
  WorkRequestSummaryCollection:
    description: A list of work requests. Can contain both work requests and other information, such as
      metadata.
    properties:
      items:
        description: A list of work requests.
        items:
          $ref: '#/definitions/WorkRequestSummary'
        type: array
    required:
    - items
    type: object
parameters:
  AddonLifecycleStateQueryParam:
    description: 'A filter to return only resources that match the given lifecycle state. The

      state value is case-insensitive.

      '
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/KafkaClusterAddon/lifecycleState'
  AddonNamePathParam:
    description: The unique name of the KafkaClusterAddon.
    in: path
    name: addonName
    required: true
    type: string
  ClusterLifecycleStateQueryParam:
    description: 'A filter to return only resources that match the given lifecycle state. The

      state value is case-insensitive.

      '
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/KafkaCluster/lifecycleState'
  CompartmentIdQueryParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the compartment in which
      to list resources.
    in: query
    name: compartmentId
    type: string
    x-default-description: 'null'
  ConfigLifecycleStateQueryParam:
    description: 'A filter to return only resources that match the given lifecycle state. The

      state value is case-insensitive.

      '
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/KafkaClusterConfig/lifecycleState'
  DisplayNameQueryParam:
    description: A filter to return only resources that match the given display name exactly.
    in: query
    maxLength: 255
    minLength: 1
    name: displayName
    type: string
    x-default-description: 'null'
  IfMatchHeader:
    description: 'For optimistic concurrency control. In the PUT or DELETE call for a resource, set the

      `if-match` parameter to the value of the etag from a previous GET or POST response for

      that resource. The resource will be updated or deleted only if the etag you provide

      matches the resource''s current etag value.

      '
    in: header
    name: if-match
    required: false
    type: string
  KafkaClusterConfigIdentifierPathParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaClusterConfig.
    in: path
    name: kafkaClusterConfigId
    required: true
    type: string
  KafkaClusterConfigIdentifierQueryParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaClusterConfig.
    in: query
    name: id
    type: string
    x-default-description: 'null'
  KafkaClusterConfigVersionPathParam:
    description: The versionNumber of the KafkaClusterConfig.
    in: path
    minimum: 1
    name: versionNumber
    required: true
    type: integer
  KafkaClusterConfigVersionQueryParam:
    default: versionNumber
    description: 'The field to sort by. You can provide only one sort order. Default order for `versionNumber`

      is descending.

      '
    enum:
    - versionNumber
    in: query
    name: sortBy
    type: string
  KafkaClusterIdentifierPathParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaCluster.
    in: path
    name: kafkaClusterId
    required: true
    type: string
  KafkaClusterIdentifierQueryParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the KafkaCluster.
    in: query
    name: id
    type: string
    x-default-description: 'null'
  NameFilterQueryParam:
    description: The name to filter on.
    in: query
    name: name
    type: string
  PaginationLimitQueryParam:
    default: 10
    description: 'For list pagination. The maximum number of results per page, or items to return in a

      paginated "List" call. For important details about how pagination works, see

      [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

      '
    in: query
    maximum: 1000
    minimum: 1
    name: limit
    type: integer
  PaginationTokenQueryParam:
    description: 'For list pagination. The value of the opc-next-page response header from the previous

      "List" call. For important details about how pagination works, see

      [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

      '
    in: query
    minLength: 1
    name: page
    type: string
  RequestIdHeader:
    description: 'Unique Oracle-assigned identifier for the request. If you need to contact

      Oracle about a particular request, please provide the request ID.

      The only valid characters for request IDs are letters, numbers,

      underscore, and dash.

      '
    in: header
    name: opc-request-id
    type: string
  ResourceIdentifierQueryParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the resource affected by
      the work request.
    in: query
    name: resourceId
    type: string
    x-default-description: 'null'
  RetryTokenHeader:
    description: 'A token that uniquely identifies a request so it can be retried in case of a timeout
      or

      server error without risk of running that same action again. Retry tokens expire after 24

      hours, but can be invalidated before then due to conflicting operations. For example, if a resource

      has been deleted and removed from the system, then a retry of the original creation request

      might be rejected.

      '
    in: header
    maxLength: 64
    minLength: 1
    name: opc-retry-token
    required: false
    type: string
  SortByQueryParam:
    default: timeCreated
    description: 'The field to sort by. You can provide only one sort order. Default order for `timeCreated`

      is descending. Default order for `displayName` is ascending.

      '
    enum:
    - timeCreated
    - displayName
    in: query
    name: sortBy
    type: string
  SortByTimeCreatedQueryParam:
    default: timeCreated
    description: 'The field to sort by. You can provide only one sort order. Default order for `timeCreated`

      is descending.

      '
    enum:
    - timeCreated
    in: query
    name: sortBy
    type: string
  SortOrderQueryParam:
    description: The sort order to use, either ascending (`ASC`) or descending (`DESC`).
    enum: *id015
    in: query
    name: sortOrder
    type: string
    x-default-description: 'The default value depends upon `sortBy`, and

      in general is `DESC` when sorting by time and `ASC` otherwise.

      '
    x-obmcs-top-level-enum: '#/definitions/SortOrder'
  WorkRequestIdPathParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the asynchronous work request.
    in: path
    name: workRequestId
    required: true
    type: string
  WorkRequestIdQueryParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm) of the asynchronous work request.
    in: query
    name: workRequestId
    type: string
    x-default-description: 'null'
  WorkRequestLogAndErrorSortByQueryParam:
    default: timestamp
    description: 'The field to sort by. Only one sort order may be provided. Default order for `timestamp`
      is descending.

      '
    enum:
    - timestamp
    in: query
    name: sortBy
    type: string
  WorkRequestSortByQueryParam:
    default: timeAccepted
    description: 'The field to sort by. Only one sort order may be provided. Default order for `timeAccepted`
      is descending.

      '
    enum:
    - timeAccepted
    in: query
    name: sortBy
    type: string
  WorkRequestStatusQueryParam:
    description: A filter to return only the resources that match the given lifecycle state.
    enum: *id012
    in: query
    name: status
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-top-level-enum: '#/definitions/OperationStatus'
responses:
  400:
    description: Bad Request
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
  401:
    description: Unauthorized
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
  404:
    description: Not Found
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
  405:
    description: Method Not Allowed
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
  409:
    description: Conflict
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
  412:
    description: Precondition Failed
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
  429:
    description: Too Many Requests
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
  500:
    description: Internal Server Error
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
  default:
    description: Unknown Error
    headers:
      opc-request-id: *id002
    schema:
      $ref: '#/definitions/Error'
x-anchors:
  x-headers:
    etag: *id004
    opc-next-page: *id001
    opc-request-id: *id002
    opc-work-request-id: *id006
    retry-after: *id016
  x-properties:
    definedTags: *id008
    freeformTags: *id009
    systemTags: *id010
  x-tag-groups:
    addons: *id017
    kafkaCluster: *id005
    kafkaClusterConfig: *id003
    shapes: *id018
    workRequest: *id007
x-obmcs-client-retries-enabled: true
x-oracle-package: com.oracle.oci.kafka
