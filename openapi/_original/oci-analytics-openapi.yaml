swagger: '2.0'
info:
  description: 'Analytics API.

    '
  license:
    name: Oracle Corporation
  title: Analytics API
  version: '20190331'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/9bcc53a4edfb39ed432d422fd992444b637d99197bac3f5aedeb0d97eb42188a.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the Analytics API OCI service and stored verbatim; API
      Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/9bcc53a4edfb39ed432d422fd992444b637d99197bac3f5aedeb0d97eb42188a.yaml
    what: the harvested document for Analytics API
basePath: /20190331
consumes:
- application/json
produces:
- application/json
paths:
  /analyticsInstances:
    get:
      description: 'List Analytics instances.

        '
      operationId: ListAnalyticsInstances
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/ResourceNameQueryParam'
      - $ref: '#/parameters/CapacityTypeQueryParam'
      - $ref: '#/parameters/FeatureSetQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      produces:
      - application/json
      responses:
        200:
          description: 'List of Analytics instances (paginated, see page and limit query parameters).

            '
          headers:
            opc-next-page: &id004
              description: 'For list pagination. When this header appears in the response, additional
                pages

                of results remain. For important details about how pagination works, see

                [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

                '
              maxLength: 1024
              minLength: 1
              type: string
            opc-request-id: &id001
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              type: string
          schema:
            items:
              $ref: '#/definitions/AnalyticsInstanceSummary'
            type: array
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: List Analytics instances.
      tags:
      - analytics
    post:
      description: 'Create a new AnalyticsInstance in the specified compartment. The operation is long-running

        and creates a new WorkRequest.

        '
      operationId: CreateAnalyticsInstance
      parameters:
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Analytics Instance details.

          '
        in: body
        name: CreateAnalyticsInstanceDetails
        required: true
        schema:
          $ref: '#/definitions/CreateAnalyticsInstanceDetails'
      responses:
        201:
          description: 'The Analytics instance was created. A background task is configuring various dependencies
            before the instance

            becomes fully operational.

            '
          headers:
            etag: &id003
              description: 'For optimistic concurrency control. See `if-match`.

                '
              type: string
            location:
              description: 'The full URI of the resource.

                '
              type: string
            opc-request-id: *id001
            opc-work-request-id: &id002
              description: 'The OCID of the work request. Use [GetWorkRequest](#/en/iaas/20160918/WorkRequests/GetWorkRequest)
                with this ID to track the status

                of the request.

                '
              type: string
          schema:
            $ref: '#/definitions/AnalyticsInstance'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Create an Analytics instance.
      tags:
      - analytics
  /analyticsInstances/{analyticsInstanceId}:
    delete:
      description: 'Terminates the specified Analytics instance. The operation is long-running

        and creates a new WorkRequest.

        '
      operationId: DeleteAnalyticsInstance
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        204:
          description: 'The Analytics instance is being terminated.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Delete an Analytics instance.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
    get:
      description: 'Info for a specific Analytics instance.

        '
      operationId: GetAnalyticsInstance
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: 'Expected response for a valid request.

            '
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/AnalyticsInstance'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Retrieve Analytics instance metadata.
      tags:
      - analytics
    put:
      description: 'Updates certain fields of an Analytics instance. Fields that are not provided in the

        request will not be updated.

        '
      operationId: UpdateAnalyticsInstance
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - description: 'The Analytics Instance fields to update. Fields that are not provided

          will not be updated.

          '
        in: body
        name: UpdateAnalyticsInstanceDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateAnalyticsInstanceDetails'
      responses:
        200:
          description: 'The Analytics instance was updated.

            '
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/AnalyticsInstance'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Update an Analytics instance.
      tags:
      - analytics
      x-obmcs-client-retries-enabled: false
  /analyticsInstances/{analyticsInstanceId}/actions/changeCompartment:
    post:
      description: 'Change the compartment of an Analytics instance. The operation is long-running

        and creates a new WorkRequest.

        '
      operationId: ChangeAnalyticsInstanceCompartment
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Input payload to move the resource to a different compartment.

          '
        in: body
        name: ChangeCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeCompartmentDetails'
      responses:
        202:
          description: 'The request to move compartments was accepted.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Change the compartment of an Analytics instance.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/actions/changeNetworkEndpoint:
    post:
      description: 'Change an Analytics instance network endpoint. The operation is long-running

        and creates a new WorkRequest.

        '
      operationId: ChangeAnalyticsInstanceNetworkEndpoint
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Input payload for changing an Analytics instance network endpoint.

          '
        in: body
        name: ChangeAnalyticsInstanceNetworkEndpointDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeAnalyticsInstanceNetworkEndpointDetails'
      responses:
        202:
          description: 'The request was accepted and the Analytics instance network endpoint will be updated.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Change an Analytics instance network endpoint.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/actions/scale:
    post:
      description: 'Scale an Analytics instance up or down. The operation is long-running

        and creates a new WorkRequest.

        '
      operationId: ScaleAnalyticsInstance
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Input payload for scaling an Analytics instance up or down.

          '
        in: body
        name: ScaleAnalyticsInstanceDetails
        required: true
        schema:
          $ref: '#/definitions/ScaleAnalyticsInstanceDetails'
      responses:
        202:
          description: 'The request was accepted and the Analytics instance will be scaled up or down.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Scale an Analytics instance up or down.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/actions/setFeatureBundle:
    post:
      description: 'Set the external service properties of an Analytics instance.

        '
      operationId: SetFeatureBundle
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Input payload for the feature set of an Analytics instance.

          '
        in: body
        name: SetFeatureBundleDetails
        required: true
        schema:
          $ref: '#/definitions/SetFeatureBundleDetails'
      responses:
        202:
          description: 'The request was accepted and the Analytics instance feature bundle will be set.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Set the feature bundle of an Analytics instance.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/actions/setKmsKey:
    post:
      description: 'Encrypts the customer data of this Analytics instance using either a customer OCI
        Vault Key or Oracle managed default key.

        '
      operationId: SetKmsKey
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Input payload to reset the OCI Vault encryption key.

          '
        in: body
        name: SetKmsKeyDetails
        required: true
        schema:
          $ref: '#/definitions/SetKmsKeyDetails'
      responses:
        202:
          description: 'The request was accepted and re-encryption of the customer data will begin.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Encrypts customer data with an OCI Vault key or reverts back to Oracle managed key.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/actions/start:
    post:
      description: 'Starts the specified Analytics instance. The operation is long-running

        and creates a new WorkRequest.

        '
      operationId: StartAnalyticsInstance
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        202:
          description: 'The request was accepted and the Analytics instance will be started.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Starts the specified Analytics instance.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/actions/stop:
    post:
      description: 'Stop the specified Analytics instance. The operation is long-running

        and creates a new WorkRequest.

        '
      operationId: StopAnalyticsInstance
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        202:
          description: 'The request was accepted and the Analytics instance will be stopped.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Stop the specified Analytics instance.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/privateAccessChannels:
    post:
      description: 'Create an Private access Channel for the Analytics instance. The operation is long-running

        and creates a new WorkRequest.

        '
      operationId: CreatePrivateAccessChannel
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Input payload for creating a private access channel for an Analytics instance.

          '
        in: body
        name: CreatePrivateAccessChannelDetails
        required: true
        schema:
          $ref: '#/definitions/CreatePrivateAccessChannelDetails'
      responses:
        202:
          description: 'The request was accepted and the private access channel for the Analytics instance
            will be created.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Create a Private access Channel for the Analytics instance.
      tags:
      - analytics
      x-example: "POST /20190331/analyticsInstances/<var>&lt;analytics_instance_OCID&gt;</var>/privateAccessChannels\n\
        Host: analytics.<var>&lt;region&gt;</var>.ocp.oraclecloud.com\n<var>&lt;authorization and other\
        \ headers&gt;</var>\n{\n  \"displayName\": \"example_private_access_channel\",\n  \"vcnId\": \"\
        ocid1.vcn.<var>&lt;unique_part_vcn_id&gt;</var>\",\n  \"subnetId\": \"ocid1.subnet.<var>&lt;unique_part_subnet_id&gt;</var>\"\
        ,\n  \"privateSourceDnsZones\" : [\n    { \"dnsZone\" : \"example-vcn.oraclevcn.com\", \"description\"\
        \ : \"Example OCI VCN DNS label\" },\n    { \"dnsZone\" : \"example.com\", \"description\" : \"\
        Example DNS Zone / Domain for On-Premise\" }\n  ]\n}\n"
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/privateAccessChannels/{privateAccessChannelKey}:
    delete:
      description: 'Delete an Analytics instance''s Private access channel with the given unique identifier
        key.

        '
      operationId: DeletePrivateAccessChannel
      parameters:
      - $ref: '#/parameters/PrivateAccessChannelKeyPathParam'
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        204:
          description: 'The Analytics instance''s Private Access Channel is being terminated.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Delete an Analytics instance's Private access channel with the given unique identifier
        key.
      tags:
      - analytics
      x-example: 'DELETE /20190331/analyticsInstances/<var>&lt;analytics_instance_OCID&gt;</var>/privateAccessChannels/<var>&lt;private_access_channel_Key&gt;</var>

        Host: analytics.<var>&lt;region&gt;</var>.ocp.oraclecloud.com

        <var>&lt;authorization and other headers&gt;</var>

        '
      x-related-resource: '#/definitions/AnalyticsInstance'
    get:
      description: 'Retrieve private access channel in the specified Analytics Instance.

        '
      operationId: GetPrivateAccessChannel
      parameters:
      - $ref: '#/parameters/PrivateAccessChannelKeyPathParam'
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: 'Returns Private Access Channel with the given name.

            '
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/PrivateAccessChannel'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Retrieve private access channel in the specified Analytics Instance.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
    put:
      description: 'Update the Private Access Channel with the given unique identifier key in the specified
        Analytics Instance.

        '
      operationId: UpdatePrivateAccessChannel
      parameters:
      - $ref: '#/parameters/PrivateAccessChannelKeyPathParam'
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Update the Private Access Channel with the given unique identifier key in the specified
          Analytics Instance.

          '
        in: body
        name: UpdatePrivateAccessChannelDetails
        required: true
        schema:
          $ref: '#/definitions/UpdatePrivateAccessChannelDetails'
      responses:
        202:
          description: 'The request was accepted and the Analytics instance private access channel will
            be updated.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Update the Private Access Channel.
      tags:
      - analytics
      x-example: "PUT /20190331/analyticsInstances/<var>&lt;analytics_instance_OCID&gt;</var>/privateAccessChannels/<var>&lt;private_access_channel_Key&gt;</var>\n\
        Host: analytics.<var>&lt;region&gt;</var>.ocp.oraclecloud.com\n<var>&lt;authorization and other\
        \ headers&gt;</var>\n{\n  \"displayName\": \"example_private_access_channel\",\n  \"vcnId\": \"\
        ocid1.vcn.<var>&lt;unique_part_vcn_id&gt;</var>\",\n  \"subnetId\": \"ocid1.subnet.<var>&lt;unique_part_subnet_id&gt;</var>\"\
        ,\n  \"privateSourceDnsZones\" : [\n    { \"dnsZone\" : \"example-vcn.oraclevcn.com\", \"description\"\
        \ : \"Example OCI VCN DNS label\" },\n    { \"dnsZone\" : \"example.com\", \"description\" : \"\
        Example DNS Zone / Domain for On-Premise\" }\n  ]\n}\n"
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/vanityUrls:
    post:
      description: 'Allows specifying a custom host name to be used to access the analytics instance.  This
        requires prior setup of DNS entry and certificate

        for this host.

        '
      operationId: CreateVanityUrl
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Vanity url details.

          '
        in: body
        name: CreateVanityUrlDetails
        required: true
        schema:
          $ref: '#/definitions/CreateVanityUrlDetails'
      responses:
        202:
          description: 'The request was accepted and creation of the vanity url will begin.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Create a vanity URL for the Analytics Instance.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /analyticsInstances/{analyticsInstanceId}/vanityUrls/{vanityUrlKey}:
    delete:
      description: 'Allows deleting a previously created vanity url.

        '
      operationId: DeleteVanityUrl
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/VanityUrlKeyPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        202:
          description: 'The request was accepted and deletion of the vanity url will begin.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Delete a vanity URL for the Analytics Instance.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
    put:
      description: 'Allows uploading a new certificate for a vanity url, which will have to be done when
        the current certificate is expiring.

        '
      operationId: UpdateVanityUrl
      parameters:
      - $ref: '#/parameters/AnalyticsInstanceIdPathParam'
      - $ref: '#/parameters/VanityUrlKeyPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - description: 'Vanity url details to update (certificate).

          '
        in: body
        name: UpdateVanityUrlDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateVanityUrlDetails'
      responses:
        202:
          description: 'The request was accepted and update of the vanity url will begin.

            '
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Upload a new certificate for a vanity url.
      tags:
      - analytics
      x-related-resource: '#/definitions/AnalyticsInstance'
  /workRequests:
    get:
      description: 'List all work requests in a compartment.

        '
      operationId: ListWorkRequests
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/WorkRequestResourceIdQueryParam'
      - $ref: '#/parameters/WorkRequestResourceTypeQueryParam'
      - $ref: '#/parameters/WorkRequestStatusQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/WorkRequestSortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        '200':
          description: 'The list of work request summary objects.

            '
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            items:
              $ref: '#/definitions/WorkRequestSummary'
            type: array
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: List all work requests.
      tags:
      - analytics
  /workRequests/{workRequestId}:
    delete:
      description: 'Cancel a work request that has not started yet.

        '
      operationId: DeleteWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        204:
          description: The work request was cancelled.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Cancel a work request.
      tags:
      - analytics
      x-obmcs-client-retries-enabled: false
      x-related-resource: '#/definitions/WorkRequest'
    get:
      description: 'Get the details of a work request.

        '
      operationId: GetWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        '200':
          description: 'Retrieve a work request resource.

            '
          headers:
            etag: *id003
            opc-request-id: *id001
            retry-after:
              description: 'The number of seconds a client should wait before polling this endpoint again.

                '
              type: integer
          schema:
            $ref: '#/definitions/WorkRequest'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Get work request details.
      tags:
      - analytics
  /workRequests/{workRequestId}/errors:
    get:
      description: 'Get the errors of a work request.

        '
      operationId: ListWorkRequestErrors
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      responses:
        200:
          description: 'The list of errors for the work request.

            '
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            items:
              $ref: '#/definitions/WorkRequestError'
            type: array
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Get work request errors.
      tags:
      - analytics
  /workRequests/{workRequestId}/logs:
    get:
      description: 'Get the logs of a work request.

        '
      operationId: ListWorkRequestLogs
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      responses:
        200:
          description: The list of logs for the work request.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            items:
              $ref: '#/definitions/WorkRequestLog'
            type: array
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/DefaultError'
      summary: Get work request logs.
      tags:
      - analytics
definitions:
  AccessControlServiceType:
    description: 'Various types of Oracle Service categories that can be whitelisted to access an Analytics
      Instance.

      '
    enum: &id036
    - ALL
    type: string
  AnalyticsInstance:
    description: 'Analytics Instance metadata.

      '
    properties:
      capacity: &id006
        $ref: '#/definitions/Capacity'
      compartmentId: &id007
        description: 'The OCID of the compartment.

          '
        maxLength: 255
        minLength: 1
        type: string
      definedTags: &id008
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only the String type is supported.

              '
            type: object
          description: 'Key-value pair representing a defined tag key and value, scoped to a namespace.


            Example: `{"CostCenter": "42"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a

          namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Operations": {"CostCenter": "42"}}`

          '
        type: object
      description: &id009
        description: 'Optional description.

          '
        maxLength: 512
        minLength: 0
        type: string
      domainId:
        description: 'Identity domain OCID.

          '
        type: string
      emailNotification: &id010
        description: 'Email address receiving notifications.

          '
        maxLength: 512
        minLength: 0
        type: string
      featureBundle: &id023
        description: 'The feature set of an Analytics instance.

          '
        enum: &id026
        - FAW_PAID
        - FAW_FREE
        - EE_EMBEDDED
        - SE_EMBEDDED
        type: string
        x-obmcs-top-level-enum: '#/definitions/FeatureBundle'
      featureSet: &id011
        description: 'Analytics feature set.

          '
        enum: &id027
        - SELF_SERVICE_ANALYTICS
        - ENTERPRISE_ANALYTICS
        type: string
        x-obmcs-top-level-enum: '#/definitions/FeatureSet'
      freeformTags: &id012
        additionalProperties:
          type: string
        description: 'Free-form tags for this resource. Each tag is a simple key-value pair with no

          predefined name, type, or namespace. For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"Department": "Finance"}`

          '
        type: object
      id: &id013
        description: 'The resource OCID.

          '
        maxLength: 255
        minLength: 1
        type: string
      kmsKeyId: &id024
        description: 'OCID of the OCI Vault Key encrypting the customer data stored in this Analytics
          instance. A null value indicates Oracle managed default encryption.

          '
        maxLength: 255
        minLength: 0
        type: string
      licenseType: &id014
        description: 'The license used for the service.

          '
        enum: &id028
        - LICENSE_INCLUDED
        - BRING_YOUR_OWN_LICENSE
        type: string
        x-obmcs-top-level-enum: '#/definitions/LicenseType'
      lifecycleState: &id015
        description: 'The current state of an instance.

          '
        enum: &id005
        - ACTIVE
        - CREATING
        - DELETED
        - DELETING
        - FAILED
        - INACTIVE
        - UPDATING
        type: string
        x-obmcs-top-level-enum: '#/definitions/AnalyticsInstanceLifecycleState'
      name: &id016
        description: 'The name of the Analytics instance. This name must be unique in the tenancy and
          cannot be changed.

          '
        maxLength: 32
        minLength: 1
        pattern: ^[a-zA-Z](-?[a-zA-Z0-9])*$
        type: string
      networkEndpointDetails: &id017
        $ref: '#/definitions/NetworkEndpointDetails'
      privateAccessChannels:
        additionalProperties:
          $ref: '#/definitions/PrivateAccessChannel'
        description: 'Map of PrivateAccessChannel unique identifier key as KEY and PrivateAccessChannel
          Object as VALUE.

          '
        type: object
      serviceUrl: &id018
        description: 'URL of the Analytics service.

          '
        maxLength: 1024
        minLength: 0
        type: string
      systemTags: &id019
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

            Example: `{"key": "value"}`

            '
          type: object
        description: 'System tags for this resource. These predefined keys are scoped to namespaces.

          Example: `{"orcl-cloud": {"key": "value"}}`

          '
        type: object
      timeCreated: &id020
        description: 'The date and time the instance was created, in the format defined by RFC3339.


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeUpdated: &id021
        description: 'The date and time the instance was last updated (in the format defined by RFC3339).

          This timestamp represents updates made through this API. External events do not

          influence it.

          '
        format: date-time
        type: string
      updateChannel: &id025
        description: 'Analytics instance update channel.

          '
        enum: &id037
        - REGULAR
        - EARLY
        - PHASE_2
        - PHASE_1
        type: string
        x-obmcs-top-level-enum: '#/definitions/UpdateChannel'
      vanityUrlDetails:
        additionalProperties:
          $ref: '#/definitions/VanityUrlDetails'
        description: 'Map of VanityUrl unique identifier key as KEY and VanityUrl Object as VALUE.

          '
        type: object
    required:
    - id
    - name
    - compartmentId
    - lifecycleState
    - capacity
    - networkEndpointDetails
    - featureSet
    - timeCreated
  AnalyticsInstanceLifecycleState:
    description: 'The current state of an instance.

      '
    enum: *id005
    type: string
  AnalyticsInstanceSummary:
    description: 'Analytics Instance metadata (summary view).

      '
    properties:
      capacity: *id006
      compartmentId: *id007
      definedTags: *id008
      description: *id009
      emailNotification: *id010
      featureSet: *id011
      freeformTags: *id012
      id: *id013
      licenseType: *id014
      lifecycleState: *id015
      name: *id016
      networkEndpointDetails: *id017
      serviceUrl: *id018
      systemTags: *id019
      timeCreated: *id020
      timeUpdated: *id021
    required:
    - id
    - name
    - compartmentId
    - lifecycleState
    - capacity
    - networkEndpointDetails
    - featureSet
    - timeCreated
  Capacity:
    description: 'Service instance capacity metadata (e.g.: OLPU count, number of users, ...etc...).

      '
    properties:
      capacityType:
        description: 'The capacity model to use.

          Accepted values are:

          OLPU_COUNT, USER_COUNT

          '
        enum: &id022
        - OLPU_COUNT
        - USER_COUNT
        type: string
        x-obmcs-top-level-enum: '#/definitions/CapacityType'
      capacityValue:
        description: 'The capacity value selected, either the number of OCPUs (OLPU_COUNT) or the number
          of users (USER_COUNT).

          This parameter affects the number of OCPUs, amount of memory, and other resources allocated
          to the instance.

          '
        minimum: 1
        type: integer
    required:
    - capacityType
    - capacityValue
    type: object
  CapacityType:
    description: 'Supported capacity types.

      '
    enum: *id022
    type: string
  ChangeAnalyticsInstanceNetworkEndpointDetails:
    description: 'Input payload to update an Analytics instance endpoint details.

      '
    properties:
      networkEndpointDetails: *id017
    required:
    - networkEndpointDetails
  ChangeCompartmentDetails:
    description: 'Input payload to change a resource''s compartment.

      '
    properties:
      compartmentId:
        description: 'The OCID of the new compartment.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
  CreateAnalyticsInstanceDetails:
    description: 'Input payload to create an Anaytics instance.

      '
    properties:
      adminUser:
        description: 'user name of the authorized user.

          '
        maxLength: 255
        type: string
      capacity: *id006
      compartmentId: *id007
      definedTags: *id008
      description: *id009
      domainId:
        description: 'domain id for which the user is authorized.

          '
        maxLength: 255
        type: string
      emailNotification: *id010
      featureBundle: *id023
      featureSet: *id011
      freeformTags: *id012
      idcsAccessToken:
        description: 'IDCS access token identifying a stripe and service administrator user.

          '
        maxLength: 8192
        type: string
      kmsKeyId: *id024
      licenseType: *id014
      name: *id016
      networkEndpointDetails: *id017
      updateChannel: *id025
    required:
    - name
    - compartmentId
    - capacity
    - featureSet
    - licenseType
  CreatePrivateAccessChannelDetails:
    description: 'Input payload to create a Private Access Channel.

      '
    properties:
      displayName: &id030
        description: 'Display Name of the Private Access Channel.

          '
        maxLength: 32
        minLength: 1
        pattern: ^[a-zA-Z][a-zA-Z0-9_-]*$
        type: string
      networkSecurityGroupIds: &id031
        description: 'Network Security Group OCIDs for an Analytics instance.

          '
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxLength: 5
        type: array
      privateSourceDnsZones: &id032
        description: 'List of Private Source DNS zones registered with Private Access Channel,

          where datasource hostnames from these dns zones / domains will be resolved in the peered VCN
          for access from Analytics Instance.

          Min of 1 is required and Max of 30 Private Source DNS zones can be registered.

          '
        items:
          $ref: '#/definitions/PrivateSourceDnsZone'
        maxLength: 30
        minLength: 1
        type: array
      privateSourceScanHosts: &id033
        description: 'List of Private Source DB SCAN hosts registered with Private Access Channel for
          access from Analytics Instance.

          '
        items:
          $ref: '#/definitions/PrivateSourceScanHost'
        maxLength: 15
        type: array
      subnetId: &id034
        description: 'OCID of the customer subnet connected to private access channel.

          '
        maxLength: 255
        minLength: 1
        type: string
      vcnId: &id035
        description: 'OCID of the customer VCN peered with private access channel.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - displayName
    - vcnId
    - subnetId
    - privateSourceDnsZones
    type: object
  CreateVanityUrlDetails:
    description: 'Input payload to create a vanity url.

      '
    properties:
      caCertificate: &id038
        description: 'PEM CA certificate(s) for HTTPS connections. This may include multiple PEM certificates.

          '
        minLength: 1
        type: string
      description: *id009
      hosts: &id042
        description: 'List of fully qualified hostnames supported by this vanity URL definition (max of
          3).

          '
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 3
        minItems: 1
        type: array
      passphrase: &id039
        description: 'Passphrase for the PEM Private key (if any).

          '
        type: string
      privateKey: &id040
        description: 'PEM Private key for HTTPS connections.

          '
        minLength: 1
        type: string
      publicCertificate: &id041
        description: 'PEM certificate for HTTPS connections.

          '
        minLength: 1
        type: string
    required:
    - hosts
    - caCertificate
    - privateKey
    - publicCertificate
  Error:
    properties:
      code:
        description: 'A short error code that defines the error, meant for programmatic parsing. See

          [API Errors](/Content/API/References/apierrors.htm).

          '
        type: string
      message:
        description: 'A human-readable error string.

          '
        type: string
    required:
    - code
    - message
  FeatureBundle:
    description: 'Feature Bundle Enum.

      '
    enum: *id026
    type: string
  FeatureSet:
    description: 'Analytics feature set.

      '
    enum: *id027
    type: string
  LicenseType:
    description: 'The license type of the instance.

      '
    enum: *id028
    type: string
  NetworkEndpointDetails:
    description: 'Base representation of a network endpoint.

      '
    discriminator: networkEndpointType
    properties:
      networkEndpointType:
        description: 'The type of network endpoint.

          '
        enum: &id029
        - PUBLIC
        - PRIVATE
        type: string
        x-obmcs-top-level-enum: '#/definitions/NetworkEndpointType'
    required:
    - networkEndpointType
    type: object
  NetworkEndpointType:
    description: 'Public/Private endpoint access type.

      '
    enum: *id029
    type: string
  PrivateAccessChannel:
    description: 'Analytics Instance Private Access Channel model.

      '
    properties:
      displayName: *id030
      egressSourceIpAddresses:
        description: 'The list of IP addresses from the customer subnet connected to private access channel,
          used as a source Ip by Private Access Channel

          for network traffic from the AnalyticsInstance to Private Sources.

          '
        items:
          maxLength: 46
          minLength: 8
          type: string
        type: array
      ipAddress:
        description: 'IP Address of the Private Access channel.

          '
        maxLength: 255
        minLength: 1
        type: string
      key:
        description: 'Private Access Channel unique identifier key.

          '
        maxLength: 255
        minLength: 1
        type: string
      networkSecurityGroupIds: *id031
      privateSourceDnsZones: *id032
      privateSourceScanHosts: *id033
      subnetId: *id034
      vcnId: *id035
    required:
    - key
    - displayName
    - vcnId
    - subnetId
    - ipAddress
    - egressSourceIpAddresses
  PrivateEndpointDetails:
    allOf:
    - $ref: '#/definitions/NetworkEndpointDetails'
    - discriminator: PRIVATE
      properties:
        networkSecurityGroupIds: *id031
        subnetId:
          description: 'The subnet OCID for the private endpoint.

            '
          maxLength: 255
          minLength: 1
          type: string
        vcnId:
          description: 'The VCN OCID for the private endpoint.

            '
          maxLength: 255
          minLength: 1
          type: string
      required:
      - vcnId
      - subnetId
    description: 'Private endpoint configuration details.

      '
  PrivateSourceDnsZone:
    description: 'Private source DNS Zone model.

      '
    properties:
      description:
        description: 'Description of private source dns zone.

          '
        maxLength: 255
        minLength: 0
        type: string
      dnsZone:
        description: 'Private Source DNS Zone. Ex: example-vcn.oraclevcn.com, corp.example.com.

          '
        maxLength: 253
        minLength: 1
        type: string
    required:
    - dnsZone
  PrivateSourceScanHost:
    description: 'Private source Scan Hostname model.

      '
    properties:
      description:
        description: 'Description of private source scan host zone.

          '
        maxLength: 255
        minLength: 1
        type: string
      scanHostname:
        description: 'Private Source Scan hostname. Ex: db01-scan.corp.example.com, prd-db01-scan.mycompany.com.

          '
        maxLength: 255
        minLength: 1
        type: string
      scanPort:
        description: 'Private Source Scan host port. This is the source port where SCAN protocol will
          get connected (e.g. 1521).

          '
        minimum: 1
        type: integer
    required:
    - scanHostname
    - scanPort
  PublicEndpointDetails:
    allOf:
    - $ref: '#/definitions/NetworkEndpointDetails'
    - discriminator: PUBLIC
      properties:
        whitelistedIps: &id043
          description: 'Source IP addresses or IP address ranges in ingress rules.

            '
          items:
            maxLength: 255
            minLength: 1
            type: string
          type: array
        whitelistedServices:
          description: 'Oracle Cloud Services that are allowed to access this Analytics instance.

            '
          items:
            enum: *id036
            type: string
            x-obmcs-top-level-enum: '#/definitions/AccessControlServiceType'
          type: array
        whitelistedVcns:
          description: 'Virtual Cloud Networks allowed to access this network endpoint.

            '
          items:
            $ref: '#/definitions/VirtualCloudNetwork'
          type: array
    description: 'Public endpoint configuration details.

      '
  ScaleAnalyticsInstanceDetails:
    description: 'Input payload to scale an Analytics instance up or down.

      '
    properties:
      capacity: *id006
    required:
    - capacity
  SetFeatureBundleDetails:
    description: 'Input payload for the feature set of an Analytics instance.

      '
    properties:
      featureBundle: *id023
  SetKmsKeyDetails:
    description: 'Input payload to reset the OCI Vault encryption key.

      '
    properties:
      kmsKeyId:
        description: 'OCID of the OCI Vault Key encrypting the customer data stored in this Analytics
          instance. An empty value indicates Oracle managed default encryption (null is not supported
          in this API).

          '
        maxLength: 255
        minLength: 0
        type: string
    required:
    - kmsKeyId
  SortBy:
    description: 'The field to sort by (one column only). Default sort order is

      ascending exception of `timeCreated` column (descending).

      '
    enum: &id055
    - capacityType
    - capacityValue
    - featureSet
    - lifecycleState
    - name
    - timeCreated
    type: string
  SortOrder:
    description: 'The sort order to use, either ascending (`ASC`) or descending (`DESC`).

      '
    enum: &id056
    - ASC
    - DESC
    type: string
  UpdateAnalyticsInstanceDetails:
    description: 'Input payload to update an Analytics instance. Fields that are not provided

      will not be updated.

      '
    properties:
      definedTags: *id008
      description: *id009
      emailNotification: *id010
      freeformTags: *id012
      licenseType: *id014
      updateChannel: *id025
  UpdateChannel:
    description: "EARLY - Early update channel recommended for pre-production use. \nREGULAR - Regular\
      \ update channel recommended for production use. \nPHASE_1 - Reserved for future use. \nPHASE_2\
      \ - Reserved for future use.\n"
    enum: *id037
    type: string
  UpdatePrivateAccessChannelDetails:
    description: 'Input payload to update a Private Access Channel.

      '
    properties:
      displayName: *id030
      networkSecurityGroupIds: *id031
      privateSourceDnsZones: *id032
      privateSourceScanHosts: *id033
      subnetId: *id034
      vcnId: *id035
    type: object
  UpdateVanityUrlDetails:
    description: 'Input payload to update a vanity url.

      '
    properties:
      caCertificate: *id038
      passphrase: *id039
      privateKey: *id040
      publicCertificate: *id041
    required:
    - caCertificate
    - privateKey
    - publicCertificate
  VanityUrlDetails:
    description: 'Vanity url configuration details.

      '
    properties:
      description:
        description: 'Description of the vanity url.

          '
        maxLength: 512
        minLength: 1
        type: string
      hosts: *id042
      key:
        description: 'The vanity url unique identifier key.

          '
        maxLength: 255
        minLength: 1
        type: string
      publicCertificate: *id041
      urls:
        description: 'List of urls supported by this vanity URL definition (max of 3).

          '
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 3
        minItems: 1
        type: array
    type: object
  VirtualCloudNetwork:
    description: 'Virtual Cloud Network definition.

      '
    properties:
      id:
        description: 'The Virtual Cloud Network OCID.

          '
        maxLength: 255
        minLength: 1
        type: string
      whitelistedIps: *id043
    required:
    - id
    type: object
  WorkRequest:
    description: 'An asynchronous work request.

      '
    properties:
      compartmentId: &id048
        description: 'The compartment OCID of this work request.

          '
        maxLength: 255
        minLength: 1
        type: string
      id: *id013
      operationType: &id049
        description: 'The operation performed by the work request.

          '
        enum: &id044
        - CREATE_ANALYTICS_INSTANCE
        - DELETE_ANALYTICS_INSTANCE
        - START_ANALYTICS_INSTANCE
        - STOP_ANALYTICS_INSTANCE
        - SCALE_ANALYTICS_INSTANCE
        - CHANGE_ANALYTICS_INSTANCE_COMPARTMENT
        - CHANGE_ANALYTICS_INSTANCE_NETWORK_ENDPOINT
        - CREATE_VANITY_URL
        - UPDATE_VANITY_URL
        - DELETE_VANITY_URL
        - CREATE_PRIVATE_ACCESS_CHANNEL
        - UPDATE_PRIVATE_ACCESS_CHANNEL
        - DELETE_PRIVATE_ACCESS_CHANNEL
        - UPDATE_INSTANCE_ENCRYPTION_KEY
        type: string
        x-obmcs-top-level-enum: '#/definitions/WorkRequestOperationType'
      percentComplete:
        description: 'Percentage of the request completed.

          '
        format: float
        type: number
      resources: &id050
        description: 'The resources this work request affects.

          '
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status: &id051
        description: 'The current status of the work request.

          '
        enum: &id047
        - ACCEPTED
        - IN_PROGRESS
        - FAILED
        - SUCCEEDED
        - CANCELING
        - CANCELED
        type: string
        x-obmcs-top-level-enum: '#/definitions/WorkRequestStatus'
      timeAccepted: &id052
        description: 'The time the work request was accepted, in the format defined by RFC3339.


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeFinished: &id053
        description: 'The time the work request was finished, in the format defined by RFC3339.


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeStarted: &id054
        description: 'The time the work request was started, in the format defined by RFC3339.


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - id
    - operationType
    - status
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
    type: object
  WorkRequestActionResult:
    description: 'The way in which a resource was affected by a work request.

      '
    enum: &id045
    - COMPARTMENT_CHANGED
    - CREATED
    - DELETED
    - STARTED
    - STOPPED
    - SCALED
    - NETWORK_ENDPOINT_CHANGED
    - VANITY_URL_CREATED
    - VANITY_URL_UPDATED
    - VANITY_URL_DELETED
    - PRIVATE_ACCESS_CHANNEL_CREATED
    - PRIVATE_ACCESS_CHANNEL_UPDATED
    - PRIVATE_ACCESS_CHANNEL_DELETED
    - NONE
    type: string
  WorkRequestError:
    description: 'Error encountered during the execution of a work request.

      '
    properties:
      code:
        description: 'A short error code that defines the error, meant for programmatic parsing.

          '
        type: string
      message:
        description: 'Error message.

          '
        type: string
      timestamp:
        description: 'The date and time the error occured, in the format defined by RFC3339.


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - code
    - message
    - timestamp
    type: object
  WorkRequestLog:
    description: 'Log entries related to a specific work request.

      '
    properties:
      message:
        description: 'The description of the event that occurred.

          '
        type: string
      timestamp:
        description: 'The date and time the log entry occured, in the format defined by RFC3339.


          Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - message
    - timestamp
    type: object
  WorkRequestOperationType:
    description: 'The types of work request operations.

      '
    enum: *id044
    type: string
  WorkRequestResource:
    properties:
      actionResult:
        description: 'The way in which this resource was affected by this work request.

          '
        enum: *id045
        type: string
        x-obmcs-top-level-enum: '#/definitions/WorkRequestActionResult'
      identifier:
        description: The OCID of the resource the work request is affecting.
        maxLength: 255
        minLength: 1
        type: string
      metadata:
        additionalProperties:
          type: string
        description: 'Additional metadata of the resource.

          '
        type: object
      resourceType:
        description: 'The type of the resource the work request is affecting.

          '
        enum: &id046
        - ANALYTICS_INSTANCE
        type: string
        x-obmcs-top-level-enum: '#/definitions/WorkRequestResourceType'
      resourceUri:
        description: 'The URI of the affected resource.

          '
        type: string
    required:
    - actionResult
    - resourceType
    - identifier
    - resourceUri
  WorkRequestResourceType:
    description: 'Type of the resource associated with a work request.

      '
    enum: *id046
    type: string
  WorkRequestSortBy:
    description: 'The supported work request `sort by` columns.

      '
    enum: &id057
    - id
    - operationType
    - status
    - timeAccepted
    - timeStarted
    - timeFinished
    type: string
  WorkRequestStatus:
    description: 'The status of a work request.

      '
    enum: *id047
    type: string
  WorkRequestSummary:
    description: An asynchronous work request.
    properties:
      compartmentId: *id048
      id: *id013
      operationType: *id049
      resources: *id050
      status: *id051
      timeAccepted: *id052
      timeFinished: *id053
      timeStarted: *id054
    required:
    - id
    - operationType
    - status
    - compartmentId
    - resources
    - timeAccepted
    type: object
parameters:
  AnalyticsInstanceIdPathParam:
    description: 'The OCID of the AnalyticsInstance.

      '
    in: path
    name: analyticsInstanceId
    required: true
    type: string
  CapacityTypeQueryParam:
    description: 'A filter to only return resources matching the capacity type enum. Values are

      case-insensitive.

      '
    enum: *id022
    in: query
    name: capacityType
    required: false
    type: string
    x-obmcs-top-level-enum: '#/definitions/CapacityType'
  CompartmentIdQueryParam:
    description: 'The OCID of the compartment.

      '
    in: query
    maxLength: 255
    minLength: 1
    name: compartmentId
    required: true
    type: string
  FeatureSetQueryParam:
    description: 'A filter to only return resources matching the feature set. Values are

      case-insensitive.

      '
    enum: *id027
    in: query
    name: featureSet
    required: false
    type: string
    x-obmcs-top-level-enum: '#/definitions/FeatureSet'
  IfMatchHeader:
    description: 'For optimistic concurrency control. In the PUT or DELETE call for a resource, set the
      `if-match`

      parameter to the value of the etag from a previous GET or POST response for that resource.  The
      resource

      will be updated or deleted only if the etag you provide matches the resource''s current etag value.

      '
    in: header
    name: if-match
    required: false
    type: string
  LifecycleStateQueryParam:
    description: 'A filter to only return resources matching the lifecycle state. The state

      value is case-insensitive.

      '
    enum: *id005
    in: query
    name: lifecycleState
    required: false
    type: string
    x-obmcs-top-level-enum: '#/definitions/AnalyticsInstanceLifecycleState'
  PaginationLimitQueryParam:
    description: 'For list pagination. The maximum number of results per page, or items to return in a
      paginated

      "List" call. For important details about how pagination works, see

      [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).


      Example: `50`

      '
    in: query
    maximum: 1000
    minimum: 1
    name: limit
    required: false
    type: integer
  PaginationTokenQueryParam:
    description: 'For list pagination. The value of the `opc-next-page` response header from the previous
      "List"

      call. For important details about how pagination works, see

      [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

      '
    in: query
    maxLength: 512
    minLength: 1
    name: page
    required: false
    type: string
  PrivateAccessChannelKeyPathParam:
    description: 'The unique identifier key of the Private Access Channel.

      '
    in: path
    maxLength: 255
    minLength: 1
    name: privateAccessChannelKey
    required: true
    type: string
  RequestIdHeader:
    description: 'Unique identifier for the request.

      If you need to contact Oracle about a particular request, please provide the request ID.

      '
    in: header
    name: opc-request-id
    pattern: ^[a-zA-Z0-9_./-]+$
    required: false
    type: string
  ResourceNameQueryParam:
    description: 'A filter to return only resources that match the given name exactly.

      '
    in: query
    maxLength: 255
    minLength: 1
    name: name
    required: false
    type: string
  RetryTokenHeader:
    description: 'A token that uniquely identifies a request so it can be retried in case of a timeout
      or

      server error without risk of executing that same action again. Retry tokens expire after 24

      hours, but can be invalidated before then due to conflicting operations (for example, if a resource

      has been deleted and purged from the system, then a retry of the original creation request

      may be rejected).

      '
    in: header
    maxLength: 64
    minLength: 1
    name: opc-retry-token
    required: false
    type: string
  SortByQueryParam:
    description: 'The field to sort by (one column only). Default sort order is

      ascending exception of `timeCreated` column (descending).

      '
    enum: *id055
    in: query
    name: sortBy
    required: false
    type: string
    x-obmcs-top-level-enum: '#/definitions/SortBy'
  SortOrderQueryParam:
    description: 'The sort order to use, either ascending (`ASC`) or descending (`DESC`).

      '
    enum: *id056
    in: query
    name: sortOrder
    required: false
    type: string
    x-obmcs-top-level-enum: '#/definitions/SortOrder'
  VanityUrlKeyPathParam:
    description: 'Specify unique identifier key of a vanity url to update or delete.

      '
    in: path
    name: vanityUrlKey
    required: true
    type: string
  WorkRequestIdPathParam:
    description: 'The OCID of the work request.

      '
    in: path
    name: workRequestId
    required: true
    type: string
  WorkRequestResourceIdQueryParam:
    description: 'The OCID of the resource associated with a work request.

      '
    in: query
    name: resourceId
    required: false
    type: string
  WorkRequestResourceTypeQueryParam:
    description: 'Type of the resource associated with a work request.

      '
    enum: *id046
    in: query
    name: resourceType
    required: false
    type: string
    x-obmcs-top-level-enum: '#/definitions/WorkRequestResourceType'
  WorkRequestSortByQueryParam:
    description: 'The field used for sorting work request results.

      '
    enum: *id057
    in: query
    name: sortBy
    required: false
    type: string
    x-obmcs-top-level-enum: '#/definitions/WorkRequestSortBy'
  WorkRequestStatusQueryParam:
    collectionFormat: multi
    description: 'One or more work request status values to filter on.

      '
    in: query
    items:
      enum: *id047
      type: string
      x-obmcs-top-level-enum: '#/definitions/WorkRequestStatus'
    name: status
    required: false
    type: array
responses:
  400:
    description: 'Bad Request (invalid query parameters, malformed headers, ...etc...).

      '
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  401:
    description: 'Unauthorized (missing or expired credentials, ...etc...).

      '
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  403:
    description: 'Forbidden (missing permissions for operation, request denied for unspecified reason,
      ...etc...).

      '
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  404:
    description: 'Not Found. The requested resource was not found or was not authorized.

      '
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  409:
    description: 'Conflict (the operation: results in constraint violation,

      the operation is incompatible with current state, ...etc...).

      '
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  412:
    description: 'Precondition Failed. One or more preconditions are not satisfied for

      the operation to proceed.

      '
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  429:
    description: 'Too Many Requests.

      '
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  500:
    description: 'Internal Server Error. The server encountered an unexpected condition

      preventing fulfilment of the request.

      '
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  DefaultError:
    description: An error has occurred.
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
x-obmcs-client-circuit-breaker-enabled: true
x-obmcs-client-retries-enabled: true
x-oracle-package: com.oracle.bmc.analytics
