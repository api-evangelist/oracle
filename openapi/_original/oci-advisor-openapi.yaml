swagger: '2.0'
info:
  description: 'Use the Cloud Advisor API to find potential inefficiencies in your tenancy and address
    them.

    Cloud Advisor can help you save money, improve performance, strengthen system resilience, and improve
    security.

    For more information, see [Cloud Advisor](/Content/CloudAdvisor/Concepts/cloudadvisoroverview.htm).

    '
  title: Cloud Advisor API
  version: '20200606'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/321b6e72c522371194cdfccd7f4baba8c0059033e00f99013ea466ec44921ca3.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the Cloud Advisor API OCI service and stored verbatim;
      API Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/321b6e72c522371194cdfccd7f4baba8c0059033e00f99013ea466ec44921ca3.yaml
    what: the harvested document for Cloud Advisor API
host: 127.0.0.1
basePath: /20200606
schemes:
- http
- https
consumes:
- application/json
produces:
- application/json
paths:
  /actions/filterResourceActions:
    post:
      description: 'Queries the Cloud Advisor resource actions that are supported.

        '
      operationId: FilterResourceActions
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/CompartmentIdInSubtreeQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      - description: The request parameters that describe the query criteria.
        in: body
        name: QueryDetails
        required: true
        schema:
          $ref: '#/definitions/QueryDetails'
      responses:
        200:
          description: The resource actions were retrieved.
          headers:
            opc-next-page: &id001
              description: 'For pagination of a list of items. When paging through a list, if this header
                appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter
                for the

                subsequent GET request to get the next batch of items.

                '
              type: string
            opc-prev-page: &id002
              description: 'For list pagination. When this header appears in the response, previous pages
                of results exist.

                For important details about how pagination works, see [List Pagination](/iaas/Content/API/Concepts/usingapi.htm#nine).

                '
              type: string
            opc-request-id: &id003
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/ResourceActionCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Include a list of resource actions.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/ResourceActionSummary'
  /categories:
    get:
      description: 'Lists the supported Cloud Advisor categories.

        '
      operationId: ListCategories
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/CompartmentIdInSubtreeQueryParam'
      - $ref: '#/parameters/FilterByNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/CategoryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Include a list of optimizer categories that are currently supported.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/CategorySummary'
  /categories/{categoryId}:
    get:
      description: 'Gets the category that corresponds to the specified OCID.

        '
      operationId: GetCategory
      parameters:
      - $ref: '#/parameters/CategoryIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The category was retrieved.
          headers:
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/Category'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the category for the OCID specified.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
  /enrollmentStatus:
    get:
      description: 'Lists the Cloud Advisor enrollment statuses.

        '
      operationId: ListEnrollmentStatuses
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/OptimizerEnrollmentStatusQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The enrollment status list was retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/EnrollmentStatusCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists the Cloud Advisor enrollment statuses.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/EnrollmentStatusSummary'
  /enrollmentStatus/{enrollmentStatusId}:
    get:
      description: 'Gets the Cloud Advisor enrollment status.

        '
      operationId: GetEnrollmentStatus
      parameters:
      - $ref: '#/parameters/EnrollmentStatusIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The enrollment status was retrieved.
          headers:
            etag: &id004
              description: 'For optimistic concurrency control. See `if-match`.

                '
              type: string
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/EnrollmentStatus'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Returns the optimizer enrollment status.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
    put:
      description: 'Updates the enrollment status of the tenancy.

        '
      operationId: UpdateEnrollmentStatus
      parameters:
      - $ref: '#/parameters/EnrollmentStatusIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      - $ref: '#/parameters/OpcIfMatchHeaderParam'
      - description: The request object for updating the enrollment status.
        in: body
        name: UpdateEnrollmentStatusDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateEnrollmentStatusDetails'
      responses:
        200:
          description: The enrollment status was updated.
          headers:
            etag: *id004
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/EnrollmentStatus'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the enrollment status of the tenancy.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
  /histories:
    get:
      description: "Lists changes to the recommendations based on user activity. \nFor example, lists\
        \ when recommendations have been implemented, dismissed, postponed, or reactivated.\n"
      operationId: ListHistories
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/CompartmentIdInSubtreeQueryParam'
      - $ref: '#/parameters/FilterByNameQueryParam'
      - $ref: '#/parameters/RecommendationNameQueryParam'
      - $ref: '#/parameters/OptionalRecommendationIdQueryParam'
      - $ref: '#/parameters/ResourceTypeQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/StatusQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The history was retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/HistoryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Included a list of recommendation history changes.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/HistorySummary'
  /profileLevels:
    get:
      description: 'Lists the existing profile levels.

        '
      operationId: ListProfileLevels
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/CompartmentIdInSubtreeQueryParam'
      - $ref: '#/parameters/FilterByNameQueryParam'
      - $ref: '#/parameters/RecommendationNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/ProfileLevelCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists the existing profile levels.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/ProfileLevelSummary'
  /profiles:
    get:
      description: 'Lists the existing profiles.

        '
      operationId: ListProfiles
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/FilterByNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/ProfileCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists the existing profiles.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/ProfileSummary'
    post:
      description: 'Creates a new profile.

        '
      operationId: CreateProfile
      parameters:
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      - $ref: '#/parameters/OpcRetryTokenHeader'
      - description: Details for creating the profile.
        in: body
        name: CreateProfileDetails
        required: true
        schema:
          $ref: '#/definitions/CreateProfileDetails'
      responses:
        200:
          description: The profile is being created.
          headers:
            etag: *id004
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/Profile'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new profile.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
  /profiles/{profileId}:
    delete:
      description: 'Deletes the specified profile. Uses the profile''s OCID to determine which profile
        to delete.

        '
      operationId: DeleteProfile
      parameters:
      - $ref: '#/parameters/ProfileIdPathParam'
      - $ref: '#/parameters/OpcIfMatchHeaderParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        204:
          description: The profile is being deleted.
          headers:
            opc-request-id: *id003
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Deletes the specified profile. Uses the profile's OCID to determine which profile to delete.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/Profile'
    get:
      description: 'Gets the specified profile''s information. Uses the profile''s OCID to determine which
        profile to retrieve.

        '
      operationId: GetProfile
      parameters:
      - $ref: '#/parameters/ProfileIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The profile was retrieved.
          headers:
            etag: *id004
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/Profile'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets the specified profile's information. Uses the profile's OCID to determine which profile
        to retrieve.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
    put:
      description: 'Updates the specified profile. Uses the profile''s OCID to determine which profile
        to update.

        '
      operationId: UpdateProfile
      parameters:
      - $ref: '#/parameters/ProfileIdPathParam'
      - $ref: '#/parameters/OpcIfMatchHeaderParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      - description: The profile information to use for the update.
        in: body
        name: UpdateProfileDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateProfileDetails'
      responses:
        200:
          description: The profile was updated.
          headers:
            etag: *id004
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/Profile'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates the specified profile. Uses the profile's OCID to determine which profile to update.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
  /recommendationStrategies:
    get:
      description: 'Lists the existing strategies.

        '
      operationId: ListRecommendationStrategies
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/CompartmentIdInSubtreeQueryParam'
      - $ref: '#/parameters/FilterByNameQueryParam'
      - $ref: '#/parameters/RecommendationNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/RecommendationStrategyCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists the existing strategies.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RecommendationStrategySummary'
  /recommendations:
    get:
      description: 'Lists the Cloud Advisor recommendations that are currently supported.

        '
      operationId: ListRecommendations
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/CompartmentIdInSubtreeQueryParam'
      - $ref: '#/parameters/CategoryIdQueryParam'
      - $ref: '#/parameters/FilterByNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/StatusQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/RecommendationCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Include a list of optimizer recommendations.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/RecommendationSummary'
  /recommendations/{recommendationId}:
    get:
      description: 'Gets the recommendation for the specified OCID.

        '
      operationId: GetRecommendation
      parameters:
      - $ref: '#/parameters/RecommendationIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The recommendation was retrieved.
          headers:
            etag: *id004
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/Recommendation'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the recommendation for the OCID specified.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
    put:
      description: "Updates the recommendation that corresponds to the specified OCID.\nUse this operation\
        \ to implement the following actions:\n\n  * Postpone recommendation\n  * Dismiss recommendation\n\
        \  * Reactivate recommendation\n"
      operationId: UpdateRecommendation
      parameters:
      - $ref: '#/parameters/RecommendationIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      - $ref: '#/parameters/OpcIfMatchHeaderParam'
      - description: The request object for updating the recommendation details.
        in: body
        name: UpdateRecommendationDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateRecommendationDetails'
      responses:
        200:
          description: The recommendation was updated.
          headers:
            etag: *id004
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/Recommendation'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates the recommendation with the given OCID.
      tags:
      - optimizer
  /recommendations/{recommendationId}/actions/bulkApplyRecommendations:
    post:
      description: 'Applies the specified recommendations to the resources.

        '
      operationId: BulkApplyRecommendations
      parameters:
      - $ref: '#/parameters/RecommendationIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      - $ref: '#/parameters/OpcRetryTokenHeader'
      - description: Details about bulk recommendation actions.
        in: body
        name: BulkApplyRecommendationsDetails
        required: true
        schema:
          $ref: '#/definitions/BulkApplyRecommendationsDetails'
      responses:
        202:
          description: The request was accepted for processing.
          headers:
            opc-request-id: *id003
            opc-work-request-id: &id019
              description: 'Unique Oracle-assigned identifier for the asynchronous request.

                You can use this to query status of the asynchronous operation.

                '
              type: string
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Applies the specified recommendations to the resources.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/Recommendation'
  /resourceActions:
    get:
      description: 'Lists the Cloud Advisor resource actions that are supported.

        '
      operationId: ListResourceActions
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/CompartmentIdInSubtreeQueryParam'
      - $ref: '#/parameters/FilterByNameQueryParam'
      - $ref: '#/parameters/ResourceTypeQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/StatusQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The resource actions were retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/ResourceActionCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Include a list of resource actions.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/ResourceActionSummary'
  /resourceActions/actions/getQueryableFields:
    get:
      description: 'Lists the fields that are indexed for querying and their associated value types.

        '
      operationId: ListResourceActionQueryableFields
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/CompartmentIdInSubtreeQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-prev-page: *id002
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/QueryableFieldCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List of all the fields and their value type that are indexed for querying resource actions.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/QueryableFieldSummary'
  /resourceActions/{resourceActionId}:
    get:
      description: 'Gets the resource action that corresponds to the specified OCID.

        '
      operationId: GetResourceAction
      parameters:
      - $ref: '#/parameters/ResourceActionIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The resource action was retrieved.
          headers:
            etag: *id004
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/ResourceAction'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get the resource action for the OCID specified.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
    put:
      description: "Updates the resource action that corresponds to the specified OCID. \nUse this operation\
        \ to implement the following actions:\n\n  * Postpone resource action\n  * Ignore resource action\n\
        \  * Reactivate resource action\n"
      operationId: UpdateResourceAction
      parameters:
      - $ref: '#/parameters/ResourceActionIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      - $ref: '#/parameters/OpcIfMatchHeaderParam'
      - description: The resource action information to be updated.
        in: body
        name: UpdateResourceActionDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateResourceActionDetails'
      responses:
        200:
          description: The resource action was updated.
          headers:
            etag: *id004
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/ResourceAction'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Updates the resource action with the given OCID.
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
  /workRequests:
    get:
      description: 'Lists the work requests in the tenancy. The tenancy is the root compartment.

        '
      operationId: ListWorkRequests
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/WorkRequestCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List Work Requests
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}:
    get:
      description: Gets the status of the work request associated with the specified ID.
      operationId: GetWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The work request status was retrieved.
          headers:
            opc-request-id: *id003
            retry-after: &id020
              description: A decimal number representing the number of seconds the client should wait
                before polling this endpoint again.
              format: float
              type: number
          schema:
            $ref: '#/definitions/WorkRequest'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get Work Request Status
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}/errors:
    get:
      description: 'Lists errors associated with the specified work request.

        '
      operationId: ListWorkRequestErrors
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/WorkRequestErrorCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists work request errors
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/WorkRequestError'
  /workRequests/{workRequestId}/logs:
    get:
      description: 'Lists the logs associated with the specified work request.

        '
      operationId: ListWorkRequestLogs
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/OpcRequestIdHeaderParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id001
            opc-request-id: *id003
          schema:
            $ref: '#/definitions/WorkRequestLogEntryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists the work request logs
      tags:
      - optimizer
      x-obmcs-client-retries-enabled: true
      x-related-resource: '#/definitions/WorkRequestLogEntry'
definitions:
  Action:
    description: 'Details about the recommended action.

      '
    properties:
      description:
        description: Text describing the recommended action.
        maxLength: 400
        minLength: 1
        type: string
      type:
        description: The status of the resource action.
        enum: &id005
        - KB_ARTICLE
        type: string
        x-obmcs-top-level-enum: '#/definitions/ActionType'
      url:
        description: The URL path to documentation that explains how to perform the action.
        maxLength: 255
        minLength: 1
        type: string
    required:
    - type
    - description
    - url
    type: object
  ActionType:
    description: Possible recommended actions.
    enum: *id005
    type: string
  BulkApplyRecommendationsDetails:
    description: Details about bulk recommendation actions.
    properties:
      actions:
        description: The unique resource actions that recommendations are applied to.
        items:
          $ref: '#/definitions/BulkApplyResourceAction'
        type: array
        uniqueItems: true
      resourceActionIds:
        description: 'The unique OCIDs of the resource actions that recommendations are applied to.


          This field is deprecated.

          '
        items:
          maxLength: 255
          minLength: 1
          type: string
        type: array
        uniqueItems: true
      status:
        description: The current status of the recommendation.
        enum: &id006
        - PENDING
        - DISMISSED
        - POSTPONED
        - IMPLEMENTED
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      timeStatusEnd:
        description: 'The date and time the current status will change. The format is defined by RFC3339.


          For example, "The current `postponed` status of the resource action will end and change to `pending`
          on this

          date and time."

          '
        format: date-time
        type: string
    required:
    - status
    type: object
  BulkApplyResourceAction:
    description: The resource action that a recommendation will be applied to.
    properties:
      parameters:
        additionalProperties:
          type: object
        description: 'Additional parameter key-value pairs defining the resource action.

          For example:


          `{"timeAmount": 15, "timeUnit": "seconds"}`

          '
        type: object
      resourceActionId:
        description: The unique OCIDs of the resource actions that recommendations are applied to.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: The current status of the recommendation.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      strategyName:
        description: The name of the strategy.
        maxLength: 255
        minLength: 1
        type: string
      timeStatusEnd:
        description: 'The date and time the current status will change. The format is defined by RFC3339.


          For example, "The current `postponed` status of the resource action will end and change to `pending`
          on this

          date and time."

          '
        format: date-time
        type: string
    required:
    - resourceActionId
    type: object
  Category:
    description: 'The metadata associated with the category.

      '
    properties:
      compartmentId:
        description: The OCID of the tenancy. The tenancy is the root compartment.
        maxLength: 255
        minLength: 1
        type: string
      description:
        description: Text describing the category.
        maxLength: 400
        minLength: 1
        type: string
      estimatedCostSaving:
        description: The estimated cost savings, in dollars, for the category. If cost information is
          not available, the value is -1.
        format: double
        minimum: 0
        type: number
      extendedMetadata:
        additionalProperties:
          type: string
        description: 'Additional metadata key/value pairs for the category.


          For example:


          `{"EstimatedSaving": "200"}`

          '
        maxLength: 255
        maxProperties: 25
        type: object
      id:
        description: The unique OCID of the category.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The category's current state.
        enum: &id007
        - ACTIVE
        - FAILED
        - INACTIVE
        - ATTACHING
        - DETACHING
        - DELETING
        - DELETED
        - UPDATING
        - CREATING
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      name:
        description: The name assigned to the category.
        maxLength: 255
        minLength: 1
        type: string
      recommendationCounts:
        description: An array of `RecommendationCount` objects grouped by the level of importance assigned
          to the recommendation.
        items:
          $ref: '#/definitions/RecommendationCount'
        type: array
      resourceCounts:
        description: An array of `ResourceCount` objects grouped by the status of the recommendation.
        items:
          $ref: '#/definitions/ResourceCount'
        type: array
      timeCreated:
        description: The date and time the category details were created, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the category details were last updated, in the format defined by
          RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - name
    - description
    - recommendationCounts
    - resourceCounts
    - lifecycleState
    - estimatedCostSaving
    - timeCreated
    - timeUpdated
    type: object
  CategoryCollection:
    description: 'A list of categories that match filter criteria, if any. Results contain `CategorySummary`
      objects.

      '
    properties:
      items:
        description: A collection of category summaries.
        items:
          $ref: '#/definitions/CategorySummary'
        type: array
    required:
    - items
    type: object
  CategorySummary:
    description: 'The metadata associated with the category summary.

      '
    properties:
      compartmentId:
        description: The OCID of the tenancy. The tenancy is the root compartment.
        maxLength: 255
        minLength: 1
        type: string
      description:
        description: Text describing the category.
        maxLength: 400
        minLength: 1
        type: string
      estimatedCostSaving:
        description: The estimated cost savings, in dollars, for the category. If cost information is
          not available, the value is -1.
        format: double
        minimum: 0
        type: number
      extendedMetadata:
        additionalProperties:
          type: string
        description: 'Additional metadata key/value pairs for the category summary.


          For example:


          `{"EstimatedSaving": "200"}`

          '
        maxLength: 255
        maxProperties: 25
        type: object
      id:
        description: The unique OCID of the category.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The category's current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      name:
        description: The name assigned to the category.
        maxLength: 255
        minLength: 1
        type: string
      recommendationCounts:
        description: An array of `RecommendationCount` objects grouped by the level of importance assigned
          to each recommendation.
        items:
          $ref: '#/definitions/RecommendationCount'
        type: array
      resourceCounts:
        description: An array of `ResourceCount` objects grouped by the status of each recommendation.
        items:
          $ref: '#/definitions/ResourceCount'
        type: array
      timeCreated:
        description: The date and time the category details were created, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the category details were last updated, in the format defined by
          RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - name
    - description
    - recommendationCounts
    - resourceCounts
    - lifecycleState
    - estimatedCostSaving
    - timeCreated
    - timeUpdated
    type: object
  CreateProfileDetails:
    description: 'Details for creating a profile.

      '
    properties:
      aggregationIntervalInDays:
        description: The time period over which to collect data for the recommendations, measured in number
          of days.
        maximum: 90
        minimum: 1
        type: integer
      compartmentId:
        description: The OCID of the tenancy. The tenancy is the root compartment.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: &id009
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer, and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.


            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm).


          Example: `{"foo-namespace": {"bar-key": "value"}}`

          '
        type: object
      description:
        description: Text describing the profile. Avoid entering confidential information.
        maxLength: 400
        minLength: 1
        type: string
      freeformTags: &id010
        additionalProperties:
          type: string
        description: 'Simple key-value pair applied without any predefined name, type, or namespace.

          For more information, see [Resource Tags](/Content/General/Concepts/resourcetags.htm). Exists
          for cross-compatibility only.


          Example: `{"bar-key": "value"}`

          '
        type: object
      levelsConfiguration:
        $ref: '#/definitions/LevelsConfiguration'
      name:
        description: The name assigned to the profile. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      targetCompartments:
        $ref: '#/definitions/TargetCompartments'
      targetTags:
        $ref: '#/definitions/TargetTags'
    required:
    - compartmentId
    - name
    - description
    - levelsConfiguration
    type: object
  EnrollmentStatus:
    description: 'The metadata associated with the enrollment status.

      '
    properties:
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      id:
        description: The OCID of the enrollment status.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The enrollment status' current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      status:
        description: The current Cloud Advisor enrollment status.
        enum: &id008
        - ACTIVE
        - INACTIVE
        type: string
        x-obmcs-top-level-enum: '#/definitions/OptimizerEnrollmentStatus'
      statusReason:
        description: The reason for the enrollment status of the tenancy.
        maxLength: 400
        minLength: 1
        type: string
      timeCreated:
        description: The date and time the enrollment status was created, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the enrollment status was last updated, in the format defined by
          RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - status
    type: object
  EnrollmentStatusCollection:
    description: 'A list of enrollment statuses that match filter criteria, if any. Results contain `EnrollmentStatusSummary`
      objects.

      '
    properties:
      items:
        description: A collection of enrollment status summaries.
        items:
          $ref: '#/definitions/EnrollmentStatusSummary'
        type: array
    required:
    - items
    type: object
  EnrollmentStatusSummary:
    description: 'The metadata associated with the enrollment status summary.

      '
    properties:
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      id:
        description: The OCID of the enrollment status.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The enrollment status' current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      status:
        description: The current Cloud Advisor enrollment status.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/OptimizerEnrollmentStatus'
      statusReason:
        description: The reason for the enrollment status of the tenancy.
        maxLength: 400
        minLength: 1
        type: string
      timeCreated:
        description: The date and time the enrollment status was created, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the enrollment status was last updated, in the format defined by
          RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - status
    type: object
  Error:
    description: The representation of an error.
    properties:
      code:
        description: 'A short error code that defines the error, meant for programmatic parsing.

          See [API Errors](/Content/API/References/apierrors.htm).

          '
        type: string
      message:
        description: A human-readable error string.
        type: string
    required:
    - code
    - message
    type: object
  EvaluatedMetric:
    description: One of the metrics that will be evaluated by profiles using this profile level.
    properties:
      name:
        description: The name of the metric (e.g., `CpuUtilization`).
        maxLength: 255
        minLength: 1
        type: string
      statistic:
        description: The name of the statistic (e.g., `p95`).
        maxLength: 255
        minLength: 1
        type: string
      target:
        description: Optional. The metric value that the recommendation will target.
        format: double
        type: number
      threshold:
        description: The threshold that must be crossed for the recommendation to appear.
        format: double
        type: number
    required:
    - name
    - statistic
    - threshold
    type: object
  HistoryCollection:
    description: 'A list containing the recommendation history items that match filter criteria, if any.
      Results contain `HistorySummary` objects.

      '
    properties:
      items:
        description: A collection of history summaries.
        items:
          $ref: '#/definitions/HistorySummary'
        type: array
    required:
    - items
    type: object
  HistorySummary:
    description: 'The metadata associated with the recommendation history and its related resources.

      '
    properties:
      action:
        $ref: '#/definitions/Action'
      categoryId:
        description: The unique OCID associated with the category.
        maxLength: 255
        minLength: 1
        type: string
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      compartmentName:
        description: The name assigned to the compartment.
        maxLength: 100
        minLength: 1
        type: string
      estimatedCostSaving:
        description: The estimated cost savings, in dollars, for the resource action. If cost information
          is not available, the value is -1.
        format: double
        minimum: 0
        type: number
      extendedMetadata:
        additionalProperties:
          type: object
        description: 'Additional metadata key/value pairs that you provide.

          They serve the same purpose and functionality as fields in the `metadata` object.


          They are distinguished from `metadata` fields in that these can be nested JSON objects (whereas
          `metadata` fields are string/string maps only).


          For example:


          `{"CurrentShape": {"name":"VM.Standard2.16"}, "RecommendedShape": {"name":"VM.Standard2.8"}}`

          '
        type: object
      id:
        description: The unique OCID associated with the recommendation history.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The recommendation history's current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      metadata:
        additionalProperties:
          type: string
        description: "Custom metadata key/value pairs for the resource action.\n\n **Metadata Example**\n\
          \n      \"metadata\" : {\n         \"cpuRecommendedShape\": \"VM.Standard1.1\",\n         \"\
          computeMemoryUtilization\": \"26.05734124418388\",\n         \"currentShape\": \"VM.Standard1.2\"\
          ,\n         \"instanceRecommendedShape\": \"VM.Standard1.1\",\n         \"computeCpuUtilization\"\
          : \"7.930035319720132\",\n         \"memoryRecommendedShape\": \"None\"\n      }\n"
        type: object
      name:
        description: The name assigned to the resource.
        maxLength: 255
        minLength: 1
        type: string
      recommendationId:
        description: The unique OCID associated with the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      recommendationName:
        description: The name assigned to the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      resourceActionId:
        description: The unique OCID associated with the resource action.
        maxLength: 255
        minLength: 1
        type: string
      resourceId:
        description: The unique OCID associated with the resource.
        maxLength: 255
        minLength: 1
        type: string
      resourceType:
        description: The kind of resource.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: The current status of the resource action.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      timeCreated:
        description: The date and time the recommendation history was created, in the format defined by
          RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - name
    - resourceType
    - categoryId
    - recommendationId
    - recommendationName
    - resourceId
    - resourceActionId
    - action
    - compartmentId
    - compartmentName
    - lifecycleState
    - estimatedCostSaving
    - status
    type: object
  Importance:
    description: Possible importance types.
    enum: &id011
    - CRITICAL
    - HIGH
    - MODERATE
    - LOW
    - MINOR
    type: string
  LevelConfiguration:
    description: Details about the configuration level for the recommendation.
    properties:
      level:
        description: The pre-defined profile level.
        maxLength: 255
        minLength: 1
        type: string
      recommendationId:
        description: The unique OCID of the recommendation.
        maxLength: 255
        minLength: 1
        type: string
    type: object
  LevelsConfiguration:
    description: A list of configuration levels for each recommendation.
    properties:
      items:
        description: The array of configuration levels.
        items:
          $ref: '#/definitions/LevelConfiguration'
        type: array
    type: object
  LifecycleState:
    description: Possible lifecycle states.
    enum: *id007
    type: string
  OperationStatus:
    description: Possible operation statuses.
    enum: &id015
    - ACCEPTED
    - IN_PROGRESS
    - FAILED
    - SUCCEEDED
    - CANCELING
    - CANCELED
    type: string
  OperationType:
    description: Possible operation types.
    enum: &id014
    - BULK_APPLY_RECOMMENDATIONS
    type: string
  OptimizerEnrollmentStatus:
    description: Possible enrollment statuses.
    enum: *id008
    type: string
  Profile:
    description: 'The metadata associated with the profile.

      '
    properties:
      aggregationIntervalInDays:
        description: The time period over which to collect data for the recommendations, measured in number
          of days.
        maximum: 90
        minimum: 1
        type: integer
      compartmentId:
        description: The OCID of the tenancy. The tenancy is the root compartment.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id009
      description:
        description: Text describing the profile. Avoid entering confidential information.
        maxLength: 400
        minLength: 1
        type: string
      freeformTags: *id010
      id:
        description: The unique OCID of the profile.
        maxLength: 255
        minLength: 1
        type: string
      levelsConfiguration:
        $ref: '#/definitions/LevelsConfiguration'
      lifecycleState:
        description: The profile's current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      name:
        description: The name assigned to the profile. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      targetCompartments:
        $ref: '#/definitions/TargetCompartments'
      targetTags:
        $ref: '#/definitions/TargetTags'
      timeCreated:
        description: The date and time the profile was created, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the profile was last updated, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - name
    - description
    - lifecycleState
    - timeCreated
    - timeUpdated
    type: object
  ProfileCollection:
    description: 'A list of profiles that match filter criteria, if any. Results contain `ProfileSummary`
      objects.

      '
    properties:
      items:
        description: A collection of profile summaries.
        items:
          $ref: '#/definitions/ProfileSummary'
        type: array
    required:
    - items
    type: object
  ProfileLevelCollection:
    description: 'A list of profile levels that match the filter criteria, if any. The result contains
      `ProfileLevelSummary` objects.

      '
    properties:
      items:
        description: A collection of profile levels.
        items:
          $ref: '#/definitions/ProfileLevelSummary'
        type: array
    required:
    - items
    type: object
  ProfileLevelSummary:
    description: 'The metadata associated with the profile level summary.

      '
    properties:
      defaultInterval:
        description: 'The default aggregation interval (in days) for profiles using this profile level.

          '
        minimum: 1
        type: integer
      metrics:
        description: The metrics that will be evaluated by profiles using this profile level.
        items:
          $ref: '#/definitions/EvaluatedMetric'
        type: array
      name:
        description: A unique name for the profile level.
        maxLength: 255
        minLength: 1
        type: string
      recommendationName:
        description: The name of the recommendation this profile level applies to.
        maxLength: 255
        minLength: 1
        type: string
      timeCreated:
        description: The date and time the category details were created, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the category details were last updated, in the format defined by
          RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      validIntervals:
        description: 'An array of aggregation intervals (in days) allowed for profiles using this profile
          level.

          '
        items:
          minimum: 1
          type: integer
        type: array
        uniqueItems: true
    required:
    - name
    - recommendationName
    - metrics
    - defaultInterval
    - validIntervals
    - timeCreated
    - timeUpdated
    type: object
  ProfileSummary:
    description: 'The metadata associated with the profile summary.

      '
    properties:
      aggregationIntervalInDays:
        description: The time period over which to collect data for the recommendations, measured in number
          of days.
        maximum: 90
        minimum: 1
        type: integer
      compartmentId:
        description: The OCID of the tenancy. The tenancy is the root compartment.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id009
      description:
        description: Text describing the profile.
        maxLength: 400
        minLength: 1
        type: string
      freeformTags: *id010
      id:
        description: The unique OCID of the profile.
        maxLength: 255
        minLength: 1
        type: string
      levelsConfiguration:
        $ref: '#/definitions/LevelsConfiguration'
      lifecycleState:
        description: The profile's current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      name:
        description: The name assigned to the profile.
        maxLength: 255
        minLength: 1
        type: string
      targetCompartments:
        $ref: '#/definitions/TargetCompartments'
      targetTags:
        $ref: '#/definitions/TargetTags'
      timeCreated:
        description: The date and time the profile was created, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the profile was last updated, in the format defined by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - name
    - description
    - lifecycleState
    - timeCreated
    - timeUpdated
    type: object
  QueryDetails:
    description: The request object for querying the resource action details.
    properties:
      query:
        description: 'The query describing which resources to search for.

          For more information, see [Query Language Syntax](/iaas/Content/CloudAdvisor/Reference/query-syntax.htm).

          '
        type: string
    type: object
    x-example: "{\n  \"query\": \"query resourceAction where resourceType = 'compute' && name =~ 'VM instance'\
      \ && ( definedTags.tagNamespace = 'categorization' && definedTags.tagKey = 'region' && definedTags.tagValue\
      \ = 'phx') sorted by name desc\"\n}\n"
  QueryableFieldCollection:
    description: 'List of the fields that are indexed for querying and their associated value types. Results
      contain `QueryableFieldSummary` objects.

      '
    properties:
      items:
        description: A collection of queryable field summaries.
        items:
          $ref: '#/definitions/QueryableFieldSummary'
        type: array
    required:
    - items
    type: object
  QueryableFieldSummary:
    description: 'An individual field that can be used as part of a query filter.

      '
    properties:
      fieldName:
        description: 'The name of the field to use when constructing the query. Field names are present
          for all types except `OBJECT`.

          '
        type: string
      fieldType:
        description: 'The type of the field, which dictates the semantics and query constraints that you
          can use when searching or querying.

          '
        enum:
        - STRING
        - INTEGER
        - BOOLEAN
        - DATE_TIME
        - OBJECT
        type: string
      objectProperties:
        description: 'If the field type is `OBJECT`, this property lists the individual properties of
          the object that can be queried.

          '
        items:
          $ref: '#/definitions/QueryableFieldSummary'
        type: array
    required:
    - fieldType
    - fieldName
    type: object
    x-example: "[\n  {\n    \"fieldName\": \"name\",\n    \"fieldType\": \"STRING\",\n    \"objectProperties\"\
      : null\n  },\n  {\n      \"fieldName\": \"definedTags\",\n      \"fieldType\": \"OBJECT\",\n   \
      \   \"possibleValues\": null,\n      \"objectProperties\": [\n        {\n          \"fieldName\"\
      : \"definedTags.key\",\n          \"fieldType\": \"STRING\",\n          \"objectProperties\": null\n\
      \        },\n        {\n          \"fieldName\": \"definedTags.namespace\",\n          \"fieldType\"\
      : \"STRING\",\n          \"objectProperties\": null\n        },\n        {\n          \"fieldName\"\
      : \"definedTags.value\",\n          \"fieldType\": \"STRING\",\n          \"objectProperties\":\
      \ null\n        }\n      ]\n    }\n]\n"
  Recommendation:
    description: 'The metadata associated with the recommendation.

      '
    properties:
      categoryId:
        description: The unique OCID associated with the category.
        maxLength: 255
        minLength: 1
        type: string
      compartmentId:
        description: The OCID of the tenancy. The tenancy is the root compartment.
        maxLength: 255
        minLength: 1
        type: string
      description:
        description: Text describing the recommendation.
        maxLength: 400
        minLength: 1
        type: string
      estimatedCostSaving:
        description: The estimated cost savings, in dollars, for the recommendation. If cost information
          is not available, the value is -1.
        format: double
        minimum: 0
        type: number
      extendedMetadata:
        additionalProperties:
          type: string
        description: 'Additional metadata key/value pairs for the recommendation.


          For example:


          `{"EstimatedSaving": "200"}`

          '
        maxLength: 255
        maxProperties: 25
        type: object
      id:
        description: The unique OCID associated with the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      importance:
        description: The level of importance assigned to the recommendation.
        enum: *id011
        type: string
        x-obmcs-top-level-enum: '#/definitions/Importance'
      lifecycleState:
        description: The recommendation's current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      name:
        description: The name assigned to the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      resourceCounts:
        description: An array of `ResourceCount` objects grouped by the status of the resource actions.
        items:
          $ref: '#/definitions/ResourceCount'
        type: array
      status:
        description: The current status of the recommendation.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      supportedLevels:
        $ref: '#/definitions/SupportedLevels'
      timeCreated:
        description: The date and time the recommendation details were created, in the format defined
          by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeStatusBegin:
        description: 'The date and time that the recommendation entered its current status. The format
          is defined by RFC3339.


          For example, "The status of the recommendation changed from `pending` to `current(ignored)`
          on this date and time."

          '
        format: date-time
        type: string
      timeStatusEnd:
        description: 'The date and time the current status will change. The format is defined by RFC3339.


          For example, "The current `postponed` status of the recommendation will end and change to `pending`
          on this

          date and time."

          '
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the recommendation details were last updated, in the format defined
          by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - categoryId
    - name
    - description
    - importance
    - resourceCounts
    - lifecycleState
    - estimatedCostSaving
    - status
    - timeStatusBegin
    type: object
  RecommendationCollection:
    description: 'A list of recommendations that match filter criteria, if any. Results contain `RecommendationSummary`
      objects.

      '
    properties:
      items:
        description: A collection of recommendations.
        items:
          $ref: '#/definitions/RecommendationSummary'
        type: array
    required:
    - items
    type: object
  RecommendationCount:
    description: The count of recommendations in a category, grouped by importance.
    properties:
      count:
        description: The count of recommendations.
        minimum: 0
        type: integer
      importance:
        description: The level of importance assigned to the recommendation.
        enum: *id011
        type: string
        x-obmcs-top-level-enum: '#/definitions/Importance'
    required:
    - importance
    - count
    type: object
  RecommendationStrategyCollection:
    description: 'A list of strategies that match filter criteria, if any. Results contain `RecommendationStrategySummary`
      objects.

      '
    properties:
      items:
        description: A collection of recommendation strategy summaries.
        items:
          $ref: '#/definitions/RecommendationStrategySummary'
        type: array
    required:
    - items
    type: object
  RecommendationStrategySummary:
    description: 'The metadata associated with the recommendation strategy.

      '
    properties:
      name:
        description: The display name of the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      strategies:
        description: The list of strategies used.
        items:
          $ref: '#/definitions/Strategy'
        type: array
    required:
    - name
    - strategies
    type: object
  RecommendationSummary:
    description: 'The metadata associated with the recommendation summary.

      '
    properties:
      categoryId:
        description: The unique OCID associated with the category.
        maxLength: 255
        minLength: 1
        type: string
      compartmentId:
        description: The OCID of the tenancy. The tenancy is the root compartment.
        maxLength: 255
        minLength: 1
        type: string
      description:
        description: Text describing the recommendation.
        maxLength: 400
        minLength: 1
        type: string
      estimatedCostSaving:
        description: The estimated cost savings, in dollars, for the recommendation. If cost information
          is not available, the value is -1.
        format: double
        minimum: 0
        type: number
      extendedMetadata:
        additionalProperties:
          type: string
        description: 'Additional metadata key/value pairs for the recommendation summary.


          For example:


          `{"EstimatedSaving": "200"}`

          '
        maxLength: 255
        maxProperties: 25
        type: object
      id:
        description: The unique OCID associated with the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      importance:
        description: The level of importance assigned to the recommendation.
        enum: *id011
        type: string
        x-obmcs-top-level-enum: '#/definitions/Importance'
      lifecycleState:
        description: The recommendation's current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      name:
        description: The name assigned to the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      resourceCounts:
        description: An array of `ResourceCount` objects grouped by the status of the resource actions.
        items:
          $ref: '#/definitions/ResourceCount'
        type: array
      status:
        description: The current status of the recommendation.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      supportedLevels:
        $ref: '#/definitions/SupportedLevels'
      timeCreated:
        description: The date and time the recommendation details were created, in the format defined
          by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeStatusBegin:
        description: 'The date and time that the recommendation entered its current status. The format
          is defined by RFC3339.


          For example, "The status of the recommendation changed from `pending` to `current(ignored)`
          on this date and time."

          '
        format: date-time
        type: string
      timeStatusEnd:
        description: 'The date and time the current status will change. The format is defined by RFC3339.


          For example, "The current `postponed` status of the recommendation will end and change to `pending`
          on this

          date and time."

          '
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the recommendation details were last updated, in the format defined
          by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - categoryId
    - name
    - description
    - importance
    - resourceCounts
    - lifecycleState
    - estimatedCostSaving
    - status
    - timeStatusBegin
    type: object
  ResourceAction:
    description: 'The metadata associated with the resource action.

      '
    properties:
      action:
        $ref: '#/definitions/Action'
      categoryId:
        description: The unique OCID associated with the category.
        maxLength: 255
        minLength: 1
        type: string
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      compartmentName:
        description: The name associated with the compartment.
        maxLength: 100
        minLength: 1
        type: string
      estimatedCostSaving:
        description: The estimated cost savings, in dollars, for the resource action. If cost information
          is not available, the value is -1.
        format: double
        minimum: 0
        type: number
      extendedMetadata:
        additionalProperties:
          type: object
        description: 'Additional metadata key/value pairs that you provide.

          They serve the same purpose and functionality as fields in the `metadata` object.


          They are distinguished from `metadata` fields in that these can be nested JSON objects (whereas
          `metadata` fields are string/string maps only).


          For example:


          `{"CurrentShape": {"name":"VM.Standard2.16"}, "RecommendedShape": {"name":"VM.Standard2.8"}}`

          '
        type: object
      id:
        description: The unique OCID associated with the resource action.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The resource action's current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      metadata:
        additionalProperties:
          type: string
        description: "Custom metadata key/value pairs for the resource action.\n\n **Metadata Example**\n\
          \n      \"metadata\" : {\n         \"cpuRecommendedShape\": \"VM.Standard1.1\",\n         \"\
          computeMemoryUtilization\": \"26.05734124418388\",\n         \"currentShape\": \"VM.Standard1.2\"\
          ,\n         \"instanceRecommendedShape\": \"VM.Standard1.1\",\n         \"computeCpuUtilization\"\
          : \"7.930035319720132\",\n         \"memoryRecommendedShape\": \"None\"\n      }\n"
        type: object
      name:
        description: The name assigned to the resource.
        maxLength: 255
        minLength: 1
        type: string
      recommendationId:
        description: The unique OCID associated with the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      resourceId:
        description: The unique OCID associated with the resource.
        maxLength: 255
        minLength: 1
        type: string
      resourceType:
        description: The kind of resource.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: The current status of the resource action.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      timeCreated:
        description: The date and time the resource action details were created, in the format defined
          by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeStatusBegin:
        description: 'The date and time that the resource action entered its current status. The format
          is defined by RFC3339.


          For example, "The status of the resource action changed from `pending` to `current(ignored)`
          on this date and time."

          '
        format: date-time
        type: string
      timeStatusEnd:
        description: 'The date and time the current status will change. The format is defined by RFC3339.


          For example, "The current `postponed` status of the resource action will end and change to `pending`
          on this

          date and time."

          '
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the resource action details were last updated, in the format defined
          by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - categoryId
    - recommendationId
    - resourceId
    - name
    - resourceType
    - compartmentId
    - compartmentName
    - action
    - lifecycleState
    - estimatedCostSaving
    - status
    - timeStatusBegin
    type: object
  ResourceActionCollection:
    description: 'A list of resource actions that match filter criteria, if any. Results contain `ResourceActionSummary`
      objects.

      '
    properties:
      items:
        description: A collection of resource actions.
        items:
          $ref: '#/definitions/ResourceActionSummary'
        type: array
    required:
    - items
    type: object
  ResourceActionSummary:
    description: 'The metadata associated with the resource action summary.

      '
    properties:
      action:
        $ref: '#/definitions/Action'
      categoryId:
        description: The unique OCID associated with the category.
        maxLength: 255
        minLength: 1
        type: string
      compartmentId:
        description: The OCID of the compartment.
        maxLength: 255
        minLength: 1
        type: string
      compartmentName:
        description: The name associated with the compartment.
        maxLength: 100
        minLength: 1
        type: string
      estimatedCostSaving:
        description: The estimated cost savings, in dollars, for the resource action. If cost information
          is not available, the value is -1.
        format: double
        minimum: 0
        type: number
      extendedMetadata:
        additionalProperties:
          type: object
        description: 'Additional metadata key/value pairs that you provide.

          They serve the same purpose and functionality as fields in the `metadata` object.


          They are distinguished from `metadata` fields in that these can be nested JSON objects (whereas
          `metadata` fields are string/string maps only).


          For example:


          `{"CurrentShape": {"name":"VM.Standard2.16"}, "RecommendedShape": {"name":"VM.Standard2.8"}}`

          '
        type: object
      id:
        description: The unique OCID associated with the resource action.
        maxLength: 255
        minLength: 1
        type: string
      lifecycleState:
        description: The resource action's current state.
        enum: *id007
        type: string
        x-obmcs-top-level-enum: '#/definitions/LifecycleState'
      metadata:
        additionalProperties:
          type: string
        description: "Custom metadata key/value pairs for the resource action.\n\n **Metadata Example**\n\
          \n      \"metadata\" : {\n         \"cpuRecommendedShape\": \"VM.Standard1.1\",\n         \"\
          computeMemoryUtilization\": \"26.05734124418388\",\n         \"currentShape\": \"VM.Standard1.2\"\
          ,\n         \"instanceRecommendedShape\": \"VM.Standard1.1\",\n         \"computeCpuUtilization\"\
          : \"7.930035319720132\",\n         \"memoryRecommendedShape\": \"None\"\n      }\n"
        type: object
      name:
        description: The name assigned to the resource.
        maxLength: 255
        minLength: 1
        type: string
      recommendationId:
        description: The unique OCID associated with the recommendation.
        maxLength: 255
        minLength: 1
        type: string
      resourceId:
        description: The unique OCID associated with the resource.
        maxLength: 255
        minLength: 1
        type: string
      resourceType:
        description: The kind of resource.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: The current status of the resource action.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      timeCreated:
        description: The date and time the resource action details were created, in the format defined
          by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
      timeStatusBegin:
        description: 'The date and time that the resource action entered its current status. The format
          is defined by RFC3339.


          For example, "The status of the resource action changed from `pending` to `current(ignored)`
          on this date and time."

          '
        format: date-time
        type: string
      timeStatusEnd:
        description: 'The date and time the current status will change. The format is defined by RFC3339.


          For example, "The current `postponed` status of the resource action will end and change to `pending`
          on this

          date and time."

          '
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the resource action details were last updated, in the format defined
          by RFC3339.
        example: '2020-08-25T21:10:29.600Z'
        format: date-time
        type: string
    required:
    - id
    - categoryId
    - recommendationId
    - resourceId
    - name
    - resourceType
    - compartmentId
    - compartmentName
    - action
    - lifecycleState
    - estimatedCostSaving
    - status
    - timeStatusBegin
    type: object
  ResourceCount:
    description: The count of resources in a category, grouped by status.
    properties:
      count:
        description: The count of resources.
        minimum: 0
        type: integer
      status:
        description: The recommendation status of the resource.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
    required:
    - status
    - count
    type: object
  SortOrder:
    description: Possible sort orders.
    enum: &id018
    - ASC
    - DESC
    type: string
  Status:
    description: Possible recommendation statuses.
    enum: *id006
    type: string
  Strategy:
    description: 'The metadata associated with the strategy. The strategy is the method used to apply
      the recommendation.

      '
    properties:
      isDefault:
        description: Whether this is the default recommendation strategy.
        type: boolean
      parametersDefinition:
        description: The list of strategies for the parameters.
        items:
          $ref: '#/definitions/StrategyParameter'
        type: array
      strategyName:
        description: The name of the strategy.
        maxLength: 255
        minLength: 1
        type: string
    required:
    - strategyName
    - isDefault
    type: object
  StrategyParameter:
    description: 'The metadata associated with the strategy parameter.

      '
    properties:
      defaultValue:
        description: A default value used for the strategy parameter.
        type: object
      description:
        description: Text describing the strategy parameter.
        maxLength: 400
        minLength: 1
        type: string
      isRequired:
        description: Whether this parameter is required.
        type: boolean
      name:
        description: The name of the strategy parameter.
        maxLength: 255
        minLength: 1
        type: string
      possibleValues:
        description: The list of possible values used for these strategy parameters.
        items:
          type: object
        type: array
      type:
        description: The type of strategy parameter.
        enum: &id012
        - STRING
        - BOOLEAN
        - NUMBER
        - DATETIME
        type: string
        x-obmcs-top-level-enum: '#/definitions/StrategyParameterType'
    required:
    - name
    - type
    - description
    - isRequired
    type: object
  StrategyParameterType:
    description: Possible strategy types.
    enum: *id012
    type: string
  SupportedLevel:
    description: 'A system defined profile level supported by the recommendation.

      '
    properties:
      name:
        description: The name of the profile level.
        maxLength: 255
        minLength: 1
        type: string
    type: object
  SupportedLevels:
    description: 'Optional. The profile levels supported by a recommendation.

      For example, profile level values could be `Low`, `Medium`, and `High`.

      Not all recommendations support this field.

      '
    properties:
      items:
        description: The list of supported levels.
        items:
          $ref: '#/definitions/SupportedLevel'
        type: array
    type: object
  TagValueType:
    description: 'Specifies which tag value types in the `tagValues` field result in overrides of the
      recommendation criteria. Allowed values are `ANY` and `VALUE`.


      When the `tagValueType` field value is `ANY`, the `tagValues` field should be empty, which enforces
      overrides to the recommendation for resources with any tag values attached to them.

      When the `tagValueType` field value is `VALUE`, the `tagValues` field must include a specific value
      or list of values. Overrides to the recommendation criteria only occur for resources that match
      the values in the `tagKey` and the `tagValues` fields.


      For example, if the `tagKey` value is `B`, the `tagValueType` value is `ANY`, and the `tagValues`
      field is empty, overrides to the recommendation criteria occur for any resources that have the tag
      key B.

      If the `tagKey` value is `B`, the `tagValueType` value is `VALUE`, and the `tagValues` value is
      `s1`, overrides to the recommendation criteria only occur for resources that have the tag key B
      with the associated tag value s1.

      '
    enum: &id013
    - VALUE
    - ANY
    type: string
  TargetCompartments:
    description: 'Optional. The compartments specified in the profile override for a recommendation.

      '
    properties:
      items:
        description: The list of OCIDs attached to the compartments specified in the current profile override.
        items:
          maxLength: 255
          minLength: 1
          type: string
        type: array
    required:
    - items
    type: object
  TargetTag:
    description: 'A tag key definition used in the current profile override, including the tag namespace,
      tag key, tag value type, and tag values.

      Only defined tags are supported.

      For more information about tagging, see [Tagging Overview](/iaas/Content/Tagging/Concepts/taggingoverview.htm)

      '
    properties:
      tagDefinitionName:
        description: The name you use to refer to the tag, also known as the tag key.
        maxLength: 100
        minLength: 1
        type: string
      tagNamespaceName:
        description: The name of the tag namespace.
        maxLength: 100
        minLength: 1
        type: string
      tagValueType:
        description: 'Specifies which tag value types in the `tagValues` field result in overrides of
          the recommendation criteria.


          When the value for this field is `ANY`, the `tagValues` field should be empty, which enforces
          overrides to the recommendation

          for resources with any tag values attached to them.


          When the value for this field value is `VALUE`, the `tagValues` field must include a specific
          value or list of values.

          Overrides to the recommendation criteria only occur for resources that match the values in the
          `tagValues` fields.

          '
        enum: *id013
        type: string
        x-obmcs-top-level-enum: '#/definitions/TagValueType'
      tagValues:
        description: The list of tag values. The tag value is the value that the user applying the tag
          adds to the tag key.
        items:
          maxLength: 256
          minLength: 1
          type: string
        type: array
    required:
    - tagNamespaceName
    - tagDefinitionName
    - tagValueType
    type: object
  TargetTags:
    description: 'Optional. The tags specified in the profile override for a recommendation.

      '
    properties:
      items:
        description: The list of tags specified in the current profile override.
        items:
          $ref: '#/definitions/TargetTag'
        type: array
    required:
    - items
    type: object
  UpdateEnrollmentStatusDetails:
    description: 'The request object for updating the enrollment status details.

      '
    properties:
      status:
        description: The Cloud Advisor enrollment status.
        enum: *id008
        type: string
        x-obmcs-top-level-enum: '#/definitions/OptimizerEnrollmentStatus'
    required:
    - status
    type: object
  UpdateProfileDetails:
    description: 'Details for updating a profile.

      '
    properties:
      aggregationIntervalInDays:
        description: The time period over which to collect data for the recommendations, measured in number
          of days.
        maximum: 90
        minimum: 1
        type: integer
      definedTags: *id009
      description:
        description: Text describing the profile. Avoid entering confidential information.
        maxLength: 400
        minLength: 1
        type: string
      freeformTags: *id010
      levelsConfiguration:
        $ref: '#/definitions/LevelsConfiguration'
      name:
        description: The name assigned to the profile. Avoid entering confidential information.
        maxLength: 255
        minLength: 1
        type: string
      targetCompartments:
        $ref: '#/definitions/TargetCompartments'
      targetTags:
        $ref: '#/definitions/TargetTags'
    type: object
  UpdateRecommendationDetails:
    description: The request object for updating the recommendation details.
    properties:
      status:
        description: The status of the recommendation.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      timeStatusEnd:
        description: 'The date and time the current status will change. The format is defined by RFC3339.


          For example, "The current `postponed` status of the recommendation will end and change to `pending`
          on this

          date and time."

          '
        format: date-time
        type: string
    required:
    - status
    type: object
  UpdateResourceActionDetails:
    description: The request object for updating the resource action details.
    properties:
      status:
        description: The status of the resource action.
        enum: *id006
        type: string
        x-obmcs-top-level-enum: '#/definitions/Status'
      timeStatusEnd:
        description: 'The date and time the current status will change. The format is defined by RFC3339.


          For example, "The current `postponed` status of the resource action will end and change to `pending`
          on this

          date and time."

          '
        format: date-time
        type: string
    required:
    - status
    type: object
  WorkRequest:
    description: The asynchronous API request does not take effect immediately. This request spawns an
      asynchronous workflow to fulfill the request. WorkRequest objects provide visibility for in-progress
      workflows.
    properties:
      compartmentId:
        description: 'The OCID of the compartment that contains the work request.

          '
        type: string
      id:
        description: The OCID of the work request.
        type: string
      operationType:
        description: An enum-like description of the type of work the work request is doing.
        enum: *id014
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: How much progress the operation has made.
        format: float
        type: number
      resources:
        description: The resources this work request affects.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: The current status of the work request.
        enum: *id015
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: 'Date and time the work was accepted, in the format defined by RFC3339. Example:
          `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeFinished:
        description: 'Date and time the work completed, in the format defined by RFC3339. Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
      timeStarted:
        description: 'Date and time the work started, in the format defined by RFC3339. Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
    type: object
  WorkRequestActionType:
    description: Possible work request resource actions.
    enum: &id016
    - CREATED
    - UPDATED
    - DELETED
    - IN_PROGRESS
    - RELATED
    type: string
  WorkRequestCollection:
    description: 'A list of work requests that match filter criteria, if any. Results contain `WorkRequest`
      objects.

      '
    properties:
      items:
        description: A collection of work requests.
        items:
          $ref: '#/definitions/WorkRequest'
        type: array
    required:
    - items
    type: object
  WorkRequestError:
    description: Details about errors encountered while executing a work request.
    properties:
      code:
        description: 'A machine-usable code for the error that occurred.

          '
        type: string
      message:
        description: A human-readable error string.
        type: string
      timestamp:
        description: 'Date and time the error happened, in the format defined by RFC3339. Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - code
    - message
    - timestamp
    type: object
  WorkRequestErrorCollection:
    description: 'A list of work request errors that match filter criteria, if any. Results contain `WorkRequestError`
      objects.

      '
    properties:
      items:
        description: A collection of work request errors.
        items:
          $ref: '#/definitions/WorkRequestError'
        type: array
    required:
    - items
    type: object
  WorkRequestLogEntry:
    description: Details about the log entity.
    properties:
      message:
        description: A human-readable error string.
        type: string
      timestamp:
        description: 'Date and time the log was written, in the format defined by RFC3339. Example: `2016-08-25T21:10:29.600Z`

          '
        format: date-time
        type: string
    required:
    - message
    - timestamp
    type: object
  WorkRequestLogEntryCollection:
    description: 'A list of work request logs that match filter criteria, if any. Results contain `WorkRequestLogEntry`
      objects.

      '
    properties:
      items:
        description: A collection of work request log entries.
        items:
          $ref: '#/definitions/WorkRequestLogEntry'
        type: array
    required:
    - items
    type: object
  WorkRequestResource:
    description: Details about the resource entity.
    properties:
      actionType:
        description: 'The way in which this resource was affected by the work tracked by the work request.


          A resource being created, updated, or deleted remains in the `IN_PROGRESS` state until

          work is complete for that resource. At that point, the resource transitions to the `CREATED`,
          `UPDATED`,

          or `DELETED` state.

          '
        enum: *id016
        type: string
        x-obmcs-top-level-enum: '#/definitions/WorkRequestActionType'
      entityType:
        description: The resource type the work request affects.
        type: string
      entityUri:
        description: The URI path that the user can do a GET on to access the resource metadata
        type: string
      identifier:
        description: The resource identifier the work request affects.
        type: string
      metadata:
        additionalProperties:
          type: string
        description: Additional information about the resource.
        enum: &id017
        - OPERATION_NAME
        type: object
        x-obmcs-top-level-enum: '#/definitions/WorkRequestResourceMetadataKey'
    required:
    - actionType
    - entityType
    - identifier
    type: object
  WorkRequestResourceMetadataKey:
    description: Keys for work request resource metadata.
    enum: *id017
    type: string
parameters:
  CategoryIdPathParam:
    description: The unique OCID associated with the category.
    in: path
    maxLength: 255
    minLength: 1
    name: categoryId
    required: true
    type: string
  CategoryIdQueryParam:
    description: The unique OCID associated with the category.
    in: query
    maxLength: 255
    minLength: 1
    name: categoryId
    type: string
    x-default-description: 'null'
  CategoryNameQueryParam:
    description: Optional. A filter that returns results that match the category name specified.
    in: query
    maxLength: 255
    minLength: 1
    name: categoryName
    required: false
    type: string
    x-default-description: 'null'
  CompartmentIdInSubtreeQueryParam:
    description: 'When set to true, the hierarchy of compartments is traversed and all compartments and
      subcompartments in the tenancy are returned depending on the the setting of `accessLevel`.


      Can only be set to true when performing ListCompartments on the tenancy (root compartment).

      '
    in: query
    name: compartmentIdInSubtree
    required: true
    type: boolean
  CompartmentIdQueryParam:
    description: The OCID of the compartment.
    in: query
    maxLength: 255
    minLength: 1
    name: compartmentId
    required: true
    type: string
  EnrollmentStatusIdPathParam:
    description: The unique OCID associated with the enrollment status.
    in: path
    maxLength: 255
    minLength: 1
    name: enrollmentStatusId
    required: true
    type: string
  FilterByNameQueryParam:
    description: Optional. A filter that returns results that match the name specified.
    in: query
    maxLength: 255
    minLength: 1
    name: name
    required: false
    type: string
    x-default-description: 'null'
  LifecycleStateQueryParam:
    description: 'A filter that returns results that match the lifecycle state specified.

      '
    enum: *id007
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: Could be one of the possible lifecycle state values
    x-obmcs-top-level-enum: '#/definitions/LifecycleState'
  OpcIfMatchHeaderParam:
    description: 'For optimistic concurrency control. In the PUT or DELETE call

      for a resource, set the `if-match` parameter to the value of the

      etag from a previous GET or POST response for that resource.

      The resource will be updated or deleted only if the etag you

      provide matches the resource''s current etag value.

      '
    in: header
    maxLength: 255
    name: if-match
    required: false
    type: string
  OpcRequestIdHeaderParam:
    description: 'Unique Oracle-assigned identifier for the request.

      If you need to contact Oracle about a particular request, please provide the request ID.

      '
    in: header
    name: opc-request-id
    required: false
    type: string
  OpcRetryTokenHeader:
    description: 'A token that uniquely identifies a request so it can be retried in case of a timeout
      or

      server error without risk of executing that same action again. Retry tokens expire after 24

      hours, but can be invalidated before then due to conflicting operations (for example, if a resource

      has been deleted and purged from the system, then a retry of the original creation request

      may be rejected).

      '
    in: header
    maxLength: 64
    minLength: 1
    name: opc-retry-token
    type: string
  OptimizerEnrollmentStatusQueryParam:
    description: 'A filter that returns results that match the Cloud Advisor enrollment status specified.

      '
    enum: *id008
    in: query
    name: status
    required: false
    type: string
    x-default-description: A filter that returns results that match the Cloud Advisor enrollment status
      specified.
    x-obmcs-top-level-enum: '#/definitions/OptimizerEnrollmentStatus'
  OptionalRecommendationIdQueryParam:
    description: The unique OCID associated with the recommendation.
    in: query
    maxLength: 255
    minLength: 1
    name: recommendationId
    required: false
    type: string
    x-default-description: 'null'
  PaginationLimitQueryParam:
    default: 10
    description: The maximum number of items to return in a paginated "List" call.
    in: query
    maximum: 1000
    minimum: 1
    name: limit
    type: integer
  PaginationTokenQueryParam:
    description: 'The value of the `opc-next-page` response header from the previous "List" call.

      '
    in: query
    maxLength: 1024
    minLength: 1
    name: page
    type: string
    x-default-description: 'null'
  ProfileIdPathParam:
    description: The unique OCID of the profile.
    in: path
    maxLength: 255
    minLength: 1
    name: profileId
    required: true
    type: string
  RecommendationIdPathParam:
    description: The unique OCID associated with the recommendation.
    in: path
    maxLength: 255
    minLength: 1
    name: recommendationId
    required: true
    type: string
  RecommendationNameQueryParam:
    description: Optional. A filter that returns results that match the recommendation name specified.
    in: query
    maxLength: 255
    minLength: 1
    name: recommendationName
    required: false
    type: string
    x-default-description: 'null'
  RequiredRecommendationIdQueryParam:
    description: The unique OCID associated with the recommendation.
    in: query
    maxLength: 255
    minLength: 1
    name: recommendationId
    required: true
    type: string
  ResourceActionIdPathParam:
    description: The unique OCID associated with the resource action.
    in: path
    maxLength: 255
    minLength: 1
    name: resourceActionId
    required: true
    type: string
  ResourceTypeQueryParam:
    description: Optional. A filter that returns results that match the resource type specified.
    in: query
    maxLength: 255
    minLength: 1
    name: resourceType
    required: false
    type: string
    x-default-description: 'null'
  SortByQueryParam:
    description: 'The field to sort by. You can provide one sort order (`sortOrder`). Default order for
      TIMECREATED is descending. Default order for NAME is ascending. The NAME sort order is case sensitive.

      '
    enum:
    - NAME
    - TIMECREATED
    in: query
    name: sortBy
    required: false
    type: string
    x-default-description: By default, TIMECREATED is used to sort by.
  SortOrderQueryParam:
    description: The sort order to use, either ascending (`ASC`) or descending (`DESC`).
    enum: *id018
    in: query
    name: sortOrder
    required: false
    type: string
    x-default-description: For time-based fields, the default sort order is DESC (descending). For all
      others, it is ASC (ascending).
    x-obmcs-top-level-enum: '#/definitions/SortOrder'
  StatusQueryParam:
    description: 'A filter that returns recommendations that match the status specified.

      '
    enum: *id006
    in: query
    name: status
    required: false
    type: string
    x-default-description: Could be one of the possible status values
    x-obmcs-top-level-enum: '#/definitions/Status'
  WorkRequestIdPathParam:
    description: The OCID of the work request.
    in: path
    maxLength: 255
    minLength: 1
    name: workRequestId
    required: true
    type: string
responses:
  400:
    description: Bad Request
    headers:
      opc-request-id: *id003
    schema:
      $ref: '#/definitions/Error'
  401:
    description: Unauthorized
    headers:
      opc-request-id: *id003
    schema:
      $ref: '#/definitions/Error'
  404:
    description: Not Found
    headers:
      opc-request-id: *id003
    schema:
      $ref: '#/definitions/Error'
  409:
    description: Conflict
    headers:
      opc-request-id: *id003
    schema:
      $ref: '#/definitions/Error'
  412:
    description: Precondition failed
    headers:
      opc-request-id: *id003
    schema:
      $ref: '#/definitions/Error'
  429:
    description: Too Many Requests
    headers:
      opc-request-id: *id003
    schema:
      $ref: '#/definitions/Error'
  500:
    description: Internal Server Error
    headers:
      opc-request-id: *id003
    schema:
      $ref: '#/definitions/Error'
  default:
    description: Unknown Error
    headers:
      opc-request-id: *id003
    schema:
      $ref: '#/definitions/Error'
x-anchors:
  x-headers:
    etag: *id004
    opc-next-page: *id001
    opc-prev-page: *id002
    opc-request-id: *id003
    opc-work-request-id: *id019
    retry-after: *id020
  x-properties:
    definedTags: *id009
    freeformTags: *id010
    systemTags:
      additionalProperties:
        additionalProperties:
          description: 'The value of the tag. Only string, integer and boolean types are supported.

            '
          type: object
        description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

          Example: `{"bar-key": "value"}`

          '
        type: object
      description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

        Example: `{ "orcl-cloud": { "free-tier-retained": "true" } }`

        '
      type: object
x-oracle-package: com.oracle.oci.optimizer
