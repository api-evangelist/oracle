swagger: '2.0'
info:
  description: Use the OCI Cache API to create and manage clusters. A cluster is a memory-based storage
    solution. For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).
  title: OCI Cache API
  version: '20220315'
  x-provenance:
    method: harvested
    first_party: true
    publisher: Oracle
    source: https://docs.oracle.com/en-us/iaas/api/specs/0c0709de34f2014ed9d1948ceeb3f59f7c24fee03209fa8269bdb6c37117b40d.yaml
    harvested: '2026-08-04'
    note: Published by Oracle as the contract for the OCI Cache API OCI service and stored verbatim; API
      Evangelist added only this provenance block.
  x-evidence:
  - url: https://docs.oracle.com/en-us/iaas/api/specs/index.json
    what: Oracle's own index of every OCI service specification
  - url: https://docs.oracle.com/en-us/iaas/api/specs/0c0709de34f2014ed9d1948ceeb3f59f7c24fee03209fa8269bdb6c37117b40d.yaml
    what: the harvested document for OCI Cache API
host: 127.0.0.1
basePath: /20220315
schemes:
- http
- https
produces:
- application/json
paths:
  /ociCacheBackups:
    get:
      description: Returns a list of OCI Cache Backups.
      operationId: ListOciCacheBackups
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/OciCacheBackupLifecycleStateQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/OciCacheBackupIdentifierQueryParam'
      - $ref: '#/parameters/OciCacheBackupSourceClusterIdQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of OCI Cache Backup objects.
          headers:
            opc-next-page: &id004
              description: 'For pagination of a list of items. When paging through a list, if this header
                appears in the response,

                then a partial list might have been returned. Include this value as the `page` parameter
                for the

                subsequent GET request to get the next batch of items.

                '
              type: string
            opc-request-id: &id001
              description: 'Unique Oracle-assigned identifier for the request. If you need to contact

                Oracle about a particular request, please provide the request ID.

                '
              type: string
          schema:
            $ref: '#/definitions/OciCacheBackupCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of all OCI Cache Backups in a compartment
      tags:
      - ociCacheBackup
      x-related-resource: '#/definitions/OciCacheBackupSummary'
    post:
      description: Creates a new OCI Cache Backup.
      operationId: CreateOciCacheBackup
      parameters:
      - description: Details for the new OCI Cache Backup.
        in: body
        name: CreateOciCacheBackupDetails
        required: true
        schema:
          $ref: '#/definitions/CreateOciCacheBackupDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        201:
          description: Accepted the request. The OCI Cache Backup will be created.
          headers:
            etag: &id003
              description: 'For optimistic concurrency control. See `if-match`.

                '
              type: string
            opc-request-id: *id001
            opc-work-request-id: &id002
              description: 'Unique Oracle-assigned identifier for the asynchronous request. You can use
                this to query status of the asynchronous operation.

                '
              type: string
          schema:
            $ref: '#/definitions/OciCacheBackup'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new OCI Cache Backup
      tags:
      - ociCacheBackup
      x-related-resource: '#/definitions/OciCacheBackup'
  /ociCacheBackups/{ociCacheBackupId}:
    delete:
      description: Deletes an OCI Cache Backup resource by identifier
      operationId: DeleteOciCacheBackup
      parameters:
      - $ref: '#/parameters/OciCacheBackupIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: Accepted the request. The OCI Cache Backup will be deleted.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete a provisioned OCI Cache Backup
      tags:
      - ociCacheBackup
      x-related-resource: '#/definitions/OciCacheBackup'
    get:
      description: Gets an OCI Cache Backup by identifier
      operationId: GetOciCacheBackup
      parameters:
      - $ref: '#/parameters/OciCacheBackupIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Retrieves the OCI Cache Backup with the given id
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OciCacheBackup'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get OCI Cache Backup
      tags:
      - ociCacheBackup
      x-related-resource: '#/definitions/OciCacheBackup'
    put:
      description: Updates the OCI Cache Backup
      operationId: UpdateOciCacheBackup
      parameters:
      - $ref: '#/parameters/OciCacheBackupIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateOciCacheBackupDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateOciCacheBackupDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The OCI Cache backup was updated.
          headers:
            etag: *id003
            opc-request-id: *id001
            opc-work-request-id: *id002
          schema:
            $ref: '#/definitions/OciCacheBackup'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        405:
          $ref: '#/responses/405'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the OCI Cache Backup identified by the id
      tags:
      - ociCacheBackup
      x-related-resource: '#/definitions/OciCacheBackup'
  /ociCacheBackups/{ociCacheBackupId}/actions/changeCompartment:
    post:
      description: Moves an OCI Cache Backup resource from one compartment identifier to another. When
        provided, If-Match is checked against ETag values of the resource.
      operationId: ChangeOciCacheBackupCompartment
      parameters:
      - $ref: '#/parameters/OciCacheBackupIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: ChangeOciCacheBackupCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeOciCacheBackupCompartmentDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        202:
          description: Accepted the request. The OCI Cache Backup resource will be moved into a different
            compartment.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Moves a resource into a different compartment.
      tags:
      - ociCacheBackup
      x-related-resource: '#/definitions/OciCacheBackup'
  /ociCacheBackups/{ociCacheBackupId}/actions/exportToObjectStorage:
    post:
      description: 'Initiates an asynchronous export of the backup’s RDB file(s) to the specified Object
        Storage bucket. The service generates the object names. For sharded backups, one object is written
        per shard under the optional prefix.

        '
      operationId: ExportOciCacheBackupToObjectStorage
      parameters:
      - $ref: '#/parameters/OciCacheBackupIdentifierPathParam'
      - description: The destination Object Storage information for the export.
        in: body
        name: ExportOciCacheBackupToObjectStorageDetails
        required: true
        schema:
          $ref: '#/definitions/ExportOciCacheBackupToObjectStorageDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: Accepted the request. The OCI Cache Backup will be exported to object storage.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Export an OCI Cache Backup to Object Storage
      tags:
      - ociCacheBackup
      x-obmcs-terraform:
        actionType: SPECIAL_UPDATE
      x-related-resource: '#/definitions/OciCacheBackup'
  /ociCacheConfigSets:
    get:
      description: 'Lists the OCI Cache Config Sets in the specified compartment.

        '
      operationId: ListOciCacheConfigSets
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/OciCacheConfigSetLifecycleStateQueryParam'
      - $ref: '#/parameters/SoftwareVersionQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/OciCacheConfigSetIdentifierQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OciCacheConfigSetCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get a list of all OCI Cache Config Sets in a compartment
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSetSummary'
    post:
      description: 'Create a new OCI Cache Config Set for the given OCI cache engine version.

        '
      operationId: CreateOciCacheConfigSet
      parameters:
      - description: Details for the new OCI Cache Config Set.
        in: body
        name: CreateOciCacheConfigSetDetails
        required: true
        schema:
          $ref: '#/definitions/CreateOciCacheConfigSetDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for creating an OCI Cache Config Set.
          headers:
            etag: *id003
            opc-request-id: *id001
            opc-work-request-id: *id002
          schema:
            $ref: '#/definitions/OciCacheConfigSet'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Create a new OCI Cache Config Set for the given OCI cache engine version
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSet'
  /ociCacheConfigSets/{ociCacheConfigSetId}:
    delete:
      description: Deletes the specified OCI Cache Config Set.
      operationId: DeleteOciCacheConfigSet
      parameters:
      - $ref: '#/parameters/OciCacheConfigSetIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for deleting the OCI Cache Config Set.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete an existing OCI Cache Config Set
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSet'
    get:
      description: Retrieves the specified OCI Cache Config Set.
      operationId: GetOciCacheConfigSet
      parameters:
      - $ref: '#/parameters/OciCacheConfigSetIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Successfully retrieved the OCI Cache Config Set.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OciCacheConfigSet'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get OciCacheConfigSet
      tags:
      - ociCacheConfigSet
    put:
      description: Updates the specified OCI Cache Config Set.
      operationId: UpdateOciCacheConfigSet
      parameters:
      - $ref: '#/parameters/OciCacheConfigSetIdentifierPathParam'
      - description: The information to be updated for OCI Cache Config Set.
        in: body
        name: UpdateOciCacheConfigSetDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateOciCacheConfigSetDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for updating the OCI Cache Config Set.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the OCI Cache Config Set for the given OCI Cache Config Set id.
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSet'
  /ociCacheConfigSets/{ociCacheConfigSetId}/actions/changeCompartment:
    post:
      description: Moves an OCI Cache Config Set into a different compartment within the same tenancy.
      operationId: ChangeOciCacheConfigSetCompartment
      parameters:
      - $ref: '#/parameters/OciCacheConfigSetIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: ChangeOciCacheConfigSetCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeOciCacheConfigSetCompartmentDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      responses:
        202:
          description: The request was accepted for moving the OCI Cache Config Set to a different compartment.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Moves a resource into a different compartment.
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/OciCacheConfigSet'
  /ociCacheConfigSets/{ociCacheConfigSetId}/actions/listAssociatedOciCacheClusters:
    post:
      description: 'Gets a list of associated OCI Cache clusters for an OCI Cache Config Set.

        '
      operationId: ListAssociatedOciCacheClusters
      parameters:
      - $ref: '#/parameters/OciCacheConfigSetIdentifierPathParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of IDs of AssociatedOciCacheClusters to which the OCI Cache Config Set is
            attached.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/AssociatedOciCacheClusterCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get a list of all clusters for a OCI Cache Config Set id.
      tags:
      - ociCacheConfigSet
      x-related-resource: '#/definitions/AssociatedOciCacheClusterSummary'
  /ociCacheDefaultConfigSets:
    get:
      description: Lists the OCI Cache Default Config Sets in the specified compartment.
      operationId: ListOciCacheDefaultConfigSets
      parameters:
      - $ref: '#/parameters/CompartmentIdRequiredQueryParam'
      - $ref: '#/parameters/OciCacheDefaultConfigSetIdentifierQueryParam'
      - $ref: '#/parameters/OciCacheDefaultConfigSetLifecycleStateQueryParam'
      - $ref: '#/parameters/SoftwareVersionQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OciCacheDefaultConfigSetCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Return a list of OCI Cache Default Config Set.
      tags:
      - ociCacheDefaultConfigSet
      x-related-resource: '#/definitions/OciCacheDefaultConfigSetSummary'
  /ociCacheDefaultConfigSets/{ociCacheDefaultConfigSetId}:
    get:
      description: Retrieves the specified OCI Cache Default Config Set.
      operationId: GetOciCacheDefaultConfigSet
      parameters:
      - $ref: '#/parameters/CompartmentIdRequiredQueryParam'
      - $ref: '#/parameters/OciCacheDefaultConfigSetIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Successfully retrieved the OCI Cache Default Config Set.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OciCacheDefaultConfigSet'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get a OCI Cache Default Config Set by OCID.
      tags:
      - ociCacheDefaultConfigSet
      x-related-resource: '#/definitions/OciCacheDefaultConfigSet'
  /ociCacheUsers:
    get:
      description: Lists the OCI Cache users based on the supplied parameters.
      operationId: ListOciCacheUsers
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/OciCacheUsernameQueryParam'
      - $ref: '#/parameters/LifeCycleStateQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The OCI cache user list is being retrieved.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OciCacheUserCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of all OCI cache users based on the supplied parameters.
      tags:
      - ociCacheUser
      x-related-resource: '#/definitions/OciCacheUserSummary'
    post:
      description: Creates a new OCI Cache user. OCI Cache user is required to authenticate to OCI Cache
        cluster.
      operationId: CreateOciCacheUser
      parameters:
      - description: Details required to create a new OCI cache user.
        in: body
        name: CreateOciCacheUserDetails
        required: true
        schema:
          $ref: '#/definitions/CreateOciCacheUserDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted to create a new OCI cache user.
          headers:
            etag: *id003
            opc-request-id: *id001
            opc-work-request-id: *id002
          schema:
            $ref: '#/definitions/OciCacheUser'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new OCI Cache User.
      tags:
      - ociCacheUser
      x-related-resource: '#/definitions/CreateOciCacheUserDetails'
  /ociCacheUsers/{ociCacheUserId}:
    delete:
      description: Deletes an existing OCI Cache User based on the OCI cache user unique ID (OCID).
      operationId: DeleteOciCacheUser
      parameters:
      - $ref: '#/parameters/OciCacheUserIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for deleting the OCI cache user.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete an existing OCI cache user based on the ID.
      tags:
      - ociCacheUser
      x-related-resource: '#/definitions/OciCacheUser'
    get:
      description: Get an existing OCI Cache users based on the ID (OCID).
      operationId: GetOciCacheUser
      parameters:
      - $ref: '#/parameters/OciCacheUserIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Successfully retrieved the OCI cache user.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/OciCacheUser'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get an existing OCI Cache User for the given ID (OCID).
      tags:
      - ociCacheUser
    put:
      description: Update an existing OCI Cache User with new details.
      operationId: UpdateOciCacheUser
      parameters:
      - $ref: '#/parameters/OciCacheUserIdentifierPathParam'
      - description: The information to be updated for an existing OCI Cache User.
        in: body
        name: UpdateOciCacheUserDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateOciCacheUserDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted to update the OCI cache user.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update an existing OCI Cache User with supplied parameters.
      tags:
      - ociCacheUser
      x-related-resource: '#/definitions/OciCacheUser'
  /ociCacheUsers/{ociCacheUserId}/actions/changeCompartment:
    post:
      description: Moves an OCI Cache User from one compartment to another within the same tenancy.
      operationId: ChangeOciCacheUserCompartment
      parameters:
      - $ref: '#/parameters/OciCacheUserIdentifierPathParam'
      - description: The configuration details to move compartment operation for an OCI cache user.
        in: body
        name: ChangeOciCacheUserCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeOciCacheUserCompartmentDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for moving the OCI cache user to a different compartment.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Moves an OCI cache user from one compartment to another compartment.
      tags:
      - ociCacheUser
      x-related-resource: '#/definitions/OciCacheUser'
  /ociCacheUsers/{ociCacheUserId}/actions/getRedisClusters:
    post:
      description: Gets a list of associated redis cluster for an OCI cache user.
      operationId: ListAttachedRedisClusters
      parameters:
      - $ref: '#/parameters/OciCacheUserIdentifierPathParam'
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of AttachedOciCacheCluster to which the OciCacheUser is attached.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            items:
              $ref: '#/definitions/AttachedOciCacheCluster'
            type: array
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List associated redis cluster for an OCI cache user.
      tags:
      - ociCacheUser
      x-related-resource: '#/definitions/AttachedOciCacheCluster'
  /redisClusters:
    get:
      description: 'Lists the OCI Cache clusters in the specified compartment. A cluster is a memory-based
        storage solution. For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).

        '
      operationId: ListRedisClusters
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/LifecycleStateQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/RedisClusterIdentifierQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RedisClusterCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of all clusters in a compartment
      tags:
      - redisCluster
      x-related-resource: '#/definitions/RedisClusterSummary'
    post:
      description: 'Creates a new OCI Cache cluster. A cluster is a memory-based storage solution.

        You can optionally initialize the cluster data by restoring from an OCI Cache Backup (backupId)
        or by importing from Object Storage RDB file(s) (importFromObjectStorageDetails).

        For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).

        '
      operationId: CreateRedisCluster
      parameters:
      - description: Details for the new cluster.
        in: body
        name: CreateRedisClusterDetails
        required: true
        schema:
          $ref: '#/definitions/CreateRedisClusterDetails'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for creating a cluster.
          headers:
            etag: *id003
            opc-request-id: *id001
            opc-work-request-id: *id002
          schema:
            $ref: '#/definitions/RedisCluster'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Creates a new RedisCluster
      tags:
      - redisCluster
      x-related-resource: '#/definitions/RedisCluster'
  /redisClusters/{redisClusterId}:
    delete:
      description: Deletes the specified OCI Cache cluster. A cluster is a memory-based storage solution.
        For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).
      operationId: DeleteRedisCluster
      parameters:
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for deleting the cluster.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Delete a provisioned cluster
      tags:
      - redisCluster
      x-related-resource: '#/definitions/RedisCluster'
    get:
      description: Retrieves the specified OCI Cache cluster. A cluster is a memory-based storage solution.
        For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).
      operationId: GetRedisCluster
      parameters:
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: Successfully retrieved the cluster.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RedisCluster'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Get RedisCluster
      tags:
      - redisCluster
    put:
      description: Updates the specified OCI Cache cluster. A cluster is a memory-based storage solution.
        For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).
      operationId: UpdateRedisCluster
      parameters:
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: UpdateRedisClusterDetails
        required: true
        schema:
          $ref: '#/definitions/UpdateRedisClusterDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for updating the cluster.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Update the RedisCluster identified by the id
      tags:
      - redisCluster
      x-related-resource: '#/definitions/RedisCluster'
  /redisClusters/{redisClusterId}/actions/attachOciCacheUsers:
    post:
      description: Attach existing OCI cache users to a redis cluster.
      operationId: AttachOciCacheUsers
      parameters:
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      - description: The parameter details to attach the OCI cache users to the redis cluster.
        in: body
        name: AttachOciCacheUsersDetails
        required: true
        schema:
          $ref: '#/definitions/AttachOciCacheUsersDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted to attach OCI cache users to redis cluster.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Attach existing OCI cache users to a redis cluster.
      tags:
      - redisCluster
      x-related-resource: '#/definitions/RedisCluster'
  /redisClusters/{redisClusterId}/actions/changeCompartment:
    post:
      description: Moves an OCI Cache cluster into a different compartment within the same tenancy. A
        cluster is a memory-based storage solution. For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).
      operationId: ChangeRedisClusterCompartment
      parameters:
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      - description: The information to be updated.
        in: body
        name: ChangeRedisClusterCompartmentDetails
        required: true
        schema:
          $ref: '#/definitions/ChangeRedisClusterCompartmentDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted for moving the cluster to a different compartment.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Moves a resource into a different compartment.
      tags:
      - redisCluster
      x-related-resource: '#/definitions/RedisCluster'
  /redisClusters/{redisClusterId}/actions/detachOciCacheUsers:
    post:
      description: Detach existing OCI cache users to a redis cluster.
      operationId: DetachOciCacheUsers
      parameters:
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      - description: The parameter details to detach the OCI cache users to the redis cluster.
        in: body
        name: DetachOciCacheUsersDetails
        required: true
        schema:
          $ref: '#/definitions/DetachOciCacheUsersDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The request was accepted to detach OCI cache users to redis cluster.
          headers:
            opc-request-id: *id001
            opc-work-request-id: *id002
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
      summary: Detach existing OCI cache users to a redis cluster.
      tags:
      - redisCluster
      x-related-resource: '#/definitions/RedisCluster'
  /redisClusters/{redisClusterId}/actions/getOciCacheUsers:
    post:
      description: Gets a list of associated OCI cache users for a redis cluster.
      operationId: ListAttachedOciCacheUsers
      parameters:
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of AttachedOciCacheUser of the OciCacheCluster.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            items:
              $ref: '#/definitions/AttachedOciCacheUser'
            type: array
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List associated OCI cache users for a redis cluster.
      tags:
      - redisCluster
      x-related-resource: '#/definitions/AttachedOciCacheUser'
  /redisClusters/{redisClusterId}/actions/identityToken:
    post:
      description: Generates an identity token to sign in with the specified redis user for the redis
        cluster
      operationId: CreateIdentityToken
      parameters:
      - description: Details to create the token
        in: body
        name: CreateIdentityTokenDetails
        required: true
        schema:
          $ref: '#/definitions/CreateIdentityTokenDetails'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/RetryTokenHeader'
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      responses:
        200:
          description: The Identity token was successfully created for the specified redis user.
          headers:
            etag: *id003
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/IdentityTokenDetailsResponse'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        412:
          $ref: '#/responses/412'
        409:
          $ref: '#/responses/409'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets the Redis cluster identity token
      tags:
      - redisIdentity
      x-related-resource: '#/definitions/CreateIdentityTokenDetails'
  /redisClusters/{redisClusterId}/nodes:
    get:
      description: 'Gets the list of all nodes in a cluster.

        '
      operationId: ListRedisClusterNodes
      parameters:
      - $ref: '#/parameters/RedisClusterIdentifierPathParam'
      - $ref: '#/parameters/DisplayNameQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/SortByQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: A page of cluster nodes
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/RedisNodeCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Gets a list of nodes in a cluster
      tags:
      - redisCluster
      x-related-resource: '#/definitions/NodeSummary'
  /workRequests:
    get:
      description: 'Lists the work requests in a compartment.

        '
      operationId: ListWorkRequests
      parameters:
      - $ref: '#/parameters/CompartmentIdQueryParam'
      - $ref: '#/parameters/WorkRequestIdQueryParam'
      - $ref: '#/parameters/WorkRequestStatusQueryParam'
      - $ref: '#/parameters/ResourceIdentifierQueryParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      - $ref: '#/parameters/WorkRequestSortByQueryParam'
      responses:
        200:
          description: The list is being retrieved.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestSummaryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: List Work Requests
      tags:
      - redisCluster
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}:
    delete:
      description: Cancels the specified work request.
      operationId: CancelWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/IfMatchHeader'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        202:
          description: The work request will be cancelled.
          headers:
            opc-request-id: *id001
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        409:
          $ref: '#/responses/409'
        412:
          $ref: '#/responses/412'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Cancel the work request
      tags:
      - redisCluster
      x-related-resource: '#/definitions/WorkRequest'
    get:
      description: Gets the status of the work request with the given ID.
      operationId: GetWorkRequest
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      responses:
        200:
          description: The detail of the work request.
          headers:
            etag: *id003
            opc-request-id: *id001
            retry-after: &id013
              description: A decimal number representing the number of seconds the client should wait
                before polling this endpoint again.
              type: integer
          schema:
            $ref: '#/definitions/WorkRequest'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: GET Work Request Status
      tags:
      - redisCluster
      x-related-resource: '#/definitions/WorkRequest'
  /workRequests/{workRequestId}/errors:
    get:
      description: 'Returns a list of errors for a given work request.

        '
      operationId: ListWorkRequestErrors
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/WorkRequestSortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of work request errors.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestErrorCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists work request errors
      tags:
      - redisCluster
      x-related-resource: '#/definitions/WorkRequestError'
  /workRequests/{workRequestId}/logs:
    get:
      description: 'Returns a list of logs for a given work request.

        '
      operationId: ListWorkRequestLogs
      parameters:
      - $ref: '#/parameters/WorkRequestIdPathParam'
      - $ref: '#/parameters/RequestIdHeader'
      - $ref: '#/parameters/PaginationTokenQueryParam'
      - $ref: '#/parameters/PaginationLimitQueryParam'
      - $ref: '#/parameters/WorkRequestSortByQueryParam'
      - $ref: '#/parameters/SortOrderQueryParam'
      responses:
        200:
          description: List of work request logs.
          headers:
            opc-next-page: *id004
            opc-request-id: *id001
          schema:
            $ref: '#/definitions/WorkRequestLogEntryCollection'
        400:
          $ref: '#/responses/400'
        401:
          $ref: '#/responses/401'
        404:
          $ref: '#/responses/404'
        429:
          $ref: '#/responses/429'
        500:
          $ref: '#/responses/500'
        default:
          $ref: '#/responses/default'
      summary: Lists work request logs
      tags:
      - redisCluster
      x-related-resource: '#/definitions/WorkRequestLogEntry'
definitions:
  ActionType:
    description: Possible types of actions.
    enum: &id011
    - CREATED
    - UPDATED
    - DELETED
    - IN_PROGRESS
    - RELATED
    - FAILED
    type: string
  AssociatedOciCacheClusterCollection:
    description: Associated OCI Cache Clusters to which the OCI Cache Config Set is associated.
    properties:
      items:
        description: List of clusters with the same OCI Cache Config Set ID.
        items:
          $ref: '#/definitions/AssociatedOciCacheClusterSummary'
        type: array
    required:
    - items
    type: object
  AssociatedOciCacheClusterSummary:
    description: Summary of the Clusters associated with an OCI Cache Config Set.
    properties:
      id:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster.

          '
        type: string
    required:
    - id
    type: object
  AttachOciCacheUsersDetails:
    description: OCI cache user details to be attached with a cluster.
    properties:
      ociCacheUsers:
        description: List of OCI cache user unique IDs (OCIDs).
        items:
          type: string
        type: array
    required:
    - ociCacheUsers
    type: object
  AttachedOciCacheCluster:
    description: OciCacheCluster to which the OciCacheUser is attached.
    properties:
      ociCacheClusterId:
        description: 'OCID of the OciCacheCluster

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - ociCacheClusterId
    type: object
  AttachedOciCacheUser:
    description: OciCacheUser attached to the OciCacheCluster.
    properties:
      ociCacheUserId:
        description: 'OCID of the OciCacheUser

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - ociCacheUserId
    type: object
  AuthenticationMode:
    description: These are the Authentication details of an OCI cache user.
    discriminator: authenticationType
    properties:
      authenticationType:
        description: This is Authentication Type of OCI cache user
        enum:
        - IAM
        - PASSWORD
        type: string
    required:
    - authenticationType
  ChangeOciCacheBackupCompartmentDetails:
    description: The OCI Cache backup information to move it a different compartment.
    properties:
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment

          into which the backup should be moved.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    type: object
  ChangeOciCacheConfigSetCompartmentDetails:
    description: The information for the move operation.
    properties:
      compartmentId:
        description: 'The [OCID](/Content/General/Concepts/identifiers.htm) of the compartment

          into which the resource should be moved.

          '
        type: string
    required:
    - compartmentId
    type: object
  ChangeOciCacheUserCompartmentDetails:
    description: The configuration details for the move operation.
    properties:
      compartmentId:
        description: OCID of the compartment into which the OCI cache user will be moved.
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    type: object
  ChangeRedisClusterCompartmentDetails:
    description: The configuration details for the move operation.
    properties:
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment

          into which the cluster should be moved.

          '
        maxLength: 255
        minLength: 1
        type: string
    required:
    - compartmentId
    type: object
  ConfigurationDetails:
    description: List of OCI Cache Config Set Values.
    properties:
      items:
        description: List of ConfigurationInfo objects.
        items:
          $ref: '#/definitions/ConfigurationInfo'
        type: array
    required:
    - items
    type: object
  ConfigurationInfo:
    description: Details of a configuration setting in the OCI Cache Config Set.
    properties:
      configKey:
        description: Key is the configuration key.
        type: string
      configValue:
        description: 'Value of the configuration as a string. Can represent a string, boolean, or number.

          Example: "true", "42", or "someString".

          '
        type: string
    required:
    - configKey
    - configValue
    type: object
  CreateIdentityTokenDetails:
    description: Generate an Identity token for Redis cluster
    properties:
      publicKey:
        description: User public key pair
        type: string
      redisUser:
        description: Redis User generating identity token.
        type: string
    required:
    - publicKey
    - redisUser
    type: object
  CreateOciCacheBackupDetails:
    description: The information to create a new OCI Cache Backup.
    properties:
      backupSource:
        default: REPLICA
        description: Specifies whether the backup was created from a replica or primary node
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheBackup/backupSource'
      compartmentId:
        description: Compartment identifier
        maxLength: 255
        minLength: 1
        type: string
      definedTags: &id005
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing predefined tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Defined tags for this resource. Each key is predefined and scoped to a namespace.

          Example: `{"foo-namespace": {"bar-key": "value"}}`

          '
        type: object
      description:
        description: Backup description
        maxLength: 2000
        minLength: 1
        type: string
      displayName:
        description: Backup display name.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: &id006
        additionalProperties:
          type: string
        description: 'Simple key-value pair that is applied without any predefined name, type or scope.
          Exists for cross-compatibility only.

          Example: `{"bar-key": "value"}`

          '
        type: object
      retentionPeriodInDays:
        default: 35
        description: Backup retention period in days.
        type: integer
      sourceClusterId:
        description: OCI Cache cluster identifier
        maxLength: 255
        minLength: 1
        type: string
    required:
    - sourceClusterId
    - compartmentId
    - displayName
    type: object
  CreateOciCacheConfigSetDetails:
    description: The information to create a new OCI Cache Config Set.
    properties:
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment
          that contains the OCI Cache Config Set.
        type: string
      configurationDetails:
        $ref: '#/definitions/ConfigurationDetails'
      definedTags: *id005
      description:
        description: Description for the custom OCI Cache Config Set.
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      freeformTags: *id006
      softwareVersion:
        description: The OCI Cache engine version that the cluster is running.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheConfigSet/softwareVersion'
    required:
    - compartmentId
    - displayName
    - configurationDetails
    - softwareVersion
    type: object
  CreateOciCacheUserDetails:
    description: Details required to create a new OCI cache user.
    properties:
      aclString:
        description: ACL string of OCI cache user.
        type: string
      authenticationMode:
        $ref: '#/definitions/AuthenticationMode'
      compartmentId:
        description: OCI cache user compartment ID.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id005
      description:
        description: Description of OCI cache user.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id006
      name:
        description: OCI cache user name is required to connect to an OCI cache cluster.
        maxLength: 255
        minLength: 1
        type: string
      status:
        description: OCI cache user status. ON enables and OFF disables the OCI cache user to login to
          the associated clusters. Default value is ON.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheUser/status'
    required:
    - name
    - description
    - compartmentId
    - authenticationMode
    - aclString
    type: object
  CreateRedisClusterDetails:
    description: The configuration details for a new OCI Cache cluster. An OCI Cache cluster is a memory-based
      storage solution. For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).
    properties:
      backupId:
        description: The ID of the OCI Cache Backup from which this cluster was created.Mutually exclusive
          with 'importFromObjectStorageDetails'.
        type: string
      clusterMode:
        description: Specifies whether the cluster is sharded or non-sharded.
        example: NONSHARDED
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/clusterMode'
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment
          that contains the cluster.
        maxLength: 255
        minLength: 1
        type: string
      definedTags: *id005
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id006
      importFromObjectStorageDetails:
        $ref: '#/definitions/ImportOciCacheFromObjectStorageDetails'
      nodeCount:
        description: The number of nodes per shard in the cluster when clusterMode is SHARDED. This is
          the total number of nodes when clusterMode is NONSHARDED.
        maximum: 5
        minimum: 1
        type: integer
      nodeMemoryInGBs:
        description: The amount of memory allocated to the cluster's nodes, in gigabytes.
        format: float
        maximum: 500
        minimum: 2
        type: number
      nsgIds:
        description: 'A list of Network Security Group (NSG) [OCIDs](/iaas/Content/General/Concepts/identifiers.htm)

          associated with this cluster. For more information,

          see [Using an NSG for Clusters](/iaas/Content/ocicache/connecttocluster.htm#connecttocluster__networksecuritygroup).

          '
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 5
        type: array
      ociCacheConfigSetId:
        description: The ID of the corresponding OCI Cache Config Set for the cluster.
        type: string
      securityAttributes: &id008
        additionalProperties:
          additionalProperties:
            description: 'Attribute value and mode. Only the String type is supported for value. Mode
              is currently always "enforce".

              '
            type: object
          description: 'Key-value pair representing a security attribute key and value, scoped to a namespace.


            Example: `{"MaxEgressCount": {"value": "42", "mode": "enforce"}}`

            '
          type: object
        description: 'Security attributes for redis cluster resource. Each key is predefined and scoped
          to a namespace.

          For more information, see [Resource Tags](/iaas/Content/General/Concepts/resourcetags.htm).


          Example: `{"Oracle-ZPR": {"MaxEgressCount": {"value": "42", "mode": "enforce"}}}`

          '
        type: object
      shardCount:
        description: The number of shards in sharded cluster. Only applicable when clusterMode is SHARDED.
        type: integer
      softwareVersion:
        description: The OCI Cache engine version that the cluster is running.
        example: REDIS_7_0
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/softwareVersion'
      subnetId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster's
          subnet.
        type: string
    required:
    - displayName
    - compartmentId
    - nodeCount
    - nodeMemoryInGBs
    - subnetId
    - softwareVersion
    type: object
  DefaultConfigurationDetails:
    description: List of OCI Cache Default Config Set Values.
    properties:
      items:
        description: List of DefaultConfigurationInfo objects.
        items:
          $ref: '#/definitions/DefaultConfigurationInfo'
        type: array
    required:
    - items
    type: object
  DefaultConfigurationInfo:
    description: Details of a configuration setting in the OCI Cache Default Config Set.
    properties:
      allowedValues:
        description: Allowed values for the configuration setting.
        type: string
      configKey:
        description: The key of the configuration setting.
        type: string
      dataType:
        description: The data type of the configuration setting.
        type: string
      defaultConfigValue:
        description: The default value for the configuration setting.
        type: string
      description:
        description: Description of the configuration setting.
        type: string
      isModifiable:
        description: Indicates if the configuration is modifiable.
        type: boolean
    required:
    - configKey
    - defaultConfigValue
    - dataType
    - isModifiable
    type: object
  DetachOciCacheUsersDetails:
    description: OCI cache user details to be detached with a cluster.
    properties:
      ociCacheUsers:
        description: List of OCI cache user unique IDs (OCIDs).
        items:
          type: string
        type: array
    required:
    - ociCacheUsers
    type: object
  Error:
    description: Specifies details for an error.
    properties:
      code:
        description: A machine-usable code for the error that occured.
        type: string
      message:
        description: A human-readable error string for the error that occured.
        type: string
    required:
    - code
    - message
  ExportOciCacheBackupToObjectStorageDetails:
    description: Parameters to export an OCI Cache Backup’s RDB file(s) to Object Storage.
    properties:
      bucketName:
        description: The target Object Storage bucket name.
        maxLength: 256
        minLength: 1
        type: string
      namespaceName:
        description: The Object Storage namespace name.
        maxLength: 256
        minLength: 1
        type: string
      prefix:
        description: Optional prefix under which the service will place the exported object(s).
        maxLength: 768
        minLength: 0
        type: string
    required:
    - namespaceName
    - bucketName
    type: object
  IamAuthenticationMode:
    allOf:
    - $ref: '#/definitions/AuthenticationMode'
    - discriminator: IAM
    description: child class of AuthenticationMode.
    type: object
  IdentityTokenDetailsResponse:
    description: Response an Identity token generated for Redis cluster
    properties:
      identityToken:
        description: Generated Identity token
        type: string
      redisUser:
        description: Redis user for the newly created identity token
        type: string
    required:
    - identityToken
    - redisUser
    type: object
  ImportOciCacheFromObjectStorageDetails:
    description: Details for importing OCI Cache data from Object Storage RDB file(s) during cluster creation.
    properties:
      bucketName:
        description: The Object Storage bucket name.
        maxLength: 256
        minLength: 1
        type: string
      namespaceName:
        description: The Object Storage namespace name.
        maxLength: 256
        minLength: 1
        type: string
      objects:
        description: The list of objects to import from the specified bucket.
        items:
          $ref: '#/definitions/ImportOciCacheFromObjectStorageObject'
        maxItems: 1024
        minItems: 1
        type: array
    required:
    - namespaceName
    - bucketName
    - objects
    type: object
  ImportOciCacheFromObjectStorageObject:
    description: An object to import from the specified bucket.
    properties:
      objectName:
        description: The name of the object in the bucket (for example, 'customerA/exports/backup_ocid/dump.rdb').
        maxLength: 1024
        minLength: 1
        type: string
    required:
    - objectName
    type: object
  Node:
    description: The details of each node in the cluster.
    properties:
      displayName:
        description: A user-friendly name of a cluster node.
        type: string
      privateEndpointFqdn:
        description: The fully qualified domain name (FQDN) of the API endpoint to access a specific node.
        type: string
      privateEndpointIpAddress:
        description: The private IP address of the API endpoint to access a specific node.
        type: string
    required:
    - privateEndpointFqdn
    - privateEndpointIpAddress
    - displayName
    type: object
  NodeCollection:
    description: The collection of  cluster nodes.
    properties:
      items:
        description: Collection of node objects.
        items:
          $ref: '#/definitions/Node'
        type: array
    required:
    - items
    type: object
  NodeSummary:
    description: The details of each node in the cluster.
    properties:
      displayName:
        description: A user-friendly name of a cluster node.
        type: string
      privateEndpointFqdn:
        description: The fully qualified domain name (FQDN) of the API endpoint to access a specific node.
        type: string
      privateEndpointIpAddress:
        description: The private IP address of the API endpoint to access a specific node.
        type: string
      redisClusterId:
        description: The OCID of the cluster
        type: string
      shardNumber:
        description: The shard number to which the node belongs to.
        type: integer
    required:
    - privateEndpointFqdn
    - privateEndpointIpAddress
    - displayName
    - redisClusterId
    type: object
  OciCacheBackup:
    description: OCI Cache cluster backup information
    properties:
      backupSizeInGBs:
        description: Backup size in GB.
        format: float
        type: number
      backupSource:
        default: REPLICA
        description: Specifies whether the backup was created from a replica or primary node
        enum:
        - REPLICA
        - PRIMARY
        type: string
      backupType:
        description: Backup Type.
        enum:
        - MANUAL
        - AUTOMATED
        type: string
      clusterMemoryInGBs:
        description: The amount of memory allocated to the cluster, in gigabytes.
        format: float
        maximum: 500
        minimum: 2
        type: number
      clusterMode:
        description: Specifies whether the cluster is sharded or non-sharded.
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/clusterMode'
      compartmentId:
        description: Backup compartment identifier
        type: string
      definedTags: *id005
      description:
        description: Backup description
        type: string
      displayName:
        description: Backup display name
        type: string
      freeformTags: *id006
      id:
        description: Unique identifier that is immutable on creation
        type: string
      lifecycleState:
        description: The current state of the backup.
        enum:
        - CREATING
        - UPDATING
        - ACTIVE
        - DELETING
        - DELETED
        - FAILED
        type: string
      retentionPeriodInDays:
        default: 35
        description: Backup retention period in days.
        type: integer
      shardCount:
        description: The number of shards in a sharded cluster. Only applicable when clusterMode is SHARDED.
        type: integer
      softwareVersion:
        description: The OCI Cache engine version that the cluster is running.
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/softwareVersion'
      sourceClusterId:
        description: The source OCI Cache Cluster OCID.
        maxLength: 255
        minLength: 1
        type: string
      systemTags: &id007
        additionalProperties:
          additionalProperties:
            description: 'The value of the tag. Only string, integer and boolean types are supported.

              '
            type: object
          description: 'Key-value pair representing system tags'' keys and values scoped to a namespace.

            Example: `{"bar-key": "value"}`

            '
          type: object
        description: 'Usage of system tag keys. These predefined keys are scoped to namespaces.

          Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`

          '
        type: object
      timeCreated:
        description: The date and time the backup was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the backup was updated. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - displayName
    - sourceClusterId
    - clusterMode
    - softwareVersion
    - backupSizeInGBs
    type: object
  OciCacheBackupCollection:
    description: Results of an OCI Cache backup search. Contains the OCI Cache Backup items and other
      information, such as metadata.
    properties:
      items:
        description: List of OCI Cache backups.
        items:
          $ref: '#/definitions/OciCacheBackupSummary'
        type: array
    required:
    - items
    type: object
  OciCacheBackupSummary:
    description: Summary of the OCI Cache Backup.
    properties:
      backupSizeInGBs:
        description: Backup size in GB.
        format: float
        type: number
      backupSource:
        default: REPLICA
        description: Specifies whether the backup was created from a replica or primary node
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheBackup/backupSource'
      backupType:
        description: Backup Type.
        enum:
        - MANUAL
        - AUTOMATED
        type: string
      clusterMode:
        description: Specifies whether the cluster is sharded or non-sharded.
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/clusterMode'
      compartmentId:
        description: Compartment identifier
        type: string
      definedTags: *id005
      displayName:
        description: Backup identifier, can be renamed
        type: string
      freeformTags: *id006
      id:
        description: Unique identifier that is immutable on creation
        type: string
      lifecycleState:
        description: The current state of the backup.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheBackup/lifecycleState'
      retentionPeriodInDays:
        default: 35
        description: Backup retention period in days.
        type: integer
      sourceClusterId:
        description: The source OCI Cache Cluster OCID.
        maxLength: 255
        minLength: 1
        type: string
      systemTags: *id007
      timeCreated:
        description: The date and time the backup was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the backup was updated. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - timeCreated
    - displayName
    - clusterMode
    - sourceClusterId
    - freeformTags
    - definedTags
    type: object
  OciCacheConfigSet:
    description: Configurations for OCI Cache to manage the behavior, performance, and functionality of
      the underlying cache engine.
    properties:
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment
          that contains the OCI Cache Config Set.
        type: string
      configurationDetails:
        $ref: '#/definitions/ConfigurationDetails'
      defaultConfigSetId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the default
          OCI Cache Config Set which the custom OCI Cache Config Set is based upon.
        type: string
      definedTags: *id005
      description:
        description: A description of the OCI Cache Config Set.
        maxLength: 500
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      freeformTags: *id006
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the OCI Cache
          Config Set.
        type: string
      lifecycleState:
        description: The current state of the OCI Cache Config Set.
        enum:
        - CREATING
        - UPDATING
        - ACTIVE
        - DELETING
        - DELETED
        - FAILED
        type: string
      softwareVersion:
        description: The OCI Cache engine version that the cluster is running.
        enum:
        - V7_0_5
        - VALKEY_7_2
        type: string
      systemTags: *id007
      timeCreated:
        description: The date and time the OCI Cache Config Set was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the OCI Cache Config Set was updated. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - lifecycleState
    - softwareVersion
    type: object
  OciCacheConfigSetCollection:
    description: A list of OCI Cache Config Sets that match filter criteria, if any.
    properties:
      items:
        description: List of OCI Cache Config Sets.
        items:
          $ref: '#/definitions/OciCacheConfigSetSummary'
        type: array
    required:
    - items
    type: object
  OciCacheConfigSetSummary:
    description: Summary of information about the OCI Cache Config Set.
    properties:
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment
          that contains the OCI Cache Config Set.
        type: string
      defaultConfigSetId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the default
          OCI Cache Config Set which the custom OCI Cache Config Set is based upon.
        type: string
      definedTags: *id005
      displayName:
        description: A user-friendly name of the OCI Cache Config Set.
        type: string
      freeformTags: *id006
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the OCI Cache
          Config Set.
        type: string
      lifecycleState:
        description: The current state of the OCI Cache Config Set.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheConfigSet/lifecycleState'
      softwareVersion:
        description: The engine version of the OCI Cache Config Set.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheConfigSet/softwareVersion'
      systemTags: *id007
      timeCreated:
        description: The date and time the configuration was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the configuration was updated. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    type: object
  OciCacheDefaultConfigSet:
    description: Default configurations for OCI Cache to manage the behavior, performance, and functionality
      of the underlying cache engine.
    properties:
      defaultConfigurationDetails:
        $ref: '#/definitions/DefaultConfigurationDetails'
      description:
        description: Description of the OCI Cache Default Config Set.
        type: string
      displayName:
        description: A user-friendly name of the OCI Cache Default Config Set.
        type: string
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the OCI Cache
          Default Config Set.
        type: string
      lifecycleState:
        description: The current state of the OCI Cache Default Config Set.
        enum:
        - ACTIVE
        - INACTIVE
        type: string
      softwareVersion:
        description: The engine version of the OCI Cache Default Config Set.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheConfigSet/softwareVersion'
      timeCreated:
        description: The date and time the OCI Cache Default Config Set was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
    required:
    - id
    - softwareVersion
    type: object
  OciCacheDefaultConfigSetCollection:
    description: A list of OCI Cache Default Config Sets.
    properties:
      items:
        description: List of OCI Cache Default Config Sets.
        items:
          $ref: '#/definitions/OciCacheDefaultConfigSetSummary'
        type: array
    required:
    - items
    type: object
  OciCacheDefaultConfigSetSummary:
    description: Summary of information about an OCI Cache Default Config Set.
    properties:
      displayName:
        description: A user-friendly name of the OCI Cache Default Config Set.
        type: string
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the OCI Cache
          Default Config Set.
        type: string
      lifecycleState:
        description: The current state of the OCI Cache Default Config Set.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheDefaultConfigSet/lifecycleState'
      softwareVersion:
        description: The engine version of the OCI Cache Default Config Set.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheConfigSet/softwareVersion'
      timeCreated:
        description: The date and time the configuration was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
    required:
    - id
    - softwareVersion
    type: object
  OciCacheUser:
    description: An OCI cache user is required to connect to an OCI cache cluster.
    properties:
      aclString:
        description: ACL string of OCI cache user.
        type: string
      authenticationMode:
        $ref: '#/definitions/AuthenticationMode'
      compartmentId:
        description: OCI Cache user compartment ID.
        type: string
      definedTags: *id005
      description:
        description: Description of OCI cache user.
        type: string
      freeformTags: *id006
      id:
        description: OCI Cache user unique ID.
        type: string
      lifecycleState:
        description: OCI Cache user lifecycle state.
        enum:
        - CREATING
        - UPDATING
        - ACTIVE
        - DELETING
        - DELETED
        - FAILED
        type: string
      name:
        description: OCI Cache user name.
        type: string
      status:
        description: OCI Cache user status. ON enables and OFF disables the OCI cache user to login to
          the cluster.
        enum:
        - 'ON'
        - 'OFF'
        type: string
      systemTags: *id007
      timeCreated:
        description: The date and time, when the OCI cache user was created.
        format: date-time
        type: string
      timeUpdated:
        description: The date and time, when the OCI cache user was updated.
        format: date-time
        type: string
    required:
    - id
    - name
    - compartmentId
    - authenticationMode
    - aclString
    - status
    - lifecycleState
    type: object
  OciCacheUserCollection:
    description: A list of OCI cache users that match filter criteria, if any.
    properties:
      items:
        description: A collection of OCI Cache users.
        items:
          $ref: '#/definitions/OciCacheUserSummary'
        type: array
    required:
    - items
    type: object
  OciCacheUserSummary:
    description: OCI Cache user summary.
    properties:
      authenticationType:
        description: OCI Cache user authentication type.
        type: string
        x-obmcs-enumref: '#/definitions/AuthenticationMode/authenticationType'
      compartmentId:
        description: OCI Cache user compartment ID.
        type: string
      definedTags: *id005
      description:
        description: Description of OCI cache user.
        type: string
      freeformTags: *id006
      id:
        description: OCI Cache user unique ID.
        type: string
      lifecycleState:
        description: OCI Cache user lifecycle state.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheUser/lifecycleState'
      name:
        description: OCI Cache user name.
        type: string
      status:
        description: OCI Cache user status. ON enables and OFF disables the OCI cache user to use the
          cluster.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheUser/status'
      systemTags: *id007
      timeCreated:
        description: The date and time, when the OCI cache user was created.
        format: date-time
        type: string
    required:
    - id
    - name
    - description
    - compartmentId
    - authenticationType
    - status
    - lifecycleState
    type: object
  OperationStatus:
    description: Possible operation status.
    enum: &id010
    - ACCEPTED
    - IN_PROGRESS
    - WAITING
    - NEEDS_ATTENTION
    - FAILED
    - SUCCEEDED
    - CANCELING
    - CANCELED
    type: string
  OperationType:
    description: Possible operation types.
    enum: &id009
    - CREATE_REDIS_CLUSTER
    - UPDATE_REDIS_CLUSTER
    - DELETE_REDIS_CLUSTER
    - MOVE_REDIS_CLUSTER
    - FAILOVER_REDIS_CLUSTER
    - CREATE_REDIS_CONFIG_SET
    - UPDATE_REDIS_CONFIG_SET
    - DELETE_REDIS_CONFIG_SET
    - MOVE_REDIS_CONFIG_SET
    - MIGRATE_CLUSTER
    - CLUSTER_ROLLBACK
    - ATTACH_OCI_CACHE_USERS
    - DETACH_OCI_CACHE_USERS
    - CREATE_OCI_CACHE_USER
    - UPDATE_OCI_CACHE_USER
    - DELETE_OCI_CACHE_USER
    - CREATE_OCI_CACHE_CONFIG_SET
    - UPDATE_OCI_CACHE_CONFIG_SET
    - DELETE_OCI_CACHE_CONFIG_SET
    - CHANGE_OCI_CACHE_CONFIG_SET_COMPARTMENT
    - CHANGE_OCI_CACHE_USER_COMPARTMENT
    - CREATE_OCI_CACHE_BACKUP
    - UPDATE_OCI_CACHE_BACKUP
    - DELETE_OCI_CACHE_BACKUP
    - MOVE_OCI_CACHE_BACKUP
    - EXPORT_OCI_CACHE_BACKUP_TO_OS
    - PATCH_OCI_CACHE_CLUSTER
    - REPLACE_OCI_CACHE_NODE
    type: string
  PasswordAuthenticationMode:
    allOf:
    - $ref: '#/definitions/AuthenticationMode'
    - discriminator: PASSWORD
      properties:
        hashedPasswords:
          description: SHA-256 hashed passwords for OCI Cache user,required if authenticationType is set
            to PASSWORD.
          items:
            type: string
          type: array
      required:
      - hashedPasswords
    description: child class of AuthenticationMode.
    type: object
  RedisCluster:
    description: An OCI Cache cluster is a memory-based storage solution. For more information, see [OCI
      Cache](/iaas/Content/ocicache/home.htm).
    properties:
      backupId:
        description: The ID of the OCI Cache Backup from which this cluster was created.
        type: string
      clusterMode:
        description: Specifies whether the cluster is sharded or non-sharded.
        enum:
        - SHARDED
        - NONSHARDED
        type: string
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment
          that contains the cluster.
        type: string
      definedTags: *id005
      discoveryEndpointIpAddress:
        description: The private IP address of the API endpoint for sharded cluster discovery.
        type: string
      discoveryFqdn:
        description: The fully qualified domain name (FQDN) of the API endpoint for sharded cluster discovery.
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      freeformTags: *id006
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster.
        type: string
      importFromObjectStorageDetails:
        $ref: '#/definitions/ImportOciCacheFromObjectStorageDetails'
      lifecycleDetails:
        description: A message describing the current state in more detail. For example, the message might
          provide actionable information for a resource in `FAILED` state.
        type: string
      lifecycleState:
        description: The current state of the cluster.
        enum:
        - CREATING
        - UPDATING
        - ACTIVE
        - DELETING
        - DELETED
        - FAILED
        type: string
      nodeCollection:
        $ref: '#/definitions/NodeCollection'
      nodeCount:
        description: The number of nodes per shard in the cluster when clusterMode is SHARDED. This is
          the total number of nodes when clusterMode is NONSHARDED.
        maximum: 5
        minimum: 1
        type: integer
      nodeMemoryInGBs:
        description: The amount of memory allocated to the cluster's nodes, in gigabytes.
        format: float
        maximum: 500
        minimum: 2
        type: number
      nsgIds:
        description: 'A list of Network Security Group (NSG) [OCIDs](/iaas/Content/General/Concepts/identifiers.htm)

          associated with this cluster. For more information,

          see [Using an NSG for Clusters](/iaas/Content/ocicache/connecttocluster.htm#connecttocluster__networksecuritygroup).

          '
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 5
        type: array
      ociCacheConfigSetId:
        description: The ID of the corresponding OCI Cache Config Set for the cluster.
        type: string
      primaryEndpointIpAddress:
        description: The private IP address of the API endpoint for the cluster's primary node.
        type: string
      primaryFqdn:
        description: The fully qualified domain name (FQDN) of the API endpoint for the cluster's primary
          node.
        type: string
      replicasEndpointIpAddress:
        description: The private IP address of the API endpoint for the cluster's replica nodes.
        type: string
      replicasFqdn:
        description: The fully qualified domain name (FQDN) of the API endpoint for the cluster's replica
          nodes.
        type: string
      securityAttributes: *id008
      shardCount:
        description: The number of shards in a sharded cluster. Only applicable when clusterMode is SHARDED.
        type: integer
      softwareVersion:
        description: The OCI Cache engine version that the cluster is running.
        enum:
        - V7_0_5
        - VALKEY_7_2
        type: string
      subnetId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster's
          subnet.
        type: string
      systemTags: *id007
      timeCreated:
        description: The date and time the cluster was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the cluster was updated. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
    required:
    - id
    - displayName
    - compartmentId
    - nodeCount
    - softwareVersion
    - nodeMemoryInGBs
    - nodeCollection
    - subnetId
    - primaryFqdn
    - primaryEndpointIpAddress
    - replicasFqdn
    - replicasEndpointIpAddress
    type: object
  RedisClusterCollection:
    description: A list of clusters that match filter criteria, if any. A cluster is a memory-based storage
      solution. For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).
    properties:
      items:
        description: The list of clusters.
        items:
          $ref: '#/definitions/RedisClusterSummary'
        type: array
    required:
    - items
    type: object
  RedisClusterSummary:
    description: 'Summary of information about a cluster. A cluster is a memory-based storage solution.
      For more information, see [OCI Cache](/iaas/Content/ocicache/home.htm).

      '
    properties:
      backupId:
        description: The ID of the OCI Cache Backup from which this cluster was created.
        type: string
      clusterMode:
        description: Specifies whether the cluster is sharded or non-sharded.
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/clusterMode'
      compartmentId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment
          that contains the cluster.
        type: string
      definedTags: *id005
      discoveryEndpointIpAddress:
        description: The private IP address of the API endpoint for shareded cluster discovery.
        type: string
      discoveryFqdn:
        description: The fully qualified domain name (FQDN) of the API endpoint for sharded cluster discovery.
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      freeformTags: *id006
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster.
        type: string
      lifecycleDetails:
        description: A message describing the current state in more detail. For example, the message might
          provide actionable information for a resource in `FAILED` state.
        type: string
      lifecycleState:
        description: The current state of the cluster.
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/lifecycleState'
      nodeCount:
        description: The number of nodes per shard in the cluster when clusterMode is SHARDED. This is
          the total number of nodes when clusterMode is NONSHARDED.
        maximum: 5
        minimum: 1
        type: integer
      nodeMemoryInGBs:
        description: The amount of memory allocated to the cluster's nodes, in gigabytes.
        format: float
        maximum: 500
        minimum: 2
        type: number
      nsgIds:
        description: 'A list of Network Security Group (NSG) [OCIDs](/iaas/Content/General/Concepts/identifiers.htm)

          associated with this cluster. For more information,

          see [Using an NSG for Clusters](/iaas/Content/ocicache/connecttocluster.htm#connecttocluster__networksecuritygroup).

          '
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 5
        type: array
      ociCacheConfigSetId:
        description: The ID of the corresponding OCI Cache Config Set for the cluster.
        type: string
      primaryEndpointIpAddress:
        description: The private IP address of the API endpoint for the cluster's primary node.
        type: string
      primaryFqdn:
        description: The fully qualified domain name (FQDN) of the API endpoint for the cluster's primary
          node.
        type: string
      replicasEndpointIpAddress:
        description: The private IP address of the API endpoint for the cluster's replica nodes.
        type: string
      replicasFqdn:
        description: The fully qualified domain name (FQDN) of the API endpoint for the cluster's replica
          nodes.
        type: string
      securityAttributes: *id008
      shardCount:
        description: The number of shards in a sharded cluster. Only applicable when clusterMode is SHARDED.
        type: integer
      softwareVersion:
        description: The OCI Cache engine version that the cluster is running.
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/softwareVersion'
      subnetId:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster's
          subnet.
        type: string
      systemTags: *id007
      timeCreated:
        description: The date and time the cluster was created. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
      timeUpdated:
        description: The date and time the cluster was updated. An [RFC3339](https://datatracker.ietf.org/doc/html/rfc3339)
          formatted datetime string.
        format: date-time
        type: string
    required:
    - id
    - compartmentId
    - nodeCount
    - displayName
    - softwareVersion
    - nodeMemoryInGBs
    - subnetId
    - primaryFqdn
    - primaryEndpointIpAddress
    - replicasFqdn
    - replicasEndpointIpAddress
    type: object
  RedisNodeCollection:
    description: A list of nodes in a cluster that match filter criteria, if any.
    properties:
      items:
        description: The list of nodes in a cluster.
        items:
          $ref: '#/definitions/NodeSummary'
        type: array
    required:
    - items
    type: object
  SortOrder:
    description: The sort order to use, either 'ASC' or 'DESC'.
    enum: &id012
    - ASC
    - DESC
    type: string
  UpdateOciCacheBackupDetails:
    description: The OCI Cache backup information to be updated.
    properties:
      definedTags: *id005
      description:
        description: Backup description
        maxLength: 2000
        minLength: 1
        type: string
      displayName:
        description: Backup display name
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id006
      retentionPeriodInDays:
        default: 35
        description: Backup retention period in days.
        type: integer
    type: object
  UpdateOciCacheConfigSetDetails:
    description: The information to be updated for an existing OCI Cache Config Set.
    properties:
      definedTags: *id005
      description:
        description: Description for the custom OCI Cache Config Set.
        type: string
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        type: string
      freeformTags: *id006
    type: object
  UpdateOciCacheUserDetails:
    description: Details required to update an existing OCI cache user.
    properties:
      aclString:
        description: ACL string of OCI cache user.
        type: string
      authenticationMode:
        $ref: '#/definitions/AuthenticationMode'
      definedTags: *id005
      description:
        description: Description of OCI cache user.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id006
      status:
        description: OCI cache user status. ON enables and OFF disables the OCI cache user to login to
          the associated clusters.
        type: string
        x-obmcs-enumref: '#/definitions/OciCacheUser/status'
    type: object
  UpdateRedisClusterDetails:
    description: The configuration to update for an existing cluster.
    properties:
      definedTags: *id005
      displayName:
        description: A user-friendly name. Does not have to be unique, and it's changeable. Avoid entering
          confidential information.
        maxLength: 255
        minLength: 1
        type: string
      freeformTags: *id006
      nodeCount:
        description: The number of nodes per shard in the cluster when clusterMode is SHARDED. This is
          the total number of nodes when clusterMode is NONSHARDED.
        maximum: 5
        minimum: 1
        type: integer
      nodeMemoryInGBs:
        description: The amount of memory allocated to the cluster's nodes, in gigabytes.
        format: float
        maximum: 500
        minimum: 2
        type: number
      nsgIds:
        description: 'A list of Network Security Group (NSG) [OCIDs](/iaas/Content/General/Concepts/identifiers.htm)

          associated with this cluster. For more information,

          see [Using an NSG for Clusters](/iaas/Content/ocicache/connecttocluster.htm#connecttocluster__networksecuritygroup).

          '
        items:
          maxLength: 255
          minLength: 1
          type: string
        maxItems: 5
        type: array
      ociCacheConfigSetId:
        description: The ID of the corresponding OCI Cache Config Set for the cluster.
        type: string
      securityAttributes: *id008
      shardCount:
        description: The number of shards in sharded cluster. Only applicable when clusterMode is SHARDED.
        type: integer
      softwareVersion:
        description: The OCI Cache engine version that the cluster is running.
        example: VALKEY_7_2
        type: string
        x-obmcs-enumref: '#/definitions/RedisCluster/softwareVersion'
    type: object
  WorkRequest:
    description: An asynchronous work request.
    properties:
      compartmentId:
        description: 'The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the compartment
          that contains the work request.

          '
        type: string
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the work request.
        type: string
      operationType:
        description: The type of operation that spawned the work request.
        enum: *id009
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: The percentage complete of the operation tracked by the work request.
        format: float
        type: number
      resources:
        description: The resources that are affected by the work request.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: The status of current work request.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: 'The date and time the work request was created, in the format defined by [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
      timeFinished:
        description: 'The date and time the work request completed, in the format defined by [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
      timeStarted:
        description: 'The date and time the work request was started, in the format defined by [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
  WorkRequestError:
    description: An error encountered while executing an operation that is tracked by a work request.
    properties:
      code:
        description: 'A machine-usable code for the error that occured. Error codes are listed at [API
          Errors](/iaas/Content/API/References/apierrors.htm).

          '
        type: string
      message:
        description: A human-readable error string for the error that occured.
        type: string
      timestamp:
        description: The time and time the error occured.
        format: date-time
        type: string
    required:
    - code
    - message
    - timestamp
  WorkRequestErrorCollection:
    description: A list of errors for a work request.
    properties:
      items:
        description: A collection of work request errors.
        items:
          $ref: '#/definitions/WorkRequestError'
        type: array
    required:
    - items
    type: object
  WorkRequestLogEntry:
    description: A log message from executing an operation that is tracked by a work request.
    properties:
      message:
        description: A human-readable log message.
        type: string
      timestamp:
        description: The date and time the log message was written.
        format: date-time
        type: string
    required:
    - message
    - timestamp
  WorkRequestLogEntryCollection:
    description: A list of logs for the specified work request.
    properties:
      items:
        description: A collection of work request logs.
        items:
          $ref: '#/definitions/WorkRequestLogEntry'
        type: array
    required:
    - items
    type: object
  WorkRequestResource:
    description: The resources that are affected by the work request.
    properties:
      actionType:
        description: 'The way in which this resource is affected by the work tracked in the work request.

          A resource being created, updated, or deleted will remain in the `IN_PROGRESS` state until

          work is complete for that resource at which point it will transition to `CREATED`, `UPDATED`,

          or `DELETED`, respectively.

          '
        enum: *id011
        type: string
        x-obmcs-top-level-enum: '#/definitions/ActionType'
      entityType:
        description: The resource type the work request affects.
        type: string
      entityUri:
        description: The URI path that you can use for a GET to access the resource metadata
        type: string
      identifier:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the resource
          the work request affects.
        type: string
    required:
    - actionType
    - entityType
    - identifier
  WorkRequestSummary:
    description: A description of the work request status.
    properties:
      compartmentId:
        description: 'The OCID of the compartment that contains the work request.

          '
        type: string
      definedTags: *id005
      freeformTags: *id006
      id:
        description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the work request.
        type: string
      operationType:
        description: The type of operation that spawned the work request.
        enum: *id009
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationType'
      percentComplete:
        description: Percentage of the request completed.
        format: float
        type: number
      resources:
        description: The resources affected by this work request.
        items:
          $ref: '#/definitions/WorkRequestResource'
        type: array
      status:
        description: The status of current work request.
        enum: *id010
        type: string
        x-obmcs-top-level-enum: '#/definitions/OperationStatus'
      timeAccepted:
        description: 'The date and time the work request was created, in the format defined by [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
      timeFinished:
        description: 'The date and time the work request completed, in the format defined by [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
      timeStarted:
        description: 'The date and time the work request was started, in the format defined by [RFC 3339](https://tools.ietf.org/rfc/rfc3339).

          '
        format: date-time
        type: string
    required:
    - operationType
    - status
    - id
    - compartmentId
    - resources
    - percentComplete
    - timeAccepted
  WorkRequestSummaryCollection:
    description: A list of work requests.
    properties:
      items:
        description: A collection of work requests.
        items:
          $ref: '#/definitions/WorkRequestSummary'
        type: array
    required:
    - items
    type: object
parameters:
  CompartmentIdQueryParam:
    description: The ID of the compartment in which to list resources.
    in: query
    name: compartmentId
    type: string
    x-default-description: 'null'
  CompartmentIdRequiredQueryParam:
    description: The unique identifier for the compartment.
    in: query
    name: compartmentId
    required: true
    type: string
    x-default-description: 'null'
  DisplayNameQueryParam:
    description: A filter to return only resources that match the entire display name given.
    in: query
    maxLength: 255
    minLength: 1
    name: displayName
    type: string
    x-default-description: 'null'
  IfMatchHeader:
    description: 'For optimistic concurrency control. In the PUT or DELETE call

      for a resource, set the `if-match` parameter to the value of the

      etag from a previous GET or POST response for that resource.

      The resource will be updated or deleted only if the etag you

      provide matches the resource''s current etag value.

      '
    in: header
    name: if-match
    required: false
    type: string
  LifeCycleStateQueryParam:
    description: A filter to return the resources, whose lifecycleState matches with the given lifecycleState.
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/OciCacheUser/lifecycleState'
  LifecycleStateQueryParam:
    description: A filter to return only resources their lifecycleState matches the given lifecycleState.
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/RedisCluster/lifecycleState'
  OciCacheBackupIdentifierPathParam:
    description: Unique OCI Cache Backup identifier.
    in: path
    name: ociCacheBackupId
    required: true
    type: string
  OciCacheBackupIdentifierQueryParam:
    description: Unique OCI Cache Backup identifier.
    in: query
    name: ociCacheBackupId
    type: string
    x-default-description: 'null'
  OciCacheBackupLifecycleStateQueryParam:
    description: A filter to return the OCI Cache Backup resources, whose lifecycle state matches with
      the given lifecycle state.
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/OciCacheBackup/lifecycleState'
  OciCacheBackupSourceClusterIdQueryParam:
    description: A filter to return the OCI Cache Backup resources, whose source cluster ID matches with
      the given source cluster ID.
    in: query
    name: sourceClusterId
    required: false
    type: string
    x-default-description: 'null'
  OciCacheConfigSetIdentifierPathParam:
    description: Unique OCI Cache Config Set identifier.
    in: path
    name: ociCacheConfigSetId
    required: true
    type: string
  OciCacheConfigSetIdentifierQueryParam:
    description: Unique OCI Cache Config Set identifier.
    in: query
    name: id
    type: string
    x-default-description: 'null'
  OciCacheConfigSetLifecycleStateQueryParam:
    description: A filter to return the OCI Cache Config Set resources, whose lifecycle state matches
      with the given lifecycle state.
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/OciCacheConfigSet/lifecycleState'
  OciCacheDefaultConfigSetIdentifierPathParam:
    description: Unique OCI Cache Default Config Set identifier.
    in: path
    name: ociCacheDefaultConfigSetId
    required: true
    type: string
  OciCacheDefaultConfigSetIdentifierQueryParam:
    description: Unique OCI Cache Default Config Set identifier.
    in: query
    name: id
    type: string
    x-default-description: 'null'
  OciCacheDefaultConfigSetLifecycleStateQueryParam:
    description: A filter to return the OCI Cache Default Config Set resources, whose lifecycle state
      matches with the given lifecycle state.
    in: query
    name: lifecycleState
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/OciCacheDefaultConfigSet/lifecycleState'
  OciCacheUserIdentifierPathParam:
    description: A filter to return only resources, that match with the given OCI cache user ID (OCID).
    in: path
    name: ociCacheUserId
    required: true
    type: string
  OciCacheUsernameQueryParam:
    description: A filter to return the resources that match with the given OCI cache user name.
    in: query
    maxLength: 255
    minLength: 1
    name: name
    type: string
    x-default-description: 'null'
  PaginationLimitQueryParam:
    default: 10
    description: The maximum number of items to return.
    in: query
    maximum: 1000
    minimum: 1
    name: limit
    type: integer
  PaginationTokenQueryParam:
    description: A token representing the position at which to start retrieving results. This must come
      from the `opc-next-page` header field of a previous response.
    in: query
    minLength: 1
    name: page
    type: string
    x-default-description: 'null'
  RedisClusterIdentifierPathParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster.
    in: path
    name: redisClusterId
    required: true
    type: string
  RedisClusterIdentifierQueryParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the cluster.
    in: query
    name: id
    type: string
    x-default-description: 'null'
  RequestIdHeader:
    description: The client request ID for tracing.
    in: header
    name: opc-request-id
    type: string
  ResourceIdentifierQueryParam:
    description: The [OCID](/iaas/Content/General/Concepts/identifiers.htm#Oracle) of the resource affected
      by the work request.
    in: query
    name: resourceId
    type: string
    x-default-description: 'null'
  RetryTokenHeader:
    description: 'A token that uniquely identifies a request so it can be retried in case of a timeout
      or

      server error without risk of executing that same action again. Retry tokens expire after 24

      hours, but can be invalidated before then due to conflicting operations. For example, if a resource

      has been deleted and purged from the system, then a retry of the original creation request

      might be rejected.

      '
    in: header
    maxLength: 64
    minLength: 1
    name: opc-retry-token
    required: false
    type: string
  SoftwareVersionQueryParam:
    description: A filter to return the OCI Cache Config Set resources, whose software version matches
      with the given software version.
    in: query
    name: softwareVersion
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-enumref: '#/definitions/OciCacheConfigSet/softwareVersion'
  SortByQueryParam:
    default: timeCreated
    description: 'The field to sort by. Only one sort order may be provided. Default order for timeCreated
      is descending. Default order for displayName is ascending.

      '
    enum:
    - timeCreated
    - displayName
    in: query
    name: sortBy
    type: string
  SortOrderQueryParam:
    description: The sort order to use, either 'ASC' or 'DESC'.
    enum: *id012
    in: query
    name: sortOrder
    type: string
    x-default-description: 'The default value depends upon `sortBy`, and

      in general is ''DESC'' when sorting by time and ''ASC'' otherwise.

      '
    x-obmcs-top-level-enum: '#/definitions/SortOrder'
  TenantIdQueryParam:
    description: A filter to return the resources, whose tenant ID matches with the given tenant ID.
    in: query
    maxLength: 255
    minLength: 1
    name: tenantId
    required: false
    type: string
    x-default-description: 'null'
  WorkRequestIdPathParam:
    description: The the asynchronous request ID.
    in: path
    name: workRequestId
    required: true
    type: string
  WorkRequestIdQueryParam:
    description: The ID of the asynchronous work request.
    in: query
    name: workRequestId
    type: string
    x-default-description: 'null'
  WorkRequestSortByQueryParam:
    default: timeAccepted
    description: 'The field to sort by. Only one sort order may be provided. Default order for timeAccepted
      is descending.

      '
    enum:
    - timeAccepted
    in: query
    name: sortBy
    type: string
  WorkRequestStatusQueryParam:
    description: A filter to return only resources their lifecycleState matches the given OperationStatus.
    enum: *id010
    in: query
    name: status
    required: false
    type: string
    x-default-description: 'null'
    x-obmcs-top-level-enum: '#/definitions/OperationStatus'
responses:
  400:
    description: Bad Request
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  401:
    description: Unauthorized
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  404:
    description: Not Found
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  405:
    description: Method Not Allowed
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  409:
    description: Conflict
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  412:
    description: Precondition failed
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  429:
    description: Too Many Requests
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  500:
    description: Internal Server Error
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
  default:
    description: Unknown Error
    headers:
      opc-request-id: *id001
    schema:
      $ref: '#/definitions/Error'
x-anchors:
  x-headers:
    etag: *id003
    opc-next-page: *id004
    opc-request-id: *id001
    opc-work-request-id: *id002
    retry-after: *id013
  x-properties:
    definedTags: *id005
    freeformTags: *id006
    securityAttributes: *id008
    systemTags: *id007
x-obmcs-client-retries-enabled: true
x-oracle-package: com.oracle.pic.redisservice
